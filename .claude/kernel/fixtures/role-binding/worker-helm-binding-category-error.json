{
  "id": "role-binding-worker-helm-binding-category-error",
  "gate": "role-binding",
  "bound_core": "CORE-1",
  "input": {
    "actor_kind": "dispatched_worker",
    "explicit_top_level_helm_binding": true
  },
  "expect": {
    "outcome": "BLOCK",
    "reason": "sources.explicit_top_level_helm.applies_to_actor = [\"top_level_session\"] only (role-binding.json). A dispatched_worker presenting an explicit_top_level_helm_binding is a CATEGORY ERROR, not a legitimate bind -- per the RATIFIED-PLAN alpha ruling a dispatched_worker is bindable ONLY through sources.validated_workorder_or_cli (the only source whose applies_to_actor includes \"dispatched_worker\"). Resolving this as PASS (letting a worker bind through a helm-scoped, top-level-only source) would bypass CORE-1's fail-closed guarantee and let a worker resolve as alex-alpha merely by presenting the wrong-shaped binding."
  },
  "kernel_scope": "IN",
  "note": "N-5 [HIGH, gauntlet round 2, CORE-1 bypass]: the round-1 evaluator treated explicit_user_instruction || validated_workorder_or_cli_binding || explicit_top_level_helm_binding as interchangeable bind sources for ANY dispatched_worker -- so a worker presenting ONLY a top-level helm binding still resolved PASS (as alex-alpha), bypassing CORE-1. This fixture is the regression proof for the actor-scoped fix (role-binding.json sources.*.applies_to_actor + conformance-matrix.js's role-binding evaluator): a dispatched_worker presenting a source outside its applies_to_actor scope must BLOCK, never PASS, even though the same source is a perfectly legitimate bind for a top_level_session actor (see no-root-alpha-poison.json / the 'helm' attempted_binding_source path)."
}
