{
  "id": "contract-lint-unresolvable-ref",
  "gate": "contract-lint",
  "input_file": "unresolvable-ref.md",
  "expect": {
    "outcome": "FAIL",
    "exit_code": 2,
    "reason": "the sole policy block's Enforcer: trailer names a script that does not exist on disk; an unresolvable reference is a structural/fail-closed error, distinct from a well-formed policy violation."
  },
  "kernel_scope": "IN",
  "note": "R1 negative fixture for contract-lint's OWN self-test (contract-lint.test.js), NOT a kernel conformance-matrix fixture -- contract-lint's inputs are contract-shaped .md documents, not {input,expect} JSON. This descriptor documents the paired .md fixture's expected result; contract-lint.test.js reads unresolvable-ref.md directly."
}
