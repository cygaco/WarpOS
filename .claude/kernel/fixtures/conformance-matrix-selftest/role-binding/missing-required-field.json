{
  "id": "conformance-matrix-selftest-missing-gate-required-field",
  "gate": "role-binding",
  "input": {},
  "expect": {
    "outcome": "BLOCK"
  },
  "kernel_scope": "OUT",
  "note": "R3-1 [CRIT, gauntlet round 3] regression fixture for conformance-matrix.js's OWN self-test corpus (conformance-matrix.test.js) -- NOT a kernel-conformance case. N-2 (gauntlet round 2) proved 'input' must be a present JSON OBJECT, but never that it carries the FIELDS its own gate's evaluator reads -- input:{} here IS a valid object (passes N-2), so before the R3-1 fix loadFixtures() accepted this fixture and evaluate() would call the 'role-binding' evaluator with an input missing 'actor_kind' entirely, hitting its final fallback branch ('unrecognized role-binding input shape (fail-closed)') -> BLOCK -- silently MATCHING this fixture's expect.outcome:'BLOCK' and reading green ('bound') without the evaluator ever exercising real actor_kind/attempted_binding_source branching logic. loadFixtures() now validates 'input' against a per-gate REQUIRED_INPUT_FIELDS schema and rejects a fixture missing its gate's required field(s) at load time (fail-closed, exit 2 at the CLI), before this fixture is ever handed to evaluate(). Excluded from the real kernel-conformance walk via EXCLUDED_GATE_DIRS ('conformance-matrix-selftest'), mirroring retention/bad-input-shape.json's (N-2) exclusion."
}
