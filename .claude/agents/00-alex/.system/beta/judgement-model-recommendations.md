# Alex β Mining Recommendations

Last cycle: **2026-05-30b** (operator turbo run, session continued) — `/beta:mine` + `/beta:integrate`:

## ✅ Resolved this cycle (the prior cycle's flags — operator-ruled + applied)
- **DP-gap #29** (state-conditional Class C) → APPLIED to `paths.decisionPolicy` §Class C.
- **DP-gap #30** (engine-vs-product reliability carve-out) → APPLIED to `paths.currentStage` (carve-out) + torture-reliability sprint roadmapped (0.18.1).
- **DP-gap #31** (β-vs-Director routing) → APPLIED to `paths.decisionPolicy` §Consultation routing ("compose, don't compete").
- **G-13** (β unaware of managerial layer) → substantially addressed by #31; standalone β principle deferred to the roadmapped DP-clearance session.

(See `judgement-model.md` changelog 2026-05-30. These are archived from "pending" → done.)

## New patterns discovered — FLAGGED for operator review (NOT auto-applied; new principles need a ruling)
- **[P-041] Operator-delegated rule choice = DIRECTIVE, not ESCALATE.** When the operator says "I'll trust you to pick the right rule" (DP-gap #31 routing this session), the correct β verdict is **DECIDE + apply + record the choice**, not bounce it back. Delegation of a Class-B/C *substance* call is authorization to resolve it, within the safety floor. (evidence: #31 delegated + applied this session) confidence: medium.
- **[P-042] Verify-canonical-first as a β gate.** Before authorizing work on ANY downstream-flagged gap (a WARPOS.md item, a register entry), β should require a REPRODUCES@current verdict first (ED-008). ~80% of flagged gaps are already fixed upstream; "it's in the register" is not evidence it's real. (evidence: this session's reconcile — ~80% already fixed) confidence: high.

## Decision Policy Gaps (still open — routed to the roadmapped DP-clearance session)
- #26 (flag-gated irreversible carve-out), #27 (defeasibility preamble), #19-22, #11-13 — re-surface from the archive, operator-rule each. See ROADMAP §Decision-Policy gap-clearance session.

Full prior content archived in `judgement-model-recommendations-archive.md`. Run `/beta:mine` to generate the next cycle.
