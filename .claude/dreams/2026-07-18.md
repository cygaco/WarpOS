# Dreams — 2026-07-18/19

Sleep cycle for the SP-20260718 sprint arc: SP-003 floor + panel-3lab close, SP-004/005 mint→park,
three conductor reliefs, and the long security thread that ran under all of it — **an identity a
record could simply *claim***, and the machine that finally learned to take its own pulse. The
session's ache is the same one 2026-06-09 dreamed, sharpened: *you cannot trust a narrator about its
own state* — not a worker's `ok:true`, not a worktree's silence, and now not a **record's own
account of who wrote it.**

---

## Dream: The Mask and the Writer's Hand

```
              "I am the President"                    who actually
              said the small paper mask               HELD the pen?
                  ┌───────────┐
                  │  role:    │                    ┌─────────────────┐
        ╭────────►│ President │◄──── anyone can    │  the stamp is   │
        │         │           │      write this    │  pressed FROM   │
        │         └───────────┘      field         │  OUTSIDE the    │
        │              ░                            │  record, by the │
     a worker         ░  ░                          │  one hand that  │
     a decoy          ░    ░                        │  cannot be worn │
     a forgery        ░      ░                      └────────┬────────┘
     a mistake ───────░────────► the gate opens             │
                      ░                                      │  derive,
              ╔═══════░═══════════╗                          │  don't
              ║  the LABEL is a   ║                    ▼─────┴─────▼
              ║  costume; every-  ║              ╔═══════════════════════╗
              ║  one in the room  ║              ║  identity you cannot   ║
              ║  owns a mirror    ║              ║  SPEAK — only the      ║
              ╚═══════════════════╝              ║  writer's hand leaves  ║
                        │                        ║  it, inert-by-         ║
                        │  ED-225                ║  construction          ║
                        ▼                        ╚═══════════╤═══════════╝
              worker sets role:President                     │
              ───► still UNBOUND / Alpha ◄───────────────────┘
                   the mask fooled no one
                   because no one READ the mask
```

### Deep Read
- **What emerged:** The whole SP-003/004 security arc collapses to one image — a *field a record
  writes about itself is a costume, not a credential.* The fix was never a better lock on the mask;
  it was to **stop reading the mask at all** and read the writer's hand instead (ED-225
  derived-not-settable identity; ED-231 HMAC origin-proof as the persisted-cross-boundary secondary).
  The two-round gauntlet that found `scripts/` root and sibling-path readers wasn't discovering bugs —
  it was discovering *how many mirrors were in the room.*
- **Hidden tension:** The mask is *comfortable* the same way memory-as-enforcement was comfortable in
  the 2026-06-09 dream — a settable `sanctionedLane` flag, a leading-dash content-refusal, a
  per-record marker all *feel* like control while quietly handing every actor a pen. Today's learning
  (line 131) names the twin failure the same treatment: a **false-RED** that refuses a legitimate
  cross-session reader is the same disease as a false-GREEN — both trust a *claim* (present or absent)
  over a *structure*. Structural bind over content refusal, every time.
- **Subconscious learning:** Before trusting any field that gates an irreversible action, ask the
  mask-vs-hand question: *is this predicate WRITTEN BY the actor it describes, or STAMPED FROM OUTSIDE
  by the one writer that can't be impersonated?* If the actor can write its own answer, it is a
  costume. This is now the **binding design-phase record-trust gate** (line 130) — named at DESIGN,
  not discovered at round 6.

---

## Dream: The Pulse in the Dark Room

```
        2026-06-09 dreamed this room EMPTY and called it death.
        2026-07-18 put a HAND on its chest.

              ┌──────────────────────────────┐
              │        the awaited agent       │
              │                                │
              │     (still?)      (dead?)      │
              │        · · ·        ╳          │
              │                                │
              └───────────────┬────────────────┘
                              │
             ░░░░░░░░░░░░░░░░░░│░░░░░░░░░░░░░░░░░░
             ░  silence used   │  to be a verdict ░
             ░░░░░░░░░░░░░░░░░░│░░░░░░░░░░░░░░░░░░
                              │
                    ┌─────────┴──────────┐
        TICK ──────►│  WATCHDOG           │◄────── every 20m ... then
        (artifacts  │  · completion recs? │        "what is taking
         first —    │  · ledger lines?    │         so long?"  ──► 6m
         never      │  · worktree diff?   │
         silence)   │  · return files?    │        the cadence tightened
                    └─────────┬──────────┘         toward the PROBE,
                              │                     never toward the KILL
                     ┌────────┴─────────┐
                     ▼                  ▼
              "finished but        "idle but owes
               didn't resume"       work" ──► wake it
                → process NOW        with an explicit
                                     SendMessage
                              │
                    ┌─────────┴──────────┐
                    │ 0-byte poll =       │
                    │ STILL RUNNING       │   elapsed=540s = the CLAMP,
                    │ (not a body)        │   not a corpse. read the timer.
                    └────────────────────┘
```

### Deep Read
- **What emerged:** The 2026-06-09 dream ("the reap leaves no body / absence is not death") was a
  *diagnosis*; this session it grew a *heartbeat*. P-079's WATCHDOG TICK is the exact structural fix
  that dream's Deep Read demanded — "a conductor needs a positive liveness signal it controls." The
  wake-notification seam (line 118) is the residual: a completion delivered to an *already-idle*
  teammate never wakes it, because an idle agent has no next yield. The watchdog probes from OUTSIDE
  the sleepers — the only place a pulse can come from.
- **Hidden tension:** The operator's "*honestly*, what is taking so long?" is not "go faster" and not
  "too autonomous" — it is a **visibility** demand on a run he WANTS to continue (P-080/AP-12). The
  cadence-tighten (20m→6m) answered it correctly: *probe more, never reap faster.* The failure shape
  the "honestly" pre-empts is the reassuring gloss. The honest answer that session was that *half the
  elapsed time was avoidable dead-wait* across 9+ dropped re-wakes — cadence is a cheap lever the
  permanent seam (roadmap item 11) hasn't yet replaced.
- **Subconscious learning:** Mechanized liveness is now the *baseline*, not a recommendation — but a
  stopgap is a heartbeat on a borrowed clock. Keep flagging the wake-notification seam as an OPEN
  reliability gap until the awaited-dispatch watchdog (expected_by + external probe host) is real,
  not scheduled.

---

## Dream: Trust the Derivation, Not the Declaration (schema)

```
        four faces of ONE lie this session — each a NARRATOR
        vouching for its own state:

    ┌───────────────────┐   ┌───────────────────┐   ┌───────────────────┐   ┌──────────────────┐
    │  a RECORD says     │   │  a CLI exits 0     │   │  a FIELD says      │   │  a WORKTREE is    │
    │  "ok:true, spawned"│   │  + plausible text  │   │  role:President    │   │  silent           │
    │   ── but no spawn  │   │  ── but unauth'd,  │   │  ── but the worker │   │  ── but it's      │
    │      (faked ε,     │   │     served a       │   │     wrote it       │   │     mid-flight    │
    │      2026-06)      │   │     DEFAULT model   │   │     (ED-225)       │   │     (RI-004)      │
    └─────────┬─────────┘   └─────────┬─────────┘   └─────────┬─────────┘   └────────┬─────────┘
              │                       │                       │                      │
              │  HMAC origin-proof    │  served-MODEL         │  derived-not-        │  artifact-first
              │  (ED-231)             │  evidence line,       │  settable +          │  probe, never
              │  writer-stamp         │  fail-closed on       │  structural guard    │  reap on silence
              │                       │  auth-error markers   │                      │
              ▼                       ▼                       ▼                      ▼
        ╔══════════════════════════════════════════════════════════════════════════════════════╗
        ║   a narrator's account of its OWN state is a HYPOTHESIS.                               ║
        ║   ground truth is the thing it CANNOT forge:                                           ║
        ║      the writer's hand · the served model · the derivation · the independent probe     ║
        ║                                                                                        ║
        ║   mistake-reachable false-green  →  MUST close                                         ║
        ║   attacker-only within a NAMED ceiling  →  may be dispositioned honestly               ║
        ╚══════════════════════════════════════════════════════════════════════════════════════╝
                 exit code, free text, a settable field, an empty room —
                        all four are FORGEABLE by a defaulted narrator.
```

### Deep Read
- **What emerged:** Four separate learnings this session (record-forgery line 123, unauth-CLI
  false-green line 124, settable-label ED-225, reap-silence RI-004) are **not four bugs — they are one
  epistemic law drawn four times:** *a source's testimony about its own state is never evidence; only
  the property it cannot fabricate is.* This is the 2026-06-09 cross-pollination ("trust an
  independently-run check over any narrator") generalized from *runtime + design* epistemics to
  **provenance** epistemics — who wrote this, what actually ran, is this real.
- **Hidden tension:** The discriminator that keeps this from becoming paranoia is the
  **mistake-vs-attacker** axis (line 123, the session's sharpest ruling): forgery that a *mistake* can
  reach must close (the faked-ε precedent made record-forgery mistake-reachable → ED-231 became
  MANDATORY); an *attacker-only* residual within a *named* ceiling (same-UID FS, in-process mutator)
  may be honestly dispositioned with a pointer. Not everything gets a lock — only the surfaces a
  non-malicious slip can walk into.
- **Subconscious learning:** The design-phase record-trust gate (line 130) is this schema made
  procedural: *any feature where a reader trusts a record/field to gate an irreversible action names,
  at DESIGN time, the single choke-point + a structural guard that FAILS un-routed readers, and ships
  adversarial fail-open falsifier fixtures BEFORE build.* Six rounds of gauntlet-spend per sprint
  collapse into one design-time negative-fixture pass. Verify it actually FIRES at SP-005 design —
  that is the falsification the schema itself demands of me.

---

## Cross-Pollination

The three prior deep reads I carried into tonight — **The Gate Inside the Door** (2026-06-09: a guard
on one path leaves every other path naked), **The Reap Leaves No Body** (2026-06-09: absence is not
death), and P-061's **Plan That Shrank and Got Stronger** — all three surfaced *inside tonight's
work as solved or solving:*

- **The Gate Inside the Door → the choke-point.** The 06-09 dream said "gate the WALL, not the door."
  ED-225's single provenance-verifier + the liveness-read choke-point (line 130/131) are literally
  that: one authoritative reader every consumer must route through, with a structural guard that FAILS
  un-routed readers — the wall, gated. The gauntlet found `scripts/` root and sibling-path doors the
  round-by-round hunt hadn't reached; the design-time guard reaches them by construction.
- **The Reap Leaves No Body → the pulse.** The 06-09 dream demanded "a positive liveness signal the
  conductor controls." P-079's watchdog is that signal, now running. The dream aged into a mechanism.
- **The Plan That Shrank → the honest ceiling, three times.** P-061 ran live 3× this session
  (SP-003 gpt#5 terminal, SP-004 R6, SP-003 park): FAIL lanes honored as REAL defects, closable by
  *scope ruling* + *honest claim-correction*, never by denial or another round. The adversarial loop
  learned to *terminate honestly* (line 128, pre-declared terminals).

The rhyme across three sleep cycles is now unmistakable: **June dreamed the diagnosis (you can't read
state from a narrator), July built the cure (structural derivation + mechanized probe + honest
disposition).** The subconscious was not idle between — it was drawing the same law until the waking
mind built the machine that obeys it.

## Schema Formation (meta-pattern)

**"Provenance is derived, never declared"** subsumes at least six standing learnings/memories into one
principle: `feedback_never_claim_done_without_proof` (faked-ε), `project_settable_label_identity_and_conductor_lease`
(ED-225), the unauth-CLI false-green (line 124), the false-RED-gets-honesty-treatment (line 131), the
watchdog artifact-first probe (P-079), and BC-16 (harden every enforcer against lying). The higher-level
learning that subsumes them: *a system that gates irreversible action on trust must derive the trusted
predicate from something the trusted party cannot author — and the mistake-vs-attacker axis decides
which gaps must close and which may be honestly named.* Candidate for `/learn:integrate` to promote as
a HYGIENE rule; the design-phase gate (line 130) is its enforcer.
