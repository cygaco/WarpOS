<!-- requirement-format-legacy -->
# High-Level Stories — Panel namespace + roadmap panel (ROADMAP items 23+25)

**Sprint:** `SP-20260615-001`
**PRD:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260615-001\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As the operator, I open any panel with one consistent /panel: verb instead of remembering each namespace.

**As** the user
**I want** As the operator, I open any panel with one consistent /panel: verb instead of remembering each namespace.
**So that** The operator opens any panel with one consistent verb and sees 'what's next' without asking; adding a panel is a single registry row; a registry row that points at a non-existent opener is caught by an enforcer instead of failing silently at open time.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As the operator, I run /panel:roadmap and see the ranked next-action + what's in flight + what's blocked at a glance, without asking.

**As** the user
**I want** As the operator, I run /panel:roadmap and see the ranked next-action + what's in flight + what's blocked at a glance, without asking.
**So that** The operator opens any panel with one consistent verb and sees 'what's next' without asking; adding a panel is a single registry row; a registry row that points at a non-existent opener is caught by an enforcer instead of failing silently at open time.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
