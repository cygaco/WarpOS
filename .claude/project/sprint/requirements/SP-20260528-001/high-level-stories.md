<!-- requirement-format-legacy -->
# High-Level Stories — Rename check: namespace to scan: + scan:full system scan

**Sprint:** `SP-20260528-001`
**PRD:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260528-001\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As a maintainer, I invoke scan:* instead of check:* and get identical behavior.

**As** the user
**I want** As a maintainer, I invoke scan:* instead of check:* and get identical behavior.
**So that** A single coherent scan: namespace with scan:full as the one-command full system scan; stable foundation + naming clarity before the 0.17.0 test-suite system is built on top of it. Reduces the instability the operator is feeling by making 'run all checks' a first-class, discoverable command.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As a maintainer, I run scan:full and get one full-system-scan report covering every scan.

**As** the user
**I want** As a maintainer, I run scan:full and get one full-system-scan report covering every scan.
**So that** A single coherent scan: namespace with scan:full as the one-command full system scan; stable foundation + naming clarity before the 0.17.0 test-suite system is built on top of it. Reduces the instability the operator is feeling by making 'run all checks' a first-class, discoverable command.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
