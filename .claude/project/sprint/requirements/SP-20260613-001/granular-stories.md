<!-- requirement-format-legacy -->
# Granular Stories — ED-051 enforcer — missing_product_lead_authoring finding

**Sprint:** `SP-20260613-001`
**High-level stories:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260613-001\high-level-stories.md`

> Granular stories use the `S-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`. Each granular story
> should produce roughly one ticket during `/sprint:design`.

## S-1 — Add missing_product_lead_authoring to computeFindings with the cutoff + record-backed + solo-aware predicate.

**As** the user
**I want** Add missing_product_lead_authoring to computeFindings with the cutoff + record-backed + solo-aware predicate.
**So that** The WG-3 routing rule stops being aspirational: any future sprint where alpha silently self-authors product requirements (the exact ED-051 failure) is caught at /scan:full instead of going unnoticed, so the 'who's writing the product reqs?' bug class is self-detecting.

Acceptance criteria:
- AC-1: (set by design step)

Linked: `H-1`, `R-1`.
COPY: see `copy.md`.
INPUTS: see `inputs.md`.
TRACE: see `trace.md`.

## S-2 — Read the product-lead authorship record source defined in design and the plan-contract/PRD precondition.

**As** the user
**I want** Read the product-lead authorship record source defined in design and the plan-contract/PRD precondition.
**So that** The WG-3 routing rule stops being aspirational: any future sprint where alpha silently self-authors product requirements (the exact ED-051 failure) is caught at /scan:full instead of going unnoticed, so the 'who's writing the product reqs?' bug class is self-detecting.

Acceptance criteria:
- AC-1: (set by design step)

Linked: `H-1`, `R-2`.
COPY: see `copy.md`.
INPUTS: see `inputs.md`.
TRACE: see `trace.md`.

## S-3 — Surface the new finding_type through scan:sprint-manager-consult / /scan:full.

**As** the user
**I want** Surface the new finding_type through scan:sprint-manager-consult / /scan:full.
**So that** The WG-3 routing rule stops being aspirational: any future sprint where alpha silently self-authors product requirements (the exact ED-051 failure) is caught at /scan:full instead of going unnoticed, so the 'who's writing the product reqs?' bug class is self-detecting.

Acceptance criteria:
- AC-1: (set by design step)

Linked: `H-1`, `R-3`.
COPY: see `copy.md`.
INPUTS: see `inputs.md`.
TRACE: see `trace.md`.

## S-4 — Add positive + negative planted fixtures and test cases to the enforcer test harness.

**As** the user
**I want** Add positive + negative planted fixtures and test cases to the enforcer test harness.
**So that** The WG-3 routing rule stops being aspirational: any future sprint where alpha silently self-authors product requirements (the exact ED-051 failure) is caught at /scan:full instead of going unnoticed, so the 'who's writing the product reqs?' bug class is self-detecting.

Acceptance criteria:
- AC-1: (set by design step)

Linked: `H-1`, `R-4`.
COPY: see `copy.md`.
INPUTS: see `inputs.md`.
TRACE: see `trace.md`.

## S-5 — Mark ED-051 resolved and regenerate both manifests if needed.

**As** the user
**I want** Mark ED-051 resolved and regenerate both manifests if needed.
**So that** The WG-3 routing rule stops being aspirational: any future sprint where alpha silently self-authors product requirements (the exact ED-051 failure) is caught at /scan:full instead of going unnoticed, so the 'who's writing the product reqs?' bug class is self-detecting.

Acceptance criteria:
- AC-1: (set by design step)

Linked: `H-1`, `R-5`.
COPY: see `copy.md`.
INPUTS: see `inputs.md`.
TRACE: see `trace.md`.

