<!-- requirement-format-legacy -->
# High-Level Stories — Visual interactive roadmap panel — browser GUI (ROADMAP item 25, redone)

**Sprint:** `SP-20260615-002`
**PRD:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260615-002\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As the operator, I run /panel:roadmap and a polished interactive panel opens in my browser.

**As** the user
**I want** As the operator, I run /panel:roadmap and a polished interactive panel opens in my browser.
**So that** The operator runs /panel:roadmap and a clean, modern, interactive panel opens in their browser — active sprints, the prioritized roadmap, epics with progress, and the ticket breakdown per sprint — so 'what's next / where are we' is a visual glance + drill-down, not a text dump and not asking.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As the operator, I see active sprints, the roadmap, epics (with progress), and the ticket breakdown — and can filter/expand/drill into any of them.

**As** the user
**I want** As the operator, I see active sprints, the roadmap, epics (with progress), and the ticket breakdown — and can filter/expand/drill into any of them.
**So that** The operator runs /panel:roadmap and a clean, modern, interactive panel opens in their browser — active sprints, the prioritized roadmap, epics with progress, and the ticket breakdown per sprint — so 'what's next / where are we' is a visual glance + drill-down, not a text dump and not asking.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
