<!-- requirement-format-legacy -->
# High-Level Stories — Engine skeleton — plain-node Agent SDK app, MCP stdio server, four-core tool surface, job state machine, journal, permission-level config port, and the API-key model-access seam

**Sprint:** `S-VLADW1-01`
**PRD:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\S-VLADW1-01\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As a founder, I install Vlad and it runs on MY OWN Claude subscription, locally, with no developer credentials and no one else's account in the path — so there is no separate Console signup on my way to first value.

**As** the user
**I want** As a founder, I install Vlad and it runs on MY OWN Claude subscription, locally, with no developer credentials and no one else's account in the path — so there is no separate Console signup on my way to first value.
**So that** Nothing user-visible ships in this sprint, and that is the correct shape: this is the engine, not a feature. What it buys is that every subsequent wave — the audit job, the write path, the agent face, the roadmap layer — has a real surface to attach to, exercisable end-to-end without a host, with the compliance-critical credential boundary already enforced in code rather than retrofitted. The founder-facing payoff arrives in S-VLADW1-02, which cannot start without this.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As a founder, I am confident whatever credential Vlad holds reaches Anthropic and nowhere else — no log, no telemetry, no proxy, no other process — because a build-failing enforcer proves it rather than a doc promising it.

**As** the user
**I want** As a founder, I am confident whatever credential Vlad holds reaches Anthropic and nowhere else — no log, no telemetry, no proxy, no other process — because a build-failing enforcer proves it rather than a doc promising it.
**So that** Nothing user-visible ships in this sprint, and that is the correct shape: this is the engine, not a feature. What it buys is that every subsequent wave — the audit job, the write path, the agent face, the roadmap layer — has a real surface to attach to, exercisable end-to-end without a host, with the compliance-critical credential boundary already enforced in code rather than retrofitted. The founder-facing payoff arrives in S-VLADW1-02, which cannot start without this.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
