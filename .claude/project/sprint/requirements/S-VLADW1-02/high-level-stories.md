<!-- requirement-format-legacy -->
# High-Level Stories — Job #1 — port lastmile detect/score/adapters into the Vlad engine, adopt score.js as the ONE readiness number, mint receipt v1 from portfolio dogfood

**Sprint:** `S-VLADW1-02`
**PRD:** `C:\Users\Vlad\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\S-VLADW1-02\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As a founder with a prototype, I point Vlad at my repo and get a readiness audit that names what is missing, so I know what to fix next.

**As** the user
**I want** As a founder with a prototype, I point Vlad at my repo and get a readiness audit that names what is missing, so I know what to fix next.
**So that** A founder with a prototype points Vlad at their repo and gets back an honest readiness audit: what Vlad did, what it verified, and the one thing it needs. It degrades honestly — a repo with no tests is reported 'NOT verified', never scored as if it passed. This is the first Vlad capability that reaches a user and the evidence base for the receipt contract the rest of v1 depends on.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As a founder whose repo has no tests, I get told plainly that readiness could NOT be verified, so I am not misled into thinking I passed.

**As** the user
**I want** As a founder whose repo has no tests, I get told plainly that readiness could NOT be verified, so I am not misled into thinking I passed.
**So that** A founder with a prototype points Vlad at their repo and gets back an honest readiness audit: what Vlad did, what it verified, and the one thing it needs. It degrades honestly — a repo with no tests is reported 'NOT verified', never scored as if it passed. This is the first Vlad capability that reaches a user and the evidence base for the receipt contract the rest of v1 depends on.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
