<!-- requirement-format-legacy -->
# High-Level Stories — Spinup orchestrator — wire bootstrap:spinup pipeline end-to-end (0.15.0 sprint 3 of 3)

**Sprint:** `SP-20260525-023`
**PRD:** `C:\Users\Vladislav Zhirnov\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260525-023\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As a fresh product, running /bootstrap:spinup once takes me from intent to my core loop on screen.

**As** the user
**I want** As a fresh product, running /bootstrap:spinup once takes me from intent to my core loop on screen.
**So that** The 0.15.0 milestone's payoff: a fresh product goes from 'just WarpOS installed' to 'core loop on screen' with ONE command. This is JTBD-3 ('just WarpOS' -> canonical docs + roadmap + something running) fully realized. Sprints A (suite reconciliation) and B (canon engine) built the parts; C composes them into the on-ramp.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As an operator, a gappy install is refused at pre-flight before any phase runs.

**As** the user
**I want** As an operator, a gappy install is refused at pre-flight before any phase runs.
**So that** The 0.15.0 milestone's payoff: a fresh product goes from 'just WarpOS installed' to 'core loop on screen' with ONE command. This is JTBD-3 ('just WarpOS' -> canonical docs + roadmap + something running) fully realized. Sprints A (suite reconciliation) and B (canon engine) built the parts; C composes them into the on-ramp.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
