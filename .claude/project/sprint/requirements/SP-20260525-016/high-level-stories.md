<!-- requirement-format-legacy -->
# High-Level Stories — /roadmap:create skill — fresh-roadmap bootstrap using DoPM (milestone 0.14.0 sprint 3)

**Sprint:** `SP-20260525-016`
**PRD:** `C:\Users\Vladislav Zhirnov\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260525-016\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As an operator running /roadmap:add, I see DoPM's milestone-fit + cadence-impact analysis before the entry lands.

**As** the user
**I want** As an operator running /roadmap:add, I see DoPM's milestone-fit + cadence-impact analysis before the entry lands.
**So that** Operator using /roadmap:add gets strategic prompting that catches missing-context issues (entry doesn't feed any milestone, conflicts with cadence). /roadmap:cleanup output is more actionable (specific rule citations, not just heuristic warnings). The roadmap stays more coherent with less operator effort.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — As an operator running /roadmap:cleanup, I see DoPM-cited rule violations + recommendations.

**As** the user
**I want** As an operator running /roadmap:cleanup, I see DoPM-cited rule violations + recommendations.
**So that** Operator using /roadmap:add gets strategic prompting that catches missing-context issues (entry doesn't feed any milestone, conflicts with cadence). /roadmap:cleanup output is more actionable (specific rule citations, not just heuristic warnings). The roadmap stays more coherent with less operator effort.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
