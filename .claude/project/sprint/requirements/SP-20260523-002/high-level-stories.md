<!-- requirement-format-legacy -->
# High-Level Stories — Three-layer settings compiler — _warpos/settings/defaults.json source migration + wire compile.js into /warp:setup + /warp:update

**Sprint:** `SP-20260523-002`
**PRD:** `C:\Users\Vladislav Zhirnov\Desktop\Claude\Projects\WarpOS\.claude\project\sprint\requirements\SP-20260523-002\prd.md`

> High-level stories use the `H-N` id convention enforced by
> `scripts/hooks/requirement-format-guard.js`.

## H-1 — As an operator running /sprint:full to completion, my registry truthfully reflects sprint state without manual patching.

**As** the user
**I want** As an operator running /sprint:full to completion, my registry truthfully reflects sprint state without manual patching.
**So that** Sprint registry stays honest with actual completion state — operator can run /sprint:status and trust the output. The active-sprints.yaml registry is what other skills (/sprint:status, downstream tooling) read; manual patches today are a footgun.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-1`.

## H-2 — —

**As** the user
**I want** —
**So that** Sprint registry stays honest with actual completion state — operator can run /sprint:status and trust the output. The active-sprints.yaml registry is what other skills (/sprint:status, downstream tooling) read; manual patches today are a footgun.

Linked granular stories: see `granular-stories.md`.
Linked requirements: `R-2`.
