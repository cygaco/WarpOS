# Sprint Plan Report — SP-20260518-006

**Plan Contract:** `PC-20260518-0010`
**Created:** `2026-05-18T17:06:36.923Z`
**Status:** `needs_design`
**Next command:** `/sprint:design`

## Original Request (verbatim)

> A sprint:full command that goes through all sprint phases automatically, without a human needed to initiate each sprint command or to give approvals.

## Interpreted Intent

Add a meta-orchestrator skill `/sprint:full` that takes a single plain-language request, chains the existing sprint phases (plan → design → execute → release-prep → retrospective) without per-command operator input, and pre-authorizes the in-flight approval gates within a clearly bounded autonomy preset. Hard ceilings from CLAUDE.md autonomy table (push to remote, paid services, production deploy, destructive migrations, secret handling) remain non-bypassable regardless of preset. The skill is composition over duplication: it shells out to scripts/sprint/{plan,design,ticket,external-service,execute,release,retrospective}.js rather than reimplementing their logic. The goal is to remove the multi-prompt keyboard cadence the operator currently has to perform when running a small/medium sprint end-to-end, while preserving every existing safety property (crash-safe checkpoints, beta consultation, ralph 3-attempt rule, routing trace coverage).

## Desired Outcome

Operator types `/sprint:full "<request>"` once. The system writes a Plan Contract, scaffolds the requirements bundle, mints tickets, runs Ralph loops, prepares a release record, and writes a retrospective — without further keyboard input — UNLESS it hits a pre-declared halt condition (Class B/C decision outside preset, approval beyond preset's pre-authorization, Beta ESCALATE, production deploy, ESD requiring signup/billing/credentials, repeated-failure threshold). On halt, the skill writes a structured halt report and surfaces a single resume command. Day-to-day small/medium sprints complete in one invocation; risky sprints surface clearly at the right gate. Operator gets the leverage of the full sprint pipeline without the per-phase friction.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| .claude/commands/sprint/full.md (new) | assumed_from_request | New user-invocable skill body. Mode-aware (solo, adhoc; oneshot unsupported). Documents inputs, autonomy presets, halt conditions, recovery, approval gates, routing, relationship to mode. |

## Current Behavior

- Evidence level: `verified_from_repo`
- Sprint pipeline today requires 5+ explicit operator invocations: /sprint:plan, /sprint:design, /sprint:execute (per ticket or scope), /sprint:release, /sprint:retrospective. Each command stops at gates that require operator input (approval records, ESD setup, scope decisions). Beta is consulted per-skill in adhoc mode. The pipeline is crash-safe (per-phase checkpoints + ralph state) and routing-traced (auto-recorded in each helper). But there is no single entry point that drives the pipeline autonomously. Operator can use /loop with a sequence command but that does not encode policy or halt behavior.

## Desired Behavior

`/sprint:full "<request>" [--autonomy moderate|conservative|aggressive] [--scope minimal_safe|recommended|expanded] [--documentation-scale auto|xs|s|m|l|xl] [--mode solo|adhoc] [--sprint <SP-id>]` executes the full sprint pipeline in a single invocation. Phase transitions are automatic. Approval gates are auto-satisfied when within the preset's pre-authorization. Beta is consulted at the same gates as today; ESCALATE halts. Hard ceilings (push to remote, paid services, production deploy, destructive migrations, secrets) are never bypassed regardless of preset. On any halt, writes a structured halt report and surfaces a single resume command. On completion, writes a full sprint-full-report.md timeline.

## Scope

- Size: `l`
- Risk: `medium`

### Variants

- **Minimal safe:** Chain plan → design → execute → retrospective ONLY. Skip /sprint:release entirely; operator handles release manually. Single autonomy preset (moderate). No autonomy preset config file. Hard ceilings hardcoded inside full.js. ~250 LOC orchestrator, ~150 lines of skill body, ~80 lines AUTONOMY doc. Closes the 80% case (everyday small sprints) without touching the release surface (which is where most risk lives).
- **Recommended:** All 5 phases: plan → design → execute → release-prep → retro. Three autonomy presets (conservative/moderate/aggressive) in a config file under policy/. Operator can edit the file to extend pre-authorizations. Release phase auto-prepares the release record, runs check, records auto-approval when preset permits, but NEVER auto-deploys. Halt report + resume command on any halt. ~400 LOC orchestrator, ~250 lines of skill body, ~150 lines AUTONOMY doc + ~80 line schema + integration test harness.
- **Expanded:** All 5 phases + (a) auto-deploy gate with explicit operator pre-authorization via a per-environment policy file (.claude/agents/00-alex/.system/policy/auto-deploy-policy.json), (b) auto-promotion of retro action_items into a new sprint plan if operator opts in, (c) per-ticket Beta consultation roll-up so the orchestrator surfaces a single decision request rather than N. ~600+ LOC, larger autonomy surface, longer review cycle.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_expected`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_design`
- Confidence: `high`

## Resume Instructions

Sprint SP-20260518-001 has Plan Contract <PC-id>. Plan quality: needs_design (high confidence). Recommended scope: 'recommended' variant — all 5 phases, three autonomy presets, hard ceilings hardcoded, release-prep auto but never auto-deploy. Next: run /sprint:design --sprint SP-20260518-001 --documentation-scale m to scaffold PRD/HL/granular stories/COPY/INPUTS/TRACE/AC/QA/redteam/release-plan and mint tickets from granular_story_candidates. Beta should review (a) preset enumeration semantics, (b) hard-ceiling list completeness, (c) design-phase hand-edit quality risk, (d) cost-estimate halt gate threshold. If interrupted, read paths.sprintProgress#resume_command. Adhoc mode recommended for execution.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
