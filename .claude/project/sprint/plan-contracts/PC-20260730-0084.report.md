# Sprint Plan Report — S-VLADW1-02

**Plan Contract:** `PC-20260730-0084`
**Created:** `2026-07-30T02:27:09.080Z`
**Status:** `needs_user_clarification`
**Next command:** `/sprint:design (HELD — do not run until the three blocking operator gates clear: sibling repo name+slug+sign-off, the named >=3-repo corpus with written read-only authorization, and the API spend envelope)`

## Original Request (verbatim)

> S-VLADW1-02 (epic label SP-VLAD-W1-AUDIT) — candidate — Job #1: port lastmile detect/score/adapters into the engine; remove WarpOS refusals in the ported copy; intake fallback for undetectable stacks; adopt score.js as the ONE readiness number (checklist proxy not ported); receipt schema v1 from portfolio dogfood

## Interpreted Intent

Port the existing lastmile readiness-audit engine (detect, score, adapters) into the new Vlad product engine so Job #1 — a read-only readiness audit — runs end-to-end on a stranger repo and returns a receipt. Three things make this a port rather than a build: the WarpOS-specific refusals are edited out in the PORTED copy (canonical WarpOS assets are never modified); score.js is ADOPTED as the single readiness number rather than converged with the checklist proxy (J3); and the receipt schema's interior — left deliberately untyped by S-VLADW1-01 (J4) — is filled here from real dogfood data and minted as v1.

## Desired Outcome

A founder with a prototype points Vlad at their repo and gets back an honest readiness audit: what Vlad did, what it verified, and the one thing it needs. It degrades honestly — a repo with no tests is reported 'NOT verified', never scored as if it passed. This is the first Vlad capability that reaches a user and the evidence base for the receipt contract the rest of v1 depends on.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| The sibling Vlad product repo (all product-side files) | unknown | Does not exist. Name + slug + creation sign-off is operator gate #1 and the epic's first approval point. No path can be assigned yet, so every file-level claim in this contract is necessarily unlocated. |

## Current Behavior

- Evidence level: `verified_from_repo`
- Nothing exists product-side. The sibling repo is Verified Nonexistent as of 2026-07-29 (no path assigned; operator gate #1 unresolved). Inside WarpOS the registry entry, ROADMAP row and sprint stores exist for S-VLADW1-02 (current.yaml + progress.yaml only, both at their minted defaults); the tracker at trackers/sprints/S-VLADW1-02-vlad-audit-lastmile-port.md was authored separately because add-sprint.js does not scaffold one. No product code has been written and none may be before the design->build gate clears.

## Desired Behavior

Inside the Vlad engine, a read-only audit job runs against an arbitrary repository and completes without any WarpOS-specific refusal firing: it detects the stack (falling back to an intake flow when the stack is undetectable rather than failing), scores readiness through the single adopted score.js, and emits a receipt whose interior is now typed as v1 and derived from real dogfood observations. Exactly ONE readiness number exists in the product repo — score.js adopted, the checklist proxy absent, and the FOUNDERS_CHECKLIST-dependent dimension either re-sourced or reported as NOT SCORED. A build-failing enforcer refuses to let a second readiness number appear. Where the repo gives no evidence (no tests), the audit says 'NOT verified' rather than inferring a pass.

## Scope

- Size: `l`
- Risk: `medium`

### Variants

- **Minimal safe:** Port detect + score + adapters into the engine behind verified path references, strip the WarpOS refusals in the ported copy, adopt score.js as the sole readiness number with the checklist proxy deliberately absent, and ship the build-failing second-number enforcer. Run against ONE authorized repo. Leave the receipt interior at the untyped envelope ENGINE emits and do not mint v1.
- **Recommended:** The full sprint as scoped: port detect/score/adapters with every cited reference verified first; refusal removal in the ported copy; intake fallback for undetectable stacks; score.js adopted as the ONE readiness number with the checklist proxy not ported and the FOUNDERS_CHECKLIST-dependent dimension re-sourced or recorded NOT SCORED; a build-failing enforcer against a second readiness number; a run across the named corpus of >=3 authorized repos; and receipt v1 minted from that real data, filling the interior ENGINE leaves untyped.
- **Expanded:** Recommended, plus: broaden the corpus beyond three repos to stress the intake fallback across more stack shapes, and re-source the FOUNDERS_CHECKLIST-dependent dimension properly (a genuine stranger-repo signal) instead of degrading it to NOT SCORED.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `required`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_user_clarification`
- Confidence: `medium`

## Resume Instructions

Sprint S-VLADW1-02 is HELD at the plan->design boundary. Product-lead assessment carried forward: this sprint is better-evidenced than S-VLADW1-01 and its blockers are INPUTS, not unknowns — it clears to plan_quality=pass without re-authoring once the operator answers. Read trackers/sprints/S-VLADW1-02-vlad-audit-lastmile-port.md for the durable state and runtime/vlad-w1/w1-planning-inputs.md for the banked substance (§2 the product-lead contracts, J1-J4 the judgments). Do NOT begin product-side work: the sibling repo does not exist and the corpus is unauthorized.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
