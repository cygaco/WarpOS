# Plan Report — PC-20260531-0062 (SP-20260531-006)

**Sprint:** Guides skill suite — `guides:organize` + `guides:integrate` + `guides:coverage` (+ anchor backfill, registry, recording system).
**Mode:** adhoc (α + β + γ). **Path:** fast (Beta D1 DECIDE, conf 0.91) — sprint-tracked build, full state machine skipped, real plan-contract + honest done markers required.
**Risk:** low. **Size:** m. **Design required:** no (`execution_allowed_without_design: true`).

## Decision basis
Batched Beta plan-consult (D1–D6) returned DECIDE on all six (confidences 0.85–0.93); logged to `paths.betaEvents` as `EVT-sp20260531-006-batched-consult-d1..d6-001`. OPEN_ADR false on all.

## What shipped (verified on disk)
- **`scripts/guides/registry.js`** — single frontmatter parser + deterministic `_guides/registry.json` builder (`--check`/`--json`), shared by all three skills.
- **`scripts/checks/guides-coverage.js`** — fail-closed enforcer (6 invariants; exit 0/1/2, runner-error ≠ pass).
- **`.claude/commands/guides/{organize,integrate,coverage}.md`** — the three skills, registered in the catalog.
- **Anchor backfill** — all 7 guides carry valid contract frontmatter (anchor/shape/timing/lead_time), derived from the README when-to-do-it + order-of-operations map. `_guides/registry.json` generated (7 guides).
- **Integration (dogfooded)** — 6 anchored guides wired into `spinup.md` (DEV_SETUP @ day-zero) + `lastmile.md` (DATABASE/AUTH/PAYMENTS/EMAIL @ modules, PRIVACY_GDPR @ privacy gate); 6 markers + pointers inserted; 6 active records in `.claude/project/maps/guide-integration.jsonl`.

## Verification (two-state, proves non-vacuous)
- coverage BEFORE integrate → **FAIL exit 1** (6 INV-3 gaps — anchored-but-unwired).
- coverage AFTER integrate → **PASS exit 0** (7 guides · 6 anchored · 6 records · 6 markers).
- `registry.js --check` → fresh (idempotent). `path-lint` → 0 criticals. `framework-purity` → OK.

## Anchor map (operator-intent-grounded)
| Guide | anchor | shape | timing |
|---|---|---|---|
| DEV_SETUP | spinup:preflight | checklist | project-start |
| AUTH | lastmile:module/auth | walkthrough | at-module |
| DATABASE | lastmile:module/database | walkthrough | at-module |
| EMAIL | lastmile:module/email | walkthrough | at-module |
| PAYMENTS | lastmile:module/payments | walkthrough | at-module |
| PRIVACY_GDPR | lastmile:gate/privacy | notice | at-gate |
| README | none | notice | reference |

## Non-goals honored
No guide prose rewritten (frontmatter + structure only). guides:coverage NOT wired into scan:full (expanded variant, not asked — Beta D2). No push/tag/capsule (hard ceiling — deferred to operator, Beta D6).

## Landing
version.json → 0.12.1 (mid-release; capsule deferred). Both manifests + installed snapshot regenerated; version-coherence + manifest-honesty + views-fresh all green. Local commit only; **HALT before push**.
