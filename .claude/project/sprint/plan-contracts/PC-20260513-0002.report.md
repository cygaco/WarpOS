# Sprint Plan Report — SP-20260513-001

**Plan Contract:** `PC-20260513-0002`
**Created:** `2026-05-13T06:30:17.195Z`
**Status:** `needs_user_clarification`
**Next command:** `/sprint:plan (re-run after skill name confirmed) OR proceed to /sprint:design once name is decided.`

## Original Request (verbatim)

> A feature to 'bootstrap' a product from an idea, creating a thorough product brief. This is intended to be the first command run in a new project. The brief should capture the following concepts at minimum: problem solved, JTBDs, value chain analysis, competitive analysis, wedge, vision, wedge to full vision, and mvp. In the creation of the brief, there should be a brief discussion with the user. Use `ai-web-brief-v4` under _docs as inspiration. The final output should result in md, docx, and html versions of the brief. This process should be initiated by a skill. Confirm the skill name with me before building.

## Interpreted Intent

Add a new user-invocable skill that walks an operator through a short discussion to produce a thorough product brief covering at minimum: problem solved, JTBDs, value chain analysis, competitive analysis, wedge, vision, wedge-to-full-vision, and MVP. Skill emits MD/DOCX/HTML to a project-local docs path. Intended as the first command run in a new project. Skill name is unknown — user explicitly requested confirmation before any build step.

## Desired Outcome

A founder/operator running a fresh project gets a thorough, multi-format product brief in one sitting without having to re-derive structure each time.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| .claude/commands/<namespace>/<skill-name>.md (NEW skill file) | assumed_from_request | Skill name + namespace TBD per user. |

## Current Behavior

- Evidence level: `verified_from_repo`
- No bootstrap-brief skill exists. Closest analogue is /sprint:plan (request-level), and the reference brief lives at _docs/ai-web-brief-v4.{md,html}. No DOCX rendering pipeline in repo yet.

## Desired Behavior

Operator runs the new skill once near project start. Skill conducts a bounded discussion (≤8 AskUserQuestion turns), drafts every required section, and writes MD/HTML/DOCX outputs. Re-runs version the prior brief rather than silently overwriting.

## Scope

- Size: `m`
- Risk: `medium`

### Variants

- **Minimal safe:** MD-only output. Fixed 6-question discussion. No DOCX/HTML. Skill name confirmed first, single namespace.
- **Recommended:** MD + HTML output via templates. Adaptive 4–8 question discussion bounded by AskUserQuestion budget. DOCX via pandoc shellout if pandoc is on PATH, else a clear 'install pandoc to enable DOCX' message that still produces MD + HTML.
- **Expanded:** All three formats native (HTML via templates, DOCX via `docx` npm OR pandoc with fallback). Refinement loop with the user (review/redraft per section). Brief version history per project. Sibling helpers (/brief:rebuild, /brief:section).

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `possible`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_user_clarification`
- Confidence: `medium`

## Resume Instructions

Read plan-contracts/<PC-id>.yaml#open_questions.blocking. Surface the skill name question to the user. After confirmation, update the Plan Contract and run /sprint:design --sprint <this-sprint-id>.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
