# Sprint Plan Report — S-VLADW1-01

**Plan Contract:** `PC-20260730-0085`
**Created:** `2026-07-30T02:30:23.496Z`
**Status:** `needs_user_clarification`
**Next command:** `/sprint:design (HELD — do not run until the three blocking operator gates clear: sibling repo name+slug+sign-off, ratification of API-key-only model access with the onboarding-cliff judgment, and the API spend envelope)`

## Original Request (verbatim)

> S-VLADW1-01 (epic label SP-VLAD-W1-ENGINE) — candidate — Engine skeleton: plain-node Agent SDK app + MCP stdio server + 4-core tool surface + job state machine + journal + permission-level config port (turbo engine half) + API-key model-access seam via the TypeScript Agent SDK (the claude-CLI subscription shell-out is ToS NO-GO; the seam's residue is the credential-custody enforcer + an empirically characterized quota-exhaustion detector)

## Interpreted Intent

Build the substrate every later Vlad wave stands on: a plain-node application on the TypeScript Agent SDK that speaks MCP over stdio, runs jobs through an explicit state machine backed by an on-disk journal, reads a ported per-project permission-level config, and reaches a model through the user's OWN Anthropic API key. Two of the calls in this sprint are load-bearing far beyond it. First, the model-access seam: the ratified claude-CLI subscription shell-out is ToS-barred, so API-key is primary and ONLY, which makes a fail-closed credential-custody enforcer part of the build rather than a later hardening pass. Second, the receipt: ENGINE emits a versioned envelope with an UNTYPED interior (J4) and never validates or branches on it, because typing it here would propagate a v0 shape into the journal, get_status, the MCP signatures, the driver assertions and the ledger records before the dogfood data that defines it exists.

## Desired Outcome

Nothing user-visible ships in this sprint, and that is the correct shape: this is the engine, not a feature. What it buys is that every subsequent wave — the audit job, the write path, the agent face, the roadmap layer — has a real surface to attach to, exercisable end-to-end without a host, with the compliance-critical credential boundary already enforced in code rather than retrofitted. The founder-facing payoff arrives in S-VLADW1-02, which cannot start without this.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| The sibling Vlad product repo (all product-side files) | unknown | Does not exist. Name + slug + creation sign-off is operator gate #1 and the epic's first approval point. No product-side path can be assigned, so every file-level claim here is necessarily unlocated. |

## Current Behavior

- Evidence level: `verified_from_repo`
- Nothing exists product-side. The sibling repo is Verified Nonexistent as of 2026-07-29 (no path assigned; operator gate #1 unresolved). Inside WarpOS the registry entry, ROADMAP row and sprint stores exist for S-VLADW1-01 — which holds registry primary, the intended result of minting AUDIT first and ENGINE last — with current.yaml and progress.yaml at minted defaults; the tracker at trackers/sprints/S-VLADW1-01-vlad-engine-skeleton.md was authored separately because add-sprint.js does not scaffold one. The previously-recorded blocker that the epic asserted the ToS-barred seam as fact in four places is now VERIFIED CLOSED: all six cited locations were re-read on 2026-07-29 and each states the NO-GO correctly (epic tracker § Scope and § Open questions item 2; plan artifact § 3 Scope, § 6 Dependency map, § 7 Risk map, § 10 Gate W1), plus AC #1 repriced and the codex/gemini ToS item added. No product code has been written and none may be before the design->build gate clears.

## Desired Behavior

A plain-node application starts, registers as an MCP stdio server, and answers its four core tools. A job submitted through run_job moves through an explicit state machine (running / needs_input / proposing / done) with every transition checkpointed to an on-disk journal, so killing the editor mid-job leaves the repository clean and the next session's get_status names the interrupted job with resume and discard options. Job completion emits a versioned receipt envelope whose interior the engine treats as opaque bytes — journalled, returned and logged, never validated or branched on. Model calls go through the TypeScript Agent SDK using the user's own Anthropic API key, and no code path reads, logs, transmits, proxies or inherits an ambient ANTHROPIC_API_KEY: env passing to child processes is allowlist-based, and a fail-closed enforcer fails the build rather than reporting a warning if that boundary is crossed. A quota-exhausted or otherwise unrecognized termination classifies as could-not-run, never as success. A per-project config file selects a permission level, and at least ONE refusal is genuinely enforced in engine code rather than merely present in the vocabulary. All of it is exercisable through a host-free driver, and every surface says 'Vlad, powered by Claude'.

## Scope

- Size: `xl`
- Risk: `high`

### Variants

- **Minimal safe:** The plain-node Agent SDK app, the MCP stdio server with its four core tools, the job state machine, the journal writer, the untyped-interior receipt envelope, the API-key model-access seam WITH its fail-closed credential-custody enforcer, and the host-free driver. The driver is inside minimal_safe deliberately: without it none of the rest is exercisable end-to-end, so it is not a nice-to-have.
- **Recommended:** minimal_safe plus the permission-level config port done honestly — vocabulary, per-project config file, an in-code check, and exactly ONE genuinely enforced refusal — plus the branding guard with a named enforcer, and the quota-exhaustion detector empirically characterized against real terminations rather than specified from documentation.
- **Expanded:** Recommended plus the full permission action taxonomy with every level enforced across it, and the complete seven-tool MCP surface instead of four.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `required`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_user_clarification`
- Confidence: `medium`

## Resume Instructions

Sprint S-VLADW1-01 is HELD at the plan->design boundary. Per J1 this sprint is PLANNABLE, not blocked: roughly four-fifths of it (MCP surface, state machine, journal, memory, permission config, ledger discipline, branding guard, host-free driver) is unaffected by the gates, and 'blocked' should mean 'cannot author an honest plan', not 'execution cannot start'. It would flip to blocked only if the operator judges the API-key wall unacceptable for v1. The previously-recorded fourth blocker — the epic asserting the dead seam in four places — is VERIFIED CLOSED as of 2026-07-29 across all six cited locations in both artifacts. Read trackers/sprints/S-VLADW1-01-vlad-engine-skeleton.md for durable state and runtime/vlad-w1/w1-planning-inputs.md for the banked substance (§1 the ToS gate with verbatim primary-source quotes, §2 the product-lead contracts and J1-J4). Do NOT begin product-side work: the sibling repo does not exist and API-key-only is unratified.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
