# Sprint Plan Report — SP-20260528-001

**Plan Contract:** `PC-20260529-0057`
**Created:** `2026-05-29T06:14:15.749Z`
**Status:** `needs_design`
**Next command:** `/sprint:design`

## Original Request (verbatim)

> Make it /scan:issues and change the check: namespace to scan: and then make a scan:full that does all scans 'a full system scan'. if 'check:all' or something similar exists, extend that when you rename it. /sprint:full for this. then, start busting out the roadmap, and i want that test suite first

## Interpreted Intent

Migrate the entire check:* skill namespace to scan:* across canonical WarpOS, fold issues:scan into the new namespace as scan:issues, and promote the existing check:all aggregator to scan:full extended to run EVERY scan as one full-system-scan unified report. Treat as a cross-cutting literal rename governed by CLAUDE.md Refactor & Rename Hygiene (grep every occurrence of the old literal; a missed caller is the whole bug class).

## Desired Outcome

A single coherent scan: namespace with scan:full as the one-command full system scan; stable foundation + naming clarity before the 0.17.0 test-suite system is built on top of it. Reduces the instability the operator is feeling by making 'run all checks' a first-class, discoverable command.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| _warpos/commands/check/* (source-of-truth skill defs) | verified_from_repo | Rename dir -> _warpos/commands/scan/; move issues/scan.md -> scan/issues.md. |

## Current Behavior

- Evidence level: `verified_from_repo`
- ~32 check:* skills under _warpos/commands/check + compiled .claude/commands/check; check:all is a parallel runner producing a unified report; issues:scan lives in the issues: namespace. No scan: namespace exists.

## Desired Behavior

All check:* skills available as scan:* with identical behavior; issues:scan available as scan:issues; check:all promoted to scan:full extended to run every scan (all scan:* + scan:issues) as one full-system-scan report; deprecation alias shims resolve old high-traffic names during transition; zero stale check:/issues:scan literals remain except in history/changelog/ROADMAP-archive.

## Scope

- Size: `l`
- Risk: `high`

### Variants

- **Minimal safe:** Hard-rename source + regen views/manifest + update direct script/hook callers + grep-clean. scan:full is a thin rename of check:all. No alias shims.
- **Recommended:** Hard-rename + full caller sweep (scripts/agents/hooks/docs/manifest) + regen + grep-clean gate + deprecation alias shims for high-traffic skills (scan:full<-check:all, scan:install, scan:framework-purity, scan:framework-views-fresh) + scan:full EXTENDED to run every scan with one unified report.
- **Expanded:** Recommended + a generic alias-resolution layer so EVERY old check:* forwards, + a write-time guard flagging new check: literals, + coordinated downstream-product update.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_expected`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_design`
- Confidence: `high`

## Resume Instructions

Sprint SP-20260528-001 has Plan Contract PC-20260529-0057. Run the next_recommended_command. If interrupted, read .claude/project/sprint/sprint-progress.yaml#resume_command.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
