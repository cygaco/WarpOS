# Sprint Plan Report — SP-20260531-005

**Plan Contract:** `PC-20260531-0061`
**Created:** `2026-05-31T18:46:03.724Z`
**Status:** `needs_design`
**Next command:** `/sprint:design`

## Original Request (verbatim)

> another session is building out more _guides guides. make sure they are integrated with the bootstrap pipeline at the right spots, in the right shape. some portions should be walkthrough, some checklist, some just a quick notice, etc. the jtbd is to ensure the end user accomplishes the right thing at the right time so they dont end up with a completed product but a botched sso integration and waiting for dev account approvals or unreasonably stuck for some reason.

## Interpreted Intent

Wire the standalone _guides/ content into the bootstrap pipeline so each guide surfaces at the correct phase, in the correct shape, and at the correct TIME relative to its real-world lead time. The load-bearing JTBD is timing: long-lead external dependencies (Apple/Google/Play dev-account approval, SSO/OAuth app review, DNS) must be surfaced at project START (spinup), not discovered at last-mile — otherwise the user reaches a finished product and then stalls for days. Shorter-lead integration guidance (SSO wiring walkthrough, deploy checklist) surfaces at its relevant lastmile module/gate.

## Desired Outcome

A vibe-coder going idea -> on-screen -> paid never reaches 'product done' only to be blocked: the right guide (walkthrough | checklist | notice) appears at the right pipeline moment, started early enough to clear its lead time. Eliminates the launch-ready-but-blocked failure (botched SSO, pending dev-account approvals, stuck). Operationalizes ED-012 (do account signups at project START) + the pre-flight-blocker-clearing roadmap entry.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| .claude/commands/bootstrap/spinup.md | verified_from_repo | Pre-flight + Phase 1 (Intent) are the project-START anchor for long-lead, day-zero guides (dev-account signups). Currently references NO _guides content. |

## Current Behavior

- Evidence level: `verified_from_repo`
- _guides/ holds DEV_SETUP_GUIDE.md but the bootstrap skills reference it nowhere (grep: zero _guides/DEV_SETUP refs in .claude/commands/bootstrap/). spinup/lastmile have their own INLINE checklists (env-var, launch, security) but the standalone guides are not surfaced at any phase. So a user can complete spinup+lastmile without ever being pointed at the day-zero signup playbook -> the launch-ready-but-blocked failure.

## Desired Behavior

Each _guides/*.md declares its pipeline ANCHOR (which spinup/lastmile phase or module), SHAPE (walkthrough = interactive step-by-step; checklist = tickable items with a done-gate; notice = brief heads-up/warning), and TIMING (incl. real-world lead_time). The bootstrap pipeline surfaces each guide at its anchor in its shape: long-lead day-zero guides as a project-START notice/checklist in spinup Pre-flight/Intent; integration guides (SSO) as walkthroughs at the lastmile Auth module; platform/dev-account guides as checklists at the Platform module + human-approval gates. A scan enforces every guide is anchored + surfaced (no orphans). Content is authored elsewhere; this sprint wires placement/shape/timing.

## Scope

- Size: `m`
- Risk: `low`

### Variants

- **Minimal safe:** Hand-wire the existing DEV_SETUP_GUIDE.md into spinup Pre-flight/Intent (a project-START 'day-zero setup' NOTICE: start Apple/Google/Play signups now, they take days) + a lastmile Readiness-Audit reference. No contract; just two references to the one guide.
- **Recommended:** A guide-anchor CONTRACT (frontmatter: anchor, shape, timing, lead_time) + pipeline surfacing logic that reads it, so guides slot in by data. Wire DEV_SETUP (day-zero notice at spinup start) + map the new guides to anchors (SSO walkthrough @ lastmile Auth; dev-account checklist @ Platform + approval gate; etc.). Add /scan:guide-coverage (every _guides/*.md is anchored + surfaced; mirrors scan-coverage). Lead-time-aware: long-lead guides surface in spinup, not lastmile.
- **Expanded:** recommended PLUS: a guide registry/index, per-guide right-time GATING (e.g., lastmile execute refuses to proceed past the auth/deploy gate until the dev-account checklist is acknowledged), completion telemetry, and integration with the pre-flight-blocker pass (surface ALL ESD guides as one batched upfront operator-clearance step).

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_for_sprint`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_design`
- Confidence: `high`

## Resume Instructions

Plan Contract written for SP-20260531-005. Run /sprint:design to pin the guide-anchor contract + the anchor map (coordinate with the other session's _guides list). If interrupted, read .claude/project/sprint/sprint-progress.yaml#resume_command.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
