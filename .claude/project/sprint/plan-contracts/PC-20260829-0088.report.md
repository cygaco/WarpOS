# Sprint Plan Report — S-VLADW1-05

**Plan Contract:** `PC-20260829-0088`
**Created:** `2026-08-29T20:25:25.358Z`
**Status:** `design_complete`
**Next command:** `/sprint:execute`

## Original Request (verbatim)

> The class this sprint is aimed at, in β's words: coverage asserted at a coarser granularity than the mechanism has — 'the two scripts the fold DOES cover' asserted a script-level closure over a letter-level sample (CONFUSABLE_FOLD contains no lowercase l, n, g — three of Ceiling's seven letters). Three instances: S4-1a, S4-1b, S4-1c. Aim at the class, not the three sentences. || Why this sprint exists, stated plainly: S-VLADW1-04's fix attempt closed every finding gauntlet-1 raised, discharged S4-2(d) twice over, held four of six criteria, and grew the suite 339 -> 366 with every mutant guarded. It failed on the same class the sprint pair exists to close, one layer out: it authored new false sentences about its own mechanisms, and it left one dimension of the rendered-form property implemented beside the shared transform rather than inside it. The finding to carry above every process one: those sentences were in prose the conductor reviewed and passed, and one carried β's recommendation and α's approval. The approval chain is not a truth check. || Residual groups carried VERBATIM from the ruling: (1) S4-1a — confusable-fold calibration paragraph false: the fold is a 56-entry map (36/52 Latin skeleton letters); l n g etc. unmapped in both scripts; in-script homoglyphs of unmapped letters evade; the 'mistake-reachable scripts closed / remainder attacker-only' framing is false because unmapped letters are reachable by the paragraph's own accident vector. Fix shape: state coverage as the letter set actually mapped (or vendor a confusables table), never as 'scripts closed'; enforcer comment corrected to match. (2) S4-1b — 'two escapes REMAIN' false: ATX-heading-prefixed (and twelve other prefix shapes) bolded lead-ins silently skipped. Fix shape: refuse-not-skip must extend to heading/list/blockquote prefixes on the LEAD-IN path, and the escape count must be replaced by a named class, not a number. (3) S4-2(c) — emphasis canonicalization is not on the status-token path; **ASSERTED** — NOT VERIFIED plants green in Proven. Fix shape: move the emphasis fold INTO canonicalizeClaimText (or have containsStatusToken call flattenForAssertionScan) so both sites share one transform as G's comment already requires; near-miss battery gains emphasis-split status-token variants, controls first. (3b) S4-1c — the NOT-bound enumeration omits P1-P4 headings (an inverted P2 heading ships green): a coverage claim stated at a coarser granularity than the mechanism has, with an execution-proven evasion, inside a list the preamble introduces as 'said plainly rather than generalised.' Third instance of the S4-1a/S4-1b class. (4) Security lane MEDIUM — spawn-shim.js's split-secret worked example is false (fail-safe direction); qa MEDIUM — a third unnamed escape class; qa LOWs as filed. (5) agy fallback — the cross-family lane served on gemini-3.1-pro-high, not the pinned model (ED-230 class); noted, not counted against any criterion. (6) Successor-carried from fix attempt 1: the transform's description paragraph is UNBOUND (needs the fixture builder); 'unbound != re-wrap is free' (header substrings pinned across wraps); the task-4 assertion message ('reviewer read') in a non-shipping test; a text-matching enforcer cannot distinguish a violation from a description of one. (7) ED-340 remains OPEN, carried; ED-354 OPEN; ED-358 OPEN — this ruling is its second instance one layer out. New this sprint and cited: ED-360, ED-361, ED-362, ED-363; ED-257 amendment; ED-356 recurrence x3. (8) Process residuals for the retro: three brief-premise defects; the excerpt-window false HIGHs; ε's idle-while-polling pattern; ε's 'clean tree' reading a mid-write tree; the α/ε WarpOS commit race; usage-limit risk not realised.

## Interpreted Intent

Make the custody bind's coverage claims true at the granularity the mechanisms actually have; put the emphasis fold inside the one shared transform so both comparison sites receive it; bind the transform's own description; and re-run the S4-style gauntlet under a fresh pre-committed rule. Aim at the CLASS (coverage asserted coarser than the mechanism), not at the three known sentences.

## Desired Outcome

The shipped custody surface stops making claims about its own mechanisms that the mechanisms do not support. The predecessor failed twice on exactly this; the approval chain (β recommended, α approved, conductor reviewed) did not catch it, so the remedy has to be mechanical — emitted coverage sets and a transform that cannot silently stop being shared.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| engine/scripts/checks/custody-claim-lint.js | verified_from_repo | Verified at vlad wt/S-VLADW1-01-engine @6a105f2 (HEAD + engine/ clean re-confirmed by ε at mint time). β row 320 read at source: containsStatusToken (~954-956) puts both sides through the transform with NO emphasis handling — S4-2(c) confirmed. BLOCK_PREFIX (~612) covers list/ordered/blockquote only; headings #, |, <p>, quotes/parens/brackets, bullet, 'Note:' genuinely uncovered — S4-1b's third class real. CONFUSABLE_FOLD (~213-230): d/g/n/t are case-asymmetry gaps (uppercase partners mapped, lowercasing happens LAST at ~254 so an uppercase-only entry gives the lowercase letter no protection); R/r/l have no entry in either case and may be genuinely unmappable from the two covered scripts. THREE emphasis-handling sites, not two: flattenForAssertionScan (~1358) and resemblesBindableLeadIn TWICE (opener ~672, closing strip ~680). |

## Current Behavior

- Evidence level: `verified_from_repo`
- vlad wt/S-VLADW1-01-engine @6a105f2, engine/ clean, DELIBERATELY UNMERGED to vlad main. Suite 366/366, check:ship exit 0, check:pointers exit 1 by design (34/48, outside check:ship). The three S4 defects are live at this commit and three of them were re-verified at source by β at row 320.

## Desired Behavior

One shared transform reaches every comparison site that needs it, with the asymmetry between sites handled explicitly rather than by a comment; coverage claims state the letter set / prefix class / paragraph keys the mechanism actually enumerates, emitted from the data rather than hand-typed; escape counts are replaced by named classes; the transform's own description is bound; and the four un-audited shipped files are disclosed by name with what each lane actually said.

## Scope

- Size: `l`
- Risk: `high`

### Variants

- **Minimal safe:** S4-2(c) fix + the three S4-1 sentences rewritten at mechanism granularity.
- **Recommended:** PINNED BY β ROW 317. minimal + refuse-not-skip on all lead-in prefix classes + bind the description paragraph via the fixture builder. Bundles M, N, O, P, Q in that order; CUSTODY.md never has two editors; P runs LAST because its input is O's emitted set.
- **Expanded:** RULED OUT by β row 317 (DECIDE 0.89): + a vendored Unicode confusables table. 'A vendored table has its own version and curated ceiling — it relocates the discipline.' If vendored later, S5-2 applies unchanged: name the version and the ceiling.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_expected`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `design_complete`
- Confidence: `high`

## Resume Instructions

Sprint S-VLADW1-05 has Plan Contract PC-20260829-0088. Run the next_recommended_command. If interrupted, read .claude/project/sprint/sprint-progress.yaml#resume_command.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
