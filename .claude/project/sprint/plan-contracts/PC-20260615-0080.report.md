# Sprint Plan Report — SP-20260615-002

**Plan Contract:** `PC-20260615-0080`
**Created:** `2026-06-15T16:22:28.943Z`
**Status:** `pass`
**Next command:** `/sprint:design --sprint SP-20260615-002 --documentation-scale m`

## Original Request (verbatim)

> That was a mistake on your part. I explicitly asked for a nice visual panel. /sprint:full to get it done; and I want it to look nice, and be interactive. I need info on current ongoing sprints, the roadmap, epics, and sprint breakdown.

## Interpreted Intent

Replace the v1 text-only /panel:roadmap with a real browser GUI panel (like /models:router's Dispatch Console): visually polished + interactive, surfacing the four data areas the operator named so they can SEE the portfolio state at a glance and drill in.

## Desired Outcome

The operator runs /panel:roadmap and a clean, modern, interactive panel opens in their browser — active sprints, the prioritized roadmap, epics with progress, and the ticket breakdown per sprint — so 'what's next / where are we' is a visual glance + drill-down, not a text dump and not asking.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| scripts/panel/roadmap.js (extend → shared data aggregator) | verified_from_repo | Already parses ROADMAP §Prioritized + TRACKER + active-sprints.yaml + open-gaps with --json + fail-soft. Extend it to ALSO aggregate epics (trackers/epics/*) + per-sprint ticket breakdown (sprint records). Stays the read-only, fail-soft data layer + the --text fallback. |

## Current Behavior

- Evidence level: `verified_from_repo`
- /panel:roadmap (v1, SP-20260615-001) prints a static TEXT board to stdout — functional but NOT the visual panel requested. The other panels (/panel:models → Dispatch Console gui.js, /panel:admin → browser preview) DO open the browser; /panel:roadmap does not. No epics or per-sprint ticket breakdown in the v1 board.

## Desired Behavior

/panel:roadmap opens a polished interactive panel in the browser. It shows: (a) current ongoing/active sprints (id, status, phase, ticket counts); (b) the roadmap (prioritized do-next order + items/sections); (c) epics (state, % complete, child sprints); (d) per-sprint ticket breakdown (tickets + statuses). The operator can filter, expand/collapse, drill into a sprint/epic, and click an item for detail. Read-only (ROADMAP stays the source of truth). It genuinely LOOKS NICE (design-quality + visual-review gauntlet pass). The v1 text board remains as --text + the shared data layer.

## Scope

- Size: `m`
- Risk: `medium`

### Variants

- **Minimal safe:** Serve the existing 4-section text board as HTML in the browser (minimal styling).
- **Recommended:** Full: extend the aggregator (epics + ticket breakdown), a gui.js-pattern server, a polished interactive frontend (cards/columns/progress/drill/filter) for all 4 data areas, /panel:roadmap rewired to open it, design-quality + visual-review gauntlet pass.
- **Expanded:** Recommended + edit-from-panel (reorder/mark-done writing back to ROADMAP/TRACKER).

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_expected`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `pass`
- Confidence: `high`

## Resume Instructions

Product-lead + design-lead author the requirement bundle + visual direction for SP-20260615-002 (the browser GUI roadmap panel) in design; then mint tickets per the five workstreams, build (frontend + backend builders), and run the design-quality/visual-review gauntlet against the running served page before close.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
