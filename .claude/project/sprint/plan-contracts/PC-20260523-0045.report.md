# Sprint Plan Report — SP-20260525-011

**Plan Contract:** `PC-20260523-0045`
**Created:** `2026-05-23T08:48:19.171Z`
**Status:** `needs_design`
**Next command:** `/sprint:design`

## Original Request (verbatim)

> events.jsonl crosses ~6MB in real-world usage. sleep:deep handles compaction manually today. Automate: rotate above threshold (e.g. 10MB), compress old segment to .gz, keep last N rotations.

## Interpreted Intent

paths.eventsFile (events.jsonl) is append-only and grows monotonically. At 6MB it's already large; future growth degrades read performance for /events:tail + /events:query + smart-context's append. Add a rotation policy: when events.jsonl > 10MB, rotate to events.jsonl.<ISO>.gz; keep last 5 rotations; clean older. Triggered on append from logger.js OR via periodic check skill.

## Desired Outcome

events.jsonl stays bounded in size. /events:tail / /events:query / smart-context loaders stay fast. Operator stops needing to manually compact via /sleep:deep.

## Affected Surfaces

| Surface | Evidence Level | Notes |
|---|---|---|
| scripts/lib/logger.js | verified_from_repo | Add rotation check before append: if file > threshold, rotate; then append. |

## Current Behavior

- Evidence level: `verified_from_repo`
- events.jsonl grows unboundedly. /sleep:deep manually compacts via aggregation. No automatic rotation.

## Desired Behavior

(1) logger.js checks file size on append; rotates above threshold. (2) Rotated files are gzipped + timestamped. (3) Last N=5 rotations kept; older deleted. (4) /events:tail + /events:query include rotated archives in their scan (or have a --no-archives flag for current-only). (5) Tests cover rotation + cleanup. (6) Local commit. Push operator-scoped.

## Scope

- Size: `s`
- Risk: `low`

### Variants

- **Minimal safe:** Rotation only (no gzip, no cleanup) — when > threshold, rename to events.jsonl.<ISO>, start fresh.
- **Recommended:** minimal_safe + gzip rotation + N-keep cleanup + /events:* skills handle archive scanning + atomic-swap safety for concurrent writers.
- **Expanded:** recommended + structured archive index (events.archive-index.json) so /events:query can target specific time ranges without decompressing every archive + retention policy config in .claude/paths.json or settings.local.json.

## Assumptions

- Safe: see Plan Contract.
- Unsafe: see Plan Contract.
- Needs review: see Plan Contract.

## Open Questions

- Blocking: see Plan Contract.
- Non-blocking: see Plan Contract.

## Non-Goals

See Plan Contract.

## Requirement Areas

See Plan Contract.

## Candidate Stories

- High-level: see Plan Contract.
- Granular: see Plan Contract.

## Workstream Candidates

See Plan Contract.

## External Service Dependencies

Status: `none_expected`

See Plan Contract.

## Approval Boundaries

See Plan Contract.

## Beta Review

Required: `true`

## Plan Quality

- Status: `needs_design`
- Confidence: `high`

## Resume Instructions

Re-run /sprint:design (idempotent) or /sprint:plan to re-plan.

---

This report is the human-readable companion to the durable Plan Contract
YAML. The YAML is the source of truth; this report exists for quick
review.
