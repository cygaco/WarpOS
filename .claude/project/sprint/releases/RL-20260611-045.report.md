# Release Report - RL-20260611-045

**Sprint:** `S-PF-01`
**Title:** S-PF-01 W0 telemetry seam
**Version:** ``
**Status:** `preparing`
**Deployed at:** not deployed

## What shipped

S-PF-01 adds the scaffold-time telemetry seam:

- `framework/templates/app-scaffold/src/lib/telemetry/{events,sink,track,chain}.ts.tmpl`
- layout/page wiring for activation provenance and one explicit core-loop telemetry example
- optional PostHog env names with no-op behavior when unset
- lastmile analytics canonical-base/enrichment split
- scaffold coverage checks and planted false-green regression tests
- regenerated framework and `_warpos` manifests

## Tickets included

| Ticket | Status |
|---|---|
| `T-20260611-326` | done |
| `T-20260611-327` | done |
| `T-20260611-328` | done |
| `T-20260611-329` | done |
| `T-20260611-330` | done |
| `T-20260611-331` | done |

## Issues resolved / deferred

No linked issue records. Prior release-prep blocker `BC-02 Manifest coverage/honesty drift` was resolved by regenerating `_warpos/MANIFEST.json`; `scripts/testsuite/enforce.js` then reported 0 new regressions.

## External services

No external service setup is required. PostHog env names are optional; the scaffold sink resolves to no-op when unset.

## QA / Red-team Results

- QA: passed via focused S-PF regression sweep, scaffold coverage, scaffold materialization smoke, scaffold app tests, lastmile orchestrate tests, and manifest validators.
- Red-team: passed via planted false-green fixtures for missing seam, duplicate raw sink, event vocabulary drift, and unresolved activation predicate. No independent external reviewer has been dispatched yet; routing evidence is recorded as `single_vendor_session`.

## Approval

- Required: `false` in `S-PF-01/current.yaml`
- Recorded: none
- Deployment: not performed

## Migration / Rollback

- Migration plan: no data migration; template-only framework change.
- Rollback plan: revert `a404f02` and this release bookkeeping, regenerate manifests, rerun scaffold coverage and manifest validators.

## Post-release Monitoring

- Re-run `node scripts/checks/scaffold-coverage-scan.js --json` after any scaffold template edit.
- Re-run `node tests/regression/S-PF-01/manifest-shipping.test.js` after manifest regeneration.

## Learning Candidates

- S-PF-01 closed a scaffold telemetry false-green class by making the enforcer execute planted bad fixtures.
- Release gating depends on `_warpos/MANIFEST.json` after sprint bookkeeping as well as after framework template edits.

## Resume / Rollback Instructions

Release is prepared but not deployed. Resume by obtaining explicit release/deploy approval if deployment is desired, then run the release approval/deploy commands. Rollback by reverting `a404f02` plus the release bookkeeping commit and regenerating manifests.
