# Retro Addendum — SP-20260531-006 (2026-06-01)

> The sibling `retro.md` is the auto-generated **light/skeleton** retro (Beta D8: light idempotent auto-retro now, **deep retro deferred to milestone close** per RI-001). This addendum fills the skeleton's `<TO FILL>` sections with the real outcomes. (History is append-only; the skeleton is preserved as-generated.)

## Summary
Completed the guides skill suite on the already-shipped `guides:write` contract. Built `guides:organize` + `guides:integrate` + `guides:coverage`, a shared registry engine, and a fail-closed coverage enforcer; backfilled the guide-anchor contract onto all 7 guides (which carried none); dogfood-ran the suite end-to-end. **Fast path** (Beta D1 DECIDE, conf 0.91): real plan contract (PC-20260531-0062) + light verification; full 4-phase state machine skipped. Landed at version 0.12.1 (mid-release); push/tag/capsule deferred to operator (hard ceiling, Beta D6).

## Shipped (recommended variant — all of it)
- `.claude/commands/guides/{organize,integrate,coverage}.md` (3 skills, catalog-registered)
- `scripts/guides/registry.js` (frontmatter parser + deterministic `_guides/registry.json`)
- `scripts/checks/guides-coverage.js` (fail-closed enforcer; 6 invariants; runner-error ≠ pass)
- Anchor backfill onto 7 guides + `_guides/registry.json`
- Recording system `.claude/project/maps/guide-integration.jsonl` (6 active records)
- Dogfood: 6 markers wired into `spinup.md`/`lastmile.md`; coverage **fails-closed before integrate, green after**

## Plan quality / scope
- Predicted: m / low-risk / recommended variant → **matched**. No scope creep (expanded variant — scan:full wiring + commit guard — correctly NOT done, Beta D2). No guide prose rewritten (frontmatter + structure only).

## Friction
- **[low / process]** Hit **RI-002** during the land: bumping `version.json` to 0.12.1 required manually regenerating both manifests + snapshot-installed + fixing `manifest.json`/`install.ps1` version stamps before `version-coherence` passed. Recoverable, but it's the documented fresh-minor manual-regen debt.
- **[low / tooling]** `maps-coverage` flagged `guide-integration.jsonl` as an orphan map file (it lives under `.claude/project/maps/` per the DUMP design but is a recording store, not a `/maps:*` output) → allowlisted with a reason.

## Action items
- **Fix RI-002 in a focused release-hardening sprint** (Beta D7 DECIDE-defer, conf 0.91): `release-canonical.js` should, after stage-6 build-capsule, regen BOTH manifests + run `snapshot-installed.js` + bump `manifest.json`/`install.ps1` version stamps **before** stage-7 gates. Release-engine code → own sprint + test coverage. _(owner: alpha)_

## Beta decisions reviewed
D1–D6 (batched plan-consult) all DECIDE; D7 (defer RI-002) + D8 (light retro now) DECIDE. Logged to `paths.betaEvents` (`EVT-sp20260531-006-*`). OPEN_ADR false on all.

## Learning candidate
The "contract defined but not applied" drift class (a guide-anchor contract that no guide carried, no registry, nothing wired) is exactly what a coverage enforcer must fail-closed on — shipping `guides:coverage` before the anchors existed would have been a vacuous green. Build the data + the enforcer together; prove non-vacuous with a before(fail)/after(pass) demonstration.
