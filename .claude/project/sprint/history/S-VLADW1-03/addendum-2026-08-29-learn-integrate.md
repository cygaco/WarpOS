# S-VLADW1-03 — addendum 2026-08-29: learning_candidates promotion record

`retro.yaml` is append-only under the sprint-history guard, so the promotion status of its six
`learning_candidates` is recorded here rather than by editing the candidate blocks in place.
All six were promoted by `/learn:integrate` on 2026-08-29. None were skipped.

| # | Candidate (opening words) | Promoted to | ED |
|---|---|---|---|
| 1 | "Watching a control fire tells you nothing about what it cannot see…" | `paths.reference`/`reasoning-frameworks.md` § *Confirmation is not coverage* — preamble + clause 1 | ED-358 (cited, open) |
| 2 | "A detector that SKIPS a non-matching input is fail-open…" | same § clause 2; `/enforcement:log` skill body § *Before you accept an enforcer as closing the debt* item 1 | ED-358 (cited, open) |
| 3 | "Binding a claim byte-for-byte makes it un-editable, not true…" | same § clause 4 (incl. the atomic claim-text + canonical-copy corollary from the S-03 action items) | — |
| 4 | "A dispatch record and the worktree are two independent witnesses…" | `paths.agentDispatchGuide` § *Window budgeting and dead-dispatch triage*, item 3 (two-witness discriminator) | ED-353 (cited, open) |
| 5 | "A cross-family reviewer is not redundancy…" | `paths.agentDispatchGuide` §3, blockquote under *Cross-provider diversity is mandatory* | — |
| 6 | "Naming a lane's own prior failure mode in its brief changes its behaviour…" | `paths.agentDispatchGuide` §6 *Brief content contract*, clause 3 | **ED-378** (new) |

**New enforcement debt filed:** ED-378 (brief must carry a DISPATCH NOTE naming the lane's own prior
failure mode + the builder's standing permission to refuse a premise). Candidates 1–5 either cite
existing open debt (ED-353, ED-358) or land as reasoning discipline whose enforcement half is already
tracked; no duplicate ED was filed.

Written by `/learn:integrate`. The candidate blocks in `retro.yaml` are unchanged — the guard's
refusal to edit a closed sprint record is the reason, not an oversight.
