# Agents / Roles / Instruction Files — Discovery Report (disc-roles, 2026-07-09)

## Census
**34 roles** in `.claude/agents/_org/role-registry.json` roles{} (:29-69). By tier: **5 faces** (α/β/γ/δ/ε, all `claude`, singletons, home `president`) · **3 directors** (product·openai, engineering·claude, growth·openai) · **10 leads** (product/design·openai; quality/frontend/backend/security/research/copy/conversion/marketing·claude) · **12 workers** (builders/fixers·claude·`build_chain:true`; frontend/backend-reviewer·openai; qa-reviewer·openai; security-reviewer·gemini; design-quality+visual-review·claude-pinned) · **4 tools** (test-runner·sonnet, skeleton-builder·sonnet, ops-analyst·openai, cabinet·openai).
Provider split: **~24 claude · 9 openai · 1 gemini** (security-reviewer `:57`, primary gemini + openai `second_pass` + claude `third_pass`).
**Fallback:** every openai/gemini role carries `"fallback":"claude"`; native-claude roles omit it.
**claude_pinned (2):** design-quality (:42), visual-review (:43). **Justified, not inertia** — both drive Playwright-MCP visual judgment; independence is the *binding verdict* (grade, don't author), so provider-diversity is waived by a documented `carve_outs` block (:15-19). test-runner is claude-but-unpinned (low-reasoning Sonnet tool, no flag).

## Parity (real output)
`node scripts/checks/role-parity-scan.js` → **exit 0**: `OK [role-parity] org map ↔ dispatch catalog ↔ team-guard agree (role universe coherent)`.
`consumers_to_rewire` (registry :82-96) — **most are now rewired** to `registryRoles.deriveOrFallback(...)` with loud-fallback literals as recovery nets:
- ✓ `catalog.js:312/320` (PROVIDER/EFFORT) · `providers.js:452` (AGENT_PROVIDERS) · `state.js:64/74` (FLAGSHIP_OPENAI/GEMINI) · `dispatch-route-guard.js:142` (BUILD_CHAIN) · `org-roles.js:74/101/243` (REMEDIATION/buildChainDoer/domainRoles).
- **Still pure literal (residual risk):** `state.js:73 MINI_OPENAI_ROLES=["qa"]` (qa is scrapped) · `org-roles.js:38 STATIC_GAMMA_ONLY_AUGMENT` (intentional) · `scope-contract-guard.js:73` hardcodes `qa-reviewer|compliance|req-reviewer|qa` regex.
- **Not re-verified from source (flag for rebuild audit):** `store-validator` heartbeat enum · `GAMMA_RESULT/DELTA_RESULT` schema gate_check field names · the agent-dispatch-guide role→provider table.
- **No `role-aliases.json`** — aliasing lives in code at `scripts/hooks/lib/role-aliases.js` (`normalizeRole`: redteam→security-reviewer, qa/compliance→qa-reviewer).

## Identity binding today
Bound in **three uncoordinated places**: (1) root `CLAUDE.md:5` "You are **Alex** — the **President**"; (2) each agent spec — frontmatter `name:` + body prompt (`alpha.md:12`; `builder.md:23` "You are a Frontend Builder agent"); (3) `scripts/mode-set.js` writes `runtime/mode.json` but sets *mode only*, not identity (advisory posture banner, :39-47).
**Alpha-poison CONFIRMED, no mechanical counter.** Build-chain workers spawn via `dispatch-claude.js:332` = `claude -p --agent <role>`. The `--agent` spec is the worker's system prompt (the *only* counterweight), but root `CLAUDE.md` carries "IMPORTANT: These instructions OVERRIDE any default behavior" and loads as project context, so a headless worker still ingests "You are Alex." **No `no-root-alpha-poison` enforcer exists** — grep over `scripts/` returns zero checks. Classification: **PROSE** (spec-body override only) → a rebuild need.

## Provider-neutrality
Registry cleanly separates `provider`/`model`/`effort`/`fallback` as first-class per-role fields (+ `second_pass`/`third_pass`), so a Codex/Antigravity runtime is **partially** drivable from the registry *today* — cross-provider roles already route by these fields through `dispatch-agent.js`, which builds its OWN prompt and never loads the `.md`. What is **NOT** neutral: the spec `.md` frontmatter is Claude-harness-specific — `tools:`, `disallowedTools:`, `isolation: worktree`, `permissionMode:`, `maxTurns`, `model:` (duplicated from registry), `color`. These bind only on the Claude `--agent` path. So identity/model/effort are separable, but the RoleSpec *body* has **no consumer for a non-Claude runtime**.

## class_derivation
`.claude/agents/_org/dispatch-contract.json:68-86` derives a dispatch CLASS from registry attributes via ordered first-match rules, then merges `defaults ← class ← role_overrides`. Classes: face · build_chain_worker · claude_pinned_reviewer · cross_provider_reviewer · cross_provider_consult_lead · manager · tool_claude · tool_cross_provider (`fallback_class:manager`).
**The `{tier:director, provider:openai}` gap is CLOSED**: rule `:77` routes it to `cross_provider_consult_lead` (added E-DISPATCH-PERFECT W2, β DECIDE 2026-06-17). Claude directors fall to `manager` via `:78`. Worth confirming during rebuild that no OTHER under-routing survives.

## Rebuild needs
1. **RoleSpec schema migration (registry → typed YAML rolespecs):** promote each `roles{}` entry to a typed RoleSpec with a **runtime-neutral core** (`role_id`, `home`/`sub_home`, `tier`, `kind`, `build_chain`, `binding_verdict`, `claude_pinned`, `dispatchable_by`) separated from a **binding block** (`provider`/`model`/`effort`/`fallback`/passes) and a **harness-adapter block** (today's Claude `.md` frontmatter). Un-duplicate `model` (registry vs `.md` frontmatter — two sources). Migration is LOW-RISK: `registry-roles.js` is already the single derive point + every consumer carries `deriveOrFallback` + loud-fallback literals — flip readers to the YAML source behind the green parity gates, cut literals last (`test-registry-roles.js` asserts derived == literal per consumer).
2. **Binding-order (explicit > WorkOrder.role > mode > runtime > default alex-alpha):** NO such resolver exists today — identity is spec-body prose. Implement as a pure `resolveRole(ctx)` beside `registry-roles.js`, consumed by dispatchers. `mode.json` supplies the mode tier; dispatch-contract `mode_profiles` (:199) already narrows *shape* per mode and can host the mode→role binding.
3. **no-root-alpha-poison check:** net-new. `scripts/checks/` scanner asserting (a) no non-face spec body inherits "You are Alex"/President identity, and (b) the compiled headless-worker context (root CLAUDE.md + `--agent` spec) resolves to the WORKER's role, not Alpha. Pairs with the instruction compiler (AGENTS.md/CLAUDE.md/CODEX.md/GEMINI.md from one source) — **GEMINI.md is ABSENT today**; CODEX.md exists; AGENTS.md + AGENT-STRUCTURE.md are hand-maintained, snapshot-dated 2026-06-05, drift-prone.

**Key files:** `.claude/agents/_org/role-registry.json` · `.claude/agents/_org/dispatch-contract.json` · `scripts/dispatch/registry-roles.js` · `scripts/dispatch/org-roles.js` · `scripts/dispatch-claude.js:332` (identity-injection site) · `scripts/hooks/lib/role-aliases.js` · `scripts/checks/role-parity-scan.js` · `scripts/mode-set.js`.
