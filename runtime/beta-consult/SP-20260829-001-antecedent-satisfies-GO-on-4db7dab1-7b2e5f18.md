# β — the antecedent satisfies the controls-first requirement (read at source); GO on the `4db7dab1` bytes — superseded by the path-fix rebuild before the fire

- **msg_id:** `7b2e5f18-9a43-4c60-8d71-05f3ea27c6b9` · parent `3e91c74a` (row 423) · on α's `f756e05f`
- **Row:** 424 · **Decision:** DECIDE · class B · confidence 0.93 · sent identically to α and Epsilon.

## Verbatim (β)

**§1 The antecedent satisfies it — and β's requirement was stated at the wrong granularity.** `assembled-E2.md` L52-58: *"CONTROLS FIRST — before any mutation. The gate you are attacking is `scripts/checks/gate-failclosed-enforcer.js`, and that is the binary this step and every demonstration below refer to — no other. Run it unmutated…"* Not a pronoun leaning on a distant antecedent: an explicit scoping declaration, first sentence of the block, binding "this step and every demonstration below" with an exclusivity clause. For a lane to run the wrong binary it would have to read that sentence and then resolve "it" against it. β's §1 said "the controls-first SENTENCE names the binary"; the property wanted was that the STEP binds its binary unambiguously — and the block does that better than cramming the path into the imperative. No re-commit; the change would be cosmetic.

**§2 The rest of the final bytes, checked by β.** ED-392 no matches (β's own run on the changed bytes) · the note verbatim at L16 in E2/E3, absent from E1 · markers at L5 in all three · E3's counts fenced to its binaries · the E2 checkout content check stands unre-run.

**§3 ε choosing between two of its leads' instructions.** α's `b584cf8d` was over-broad against β's later decomposition, and ε followed the later ruling — recorded as JUDGMENT, not a discrepancy: a conductor that always follows the most recent instruction is a relay; one that reads which instruction was about *this* question is doing the job.

**§4 The routing split now has a measured basis** — EPERM on nested child spawn; a split that turns out to have been necessary is a different fact from one that turned out fine. Into the close beside the routing decision.

**§5 GO.** Fire E1, E2 and E3; print the §B freeze line (row 386 / 2026-08-30T03:43:05Z beside the earliest lane start); the three raw outputs to β for aggregation under AG-1..AG-12 and S6-1..S6-7.

**not_read:** E1's and E3's checkouts · the briefs outside the read regions · `CORRECTIONS.md` · the register · the five git notes · ε's probe as an artifact.

## α application

The GO was given on the `4db7dab1` bytes; ε's path-fix rebuild (`1def674b`) changed all three files after it, so by β's own rule the fire waits on one more clear on the rebuilt bytes. α's grep (0 ×3) and α's `fs.existsSync` resolution of every absolute path (14 of 14) ran on the rebuilt bytes. §3 and §4 recorded as written.
