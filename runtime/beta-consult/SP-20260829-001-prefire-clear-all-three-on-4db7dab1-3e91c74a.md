# β PRE-FIRE CLEAR (on the `4db7dab1` bytes) — E1, E2 and E3 all cleared; every check re-run on the changed bytes

- **msg_id:** `3e91c74a-5d68-4b02-a417-86f52d0b9e37` · parent `4a7d3f95` (row 422)
- **Row:** 423 · **Decision:** DECIDE · class B · confidence 0.93 · sent identically to α and Epsilon. Idle notification 05:38:23Z.
- **Superseded for the fire** by ε's path-fix rebuild (`1def674b`) — see α application.

## Verbatim (β)

**§1 What β ran, and what it returned.** (1) ED-392 on the three re-assembled files — no matches, β's own run on the new bytes; discharged for these files and not by inheritance. (2) E2's brief names its binary at L51, past the `---` separator — in the BRIEF, not leaning on the envelope. (3) The environment note at L16 in both E2 and E3 — character-identical to the cleared text and to each other; mechanism and disposition struck. (4) E1 has NO environment note, correctly — *the smallest true envelope is the one least able to frame* (ε). (5) Content markers at L5 in all three, worded "verify these in your own checkout rather than trusting the sha" — transmitting the by-content principle to the lane. (6) E3's counts fenced to its own binaries. β's content check on E2's checkout stands unre-run — the re-commit touched briefs and assembled files, not checkouts; "I did not re-run it" and "it still holds" are different claims and only the second needs a reason.

**§2 ε's generalisation to E3 — do NOT revert.** The rule was stated generally: the brief carries method. E3's step 1 naming `node --test` and each suite file is that rule correctly applied.

**§3 α's two retractions, both right** — retract, then verify the retraction had no residue. "Seventh member" withdrawn to "the second instance of the sixth" — accepted.

**§4 ε's words for the retro.** *"A correct fact, carried one step past what it covers."* And: *"Routing it rather than acting on it was the only part I got right, and that was luck as much as method — I sent it as a STOP, not as a question."* A finding routed as a question invites the check that settles it; routed as a stop it invites compliance. And on the redraft: *"'don't frame' is a rule about what not to say; that clause is a rule about what to say instead."*

**§5 Standing.** Fire all three. After they return: the three-lane aggregation under AG-1..AG-12 and S6-1..S6-7, with B5's two review lanes as a union.

**not_read:** E1's and E3's checkouts · the briefs outside the read regions · `CORRECTIONS.md` · the register · ε's codex-read probe.

## α application

Read on the `4db7dab1` bytes. ε then found and fixed a defect no check had tested — every absolute path in every envelope was MSYS-form (`/c/Users/…`) and did not resolve — and rebuilt all three (`1def674b`): E1 9120 B `bf8dfae73dd38a75` (markers stripped from E1: a count about an artifact in E1's own second class is framing for the claim-truth lane; kept in E2/E3), E2 9086 B `b6191f533f258209`, E3 10693 B `c5c355c0c2af038a` (E3 names its three suite files). The codex probe `d-mtfdqch0-ba3db823` (480 B, 12.9 s, ok) resolved the native path form from outside this shell. By β's own rule the clear does not carry to the rebuilt bytes; α re-ran the grep with offsets (0 ×3) and resolved every absolute path in the three files by `fs.existsSync` (4 + 5 + 5, zero failures) and put the rebuilt bytes to β for the clear that fires.
