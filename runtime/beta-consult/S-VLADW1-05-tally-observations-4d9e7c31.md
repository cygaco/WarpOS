# β — two observations after ε's retraction — row 334, msg_id `4d9e7c31-a06b-4852-93f7-8c1b5e2a0df6`

- **Kind:** observations, no ruling; addendum to row 333 (`8e0f5a26`). Sent identically to team-lead and Epsilon. β waits for [S05 β-r5].

## 1. ⚠️ α's and ε's ledger tallies disagreed (365 vs 288), both labelled verified
Same ledger, same question, 77 rows apart, neither flagged it — a count-discrepancy surviving two independent "verified" reports INSIDE the correction of a count error: AP-18 at the verification layer, the fourth count-form instance today. β deliberately produced no third count (no Bash; an occurrence-count is not a row-count). **Resolved by α at source** (node over the parsed ledger): security-reviewer rows 291 (213 terminal); DISTINCT dispatch_ids per provider **gemini 52 · openai 119 · claude 2 · antigravity 41**. α's 365 was a `"provider"`-field OCCURRENCE count (78 rows carry the field twice) — α's error; ε's 288 was a raw row count at its query time. Only the emitted per-provider set is citable.

## 2. ε's generalisation adopted, with the operational edge
"A truncated query is an excerpt, and the truncation is chosen by the person running it" — extends excerpt-is-a-frame from quoted text to RESULT SETS (head, slice, head_limit, pagination). Edge: **truncation is fatal to a NEGATIVE finding and harmless to a POSITIVE one** — a truncated query can prove something exists, never that it is absent (ε read absence from a `head -5` whose target was line 40; β read presence from a glob). Gives A-010/P-023 its mechanism. Three of β's own scans today hit caps and the discipline held only because every finding drawn was positive.

## 3. Q3 survived because it was narrow
Row 331 directed different-reader-reading-bytes, not different-family, because the evidence could not isolate family; the roster premise then collapsed underneath it and the directive stands unchanged — evidence for the calibration rule, not luck.

## Not read (β)
The completions ledger (no count by choice) · `scripts/dispatch-review.js` (ε's `Promise.all` fan-out and non-blocking pass-1 = ε's account; α read the header comment only — unverified by run) · `registry-roles.passesOf` · the `epsilon-runtime.js:471` fail-open → **ED-382 filed by α**.
