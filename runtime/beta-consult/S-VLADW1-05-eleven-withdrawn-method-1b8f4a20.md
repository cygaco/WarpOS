# β — "eleven" withdrawn as a METHOD error; a wrong generalisation about the codex lane averted by L64-73; the wrong-unit error at three layers; L6 not ruled from a summary — row 382, msg_id `1b8f4a20-7c36-4e59-9a04-52d7e08b6c93`

- **Decision:** DECIDE · class B · 0.92. Answers α's 922218d8. Sent identically to team-lead and Epsilon at 2026-08-30T02:56:36Z. **Crossed with ε's completion of path (a)** — all nine settled at source (4 agreed false · 4 codex-right · 2 codex over-graded · 3 definitional → β · 1 unresolved by range); α confirmed the four codex-right settlements against the lint at the pin and pre-fetched L6's verbatim sentence and the document's "run" usages for §4.

## 1. ⚠️ "Eleven" was wrong as a method, not merely as a number — withdrawn
β reached eleven by subtracting three over-gradings from the codex lane's fourteen. That silently made one lane's set the baseline and treated the other lane as an adjustment to it. The twin's own false set is four — L12, L12-17, L123-129, L133-137 — with ~31 true. So the union was never "codex's fourteen minus three." It is: items both graded false → confirmed; items where they disagree → adjudicated one at a time; and the result can be larger or smaller than either lane's count. And the number carried an unstated frame — "eleven" meant *eleven of codex's fourteen* — exactly the count-form defect β has been enforcing against all night, committed in β's own summary of the grading. Hold every number, including β's. No replacement stated.

## 2. ⚠️ And β was accumulating toward a wrong generalisation
β ruled S19, S24 and S31 over-graded — all three from the codex lane. Never written as "the codex lane is unreliable", but three findings pointing one way is exactly how that inference forms in a reader, and β supplied all three without flagging the risk. L64-73 refutes it in the sharpest possible way: the codex lane was RIGHT, and the twin UNDER-graded — verifying that each named class has *an* alternative rather than that the alternative *covers* the class. `BLOCK_PREFIX` contains `•‣◦` and not U+25AA ▪, so `▪ **Ceiling — …**` → zero violations stands. ε's line adopted without qualification: **neither lane is the better lane.** One over-graded, one under-graded, one definitional. Neither set is adopted wholesale, and β's three rulings should not be read as a verdict on a lane.

## 3. L64-73 is a grain error in a lane's grading — the sprint's own subject matter, one level up
The shipped sentence names classes ("list items", "ordered-list markers"); the regex enumerates six bullet characters. A class-level claim over a character-level mechanism — P-130 grain-match, exactly the defect family this sprint exists to repair — and the twin's grading reproduced it by checking at the wrong grain. The round now has the wrong-unit error at three levels: in the artifact's prose, in a lane's grading of that prose, and (in β's "eleven") in β's summary of the grading. For the close's method notes — not as a curiosity, but because it shows the defect is not a property of careless authors; it is what happens whenever a claim's grain is not checked against its mechanism's grain, at any layer.

## 4. L6 — β will not rule it from a summary
The S19 family: a universal whose domain is unstated. Both lanes read the code correctly — `main()` accepts `process.argv[2]` (codex) and defaults `targetPath` to `DEFAULT_CUSTODY_PATH` (twin). The question is whether "on every run" means every invocation targets this file, or this file is the default target. β resolved S19 by reading how `CUSTODY.md` uses "token comparison" in its own text; the same method applies — β needs the verbatim L6 sentence plus the document's own usage of "run" before ruling. A note on the reading: the twin's "it says nothing about how often anyone runs it" addresses frequency, but the disagreement is about targeting — the twin may have answered a question the sentence does not ask.

## 5. Path (a) and the deliverable shape are right
Sentence-by-sentence, one row per sentence, one status per row, settling quote where settled, definitional → β, range-misaligned → union or unresolved, and no count in the summary — that last is the part that matters most given §1. α's sequencing is right: α confirms each settled quote at the pin before β adjudicates — the other-party check rule (d) requires, applied to all nine rather than only the first.

## 6. The composition's correction payout, restated at its real size
Had only the codex Q1 lane run, up to nine over-gradings could have entered the close as fact. Had only the twin run, L64-73's real defect would have been missed. Coverage from the Q2 pair, correction from the Q1 pair — and the correction runs in both directions, a stronger result than either lane being better.

PRECEDENT: `3e6d1a94` ("eleven" withdrawn) · `a2f47b90` §4 · `c7e2b84f` §3 · P-130 · AP-18 · S5-2.

## Not read (β)
`Q1-twins-compare.md` — not yet on disk · the verbatim L6 sentence and `CUSTODY.md`'s usage of "run" (pre-fetched by α after this message) · `Q1-inprocess-findings.md`; the remaining unsettled disagreements; S5-4's artifact; the round-record file; Q3's evidence.
