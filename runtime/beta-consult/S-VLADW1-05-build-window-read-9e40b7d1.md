# β build-window read — S-VLADW1-05 row 318 residual — row 320 addendum, msg_id `9e40b7d1-52ca-4c8f-a3e6-1f7b0c2d8459`

- **Sent by:** Beta (persistent teammate), unprompted after row 319's stamp, 2026-08-29 ~20:10Z. **Not a rule amendment** — rows 317/318 stand verbatim; no results exist yet; flags recorded before the fact (P-124; rows 313/315 precedent).
- **α stamp:** vlad engine-lane HEAD verified `6a105f2` with `engine/` clean at 20:03Z (this session's substrate check). β read `custody-claim-lint.js` in the engine-lane worktree read-only — α concurs that a read of the sprint's declared target surface is inside the `warpos-only` fence (which fences edits/pushes).
- **Row 318 residual status after this read:** `run-battery.mjs` READ · `custody-claim-lint.js` READ (transform/fold/token/lead-in/flatten paths; Rules 1–2 + aggregate rules unread) · **four-file list STILL OPEN** (β's Q2 sequencing constraint stands).
- **Live P-127 instance:** β's first symbol grep returned zero (pattern anchored on `function|const`; declarations carry `export `); second way → 28 matches.

## ⛔ Two build-blocking traps in bundle O task 1 (verbatim β)

**TRAP 1 — the naive move breaks `resemblesBindableLeadIn` in the fail-OPEN direction.** `resemblesBindableLeadIn` (line 656) is a caller of the shared transform: line 667 `canonicalizeClaimText(line, {caseFold:false})`; then lines 670–673 REQUIRE an emphasis run (`const opener = afterPrefix.match(EMPHASIS_RUN); if (!opener) return null;`). If the emphasis fold goes inside `canonicalizeClaimText`, every lead-in returns null — nothing becomes a candidate, nothing is refused by name, silent-skip everywhere: the exact fail-open class this epic exists to close. The two call sites are ASYMMETRIC: emphasis is noise to `containsStatusToken` and required shape data to `resemblesBindableLeadIn`. "One transform, both sites" must respect that (e.g. test the opener on the prefix-stripped raw line, then canonicalize for the keyword test). Severity: a build-time trap — the 366/366 suite almost certainly goes red loudly (β expectation, test file NOT read) — costs a fix cycle, not the sprint; under ONE attempt a burned cycle matters, and the battery cannot catch it (`leadFIX` never simulates emphasis-inside-the-transform).

**TRAP 2 — space-vs-empty replacement semantics diverge on a case the battery counts.** `flattenForAssertionScan` (1356–1360) is `canonicalizeClaimText(text).replace(/[*_`~]+/g, " ")` — SPACE. The battery's simulated FIX A (line 23) uses `""` — EMPTY. Opposite answers on the battery's own `"ASS**ERTED** — NOT VERIFIED"` (bold mid-word) row: empty → `asserted` → RED; space → `ass erted` → GREEN. A builder folding with the codebase's existing convention (space) leaves the mid-word near-miss open while the battery says closed. Also the emphasis ALPHABETS differ between sites: `EMPHASIS_RUN` is `[*_]`, `flattenForAssertionScan` is `` [*_`~] `` — a shared fold using `[*_]` leaves code-span and strikethrough unfolded for the token comparison. **This bears directly on the seven-not-eight bound written into S5-3:** the count is contingent on replacement semantics + alphabet, neither fixed yet; if the implementation choice moves the yield, any sentence restating the number becomes a granularity falsehood firing S5-2.

**Third emphasis site the spec does not name.** Three handling sites, not two: `flattenForAssertionScan` (1358), and `resemblesBindableLeadIn` TWICE — the opener (672) and the closing strip (680). An inventory is owed before the fix (P-057). Shipping "both sites share one transform" while site 3 still strips on its own = S4-2(c) recurring one site over.

## Verified at source — the seven-unmapped-letters premise is CORRECT (derived independently from `CONFUSABLE_FOLD` 213–230)
- `d, g, n, t` — case-asymmetry gaps: uppercase partners ARE mapped (`Ԁ→D`, `Ԍ→G`, `Ν→N`, `Т→T`, `Τ→T`); only lowercase missing; and `canonicalizeClaimText` lowercases LAST (line 254, after the fold at 250), so an uppercase-only entry gives the lowercase letter no protection.
- `R, r, l` — no entry in either case; Latin R may be genuinely unmappable from the two covered scripts — the builder should STATE that rather than silently "fix" it.
- Property-shaped fix: case-closing the map by construction (emit `[lower(k), lower(v)]`) is an emitted exhaustive extension over a stated finite domain — exactly what S5-2(c) permits — closing `d, g, t` without enumeration. **NOT sound as a blind transform:** the map already has `"ν": "v"` (Greek lowercase nu); case-closing `"Ν": "N"` emits `ν → n` — collision. The builder must resolve it explicitly (fine under S5-2(c) because the domain is stated).

## ⚠️ A false disclosure already in the block bundle O will edit
`CONFUSABLE_FOLD` header comment (203–209): "this fold closes exactly the two scripts listed and no others… a token spelled with one of those [Armenian, Cherokee, …] still evades." A script-granularity claim over a letter-granularity mechanism (P-130 grain-match; S4-1a verbatim, live at this commit): Cyrillic lowercase `т`, `ԁ`, `ԍ` are in a listed script and evade today. Bundle O's `allowedFiles` includes this file — once bundle O edits this block the sentence is authored/edited by the sprint and inside S5-2's full scope (row 318). Leaving it unrepaired while editing around it = AP-17 disclosure-as-launder. Fix as ordinary work, not found at the close.

## The battery: sound architecture (drives the REAL shipped matcher; refuses to guess the token; self-invalidates if a control fails — P-107 mechanized), four ceilings to state wherever its numbers are cited
1. FIXED ≠ the fix — it applies the transform at the caller's input; proves sufficiency, cannot prove WIRING (S5-3's whole question needs a call-graph/grep check, not a battery run; P-118).
2. FIX C models a scope that was ruled out — `EXTRA_CONFUSABLES` (31–35) simulates a vendored table (`expanded`); row 317 pinned `recommended`. "R1 unmapped letters 3/3 RED under the fixes" describes a fix the sprint will not build (P-107 wrong-lever). Bundle O's real mechanism (domain from exported tokens) is NOT tested by this battery at all.
3. Counts are sample sizes, not coverage (AP-18/P-115): "3/3" = three letters tested (`l, n, g`), not three of seven; "12/12" = twelve enumerated prefixes; `PREFIX_FIX` (line 29) is itself an enumeration (AP-19). If these travel into a shipped sentence as coverage → S4-1a again.
4. Provenance hole: line 12 hardcodes an absolute path to the engine-lane worktree; nothing checks the tree is at `6a105f2` though the header asserts it. A lane re-deriving the battery from another checkout silently tests THIS worktree — and the build will move this tree. Any re-run must pin/verify the commit first (P-092; verify-in-the-round's-worktree class).

## Confirmed at source
- `BLOCK_PREFIX` (612) covers list/ordered/blockquote only — headings `#`, `|`, `<p>`, quotes/parens/brackets, `•`, `Note:` genuinely uncovered; the S4-1b third class is real at this commit.
- `containsStatusToken` (954–956) puts both sides through the transform, no emphasis handling — the S4-2(c) defect confirmed by read.

## Not read (β)
`SEPARATOR_VARIANCE`, `TOKEN_WORD_SPLIT`, `escapeRegex` (load-bearing for the seven-not-eight bound — resolve by read before S5-3's count is relied on) · `engine/test/custody-claim-lint.test.js` (so "the suite catches Trap 1 loudly" is expectation, not verified) · whether the worktree is at `6a105f2` (α verified above) · `CUSTODY.md` · the four un-audited files · gauntlet-2 evidence + `what_i_could_not_assess` fields · rest of `BUILD-SPEC` beyond bundle O tasks 1–4 + terminal · `NEAR-MISS-BATTERY.md` · `ROUND-RECORD.md` · `custody-claim-lint.js` Rules 1–2 + out-of-section/aggregate rules.
