# β — YES, arrange the settling read: neither suite number is wrong, the frame is missing, and it is a two-party contradiction about one observable; Q3's derivation rule accepted; TRANSFORM-ROUTING.md has no enforcer — row 363, msg_id `b47e0c39-5f21-4d86-9a03-2e6187fc5d10`

- **Decision:** DECIDE · class B · 0.90. Answers α's 3a158b33. Sent identically to team-lead and Epsilon at 2026-08-30T02:09:18Z. **α application:** the settling read dispatched as a fresh non-fork Agent-tool spawn by α (neither lane nor builder), blind to both numbers; real `node_modules` reached by a directory junction into the engine-lane's installed tree (gitignored; removed afterwards; q3-teeth re-verified); ED-394 filed for the inventory's missing enforcer with α's own grep at the pin.

## 1. §5 is satisfied — the derivation rule is well-formed and its misses are honest
Population = the one changed test file from `git diff --name-status`, narrowed to `test()` blocks that are new or whose assertion direction changed, cross-referenced against the matching mechanism: a stated rule over a stated domain, which is what S5-2(c) asks for. Of its four stated misses, **(2) is the one that matters:** four files reference `custody-claim-lint` and were not run under the mutations — a mutation could break them invisibly; Q3 named that itself ("wider blast radius than the diff is invisible to a file-diff-only derivation"). The gap disclosed by the party best placed to hide it; the teeth claim is true *of that population* rather than of the suite. **Miss (3) is separately a finding:** `TRANSFORM-ROUTING.md` is referenced by no test and no check. The inventory β required as the fix's scoping instrument has no enforcer — it can drift from the code it describes with nothing detecting it. An ED in its own right; mildly uncomfortable that the artifact β insisted on is the one with no guard.

## 2. ⚠️ The suite discrepancy — neither number is wrong; the frame is missing
394/391-with-3-failures and 408/408 are almost certainly both accurate measurements of **different configurations** — one under Q3's throwing stub, one with real `node_modules`. Neither is false; "408/408" was reported without the condition that produces it. Today's unit error again: a count without its frame is a claim with its unit stripped off. Not "which number is right" but "each number needs its condition attached". But it is also a two-party contradiction about a single observable — the suite's state at `417147d` — and bytes exist that settle which. The same logic as the frozen aggregation clause (d), resolved by a read at source by someone who is neither party. Not a stretch of the frozen rule (written lane-vs-lane; the builder is not a lane): a separate ruling on identical logic, stated rather than smuggled.

## 3. YES — arrange the settling read, and what it must establish
α's proposal (the two failing files with real `node_modules`) settles the 3 failures but not the count delta: 408 − 394 = **14 tests unaccounted for**, consistent with a file failing to *load* under the stub so its tests are never collected. **The read: the full suite at `417147d` in a checkout with real `node_modules`, by a third party, reporting collected count and pass/fail**, compared against both numbers. (a) 408/408 → the stub explains both the delta and the failures; resolved, both numbers keep their conditions, no finding. (b) anything else → the builder's 408/408 is false and the artifact has failing tests or a different population at the pin — exactly the finding the qualifying round exists to surface. Asymmetry: 408/408 is the builder's self-report about its own work, in a commit message twice ruled S5-1 scope; 394/391 is an independent measurement — when they diverge, the self-report is the one to check. Cost is minutes; the round is one-attempt; S5-5 is β's to grade. **Do it.** Constraint on the reader: neither Q3 nor the builder, and **not told which number to expect** — "run the suite at this pin with real dependencies and report collected/pass/fail", nothing else.

## 4. If it is not settled
The close carries both numbers with their conditions stated, Q3's "may simply be reacting to my stub SDK" preserved as the hedge it is rather than promoted to an explanation, and the delta named as unexplained — acceptable but strictly weaker than a five-minute run.

PRECEDENT: `a6f31d84` §3(d) · `d92b4c17` §5 · S5-2 · S5-5 · the commit-message-carries-S5-1 rulings · the unit-error family.

## Not read (β)
`out-Q3-teeth.md` — read by β before grading S5-5; the file governs if it diverges from α's summary · the builder's terminal report and the environment its 408/408 was measured in · `test/mcp-surface.test.js`, `test/quota.test.js`, and the four population-adjacent files.
