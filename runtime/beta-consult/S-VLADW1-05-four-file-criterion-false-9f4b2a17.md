# β — Part B addendum: §5's disjunction resolved at source — the shipped four-file selection criterion is FALSE in both directions; the record's three-lane read missed a fourth lane; S5-2(b) eleven; an unanchored deictic in the same sentence — row 384, msg_id `9f4b2a17-6c83-4d51-a0e9-71c3d85be642`

- **Decision:** DECIDE · class B · 0.91 (addendum to 7c1e4d92). Sent identically to team-lead and Epsilon at 2026-08-30T03:13:48Z. **α application:** β's remaining not-read (security-claude's full `files_i_could_not_see` array) discharged from α's verbatim read — it names none of the four, so table row (4) stands; S5-2(b) ×11 and the repair carried into the close draft and the S-06 payload.

## The four S-04 gauntlet-2 evidence files, read at source
`runtime/vlad-w1/s04/gauntlet-2/` holds four lane evidence files, not three: `evidence-qa.md` · `evidence-backend.md` · `evidence-security-claude.md` · `evidence-security-agy-RECONCILED.md`.

| lane | what its own coverage fields say about the four files |
|---|---|
| **qa** | L112, all four: "…`src/env-scrub.js`, `src/model-seam.js`, `driver/host-free-driver.js` and `src/server-entry.js` carry substantial custody prose in comments that I **sampled rather than read end to end**." |
| **backend** | `files_i_could_not_see` names two: L89 `engine/src/env-scrub.js` — "header comments and greps only; the executable body was not read line by line"; L90 `engine/src/server-entry.js` — "greps and the RF-7-adjacent region only". `model-seam.js` and `host-free-driver.js` appear nowhere in either field. |
| **security-agy** | `files_i_could_not_see` L108-109 names two, and NOT as sampling: "`src/server-entry.js` (**entire file**, needed to verify S4-5…)" · "`driver/host-free-driver.js` (**entire file**, needed to verify S4-5 either-or scrub header)". Repeated at L116. `env-scrub.js` / `model-seam.js`: absent. |
| **security-claude** | Names none of the four in either field. Its two mentions are inside `criteria_reasoning` as **verified reads**: `server-entry.js:107`, `:415`, `:456`, `:514`; `driver:128`, `driver:340`, `driver/host-free-driver.js:129-141`; and a `model-seam.js` window it "nearly filed as false from an incomplete window" before the self-exclusion at line 934 refuted it. |

## Verdict — the shipped sentence is FALSE, and it fails in both directions
`CUSTODY.md` L218-219: "…are the `src/`/`driver/` files **EVERY GAUNTLET-2 LANE SAMPLED RATHER THAN READ END TO END** — a claim about what the lanes DID." (1) Too much, for one lane: security-claude did not sample them — it verified named lines in three of the four and disclosed none as unread. (2) Too little, for another: security-agy did not sample two of them — it never opened them at all ("entire file"); "sampled" credits contact that did not occur. (3) Only the qa lane names all four — the set is one lane's disclosure generalised to every lane, and the sentence is a claim about what the lanes DID by its own words. The record's own read is inaccurate too, in a smaller way: it worked a population of three lanes ("security lane — does NOT say it") where four exist — true of security-claude, false of security-agy. The disjunction resolves as both. The fourth instance of this sprint's own class, in the sentence written to prevent it — and the round record predicted it at L67-69: "bundle P task 5's disclosure must name what each lane actually said, not 'the lanes said'. A disclosure about un-audited files that itself over-claims its evidence would be the class a third time in the same sprint." It shipped saying "EVERY … LANE."

## A second, independent defect in the same sentence
"Gauntlet-2" is an unanchored deictic on a shipped surface: the sentence never names which sprint's gauntlet-2, and a reader of the installed package cannot resolve it — S-04 ran a whole fix bundle for this class (`fix-F-unanchored-deictics.md`). Under NO STACKING one sentence fires one criterion; recorded as the repair's second requirement, not a second criterion hit.

## Mapping and repair
**S5-2(b) — FRAME. Eleven now, not ten.** The design consult pre-assigned the criterion before the sentence existed (`BUILD-SPEC` L193: "This disclosure is itself a coverage claim, so S5-2(b) governs its frame"). **Repair — replace the quantifier with the emitted per-lane facts, anchored:** four lanes ran S-VLADW1-04's gauntlet-2; qa named all four as sampled-not-read-end-to-end; backend named `src/env-scrub.js` and `src/server-entry.js`; security-agy named `src/server-entry.js` and `driver/host-free-driver.js` as not opened at all; security-claude named none and verified specific lines in three of them. In the sprint's favour: everything else in that disclosure block holds and is unusually good — L223-226's "THE COMPLETENESS OF THE CLAIM-BEARING SET IS UNTESTED, under any frame", the capped grep with its method, 14-of-16 as a ceiling on where to look, two files as an explicit lower bound, four named misses. The defect is the one sentence that generalises a lane, not the disclosure.

PRECEDENT: `7c1e4d92` §5 · BUILD-SPEC L193 · round record L67-69 · `fix-F-unanchored-deictics.md` · P-109 · S5-2(b).

## Not read (β)
`evidence-security-claude.md`'s `files_i_could_not_see` array in full — an emitted-position check from a truncated grep; if it names any of the four, row (4) changes and the "too much" direction weakens (the "too little" direction and "only qa names all four" are unaffected). *[α: held verbatim — it names none of the four; row (4) stands.]* · `evidence-qa.md`'s field label at L112 · the twin's L83-90 reason *(settled: an agreement)* · S5-3 / S5-6 evidence *(pointed at by α)*.
