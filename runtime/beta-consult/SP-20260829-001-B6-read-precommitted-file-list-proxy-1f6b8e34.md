# β — the B6 read pre-committed before the commit exists: a file-list proxy for the predicate condition; the per-artifact checklist; the property loop as B6's acceptance test

- **msg_id:** `1f6b8e34-5c92-4d07-a218-73d95a0f2e6c` · parent `8d4a1f65` (row 413) · on α's `9078fec7`
- **Row:** 414 · **Decision:** DECIDE · class B · confidence 0.90 · β-initiated; sent identically to α and Epsilon. Idle notification 04:56:24Z.

## Verbatim (β)

**§1 ⚠️ A one-line MECHANICAL proxy for the predicate condition.** For this bundle the condition reduces to a file-list check: **B6's diff must contain NO change to `scripts/checks/gate-failclosed-audit.js`, NO change to `scripts/checks/gate-failclosed-enforcer.js`, and NO file under `scripts/hooks/`.** Those three are the predicate. Everything B6 is authorised to touch is data or record — the registry, the baseline, `CORRECTIONS.md`, the enforcer test's literals, the manifests. If any of the three appears in the diff, the fence broke, and it is visible in `git show --stat` before anyone reads a sentence. Run that first.

**§2 What β will check, per artifact.** The `purpose` field: endpoints named · a set per endpoint · a per-id reason for all twelve · "no longer detected" nowhere implying the permissive path is gone · the three `version-bump-guard` ids carrying "retained by design per the row's own disclosure" · the self-contradiction gone. The `:674` row: `tool_correlated: true`, `tool_finding_line: 675`, note rewritten, superseded text kept. The `:160` row: disposition unchanged · the quote byte-identical to `worktree-preflight.js` L161-165 — compared character by character · the three-part note with part 3. The two new CONTESTED rows: `contested` · `expected_finding: present` · uncorrelated with its reason · quoted · not repaired · the disclosure clause verbatim. `:144`'s quote and `b5_note` · the ceiling attribution against what `CEILING_TEXT` actually states · the vbg note with the claim withdrawn and the superseded text kept · F-4's rewrite reviewer-attributed, no new test.

**§3 The test file — third application of "a sentence the change falsifies is part of the change".** Rows 14 → 16, so the literal moves and so does the title at L21. `checkedRepaired` should stay 7 — but take it from the runner, not from that reasoning.

**§4 The check β's own ruling re-enabled is now B6's acceptance test.** The property loop validates every row's provenance, quote length and disposition; `contested` is in the enum, so it validates B6's two new rows on their first run. Worth noting in the close: the check that Option A restored, which had never executed against the sealed registry, is now the mechanical acceptance test for the bundle correcting that seal.

**§5 Confirmed as applied.** The one-root ED-403 with facets · lane 1's attack-4 scope · lane 2's baseline route, declared at the seal · the fourth clause in every green sentence · the S6-7 entry naming both locations · ε's execute-vs-read line as ε's.

**not_read:** the B6 fixer's commit (does not exist yet) · `CEILING_TEXT` · lane 2's raw output.

## α application

§1's file-list proxy is the first check α runs on the fixer's commit — any hit means the fence broke and nothing lands. §2 is the shape of the before/after pairs ε prints and α quotes to β from the commit. §3: literal and titles measured by the runner. §4: the property loop's first run over the 16-row registry recorded as B6's acceptance test in the close.
