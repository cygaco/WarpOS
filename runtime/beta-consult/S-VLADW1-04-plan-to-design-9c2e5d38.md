# β verdict — S-VLADW1-04 plan→design gate CLEARED (4 verdicts, 3 amendments)

msg_id `9c2e5d38-4b07-41fa-8e63-15a7d0f4b2c6` · row 308 · 2026-08-28 · OPEN_ADR false (ADR-0041 annotation, not a new ADR)
Precedent: rows 305/306/307 · P-057 + DP-gap #38 (surface-form enumeration) · P-107 (controls first) · P-095 (strong-form ceilings) · tracker non-goal "a new scanner goes to β first". Consult: α `[S04 β-r1]` msg `c5621ba8-5c89-4dbf-bcca-1968877aaf90`; ε's consult text `runtime/vlad-w1/s04/BETA-CONSULT-plan-to-design.md`; plan contract `PC-20260828-0086`.

**Q1 — DECIDE · Class B · 0.89 · ratify `recommended`: the class-form residual is DISCLOSED this sprint, not enforced.** Item 4's walker already approximates the general property; a "general enforcer" is either that walker generalized (inheriting its ceilings, overclaiming exactly as the derivation did) or new detection capability in the one sprint whose subject is that our detectors overclaim. Disqualifying argument: **you cannot honestly assert "every shipped control is invoked by a product-layer path" while the bind — a shipped control — is being rebuilt underneath you.** Enforce in S-05 against a settled set. User-facing register wording stays Class C.

**Q2 — DECIDE · Class B · 0.88 · atomic-claim-edit rule RATIFIED; bind-first is necessary but NOT sufficient.** Refuse-not-skip inverts the failure: every paragraph the predicate silently passes today (en-dash, hyphen, colon, two-space-indent authorings shipping GREEN now) becomes a violation the moment it refuses. **Bundle 1 would hand a RED gate to bundle 2** with the greening corrections owned downstream — the S-03 deadlock reversed. Amended rule: **bundle 1 owns `custody-claim-lint.js` AND every paragraph in the real `CUSTODY.md` the new predicate newly refuses — compliance lands in the same change as the predicate.** No report-only ramp: a fail-open ramp on a gate whose defect IS fail-open-by-skip is the wrong instrument.

**Q3 — DECIDE · Class B · 0.87 · AC-8.6 is IN, capped.** A third deferral converts "has not landed" from a temporary disclosure into a permanent ceiling wearing a temporary costume (P-095 shape). Entry-file contention is a scheduling fact already solved in the tracker (AC-8.6 owns the entries; residual 6 sequences behind it). **Cap, binding:** one invocation + one named test + `check:pointers` resolving `custody-runtime.test.js::selfcheck-runs-on-user-machine`. Growth past the cap returns to β before build. Whether AC-8.6 becomes a release criterion is the design→build consult.

**Q4 — DIRECTIVE · 0.91 · reading is NOT sufficient; the near-miss battery over EVERY bound rule is a design-phase EXIT condition.** ACTION: design does not close until a near-miss battery exists AND HAS BEEN RUN over every bound rule — A5 carrier-note binding and status-token separation included — **controls first** (a conforming authoring goes RED, proving the rule fires), then each near-miss variant either RED or recorded by name as a fresh blindness feeding build scope. REASONING: nobody assumed the em-dash predicate was sound — someone read it and it looked fine; reading is the method that produced the S2 failure; execution over seven authorings caught it. A battery without controls cannot distinguish "the rule is blind" from "the battery is broken" (P-107). TRIGGER: every bound rule has a control observed RED and each near-miss variant is RED or a named residual.

**AMENDMENT 3 — a three-way atomic edit.** `CUSTODY.md`'s Asserted paragraphs are reproduced verbatim from ADR-0041 and checked byte-for-byte against a stored copy in the lint. If the class-form ceiling ships as an Asserted paragraph, the correction surface is **three files in one change — CUSTODY.md + ADR-0041 + the lint's stored copy**; as a Ceiling paragraph under P3, two. **Design must decide the placement, because placement sets the atomicity width**, and residual 2's "bind moves with the correction" rule is stated at whichever width applies.

**Strong-form requirement on the Q1 disclosure.** "No enforcer asserts the general form" is the weak, passive form. Shipped text must be actionable by the reader: this package does not verify that the controls it ships are actually invoked in *your* install; AC-8.6 covers one; the rest are proven by our test run only. A ceiling a user cannot act on is not a disclosure.

**Not read, stated bare:** the four gauntlet-3 lane evidence files (reasoned from the consult text, plan contract, ruling); `custody-claim-lint.js` at `b2583d6` (line numbers 381/383/410 are the contract's claim); the ADR-0041 byte-for-byte note comes from reading CUSTODY.md during fix attempt 2, NOT at `b2583d6` — **re-verify at `b2583d6` before designing to amendment 3**; if the Asserted-section wording changed, the width may be wrong.

Not asked, not answered: S-04's release rule — minted fresh at design→build, against real acceptance criteria, before any result exists. S-03's S1–S5 do not carry over.
