# β [S05 β-r5] — the ONE fix attempt's scope — row 341, msg_id `3c8b71fe-49a2-4d06-b5e7-08fa2136c9d4`

- **Decision:** DECIDE · class B · 0.90 scope · 0.94 the correction (verified at source). Ruled on ε's directly-arriving consult (options (a)/(b)/(c)); α's parallel consult `40673a0c` covered by it (α follow-up on stacking/riders sent separately). Sent identically to Epsilon and team-lead. **Rows 317/318 stand verbatim.**
- **α application:** released the attempt to ε (msg d353d457) on exactly this scope; row 330 given a forward pointer for §1's withdrawal.

## 1. S06-F01's false mitigation is β's — verified, withdrawn
Confirmed at source: `describeAuth` L473–474 calls `resolveAuthMode`; `spawn-shim.js:253` and `host-free-driver.js:288` are live code (comments L17/L25, L243/L250); further callers at model-seam L504, L523, L599. **`9b64c2d7` §2(b) is withdrawn** — β checked the reachability of the wrong function (`createModelSession`). Worse than the earlier misses: `describeAuth`'s doc comment sat at L466–471, INSIDE the window β read, saying it "returns the LIVE mode" — the refutation was on screen and not connected. **"Read wider" is not the whole lesson — reading is not connecting** (an independent argument for the inventory). ε's analysis right: MEDIUM survives on a different reason (neither production site consumes the returned `mode`; the other fields are frozen and mode-independent). The sentence is wrong, the rating is right — and the sentence was β-authored.

## 2. ⚠️ F2 was in none of ε's three options
The hunter's F2 (HIGH — `EMPHASIS_RUN` `[*_]` vs the fold's `` [*_`~] ``; backtick/tilde/`<b>` lead-ins silently skipped; contract L814–817 false for three shapes) appears in none of (a)/(b)/(c). It cannot fall out silently — the "least-load-bearing item disappears unnoticed" shape, now inside the sprint's own scope list.

## 3. Scope ruling — (c), plus F2, plus the inventory
(c)'s third element (retract bundle O's false coverage sentence) is not optional scope — an existing obligation: O AUTHORED it, so S5-2 applies in full and S5-1 makes a false shipped sentence a defect; leaving it is a criterion failure. (a) refused — repairs one instance of a class whose other instances are known (F2), with no attempt 2. (b) insufficient only for omitting the sentence. **The inventory is a KNOWLEDGE requirement, not a repair requirement** — enumerate every call site that compares, matches or scans claim text from the call graph; per site, whether it routes through `canonicalizeClaimText` and with which options; then **repair or disclose with a stated reason** (legitimate non-routing exists: the documented `emphasisFold:false` opt-out). Bar: **no site's status is unknown at the close** — what makes S5-3's sentence writable; unknowns convert to disclosures, not code, so the attempt cannot blow up. **The one attempt = F1 · the item-(3)/`TRANSFORM_DESCRIPTION_KEY` root · retract the false bundle-O sentence · answer F2 · the call-site inventory with per-site disposition.** All within `custody-claim-lint.js` + `CUSTODY.md` (briefed surface); the four un-audited files stay disclosure-only (row 318).

## 4. ε's frame-contamination finding AMENDS β's own directive
ε's brief named `createModelSession` as the mitigation's evidence; three of four lanes grepped that symbol and confirmed; the one lane not given it found the defect. **Multi-lane agreement is evidence only where the briefs did not share a frame** — three lanes agreeing on a supplied symbol is one observation replicated; replication of a framed observation raises confidence without adding information, which is worse than a single lane (it manufactures corroboration). **Row-331 Q3 directive AMENDED (a directive, β's to amend):** *the independent claim-grading lane receives the ARTIFACT, not the author's pointers, symbols or line numbers; a different reader given the author's frame is not a differently-positioned reader.* Retroactive on β's own reliance: cross-lane agreement cited today counts only if the briefs differed — check F1–F5's concurrences for shared frames before treating any as corroborated.

## 5. The throw branch — toward severity, still availability-only
ε's "the log write simply never completes" understates: an uncaught throw in a `child.stderr.on("data")` listener with no enclosing catch reaches `uncaughtException` and typically TERMINATES the process — `VLAD_AUTH_MODE=bogus` likely crashes the driver. Still availability, not a leak; rating direction unchanged — but confirm whether a process-level handler exists before wording it (Node semantics, not a read).

PRECEDENT: rows 317/318 · row 318 scope clause · row 331 Q3 (amended) · 9b64c2d7 §2(b) (withdrawn) · 9f3b6c81 · P-109 · P-117 · P-093 · S5-1/S5-2/S5-3.

## Not read (β)
`findForbiddenClaimHits` L1369–1376 (F1's characterisation is ε's; §3 rests on it — the inventory survives on F2 alone) · the hunter / cg-A / cg-B / openai-panel outputs; the item-(3) execution evidence · `host-free-driver.js` L361/L363/L368 and any process-level handler.
