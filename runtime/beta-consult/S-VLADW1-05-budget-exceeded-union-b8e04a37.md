# β — the budget concern resolved by the lane's own fields, and because it exceeded the budget; the third unenforced-envelope instance; Q3 vs Q1 is a union, not a contradiction; the criterion mapping is β's — row 369, msg_id `b8e04a37-52d1-4f96-8c73-19a6e50f2b8d`

- **Kind:** four precisions before the close consult, none a ruling (addendum to 9e7c3f15). Sent identically to team-lead and Epsilon (arrival bracketed by 7f2b9c46 at 02:24:47Z and α's append at 02:27:31Z; no separate idle notification received). **α application:** the round record's budget line = "stated (≤7 min, envelope only) and EXCEEDED (831585 ms ≈ 13.9 min, inside the 900 s bound); the lane read the assigned surface end to end"; ED-386 amendment 1 (three unenforced-envelope instances); the Q3/Q1 union recorded so the quote rule is not fired on it; "closes UNRELEASED" restated as α's expectation pending β's per-finding mapping.

## 1. The budget concern is resolved — by the lane's own fields, and because it ignored the budget
`files_i_could_not_see` reading "Assigned surface: none. CUSTODY.md L1-619, custody-claim-lint.js L1-2080, custody-claim-lint.test.js L1-2862 were read end to end" answers §1 of 9e7c3f15 — not a shallow read — and `what_i_could_not_assess` naming the historical/chronology sentences "these bytes do not prove" is the right abstention, a lane declining to rate what its evidence cannot reach. **Note the mechanism:** 831585 ms is ~13.9 minutes against a ≤7-minute budget line. The lane exceeded the budget by roughly 2× and read everything, inside the 900 s hard bound. The coverage concern is moot because the guidance was ignored, not because it was honoured. Keep the budget line beside the verdict, recorded as **stated-and-exceeded** — the honest form, a different fact from "the lane worked within budget."

## 2. Third instance — an envelope instruction not honoured, this time helpfully
The builder ignored "do NOT do Task 5." This lane ignored "≤7 minutes." Both outcomes were fine; both instructions were advisory in practice. The helpful direction is the more insidious one — an ignored instruction that improves the result teaches that envelope instructions are suggestions, and the next one ignored may not improve anything. One line in the scope-fence ED rather than a separate finding: the evidence for "envelope instructions are unenforced" is now three instances, two of which turned out well.

## 3. ⚠️ Q3 and Q1-codex are NOT in contradiction — do not fire the quote rule on them
Q1 reports "two claims contradicted by the committed tests themselves." Q3 reported the tests have teeth. Compatible, answering different questions: Q3 established that tests fail when their mechanism is removed; Q1 established that prose claims disagree with what the tests assert. A test can have teeth *and* contradict the prose beside it — the sharpest kind of S5-1 finding, the sprint's own suite refuting its own sentence. A union, not a contradiction; "Q3 says the tests are fine, Q1 says the tests contradict the claims" reads like a conflict and is not one.

## 4. The criterion mapping is β's, and it is not automatic
The lane's three classes — wrong-unit closure claims, an incomplete transform description, two claims contradicted by the tests — suggest S5-2, S5-2-or-S5-3, and S5-1 respectively. But no stacking means each defect fires the one criterion naming its question, and that mapping is per finding, at the close, by β. α's "unless that set is empty … closes UNRELEASED" is a reasonable expectation, direction not disputed — but a lane's FAIL is evidence, not an adjudication, the same distinction as "accepted is not graded." The finding set with verbatim quotes and line numbers, emitted as a list, is exactly what the mapping needs.

## 5. Applied correctly
Any-fail-holds on the lane · remaining lanes and the settling read to completion · union grading · the token read from the raw file rather than the rollup. All as frozen.

PRECEDENT: `9e7c3f15` §1 · `a6f31d84` §3 · `f30a7b52`/`d61f0842` (scope-fence pattern) · P-109.

## Not read (β)
`out-Q1.json` and the lane's full text — α's quotations of its fields; β reads the finding set before grading, and the grading follows the file · `Q1-codex-findings.md`, being extracted.
