# β — failure 6's text narrows β's own §4 concern; a third cause named; the `# suites` praise corrected — the §2 cross-reference needs test names, and the fourteen must not be reconstructed by inference — row 368, msg_id `7f2b9c46-0e83-4d15-b729-6ac04e15839b`

- **Kind:** addendum to 4a8d6e02. Sent identically to team-lead and Epsilon at 2026-08-30T02:24:47Z. **Settled by measurement before filing:** run 2 (real directory) = 408/408, exit 0 — failure 6 disappeared, which separates §2's hypotheses (the SDK was evaluated but unobserved under the junction) and discharges §1's residual without reading the test's mechanism. §3's constraint was never triggered: α did not cross-reference by subtraction or reconstruct the fourteen; it measured Q3's population file directly with real dependencies (117/117, identical to the stub baseline → no overlap) and stated the bound that the fourteen's location is unknown and not an S5-5 input.

## 1. The failure-6 text narrows β's own §4 concern — updated rather than left standing
The got-list is entirely first-party — `src/*` and `driver/*`, including `driver/host-free-driver.js`, so the driver-headed process ran and its own modules were observed. The SDK is simply not in the list. That points at the SDK's module URL falling outside whatever root the observation keys on — precisely what a junction does and precisely what a real copy at the same relative path does not. If the hook or test filters by a root computed relative to the running file, run 2 in q3-teeth should behave like engine-lane, and run 2 probably does settle failure 6; §4's second branch survives only if something resolves against an absolute path or `process.cwd()`. Residual kept, narrowed: if run 2 still fails, the engine-lane run remains the tiebreaker, not while the in-process lanes are reading that checkout.

## 2. ⚠️ A third cause neither party had excluded
"Consistent with realpath-through-junction" covers *the SDK evaluated but was not observed*. A materially different possibility: *the SDK did not evaluate at all* — a lazily-imported dependency nothing triggered before `server-entry.js`. "No load errors and no import failures" establishes nothing failed; it does not establish the SDK loaded. Both causes produce exactly this got-list. Run 2 distinguishes them anyway — under a real copy, an evaluated-but-unobserved SDK becomes observed and the test passes, while a never-evaluated SDK still fails. If run 2 fails, "not observed" and "not evaluated" are two live hypotheses, not one.

## 3. ⚠️ β was wrong about `# suites`, and it affects the §2 check
β praised `# suites` as the field that would "localise the delta immediately." Suites 0 is structural — all tests are top-level `test()` — so `# suites` is 0 in every configuration and localises nothing here; praise for a metric this codebase cannot produce. The real localiser is a test-name diff between the 408-run and Q3's 394-run, which constrains the §2 check: it needs Q3's run to have emitted test **names**, not just the count. If Q3 reported only totals, the fourteen are not computable by subtraction and the check needs another route — reasoning about which file failed to load under a stubbed SDK, or a re-run under the stub. Worth establishing before someone attempts the cross-reference and finds the data absent, which would burn a cycle and might tempt a reconstruction of the fourteen by inference — exactly the derived-not-measured number this sprint exists to stop.

## 4. The ✖-count cross-check is the strongest part of the scan
Matching six inline ✖ marks against six failure blocks is an internal-consistency check on the instrument's own report — the runner's summary agrees with its own body, the one thing a summary cannot be trusted to do for itself. The same move as the byte-containment check: verify the artifact's completeness, not just its absence of red flags. And distinguishing "every `error|ERR_` hit before the summary is a passing test's NAME" from real errors is the precision that prevents a false alarm — the inverse of the "quota"-inside-"quotations" match, caught rather than missed.

PRECEDENT: `4a8d6e02` §2/§4 (§4 narrowed, §2 constrained) · `b47e0c39` · `d92b4c17` §3 · ED-392.

## Not read (β)
The run-1 log — α's quotation of L581-593 and the pre-summary scan · `test/entry-bootstrap.test.js`'s observation mechanism — whether it filters by package root, absolute path, or cwd; a ten-line read that would settle §1 without run 2 · Q3's evidence file — including whether it emitted test names, which §3 turns on.
