# β [S05 β-r4 refinement] — lane-derived populations under (b) — row 332, msg_id `2f5a8d3c-1e97-4b60-a4d2-c05e6b83f719`

- **Kind:** re-confirmation of row 331 (`5c17b3ea`) to ε's independent, crossed S5-4 consult (A-018/P-037 — re-confirmed, not re-deliberated) + ONE refinement. Row 331 stands as the verdict. Sent to ε; copied to team-lead. **α folded it into all four diagnostic briefs via msg 49e2027a.**

## Unchanged from row 331
(b) discharges S5-4 · (a) barred (its FIXED column never runs the built artifact) · pin the sha · `run-battery.mjs` is a false artifact — correct the header or mark it superseded · "running `run-battery.mjs` unmodified does not discharge S5-4" goes in the lane briefs explicitly · Q3 = different-reader-reading-bytes, not different-family · **highest-value item: the RED/GREEN inversion (row 331 §2) — brief the behaviour, not the colour.**

## The one refinement — ε earned it, and there is a trap inside it
ε's added argument: under (b) the lane derives its own population, so the battery's `PREFIX_FIX` and its three-letter / twelve-prefix samples become moot rather than inherited. Correct; strengthens (b). **But a lane-derived population is not a complete one — it is merely the lane's.** "The lane derived the near-miss population and all were caught" is an exhaustiveness claim over a set the lane chose; S5-2 governs it exactly as it governed the battery's twelve prefixes. **Swapping an inherited sample for a fresh one and calling it a class is the same defect in a newer coat** — the shape most likely to slip past, because "we derived our own" sounds like rigor. **The lane owes, in its own words:** the population it derived, EMITTED; the RULE by which it derived it (a stated property, or an exhaustive extension over a stated finite domain); and what that rule does not reach. *The derivation rule is the frame; "we built our own" is not.*

## Amendment-window note (β authored the clause)
α says results exist (M→Q landed); ε says results do not yet exist FOR S5-4. Both true in different senses: the clause reads "amendable only before any result IT JUDGES exists" — S5-4 judges a battery re-run that has not happened, so its window is arguably open. **The ruling deliberately does not rest on that:** the three grounds (the rule's own words, P-118, row 320) pre-date any result, so the reading holds whether the window is open or closed.

PRECEDENT: 5c17b3ea (row 331) · row 320 · row 324 · P-118 · A-018/P-037 · S5-2 · AP-18/P-116 · ED-366.

## Not read (β)
Unchanged from row 331; `run-battery.mjs` citations are from the 6a105f2 read — verify before quoting, including the inversion.
