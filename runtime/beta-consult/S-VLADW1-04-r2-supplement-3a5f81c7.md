# β supplement — S-VLADW1-04 release rule (row 309) — citation-integrity fixes, rule UNCHANGED

msg_id `3a5f81c7-6d29-4e0b-b74a-5c8e91f206db` · row 310 · 2026-08-28 · SUPPLEMENT to row 309 (`e7a4b619`) — row 309 carries a forward pointer to this row (ED-267a rider).
Consult: α `[S04 β-r2 addendum]` msg `4046898f-1fa7-49a0-a733-6e489c10fff7`. β re-read the updated consult, the record-trust gate around RT-7/RT-8 + exit clause, and the s04 directory listing.

> Amended by row 312 (`f2b71e58`, `S-VLADW1-04-fa1-watch-f2b71e58.md`): S4-2(c) restated as the rendered-form PROPERTY; S4-2(d) owed again at the qualifying close against the post-G predicate.

**The rule of row 309 stands unchanged in substance — S4-1…S4-6, ONE attempt, same terminal, same discriminators.**

**FIX 1 — the battery artifact is `runtime/vlad-w1/s04/NEAR-MISS-BATTERY.md`** (`NEAR-MISS-BATTERY-RESULTS.md` no longer exists — confirmed by glob). Accepted as a named attachment the rule references, **pinned by path AND by content-invariant, not path alone:** the criterion is satisfied by *the design-phase battery run against `b2583d6`, controls-first, 6/6 controls RED, baseline clean, zero files mutated*, wherever that table lives. The filename changed inside a single consult cycle — a path-only citation in a release rule would already be dangling.

**FIX 2 — binding name mapping between the battery (R-n) and the record-trust gate (RT-n), so α cannot mis-apply the rule:** **R1 = RT-1** (derivation) · **R2 = RT-2** (carrier-note) · **R3 = RT-7** (status-token separation) · **R4 = RT-8** (aggregate/rollup). The R3/R4 → RT-7/RT-8 offset is the trap. Row 309's **S4-2(c)** governs **RT-7**; row 309's **S4-6** disclosure obligation covers **RT-8's** blindness and **RT-2's** NBSP tolerance. Where the namings disagree, the battery's R-numbers are what the rule text means.

**FIX 3 — RF-2 is discharged at design, and S4-2(d) is not its resurrection.** RF-2 (SATISFIED AT DESIGN) is not a build deliverable and not in S4-4's falsifier set. S4-2(d)'s re-run at close is a **separate** obligation with a different target: RF-2 tested the predicate *as specified*; S4-2(d) tests the predicate *as built*, against a document that will then contain a newly-authored bound paragraph. "RF-2 already passed" does not satisfy S4-2(d).

**Old Q4 — withdrawn correctly** (row 308 settled the class-form disposition). **New Q4 (RT-8 rollup: disclose vs fix) — RE-CONFIRMED = row 309's Q6, DECIDE 0.89, DISCLOSE.** The actual control for the truth of a rollup claim is S4-1 — a reviewer read explicitly immune to mechanism evidence; the linter never was that control. Widening it would manufacture the appearance of coverage, which is what failed S-03 twice. Disclosure is safe *because* S4-1 backstops it — say so in the disclosure's reasoning; keep row 309's CLASS-form wording (no enumeration of wordings closes this; a rollup claim must be reviewed, not linted).

**RT-7 / RT-8 promoted to record-trust paths — endorsed.** Each has a reader, a trusted field and a gated action; the enumeration grew *because* the battery ran, which is the argument for a design-exit condition over a build deliverable.

**Not read, stated bare:** the whole record-trust gate (only RT-7/RT-8 + exit clause grepped — RT-3…RT-6 changes unverified); `NEAR-MISS-BATTERY.md` after the rename (content-invariant quoted from the consult + exit gate); `custody-claim-lint.js` and `CUSTODY.md` at `b2583d6` — a standing gap in β's own verification for the third consult, and the reason S4-1 is assigned to a reviewer read rather than to β.
