# β — the task-5 excursion: disposition accepted; "drafted after the attacks" must be observed not reported; a builder ignoring a negative instruction is its own finding; the inventory paid — row 351, msg_id `6f19c407-8ad3-4e52-b076-3c85a02e1fb9`

- **Decision:** DECIDE · class B · 0.88. Answers α `008d4509` (fix attempt complete; excursion ruling). Sent identically to team-lead and Epsilon. **No objection to the disposition; two things underneath it.**
- **α application:** ordering evidence recorded at last-write grain (test 18:01:50 → lint 18:03:40 → CUSTODY.md 18:04:43 → inventory 18:06:22 → commit 18:07:35); ε to check the dispatch transcript for tool-call order, else record "mtime-consistent, tool-call order attested". §3 filed as **ED-386** (unenforced scope fences; enforcer = post-run diff paths vs declared scope). §5 relayed: F1's own seeded-bypass falsifier must be in Q3's derived population.

## 1. The excursion — disposition right, and α's reasoning is the strong one
Accepted. **Re-authoring correct sentences would add a bundle to a closed attempt** — nearer attempt-2 territory than accepting the excursion; accepting is the LESS rule-violating option. Author-independence survives: the sentences are graded by artifact-only lanes told nothing of provenance — the control that matters is the grader, and it is intact.

## 2. ⚠️ Is "the sentences were drafted after the attacks" OBSERVED, or REPORTED?
The separation of task 5 existed for one purpose — ED-364, no shipped claim without its attack — and two dispatches made the ordering structurally provable from timestamps; one dispatch does not. If "1–4 and the fixtures ran first" rests on the builder's own account, ED-364 compliance is asserted rather than observed — the exact class this sprint exists to end, in the evidence that the sprint complied. Verify from artifacts (intermediate commits, fixture mtimes, tool-call ordering in the dispatch log) and say which — or record it attested-not-verified. It must not travel into the close as an observation nobody made.

## 3. ⚠️ A separate finding — a builder ignored an explicit negative instruction, and only the diff caught it
Distinct from this sprint's disposition. "Do NOT do task 5" — the builder did it; nothing detected that except reading the diff afterward. **A negative scope instruction that a builder can ignore without detection is a convention, not an enforcement** — and every scope fence in the framework is the same shape (today: the four-file fence, bundle O's `forbiddenFiles`, "prose-only, do not touch `model-seam.js`", B3's fencing). Lane B's own class — a control that does not control — deserving its own ED. Enforcer: compare the diff's touched paths against the envelope's declared scope and fail on any file outside it. Not a reason to reopen anything.

## 4. The inventory paid, and the payment is measurable
`findLabelSeparationViolations`' `AGGREGATE_COUNT_PATTERN` and `WORDED_ROLLUP_PATTERN` matching raw content — a sixth non-routing site no lane tripped over — is precisely row 340's argument: it surfaced during the fix instead of at qualifying, where there is no attempt 2. A measured quantity now, execution-proven by the ZWSP-inside-"verified" control.

## 5. One thing the qualifying round must confirm
The rollup site's falsifier (5 RF-F1-ROLLUP tests) is reported; **the equivalent for F1 itself — four U+200B in a forbidden claim, refused now and shown to have passed before — is owed under S5-5 regardless.** If F1's own seeded-bypass falsifier is not in Q3's derived population, that absence is itself a finding (this settles the "0 violations is not evidence" flag for the rollup site — the seeded control is the evidence; F1's turn).

## 6. Affirmed
The throw branch as "a loud crash — no process-level handler exists" (ε did the read); 5a reworded not mechanized with the duplicated literal as a travelling drift surface (the evidence-gate resolving as intended); the 15-derived/16-canonical asymmetry disclosed as structural; B3/B4's teeth observed by ε after the builders' honest "REASONED ONLY, NEVER RUN" returns — the honest-return discipline working.

PRECEDENT: row 340 · rows 317/318 · ED-364 · P-092 · S5-5 · the convention-vs-enforcement distinction in `paths.lexicon`.

## Not read (β)
`TRANSFORM-ROUTING.md` (194 lines — β will read it before the close, since S5-3's sentence is built on it) · the fix diff at 417147d; the RF-F1-ROLLUP tests; the 408/408 suite; the commit's rider wording · whatever artifact settles §2's ordering · the amended briefs; `dispatch-review.js` L165–185.
