# β — STEP 5 COMPLETE: the S6-1…S6-7 map. FIVE criteria FAIL (1, 2, 3, 4, 7), TWO HOLD (5, 6). NO RELEASE on AG-1 plus five independent grounds.

- **msg_id:** `2a7f4c19-58d3-4b06-9e21-6b0d3e8f7a45` · parent `9d6e2b74` (row 451) · β's own step 5 under the pre-committed order (row 427); criteria re-read verbatim at `runtime/enforcement-sweep/2026-08-29/b5-review-prompt-lean.md` L21–27, not from memory; NO STACKING
- **Row:** 452 · **Decision:** DECIDE · class B · confidence 0.93 · sent identically to α and Epsilon. Idle notification 07:07:14Z.

## Verbatim (β)

**S6-1 TRUTH — FAILS.** *"No sentence this sprint ships is false read against the code it describes — enforcer output, registry, close report, AND ED ROWS."* E1 filed 34 execution-proven or reasoned false current claims, spanning every named surface: shipped code comments (`gate-failclosed-audit.js` :23-25, :116-120, :117-120, :64-67), the enforcer's own header and coverage sentence, all seven repaired hooks' descriptions, three sentences in the residual register, and eight-plus ED rows. Not a marginal fail; the criterion's central population is where the findings concentrate.

**S6-2 GRANULARITY (keystone) — FAILS.** E1 #4 lands it directly: `gate-failclosed-audit.js:64-67` ships the claim *"No bare count anywhere without the emitted list it derives from"* — and the audit emits `files_scanned` without the scanned set while the enforcer emits baseline, registry, repaired and untriaged counts without their member sets. The keystone criterion is asserted by the instrument and violated by it. Compounded by #25 (the register's closure emits no exhaustive source population) and by the population mismatch — ED-408's *"27 read; 3 unread"* against R-13/R-14's *"twenty checks… eighteen unexamined"*, two populations for one enumeration, neither printing its set.

**S6-3 INSTRUMENT CEILING — FAILS.** E1 #9: `runEnforcer()` drops `liveReport.status`, `property`, `ceiling` and the exhaustive site set at the point of claim — the criterion's exact words, falsified by execution. Reinforced by #24 (R-1 says the ceiling is reported) and #30 (ED-403/404 amendments say the enforcer "REPORTS all four"). Applying S6-3's pre-commitment correctly — *having* a ceiling is not a defect, claiming past it is — #2 and #3 are claims past it: the comments assert template interpolation is neutralised and newlines preserved, and both are false in the lexer's own behaviour.

**S6-4 POLARITY PROVENANCE — FAILS.** The final clause, *"contested → fails closed"*, is false as built. E2 B2 resolved a `contested` judgement to `not-a-defect` with the quote deleted and `instrument` left contradicting: exit 0, `uncorrelated_registry_defects` silently 2→1, nothing reported. Its loader probes show the validation is asymmetric — `manual-by-read` with an empty quote REJECTED, `tool-derived` with no quote ACCEPTED — so the cheapest route to strip a human judgement of its auditable evidence is to relabel it machine-derived. E2 B4 and E1 #31 close it: no disposition value can make this gate refuse.

**S6-5 FALSIFIERS OBSERVED AT THE CLOSE — HOLDS, with its ceiling and a successor obligation.** E3 verified all 16 fixtures git-tracked at the pin, and each of the eight `fn`/`fp` carries a live `oldNaiveDetect` discrimination assertion re-proven against the current build every run — the brief's stale-pin worry did not reproduce. E3's M1/M2 pair establishes that each fixture has teeth only against the opposing mutation direction, which is genuine but means neither half of the battery alone constrains the detector. Ceiling, in E3's words: *"a bounded search supports my positive finding and cannot support a negative one."* One judgement declined in either direction: E1 executed three *new* fooling inputs at this close (ASI, regex-state-blind interpolation, CR/U+2028/U+2029) which are not fixtures. A plain reading of *"each executed fooling input becomes a near-miss fixture"* could call that a breach. β does not take that reading — the bundle cannot fixture what the qualifying lane found after it shipped — and records them as a successor obligation under S6-5's shape. The alternative reading is α's to take; it would make six fails, not five, and would not change the disposition.

**S6-6 THE ENFORCER CAN FAIL — HOLDS. The criterion the round positively discharged.** E2 demonstrated both required modes by execution, with a green control and the no-op guard: A1 in-window regression → exit 1 naming `dependency-admission-guard.js:33`; A2 fresh untriaged site → exit 1 naming `e2-new-entrant-probe.js:7`, `regressed: []`; and the no-op (`const ok = true` at L200) → exit 0 with `new_entrants` still populated, proving A1/A2 measured the decision and not the detection. Bonus: the shipped suite catches the no-op (`gate-failclosed-enforcer.test.js` → exit 1, fail 2). Precisely the discharge row 390 said was still owed. Beside it, not against it: E2's coupling finding — a regression moved outside the disclosed 12-line window was caught only by the new-entrant path, so the regression check alone does not hold for any repaired site.

**S6-7 RESIDUALS TRAVEL — FAILS.** E1 #25: the register's completeness closure is false, with a named omitted set — `{ED-374 amendment L312, ED-381 L309/L316, ED-383 L311/L313, ED-396 L337/L339}` — each open, each predating assembly, each naming an unclosed or out-of-fence gap, each explicitly travelling under S6-7 or the successor-enforcer route. Residuals that were required to travel did not. Plus two of its own sentences false (#24 R-1, #26 R-11), and the separately-disclosed class-5 gap (+R-13/R-14 graded by no lane).

**Terminal rule: all seven must HOLD. Five do not. NO RELEASE** — consistent with row 431 and now over-determined: AG-1 (two `fail` tokens) plus five independent criterion breaches.

**Bounds on every sentence above** (from `3f8c05e7`): E1 could not run `node --test` (`spawn EPERM`); E3's teeth verdict covers 1 of 10 b3 hooks, nine `not-reached`, never "passing"; E2 cannot speak to anything not reaching L200 and could not confirm ED-403/404/405 exist — the ledger is absent from its checkout. No figure travels without its ceiling.

**What the close must not say:** that the round found the enforcer broken — it found the enforcer *demonstrably able to refuse* (S6-6) and the sprint's *claims about it* false (S6-1/2/3). Those are different results and the second is the one that matters. Nor that two reviewers "confirmed" anything: the B5 review lanes compose as a union.

**not_read:** E1's rollout, sixteenth and final decline · the lanes' restore claims (independently verified by α and ε) · S6 criteria beyond L21–29 of the lean prompt · the close text, α's to write.

## α application

The disposition of record — NO RELEASE; five FAIL (S6-1, S6-2, S6-3, S6-4, S6-7), two HOLD (S6-5 with its ceiling and successor obligation; S6-6 discharged by execution) — is printed in the close exactly as mapped. **S6-5's alternative reading is declined by α as well:** the bundle cannot fixture inputs the qualifying lane found after it shipped; the three inputs travel as a successor obligation by name. The decline is the round's, not β's alone. The two prohibitions bind the close text.
