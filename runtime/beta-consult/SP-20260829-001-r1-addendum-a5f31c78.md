# β [ENF β-r1 ADDENDUM] — SP-20260829-001 (enforcer sprint) — row 322, msg_id `a5f31c78-9d2b-4e06-b714-83c0d95e2f41`

- **Supersedes in part:** `c7a8e934-1b06-4d52-8f31-6ba09d5e7c02` (row 321, [ENF β-r1]; verbatim text held by ε → `SP-20260829-001-verdict-c7a8e934.md`).
- **Decision:** DECIDE · class B · confidence 0.88 (Q3 0.89 · Q1 0.86 · Q4 0.90 · secret-guard call 0.91). Sent identically to Epsilon and team-lead. No pre-committed rule exists yet (Q5: mint at design→build) — this is a re-ruling on a corrected premise, not a post-result amendment.
- **Consults answered:** α `999b3718` (priming finding + proposed scope) and `e5a4cfc2` (ε's 12/12 triage → 9 defects in 3 variants); ε `36c89485` + `af922656`.
- **α stamps:** secret-guard mechanism verified at source by α (readFileSync count 0; `JSON.parse(input)` L9; `catch { process.exit(0) }` L94–95) → ED-379 amendment row filed 20:26Z. WarpOS tree at `6e8501e5` + ε's `b038a8a9`; lane B mint `8d15c162`.

## 🔴 CORRECTION — secret-guard.js: right defect, WRONG MECHANISM
β read L8–96. ε's/α's description ("outer catch wraps the entire scan including the file read") is wrong on the mechanism: **there is no file read.** `content` comes from the hook payload (`event.tool_input?.content || event.tool_input?.new_string`, L11–12); the regex tests at L84–91 do not realistically throw. **The actual throw source is `JSON.parse(input)` at L9** — a malformed PreToolUse payload → `catch { process.exit(0) }` at L94–95 → the write is permitted unscanned. Conclusion survives (could-not-check → permit on a security control, ED-369 class, unfiled → now ED-379); the mechanism determines the fix — briefed as written a builder wraps a non-existent read and the real path stays open. An ED row with a false mechanism is an inaccurate disclosure (P-110/AP-17) — the S4-1 shape landing in lane B while lane A exists to end it. Correct before it lands; record as a caught-before-shipping self-correction.
Severity (P-093, discriminator first): HIGH by class (the guard does not guard and reports nothing) with UNPROVEN reachability (malformed harness-generated payload, not demonstrably attacker-reachable) — neither a live exploitable hole nor a downgrade. Fix polarity: unparseable payload = could-not-check → fail closed; absent input (L15) and `.env` skip (L18) = nothing-to-check → stay exit 0 (same partition as `tracker-completion-gate.js` L48 vs L69).

## ✅ CONFIRMED — authorization-gate.js:390 is genuinely category 3
L383–388 emits `decision:"approve"` inside the try; the catch at L390–392 exits 0 WITHOUT emitting an approve → the elevated permission is not granted; the normal path decides. Failing "open" lands restrictive. **The discriminator is a property, not a list: does the error path land on the permissive or the restrictive side of the decision THIS gate makes?** Exit code is the wrong lever; the gate's own decision semantics are the right one (P-114). Write the partition on that property; the three categories fall out of it.

## Q3 — RE-RULED: the registry ratchet is the sprint's spine. Adopt with three conditions
ε converged independently on r1's answer (blocking over a declared registry of triaged sites, remainder reported); it survives 4→65. Conditions: **(1) Emitted set, never a count** — "named counted residual" is AP-18; emit the 65 site identifiers + per-site disposition; numbers derived, never typed. **(2) The enforcer states coverage at the registry's granularity** — its output reads "N of 65 enumerated sites under enforcement, M untriaged", both derived; never "fail-open enforcement is active" (P-130). **(3) The triage IS the gate and the author controls both sides** (AP-15): classifying a site category 2/3 removes it from enforcement, so a green gate is reachable by classification instead of repair — every category-2/3 disposition carries a written justification QUOTING that site's decision semantics; the category-3 population is an explicit close review target, not a residual bucket. A registry-scoped blocking enforcer does NOT instantiate ED-374 (it can fail on members and new entrants).

## ED-369's third candidate enforcer — ε's refusal ENDORSED
A write-time lint banning bare `catch → exit 0` is the wrong mechanism: category 3 proves a syntactic lint would sometimes push the system LESS safe, and 364 raw matches across 111 files is a false-red generator (P-059, the inverse of the false-green this sprint fixes). Re-specify as **registry-driven**: fires on drift, new untriaged terminal-decision sites, regressions in repaired sites — an honest feasibility ceiling, not a deferral (P-061).

## Q1 — narrowing still right; ratify the CONTENT, not the label
α ratifies; no operator needed. Strengthened on ground independent of ε's stake: both consults said "enumerate the registered population" without knowing it was 65 across 45 live hook files. **Flag to α:** `minimal_safe` is the same label over materially larger work — R-1 (partition) and R-3 (per-site fixtures) were sized against four sites and now face 65 with a three-way taxonomy. Label-stability masking content-growth is its own grain failure — surface the new size, then ratify.

## Q4 — close language RE-RULED (supersedes r1's amendments)
> At `<sha>` the probe enumerated `<the emitted list>` terminal-decision sites across `<files>`. `<T>` were triaged against the stated discriminator; of those, `<M>` were classified as real fail-open defects and repaired, each repair evidenced by its seeded fixture failing closed after the change. `<65−T>` sites remain **untriaged and are listed by name**; they may contain further real defects of unknown number. No triaged site silently passed its seeded failure. This claims neither repository-wide discovery nor closure of the fail-open class.

Must hold: no ratio and no "most are benign" anywhere (n=4 caution binding on the close text); "repaired" observed-not-asserted; binds identically on a NO-RELEASE close.

## Standing from r1 — unchanged
Q2: block under `ENFORCE=1`; keep L48 absent-input as SKIP; capture the discarded stderr at L52–58; break-glass renders BYPASSED never PASS. Q5: design→build, confirmed. **Row 319 Q3(a) heavier:** with 45 hook-wired scripts in the population (incl. `gate-check.js`, `tracker-completion-gate.js` in lane A's path) ε owes an explicit enumeration of which population members fire on lane A's conduct — build-and-test-only until S-05's qualifying close; do not discover it at landing time.

REASONING: one property throughout — a reported field must observe what it claims (P-092) — now applied to the discriminator itself. Population growth strengthens the ratchet (never sized to the population). Position-swap on Q3 stable. ε's disclosure-against-interest (4-site repair → 65-site triage) earns the fast ruling (P-126); nothing rests on "most are probably benign".
PRECEDENT: c7a8e934 (superseded in part) · row 319 Q3(a) · P-092 · P-093 · P-110/AP-17 · P-114/AP-19 · AP-18/P-116 · P-115 · AP-15/P-123 · P-059 · P-061 · P-126 · P-130.

## Not read (β)
61 of the 65 sites (population statements are structural) · `probe-failopen.js` incl. its ED-366 self-guard (load-bearing for 65 — a lane should re-derive) · `out-plan-{dop,pl}.json` (unprimed premise still unverified; α read the briefs only) · `PC-20260829-0087.yaml` + its three `assumptions.unsafe` · `SP-20260829-001-plan-to-design-addendum-1.md` · `design.js` L193–194, `ed-dup-id-lint.js` L42–47, `untrusted-content-firewall.js:44`, `format.js:70` · whether the tree is at `8d15c162`.
