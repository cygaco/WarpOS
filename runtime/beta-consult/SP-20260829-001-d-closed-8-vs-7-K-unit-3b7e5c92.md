# β — check (d) closed on the committed envelope (conditional on commit 2 landing); two findings from the envelope read; the builder's refusal to route around the instrument

- **msg_id:** `3b7e5c92-1d48-4a06-9f37-82c145e0b6d9` · parent `1c8d4f36` (row 403) · on α's `6a38fe53`
- **Row:** 404 · **Decision:** DECIDE · class B · confidence 0.90 · sent identically to α and Epsilon. Idle notification 04:21:04Z.
- β read the committed `ENVELOPE.md` (L1-60, sha256 `d032a49460d70955`).

## Verbatim (β)

**§1 Check (d) — CLOSED, conditional on commit 2 landing.** The disposition table (L14-29) carries site, disposition, `expected_finding`, `tool_correlated` and a first-clause quote per row, with full quotes in the registry. The repair's before/after is not in the envelope — it is the diff at `51d70d42`. Accepted and preferred: a diff is the primary artifact; a table transcribing it is a copy that can drift. The condition: commit 2 is not landed, so the before/after currently exists only in an unlanded worktree commit. Land it and (d) is satisfied in the durable record; until then satisfiable, not satisfied. (a), (b), (c) hold on the committed bytes — and the table shows `:70`/`:108` as not-a-defect with their reasoning, so the scope-vs-count correction has now been vindicated twice.

**§2 ⚠️ The envelope's own arithmetic disagrees with its own emitted set.** L31: *"Every 'not-a-defect/absent' row above (**8 of them**: dependency-admission-guard:33, gate-check:57+190, ownership-guard:66+144, secret-guard:94, worktree-preflight:160, and B5-R's own ownership-guard:144 confirmed twice) was verified by executing `siteStillPermissive()`…"* The table above it holds SEVEN rows with `expected_finding: absent`. The list names seven distinct sites and then re-names `ownership-guard.js:144` as "confirmed twice." So 8 counts verification EVENTS while "rows above" counts ROWS. The round's own class in the round's own bundle record: a bare number standing beside an emitted set, where the number does not equal the set's cardinality. The honest form: *"every one of the seven `absent` rows was verified by execution; `ownership-guard.js:144` was confirmed twice, before and after its repair."* The set carries its unit; the number lost it. And it is β's to raise, not a lane's — the envelope is in none of E1's five classes.

**§3 ⚠️ K does not mean what the close will imply.** From the coverage code: `backedIds` requires `tool_correlated`, and `K = M - backedIds.size`. So K counts baseline sites with no TOOL-CORRELATED backing row — not sites nobody has looked at. "75 untriaged" claims more ignorance than exists; conservative, but this sprint grades disclosures inaccurate in either direction (AP-17). Print K with its actual meaning, or state the manually-dispositioned-but-not-correlated count beside it. Note also, without grading it: the code's own comment says "no registry-backing row at all", which is not what the code does. That sentence is inside E1's first class; not named to a lane.

**§4 α's §4 answer — accepted; β's hypothesis is dead.** No could-not-check branch, six `absent` rows enumerated by name. The question cost one message and bought certainty on a number the B5-T literal depends on.

**§5 What the envelope does better than it had to.** L38-43 is the strongest single passage in the bundle: the builder kept `expected_finding: "present"` on the three `version-bump-guard` rows, explained why from the file's own documented default, proved the alternative would produce a false positive BY EXECUTION, and then (L43) *"Not routed around by choosing a non-catch-line `line`; disclosed in the registry row's `b5_note` instead."* It found a way to make the instrument report what it wanted, and refused it. It belongs in the close beside the defects.

**not_read:** `ENVELOPE.md` beyond L60 · the registry's full quotes and `b5_note` fields · the `51d70d42` diff, unlanded · `siteStillPermissive`'s body.

## α application

- **(d)'s condition discharged:** commit 2 landed as `6f3a0685`; B5-T as `12d2aef0` = the **final qualifying pin**. Suite at the pin: `gate-failclosed-enforcer.test.js` tests 11 / pass 11 / fail 0 / skipped 0 — the property loop ran over the sealed registry for the first time and passed; `audit.test` 28/27/0/1 skipped; `b3-fault-injection` 10/10; `enforce.js` exit 0; `pin-06669fbe/` tree `0e8c80f6ca49` unchanged.
- **§2 confirmed** by α's own count of the committed table: seven absent rows. The envelope is not edited (it is the return as returned); the close carries β's honest form; the S6-7 list carries the finding.
- **§3 measured at the pin:** M=78; tool-correlated-backed = 3 (`retro-presence-check.js:70`, `:108`, `edit-watcher.js:898`); K=75. The seven absent rows' sites are **not** in K — they left the baseline when repaired — so β's inference does not hold at the pin, while β's unit correction stands. Inside K with a manual row: exactly one, `edit-watcher.js:675` (registry row `:674`, not-a-gate, present; ±2-line anchor check). Close sentence: *"75 of 78 sites carry no tool-correlated registry row — 74 with no registry row at all, 1 with a manual not-a-gate row; 3 tool-correlated; 7 repaired sites guarded by the regression check; 0 open defect rows."*
- **§5 adopted:** L38-43 goes in the close beside the defects.
