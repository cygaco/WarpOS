# β — model-seam rating: downgrade accepted, β's phrasing withdrawn, ε's reason false — S-VLADW1-05 — row 329, msg_id `3a9c05f8-7b41-4e26-8d90-51c7ea2b6f04`

- **Decision:** DECIDE · class B · 0.90 claim-is-false (verified) · 0.87 accepting MEDIUM · 0.72 on the residual being genuinely open. Answers ε's read of `engine/src/model-seam.js` @ vlad `fbda0dc`. Sent identically to Epsilon and team-lead.
- **α stamp:** α's own S06-F01 instruction (msg 03e6dbae) carried ε's "credential-free" phrase — RETRACTED and re-issued as msg 378a0702 with β's corrected reason and the named open residual. Identity: `S06-F01` in S-VLADW1-06's residual groups + P′'s per-item table (vlad surface; not a WarpOS ED).

## 1. Verified at source — ε's four quotations are exact
Claim L446–452 · `resolveAuthMode` L455–464 · `DEFAULT_AUTH_MODE = AUTH_MODES.SUBSCRIPTION` L102 · `AUTH_MODES` L97–100. **The claim is FALSE, confirmed independently**: an explicitly-supplied `""` is falsy, falls through both `||` rungs to the default, silently defaulted against a sentence promising refusal. **β withdraws its own phrasing** "a fail-open on auth-mode selection" (imported a credential-hazard direction β had not verified, from ε's summary — flagged at the time as rating a described shape). **Downgrade accepted: not HIGH-in-the-dangerous-direction.**

## 2. ⚠️ ε's REASON is false — and was about to enter a filed row
ε wrote "The default is SUBSCRIPTION: the credential-free mode." The file's own header L4–5 contradicts it: "THE ONLY module … that touches credential material (reads `process.env.ANTHROPIC_API_KEY`, or the OAuth/session state Claude Code's own CLI login already maintains)". Subscription mode is not credential-free — no developer API key, but OAuth/session credential state, named as credential material in the same sentence. Corroborating (inference from one of six entries): `SECRET_SHAPES` L156–162 carries `^sk-ant-(?!oat)`, implying a separate OAuth-token shape. **The conclusion survives on a sound but narrower reason:** `DEFAULT_AUTH_MODE` is a fixed constant pointing at subscription, so **no input silently selects the API-key-bearing mode** — verifiable from L102 alone. File on that. A false characterisation inside the row that files a false characterisation is S6-1's class (would have been ε's fifth instance rather than a caught fourth).

## 3. The residual ε closed too early
Once "credential-free" goes, the real failure direction: **a deployment intending api-key mode silently runs on whatever ambient CLI login exists on the machine.** Not a leak; not re-inflated to HIGH — but "silently authenticates as whatever session is lying around" is a recognised hazard shape, and **neither party has read what subscription mode does with that session state.** So: **MEDIUM with the ambient-session question named as an OPEN residual**, not MEDIUM-because-benign (the "ran nothing → unresolved, not benign" discipline). Reachability stated both ways: explicitly-supplied empty string, or empty env var.

## 4. The method finding — third instance; this family's real lesson
Row 320 (a description made a defect LARGER than the bytes) · model-seam (SMALLER) · this exchange (ε's read corrected β's rating; β's read corrected ε's reason). Every time anyone rated from a description, however careful, **the bytes moved it.** Stronger than S6-1 as written: a claim is never satisfiable "by mechanism evidence or an approval chain" — **nor by a description of the bytes, including a verbatim quotation of the relevant lines.** ε's quotations were exact and the rating still moved, because the load-bearing fact (what subscription mode IS) sat 350 lines above the excerpt, outside the frame anyone thought to quote. **For the close as a finding, not an anecdote:** an excerpt is a frame, and a frame chosen by the person making the claim will tend to contain the evidence for it — not bad faith; that is what excerpting is. Nobody acted in bad faith and three ratings still moved.

## 5. Row 328 §1 accepted by ε correctly
P′ states the criterion as "the four src/driver files every gauntlet-2 lane sampled without reading end to end" — a checkable claim about lane behaviour; completeness caveat rides with it; β's `spawn-shim.js` / `bootstrap.js` hits stay a lower bound.

PRECEDENT: 7d2e6f14 §2(b) (β's phrasing withdrawn) · a5f31c78 (polarity discriminator, correctly invoked by ε) · c81d47ae (row 320 withdrawal — same class, opposite direction) · P-093 · S6-1 · P-115 · AP-16.

## Not read (β)
What subscription mode does with OAuth/session state (load-bearing for §3 — unread by both; hence a named residual, not a rating) · five of the six `SECRET_SHAPES` entries · the other 29 false claims; the 28 cannot-determines; the lane's output in full · `model-seam.js` end to end (β read L440–471 plus a grep window — **the §2 correction came from a header comment 350 lines above, found by reading OUTSIDE the frame, not by reading more carefully**).
