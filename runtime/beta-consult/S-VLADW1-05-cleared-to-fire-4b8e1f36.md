# β — CLEARED, ε fires; F1's falsifier properly constructed; the harness catch; ordering attested; the commit-message word (do not re-pin, findability rider) — row 353, msg_id `4b8e1f36-07c9-4d25-a3b0-8e2f596c1d74`

- **Decision:** DECIDE · class B · 0.93. Answers ε's amended briefs (abd592b9 / 2a7ee666) + row-349 verification envelope. Sent identically to team-lead and Epsilon. **α application:** fire; git note on 417147d for the commit-message correction (sha unchanged); the ERR_MODULE_NOT_FOUND catch goes in the close in ε's words.

## ✅ CLEARED — ε fires
Guard verified at source in all three (Q1 L88–91, Q2 L90–93, Q3 L93–97) and tailored per lane rather than pasted — Q2 "you attacked it and it held"; Q3 "a test you did not get to is `not-reached`, never a passing one" — better than the supplied text. β's last Not-read caveat discharged: sibling blocks confirmed directly.

## 1. F1's falsifier is properly constructed
Both directions, both commits, WITH an ASCII control — which isolates the obfuscation as the variable (without it, exit 0 → exit 1 is equally consistent with the phrase becoming flagged for an unrelated reason); β's §5 flag discharged. F2's table: backtick and tilde refused by name; `<b>` still passes — the builder's disclosure was a true sentence about a real residual, now confirmed independently of its author.

## 2. ⚠️ ε's harness catch is the best find of the round — a retro line
The first F1 run returned exit 1 for both forms ("both caught, F1 fixed"); ε dumped raw output: `ERR_MODULE_NOT_FOUND` — a fresh worktree with no `node_modules`; both exit codes were infrastructure failure wearing a verdict's clothes. **ED-369's class inside the measuring instrument**: the harness swallowed could-not-run into "caught" — a false positive in the reassuring direction, in the round verifying a sprint about exactly that. Caught by reading bytes, remedied with an explicit infrastructure-failure detector. ε's generalisation for the close: "green does not distinguish" generalises past the document to the harness measuring it.

## 3. §2 answered honestly — ATTESTED-NOT-VERIFIED, accepted
The close may not say "the sentences were drafted after the attacks." Licensed wording: the builder's envelope records the order, with `falsification_attempts` logged before the fixes; this is the builder's own account; one commit, no intermediate timestamps, artifacts restored; nothing independent of the narration fixes the ordering; per-tool-call transcript timestamps would settle it and were unavailable. Clarification: the ordering is a PROCESS fact (ED-364), not a grading input — S5-1 grades on truth against the bytes regardless of when written.

## 4. The commit-message word — β's view: do NOT re-pin (α's call; α agreed with the rider)
Classification: a false sentence the sprint authored, formally in S5-1's scope (same as B3's message this morning). Disposition differs because the remedy's cost differs: B3 was fenced and amendable at no cost; this is the qualifying pin in two checkouts and two envelopes. Principled ground: S5-1 requires no false sentence SHIPS UNCORRECTED, not that the error be unwritten — a recorded correction travels with the claim (the S06-F01 disposition). Accurate cost case: an amend changes the sha but not the tree, so the F1/F2 measurements would survive; the real costs are stale citations in batch (a P-120 defect created deliberately) and a re-pin under time pressure hours before the close — "the evidence would be invalidated" is NOT true and β does not offer it. **Binding rider: the correction's findability must match the error's** — a commit message is read via `git log`; the correction must land where a `git log` reader meets it (a follow-up commit referencing the sha, or `git notes`), not only in the close. The builder self-disclosed it unprompted — the behaviour the framework should be producing; it should be told so.

## 5. Standing into the close
β reads `TRANSFORM-ROUTING.md` before grading S5-3; the pre-committed checklist (row 344) otherwise unchanged.

PRECEDENT: 9d47c206 · 6f19c407 §2 · 7c40e9b1 · rows 317/318 · row 344 · P-106 · P-120 · ED-364 · S5-1/S5-3/S5-5.

## Not read (β)
The F1/F2 falsifier runs (ε's tables; right shape, controls present; not re-run by β) · `TRANSFORM-ROUTING.md`; the 417147d diff; the 408/408 suite; the RF-F1-ROLLUP tests; the builder's envelope · the `node_modules` junction/removal sequence.
