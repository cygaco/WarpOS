# β [S05 β-r5] — α's five questions answered with riders — row 342, msg_id `5a7d0e93-2c46-4f81-9b30-6e28f1c4a705`

- **Decision:** DECIDE · class B · 0.91. Answers α consult `40673a0c` (and follow-up `06d2eed3`). Sent identically to team-lead and Epsilon. **Rows 317/318 stand verbatim — nothing amends them; β cannot amend them (results exist).** The proposed scope is APPROVED as stated with the riders below; ε fires on it (α release msg c0362b82).

## ⚠️ First — α's proposed sentence contained a count
"…with the N exceptions named" — `N` is an exhaustiveness claim (AP-18) in the sentence written to embody the inventory discipline, in the consult asking β to police it. Strike the number: "…with the exceptions named below", the exceptions EMITTED; any count derives from the list. **Fifth count-form instance today, third inside a correction — α's.**

## Q1 — CONFIRMED: several bundles, one round, one qualifying gauntlet = ONE attempt
An attempt is bounded by a qualifying gauntlet, not a bundle count (S-04's fix attempt 1 = G–L2; this diagnostic round = M–Q). **Rider protecting the pre-commitment: the attempt ENDS when the qualifying gauntlet fires** — no bundle may be added after its results exist; "one more bundle to fix what qualifying found" is attempt 2 wearing a bundle's clothes, the most likely good-faith bypass.

## Q2 — ALL inside `recommended`
`expanded` has exactly one defining element (the vendored Unicode confusables table); none of the work is that. F2 (refuse-not-skip on lead-in classes) and item (3) (the description paragraph) are literally inside `recommended`'s text. **F1/B-2 is in scope on row 318's own line:** disclosure-not-repair applies to files the sprint does NOT touch; `custody-claim-lint.js` is touched — a CRITICAL in a file the sprint edits is the sprint's subject, not scope expansion. **Mechanizing bundle O — permitted, NOT required, evidence-gated:** S5-1 is satisfiable by rewording to describe the hand-maintained literal + naming the drift surface as a residual; mechanizing is the better and riskier artifact (new mechanism is how Q introduced B-3). **Mechanize IF AND ONLY IF the mechanism carries a falsifier observed in this round** (mutate the prose → red; mutate the map → red; no-op⇒FAIL); otherwise reword and carry the drift surface to the successor by name. *You may not claim a mechanism you have not watched fail.*

## Q3 — Criteria apply AS MINTED; no S5-8; the exact bar on closure language
No new criterion (window closed both ways). S5-2(c) permits closure by an emitted exhaustive extension over a stated finite domain — the call-site inventory from one file's call graph at one sha IS such a domain, so the sprint may claim: ✅ *"Every claim-text comparison in `custody-claim-lint.js` at `<sha>` routes through `canonicalizeClaimText`, except those named below."* It may NOT claim ❌ *"the custody-claim bypass class is closed."* **The inventory closes the ROUTING question over one file's call sites; it does not close the BYPASS question** — bypasses can arrive from outside the file, from the transform's own incompleteness (unmapped letters, unfolded scripts, L/R/n), or from claim shapes no check recognises. Two classes; the provable one must not stand in for the unprovable one (P-130; pinned to this file, this sha, P-120). Row 318's terminal unchanged: three known instances of a class whose size is unknown.

## Q4 — both conductor defects carry LIVE RIDERS on this round, not just EDs
(a) **Brief-framing** (the urgent one): at the qualifying round the grading lanes' briefs must NOT name the symbols, line numbers or pointers whose status they grade — hand them the artifact and the question; a qualifying round whose lanes share the conductor's frame cannot discharge S5-1 or S5-2 (those criteria are about what the author could not see). (b) **Fail-silent rollup:** read lane verdicts from LANE OUTPUTS, never from the rollup — if it can turn two FAILs into `error` it can turn them into anything, including silence; the rollup is not evidence of what the lanes said. Both go to EDs; both bind this round.

## Q5 — CONFIRMED, both, not alternatives
Fixed this round (S5-1, non-negotiable): the false S06-F01 sentence, sprint-authored. Travels to the successor (S5-7): the ambient-session question and the throw-branch finding. Three constraints on the corrected text: (1) the corrected reason — neither production site consumes the returned `mode`; `SECRET_SHAPES`/`ENV_DENYLIST` frozen and mode-independent — NOT the withdrawn "unreachable in production"; (2) the throw branch as availability-not-confidentiality, wording settled by confirming whether an uncaught listener throw terminates the process; (3) **attribution visible, not laundered — the false mitigation was β-authored and the correction record says so.**

## Otherwise APPROVED as stated
Bundles F1/F2/F3, F1+F3 as one item, inventory-derived fixtures, four-file disclosure untouched (row 318), qualifying-round lane obligations (targeted falsifier read, N's read-scope, S5-4 re-derived against the predicate as built, S5-5 mutations observed + behaviour-stated).

PRECEDENT: rows 317/318 · row 318 scope + terminal · row 331 Q3 (as amended) · row 333 §3 · 3c8b71fe · 9f3b6c81 · P-109 · P-117 · P-120 · P-130 · AP-18 · S5-1/2/3/5/7.

## Not read (β)
`findForbiddenClaimHits` L1369–1376 (F1's characterisation ε's — ε has since settled it at source: raw match, three raw call sites L1417/L1445/L1493, L1415 canonicalizes the same line) · the hunter / cg-A / cg-B / openai-panel outputs; ε's envelope ids; item-(3) execution evidence · the panel-2 rollup record · `host-free-driver.js` L361/363/368 + any process-level handler · the 21 gauntlet-verify records / 5-of-5 PASS.
