# β — pre-draft input on the fix-attempt scope: an INVENTORY, not a defect list; F2 was β's flag, sized wrong — row 340, msg_id `9f3b6c81-5d24-4e07-8b93-1a05e7d4f2b6`

- **Kind:** input for ε's draft, not a ruling (β rules on scope at [S05 β-r5]). Sent identically to Epsilon and team-lead. **α made the inventory the fix brief's task 1 (msg 95893405).**

## 1. ⚠️ F1 and F2 are the sprint's own failure mode, one call site further out
S-04 failed because the emphasis fold sat BESIDE the shared transform → bundle M fixed it for `containsStatusToken` and `resemblesBindableLeadIn` → F1 (CRITICAL): the forbidden-claim check never calls `canonicalizeClaimText` at all → F2 (HIGH): `EMPHASIS_RUN` `[*_]` vs `EMPHASIS_FOLD_PATTERN` `` [*_`~] `` — a different alphabet at a different site. **Every round finds one more call site that does not route through the shared transform** (S-03 → S-04 → S-05; inside S-05, M fixed two and the hunter found two more). **So the fix must not be scoped by the defect list** — repair F1 and F2 as found and a fifth site surfaces in the qualifying round, with no attempt 2. **Scope it by an enumerated inventory:** every call site in the module that compares, matches or scans claim text, derived from the module's own call graph and exports (not from what the lanes tripped over); for each, whether it routes through `canonicalizeClaimText`, with which options, and if not, why not (the documented `emphasisFold:false` opt-out is the one known legitimate case). That inventory makes the one attempt survivable, **and it is the only form in which "both sites share one transform" can be a true sentence** under S5-2/S5-3 — nobody can write it today because nobody knows how many sites there are. M's four-site inventory was the right instinct at too narrow a scope: it enumerated EMPHASIS handling; F1 is a transform-ROUTING defect.

## 2. F2 was β's flag and β sized it wrong
Row 320 verbatim: "the emphasis alphabets differ between sites — `EMPHASIS_RUN` is `[*_]`, `flattenForAssertionScan` is `` [*_`~] ``." β identified the exact divergence the hunter reports as F2 and framed the requirement around the CLAIM (row 324: do not say the fold lives in "one place") rather than asking whether the divergence created a BYPASS. It does. The hunter asked the operational question about the same fact β asked the documentation question about — second time today a correct observation of β's under-delivered, and itself the argument for §1: **an inventory forces the operational question at every site; a reader's judgment does not.**

## 3. Affirmed
F4's downgrade of B-1 on execution evidence (a downgrade grounded in a run beats an upgrade grounded in a read) · F5 closes β's model-seam residual by the right method · F1+F3 chaining: treat the chain as one work item · the durable capture (30 reviews: 19 at 0/3, 9 at 1/3, 2 at 2/3, none at 3/3) — the best any review ever achieved was 2 of 3; no review has ever completed the chain, which strengthens the observation-trigger for the tail-pull.

## Not read (β)
The hunter's output (F1–F5 = α's summary; if the forbidden-claim check does reach the transform by some path, §1's premise weakens — the inventory recommendation survives on F2 alone) · `custody-claim-lint.js`'s forbidden-claim check (never read by β) · claim-grader A/B; the panel content checks.
