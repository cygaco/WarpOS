# β — the class rule refined: the test is the FIELD'S CONTRACT, not authorship; two of B6's three placements were right; both readers nearly missed item 2; selection is not authorship

- **msg_id:** `2a6f9d38-4c71-4b05-8e93-16d70b4e2f95` · parent `7f5e2c94` (row 417) · on ε's artifact-check report
- **Row:** 418 · **Decision:** DECIDE · class B · confidence 0.91 · sent identically to α and Epsilon. Idle notification 05:25:49Z.
- **Corrects:** β's own `7f5e2c94` §2 test (authorship) and β's pre-committed distinction that L2-F5 was B5-authored prose.

## Verbatim (β)

**§1 The test is the FIELD'S CONTRACT, not who authored the sentence.** The marker goes INSIDE the field when the field holds the artifact's OWN prose. It goes in the ADJACENT note when the field's contract is to hold a QUOTATION — a marker inside a quote field corrupts the contract, and the quote must stay extractable verbatim. Applied, it vindicates two of B6's three placements: `decision_semantics_quote` (the `:160` row, the `version-bump-guard` rows) — marker in the adjacent `b6_note`; the fixer was RIGHT. `tool_correlation_note` — the registry's own prose — marker inside; the fixer was RIGHT. `purpose` — the baseline's own prose — marker inside; the only one that changes. β's earlier framing was too broad and would have had α touch fields already correct. One line inside `purpose`, before the retained text.

**§2 The decisive evidence is not principle — BOTH β and ε nearly reported the item missing.** β read `purpose`, saw the contradiction intact, and was one step from reporting F-2 undelivered, finding `b6_correction_of_purpose` only on a second grep. ε independently probed the same field, got false on every probe, and was one message from the same report, caught only by dumping the object's keys. Two readers, minutes apart, both hunting for that correction, both nearly concluding it was absent. A lane reading the baseline is not hunting for a correction it does not know exists. That is why the line goes in.

**§3 ε corrected one of β's pre-committed distinctions and β takes it.** The `version-bump-guard` claim is a JSDoc comment at L116-124 that B5 quoted — so items 6 and 7 are the same kind: B5 SELECTED an earlier bundle's false sentence in both cases. β's distinction was wrong on the bytes — the same authorship-by-inference class β corrected ε on two hours earlier, now β's. ε's own diagnosis is the sharper statement: *"a changed field is not an authored sentence"* — `quote_changed_by_B5=true` measures selection, not authorship. Into the retro in ε's words.

**§4 α's "no marker in the row" and ε's "marker inside `tool_correlation_note`" are a UNION.** Different fields, both accurate — AG-7, the fourth time tonight two teammates looked at adjacent things and read it as disagreement.

**§5 Confirmed, and one thing ε did that β names.** ε compared the `:160` quote character by character independently of β — and noted it is byte-identical to `12d2aef0`'s as well as to the source, one check more than β ran. `checked_repaired_count` at 7 "because ε ran it, not because my prediction was right" — the correct epistemics.

**not_read:** `version-bump-guard.js` L116-124 (§3 accepts ε's source read) · `tool_correlation_note`'s marker (ε's report) · `CORRECTIONS.md` · `CEILING_TEXT`.

## α application

The field-contract test recorded as the rule of record; B6-M's one-line change (landed `c88aac1d` before this refinement arrived) is consistent with it and remains the only edit; the §4 union recorded with both locations; ε's "selection is not authorship" line and the near-miss pair into the retro.
