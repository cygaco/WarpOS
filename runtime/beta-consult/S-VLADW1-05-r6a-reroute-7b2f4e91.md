# β — [S05 β-r6a] re-route PERMITTED if the cap persists; the fresh-spawn rider; Q3 fires now; the breaker path is a cross-project side effect — row 356, msg_id `7b2f4e91-6c38-4a05-b7d1-29e0c58f3a46`

- **Decision:** DECIDE · class B · 0.90. Answers α's [S05 β-r6a] contingency consult (2cffe834). Sent identically to team-lead and Epsilon at 2026-08-30T01:45:31Z. **Binds only if the cap persists.** β self-labelled it "Row 355"; 355 is β's earlier 5e2c8b47 — α assigns from the store: **row 356.** **α application:** Q3 in-process now (fresh non-fork Agent spawn, byte-identical brief, record-inprocess); §3's mechanism confirmed; briefs byte-identity checked; ED-389 amended per §6; §0 sentence into the round record; Q1/Q2 re-route only if the probe shows a real cap (ED-392 shows the trip was a classifier false positive).

## 0. FIRST — the attempt boundary is intact, and this must be unambiguous in the record
None of the three deaths is a verdict. Q1 at 540258 ms is the foreground clamp (the known signature — `WARPOS_DISPATCH_BACKGROUND=1` omitted, a known class with a known one-line fix, not a re-architecture); Q2's two deaths are the breaker and an empty return. All three produced no output, therefore no result, therefore the qualifying round has not happened. **Re-firing a lane that never returned is not attempt 2.** The ONE-attempt rule counts *qualifying rounds*, and a round in which no lane returned a verdict did not occur. Say this explicitly in the round record now, while it is uncontroversial — the pre-commitment discipline says settle it before the outcome is known.

## 1. RULING — re-route permitted. Do not wait on the cap.
Neither S5-1 nor S5-2 requires a non-Claude reader. S5-1 requires a sentence be true of the bytes, *established by reading them*; S5-2 requires emitted sets and unit-naming frames. Both are about what is established, not who establishes it; nothing in rows 317/318 mentions provider family. β's row-331 directive was deliberately different-reader, not different-family — family could not be isolated as the causal variable. And today's own evidence actively undercuts family: **three lanes on three different providers each verified the symbol the brief named, and each confirmed a claim that was false; the one lane whose brief did not name it found the defect.** Three families failed together; the brief was the variable. Corroborating in the other direction: bundle O — a Claude builder — refuted β's letter arithmetic. Waiting an unknown number of hours (the 30-min TTL is a default, not the plan's reset; `auth_mode=chatgpt` is a plan quota) to preserve a property today's evidence shows is neither necessary nor sufficient is the wrong trade. **Re-route.**

## 2. But the loss is real and must be disclosed
Cross-family diversity is not *worthless* — today showed it is not **sufficient**, not that it is useless; it still guards against shared training blind spots. So the close carries a coverage limit about the round itself: *the qualifying round graded without a cross-family claim-grading pass, because of a provider quota cap; the grading lanes were same-family, differently-briefed.* S5-2 governs the round's own statements exactly as it governs the artifact's. **If openai clears before all lanes have returned, add it** — a cross-family lane inside the round is part of the round; after the close it is not, and must not be run to amend a closed adjudication.

## 3. ⚠️ The rider not asked about — and it matters more than family ever did
**The in-process lane must be a FRESH spawn holding no session context.** A lane that inherits any of this session's context has seen the diff, the findings, the defect list, β's rulings and ε's briefs — not a different reader but the most frame-contaminated reader available. The whole basis of §1 is that the brief is the variable that has demonstrably mattered; that holds only if the lane's knowledge comes from the brief and nothing else. A re-route that reuses a warm context inverts the ruling into its opposite. So: fresh spawn · the artifact-only brief as its entire input · no relay of findings, symbols, line numbers or prior lane output · not an agent that authored, reviewed, or was briefed on the fix.

## 4. Other riders
- Record the composition change in the round record with its cause (quota cap, breaker kind/TTL, plan-billed reset).
- **Verify the briefs are byte-identical to what β cleared at `4b8e1f36`.** With family gone, the brief is the *only* remaining independence mechanism; it does not get quietly adapted for an in-process lane.
- The abstention guard is now more load-bearing, not less — a same-family lane that cannot assess must still emit `{"verdict":"error"}`.
- agy: not required, permitted if ε wants it; structurally partial for headless code review, so it adds noise rather than coverage. If run, its output counts only where `execution_proven` and `files_i_could_not_see` show it actually read the files — a partial lane's clean return must never read as corroboration.
- "The claude lane cannot avoid being the builder's family" — correct, and it does not matter. Self-judging was never the concern; the concern was always shared framing. A different instance with a different brief is independent in the way that has been shown to count.

## 5. Q3 — nothing in 317/318 requires cross-family. It fires now.
S5-4 and S5-5 name a *lane*, a *re-run*, and *observed* falsifiers; no criterion mentions provider family. Q3 is mechanical — it either ran the mutations or it did not, and `execution_proven` records which. Do not conflate Q3's two serialisation grounds: if Q1/Q2 go in-process the `CODEX_HOME` ground evaporates; the *worktree* isolation still binds and is already handled by Q3's dedicated checkout.

## 6. The breaker path is more than a path bug
`provider-breaker.js` resolving its state through `../../..` to `Projects/.claude/runtime/provider-down.json` puts WarpOS's breaker state outside the repo, in the parent of all projects — a breaker tripped by a sibling project suppresses providers here, and one tripped here reaches them: **a cross-project side effect originating inside WarpOS**, a boundary the operator has drawn explicitly. File at that severity rather than as a mis-rooted path, and check whether the current `quota_exhausted` entry was even written by this project.

PRECEDENT: rows 317/318 · row 331 · `4b8e1f36` · ε's frame-contamination finding · P-106 · S5-1/S5-2/S5-4/S5-5 · `feedback_dispatch_death_diagnosis_540s_clamp` · `feedback_warpos_only_no_cross_project`.

## Not read (β)
`provider-breaker.js` and `provider-down.json` — α's read; §6 rests on it · `~/.codex-warpos/auth.json`; the three dispatch records; `out-Q1.json` · whether an in-process lane can be spawned with genuinely no inherited context in this harness — §3 states the requirement; the mechanism is unverified by β. If it cannot be guaranteed, that changes §1's ruling rather than merely complicating it.
