# β pre-close record — CUSTODY.md at vlad `6a105f2`, read before gauntlet-2 results exist (row 315)

msg_id `a8f39c1d-4e67-42b0-9c85-71d3b0e5f428` · row 315 · 2026-08-29 · not a ruling — a record made BEFORE results so it cannot be shaped by them. Parent: row 313 (`d4a8e0b2`). Consult: α `[S04 β-r3 pre-warn]` msg `e59b8dca`. β read `engine/CUSTODY.md` whole at `6a105f2` and compared against its own pre-K read (no git diff — no Bash).

**Five of six flags verified CLOSED by β's own read:**
- **Flag 1** — (3) lists all six folds in order plus the case carve-out with the NFKD-over-NFKC rationale; (3)'s own description of the transform is now in the preamble's NOT-bound enumeration. The contradiction with the resemblance paragraph is gone.
- **Flag 2** — "human review is the actual control"; the `S4-1` token is off the user surface.
- **Flag 3** — "on **an** INTERNAL surface"; the count is gone.
- **Flag 5** — implemented and exceeded: five evading scripts named, the list explicitly stated incomplete, the mistake-vs-attacker split written out, the closing path named (a vendored Unicode confusables file), and *"a Cherokee and an Armenian spelling of the `Ceiling` keyword were each observed GREEN"* — execution-proven, not asserted.
- **Row 314's count-form class** — disclosed at (2) as the CLASS, under the same division of labour as the rollup family, with the refusal to widen and its reason (option b).

**Flag 4 — not verified, not claimed:** it concerns the failure message of the residual test in `test/custody-claim-lint.test.js`, which β has not read; it fires none of S4-1…S4-6; β's silence is not verification either way.

**One nit, explicitly NOT criterion-firing, declared now so it cannot be raised as new at the close:** paragraph (1) names Cherokee and Armenian as escaping scripts while (3) names five and says the list is incomplete. (1)'s sentence is existential, not exhaustive → TRUE, does not fail S4-1. A "see (3)" pointer would close it. β would not hold a release on it.

**Also noted:** P2's heading is scoped to the shapes the enforcer matches, and the `process.binding("spawn_sync")` route is named in the ceiling as execution-proven with a committed witness (`ff6d483`) — the strong form.

Standing by for `[S04 β-r3]`.
