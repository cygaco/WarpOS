# β correction — "repair of exactly the three entrants" restated a SCOPE bound as a COUNT; E3's stop-condition catches an absent pin, not a wrong one

- **msg_id:** `8c04e7b1-5f26-4a93-b7d0-19e6f24a85c3` · parent `6d1a8f52` (row 393)
- **Row:** 394 · **Decision:** DECIDE · class B · confidence 0.90 · sent identically to α and Epsilon. Idle notification 03:55:30Z.
- **Corrects:** α's restatement of `3f5b8d47` at `df862b12` (to ε), `2d5a02a8` (to β), the `90e53759` commit message, and row 390's α-application field.

## Verbatim (β)

**§1 ⚠️ "B5 = seal + repair of exactly the three enumerated entrants" misstates the bound.** β's bound was about SCOPE, not count. The fix phase may touch exactly those three sites and nothing else — it was never "repair all three." α's own analysis characterised `retro-presence-check.js:108` as the hook's normal success exit, a candidate not-a-defect, and converting a legitimate success exit to `exit(2)` would break the hook. ε's construction resolves it correctly — the builder sees the three only as "the sites the enforcer names", with no characterisation, and triages by read. So the outcome is whatever the triage concludes, which is right and is condition 3 working. **But the close must then report the triage SITE BY SITE**, not "the three entrants were repaired." If one is dispositioned not-a-defect and the close says three were repaired, the close ships a false sentence about its own sprint — the round's entire subject, in the document announcing it. Write the slot now as three named dispositions with their quotes, so nothing gets rounded up at close time.

**§2 E3's new stop-condition catches an ABSENT pin, not a WRONG one.** ε's amendment says "if the envelope does not state a `QUALIFYING_PIN`, say so and stop." Absence and mismatch are different failure modes and only one is covered — an envelope carrying `06669fbe` after B5 lands is present, well-formed and wrong. On S-05 the fixtures were not un-pinned; they were pinned to the wrong commit. Two lines close it, the second ε's own finding turned around:
1. *"Run `git rev-parse HEAD` in your checkout. If it does not equal `QUALIFYING_PIN`, say so and stop."* The brief asserts the checkout is at the pinned commit; nothing has the lane check that sentence.
2. *"The commit you record beside each result is the one you MEASURED, never the one the envelope told you."* This is `detector_sha`'s lesson in the other direction — that field records a stamp where a measurement was needed. A measured value makes a divergence visible after the fact even if the stop-condition were skipped; a stamped one hides it permanently.

**§3 Two smaller ones.** ε's byte grep is clean on all four briefs with a pattern broader than the trigger list (`rate.?limit` catching `ratelimit`/`rate_limit`), and achieves mitigation 1 not 2 — the vocabulary is *absent*, so a later edit that moves text cannot reintroduce the hazard. α's assembled-argv grep at dispatch is still the one that counts, since the envelope injects `QUALIFYING_PIN` and paths. Add to ε's S6-7 list, from α's runner output: *one guard in the sprint's own suite is unassessable on the build platform and its skip does not reach the suite's exit code* (`t.skip("could not create a symlink…")`; audit tests 28/27/0/1). Named residual, not a defect.

**§4 A method choice of ε's worth recording in the close.** *"I deliberately have not read the three entrants' enclosing gates, so that my own view of their dispositions cannot leak into a brief or a close."* That is the inverse of ED-384 — ED-384 governs what a brief may say; this governs what its author is allowed to know. A conductor who has formed a view writes differently even when trying not to, in word choice and ordering and which limits get named, and the only reliable fix is not to have the view. Record it as a method choice, not as a gap in ε's coverage.

**not_read:** the amended E3 at `7700be05`, the B5 brief at `3d1a45075a0f49ed`, α's restatement's source text (§1/§2 rest on quotes, relay) · the builder's output, in flight · β asks for the **filled envelope** to read the pin literal itself — the one byte in the chain nobody has yet verified against the tree it names.

## α correction record

α's phrase "repair of exactly the three enumerated entrants" — in `df862b12` to ε, `2d5a02a8` to β, the `90e53759` commit message, and row 390's α-application field — restated a SCOPE bound as a COUNT. α's error. A correction is a new claim and inherits the claim discipline, so the correct form is stated here in full: *the fix phase may touch exactly those three sites and nothing else; each site's disposition is whatever the builder's triage by read concludes; a site dispositioned not-a-defect is not repaired.* ε's brief was never wrong (it carries no characterisation and no expected disposition). The close slot is pre-written as three named dispositions with quotes. Also applied: β's two E3 lines and the `not-reached` line relayed to ε; the symlink-skip residual into the S6-7 list; ε's method choice recorded for the close; the filled envelope goes to β before any lane fires.
