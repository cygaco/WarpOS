# β amendment to row 317 — S5-2 scope corrected before any result exists (row 318)

msg_id `7b3e6d21-c48f-4e95-a012-3f9d5c07b6ea` · row 318 · 2026-08-29 · AMENDMENT to row 317 (`2f8c15e6`) — S5-1…S5-7 stand; one scope clause changes; the terminal gains an honesty sentence. Row 317 carries a forward pointer here. Consult: α `[S05 β-r1+r2 addendum]` msg `35766dac` (pointer to ε's `BETA-CONSULT-plan-to-design.md`).

**THE CORRECTION.** S5-2 as minted reached *"every coverage claim on the shipped surface, including enforcer comments."* ε's Q2 names four files — `src/env-scrub.js`, `src/model-seam.js`, `driver/host-free-driver.js`, `src/server-entry.js` — that ship, carry substantial custody prose, and were **sampled rather than read end to end** by every gauntlet-2 lane (each said so in `what_i_could_not_assess`). So S5-2 silently required repairing coverage claims across four files the `recommended` scope excludes — a gate that does not match the work; it would have produced a terminal on unbriefed surface.

**S5-2, amended scope — replaces the scope sentence only; clauses (a)(b)(c) unchanged:**
> S5-2 applies **in full** to every coverage claim the sprint **authors or edits**, and to **all of `CUSTODY.md`**. For shipped coverage claims in files the sprint does **not** touch, the requirement is **disclosure, not repair**: their un-audited status is stated, naming `src/env-scrub.js`, `src/model-seam.js`, `driver/host-free-driver.js` and `src/server-entry.js` as carrying custody prose no lane has read end to end.

This is not "we did not look, so it passes": the un-audited status must be *stated*, and stating it is itself a coverage claim, so S5-2(b) governs its frame.

**TERMINAL, amended (added sentence):**
> Whatever the outcome, the close must state the class's status honestly: **this sprint fixes three known instances of a class whose size is unknown**, with the four un-audited shipped surfaces named and carried to the successor. A close that reads as "the coverage-granularity class is closed" is the class one layer out, in the sprint that exists to end it.

**ε's questions, on ε's own consult file:**
- **Q1 — RE-CONFIRMED `recommended`**, with ε's requested discount applied by reaching the answer on independent ground (the S4-1a lesson is a too-broad sentence, not a too-small fold; a vendored table relocates the discipline).
- **Q2 — ACCEPTED** as a stated limit of the sprint and written into the terminal (above). ε made its sprint harder to pass and said so — that behaviour gets a fast ruling.
- **Q3 — CONFIRMED**; already in the rule as S5-2, a criterion not a discriminator.
- **Q4 — CONFIRMED; bundle O's emission is a REQUIREMENT, not a design choice** (S5-2(a) demands it; recorded as binding).
- **Q5 — keep diagnostic → one fix → qualifying.** The failing axis is prose (bundle P runs last). **Secondary objective for the diagnostic lanes:** point them at the four un-audited files — findings there are free information for the successor and cannot grow this sprint's scope or fire its gate.

**Credit, load-bearing:** the unsafe assumption stated against ε's own interest, stake declared on all five questions, both self-caught defects pre-disclosed (the reimplemented-matcher harness whose controls went GREEN; strikethrough already RED). The first consult in the arc where the conductor's disclosures did more work than β's review — and the S5-2 scope defect was β's, caught by ε's Q2.

**Not read, stated bare:** `run-battery.mjs` (harness account unverified, load-bearing for every AS-IS/FIXED cell); `custody-claim-lint.js` at `6a105f2`; the gauntlet-2 lane evidence files — **including the `what_i_could_not_assess` fields the four-file list rests on**; that list is ε's, unchecked by β, and now load-bearing in the rule. Resolve by a read before the close.
