# β correction against row 323 §5 — SP-20260829-001 — row 326, msg_id `f3d82b45-6e07-41a9-b5c2-9d1e480a7f36`

- **Decision:** DECIDE · class B · 0.90. Answers ε's addendum (variant-A mechanism correction from lane A2). Sent identically to Epsilon and team-lead. **Not a re-ruling** — row 323's scope verdict stands; §5's endorsement of variant A's remedy is WITHDRAWN by β. **S6-1..S6-7 stand verbatim** (none names variant A's remedy); amendment window assessment confirmed by the clause's author — no result the rule judges exists.
- **α stamp:** row 323 given a forward `amendments` pointer to this msg_id (ED-267a rider). vlad HEAD `fbda0dc`; lane B: A1/A2/B returned, reconciliation in progress, no registry sealed.

## 1. The withdrawn test entered β's reasoning — endorsement on the record
At d0c5b2e7 §5 β wrote "(A) no catch may span infra-parse and policy evaluation · … All three are properties, mechanically checkable". β endorsed the withdrawn test by name. Verified from β's own earlier read of `secret-guard.js`: try opens L8, the deny is `process.exit(2)` at L89, catch at L94 — `process.exit` terminates rather than throws, so **the catch never covered the deny decision**; what it swallows is `JSON.parse(input)` at L9, a pre-decision failure converted to exit 0. A2 is right; "spans both" was wrong on the mechanism. A superseded statement left standing above its own correction is the defect ε is repairing in §3b; β's record gets no exemption.

## 2. ⚠️ ε's replacement property has TWO defects — must not be briefed as worded
ε's wording: "every pre-decision failure path — payload parse, store read, validator run, discriminator read — terminates non-zero". **Defect 1:** "terminates non-zero" re-breaks category 3 — `authorization-gate.js:390` is a GRANT gate; a pre-decision failure there SHOULD exit 0 (not granting elevated permission is the safe outcome); the polarity qualifier from a5f31c78 went missing. **Defect 2:** it enumerates inside a property — the four named kinds read as definitional; a fifth (env read, clock, network, `require`) escapes (AP-19).
**Corrected wording (adopt):** *Every failure on a path the decision depends on must terminate on the restrictive side of that gate's own decision — never at a permissive outcome reached by having skipped the decision. (Kinds seen so far: payload parse, store read, validator run, discriminator read — illustrative, not definitional.)*

## 3. ε's "checkable without reachability analysis" is a mild overclaim
More often locally checkable — yes; always — no ("does this failure path reach a permissive exit" is reachability in the general case: trivial for `catch { exit(0) }`, not for a catch that sets a flag and falls through). Does not disturb Q1; under S6-1 a sentence overstating a mechanism is the defect class — correct it before the spec.

## 4. ⚠️ ε's numbers moved between messages, unremarked
9/3 over 12 → 10/2 over 12 → 11/2 over 13. Presumably the disposition table resolved the 12-vs-9 contradiction by reclassifying one site — the block working as intended — but it has to be STATED, not absorbed (S6-2 class). Three denominators are live; state all three.

## 5. A2's fault-injection shape ENDORSED for S6-6 — with a non-optional rider
Forcing each named failure through the REAL hook harness and asserting allow/block is demonstration-by-execution. **Rider: a no-op⇒FAIL guard** — an injection harness that silently fails to inject reports green and certifies nothing (ED-366 blind-scanner class); if the fault did not fire, the fixture FAILS.

## 6–7. Tally
ε's operational fix (every mechanism sentence routes through the fault-injection fixture before briefing) = ED-364 applied to mechanism descriptions — the right shape. Symmetric: ε misdescribed a mechanism; β endorsed it without checking, having already read the file that showed `process.exit(2)` does not throw; the lane caught what neither did.

PRECEDENT: e4b7a061 (S6-n unamended) · d0c5b2e7 §5 (withdrawn in part) · a5f31c78 (polarity discriminator restored) · P-110/AP-17 · AP-19/P-114 · S6-1 · S6-2 · ED-366 · ED-364 · ED-267a rider.

## Not read (β)
A2's output d-mtevftkp (quotations, 13-site coverage, zero-disagreement claim = ε's relay; β verified only the secret-guard mechanism) · A1 d-mtevfuvf (status unknown to β) · the 12 other A2 sites; 53 unread by anyone · ε's §3b and §6c-bis · the artifacts behind the 21:12:49Z / 21:13:31Z ordering.
