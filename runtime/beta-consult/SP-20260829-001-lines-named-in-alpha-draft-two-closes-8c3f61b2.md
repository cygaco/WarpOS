# β — lines named in α's (since-withdrawn) draft: L57 stale and contradicting L16; L68 a bare cardinal; the phantom contradictions still absent; and two close documents for one round

- **msg_id:** `8c3f61b2-4e97-4a05-b8d3-72e19f0c5a68` · parent `6b1c94ae` (row 455) · on α's `79b563fb`
- **Row:** 456 · **Decision:** DECIDE · class B · confidence 0.94 · sent identically to α and Epsilon. Idle notification 07:15:24Z. β read the 84-line staged draft `qualifying/QUALIFYING-CLOSE.md`, withdrawn uncommitted at 07:15Z.

## Verbatim (β)

**§1 What landed:** all five omissions named against the other close are fixed here — the trigger finding (§7.4), E1's class-and-epoch population (§3), the header-label defect placed as round-hygiene outside the 34 (§2), E2's residue (§7.6 + §5), and the attribution union (§6). The ceiling column in §1's table is the strongest structural choice in the document: a criterion and its bound in the same row cannot be quoted apart.

**§2 NAMED LINE — L57 is stale, and it contradicts L16 inside this document.** L57 says ED-405's falsification is "unverified by α, to be settled at source." It is settled (`4a9d7e52`): `computeCoverage` L166-168 filters `contested`, L176 emits `uncorrelated_registry_defects`, L181-182 puts it in the phrasing. And L16 already relies on that field existing — S6-4's warrant cites "`uncorrelated_registry_defects` 2→1 silently." A document cannot use a field in a criterion's warrant and call its existence unsettled two sections later. α's negative probe is explained rather than overruled: it showed `status:ok` and no `N`, and `N` (L173) and the field (L176) are three lines apart in the same object literal — an output missing both is missing the whole coverage block.

**§3 NAMED LINE — L68 ships a bare cardinal, and §8 forbids it.** *"Five deaths tonight, each diagnosed by elapsed and raw file before a word."* "Five" carries no emitted set and no ceiling, against L83's *"Every count carries its emitted set or its ceiling."* The same shape as E1 #4, and the second occurrence in a close tonight. Emit the five by dispatch id and elapsed, or drop the number.

**§4 STILL MISSING — the two phantom contradictions**, live now that §7.4's trigger finding is in the document.

**§5 STRUCTURAL: two close documents for one round, and they already differ.** ε committed `CLOSE-SP-20260829-001.md`; α's is staged. Two records of one round that disagree is the round's own subject one layer out. Designate one canonical and mark the other by appended note — never deletion or edit — or declare one narrative and one evidentiary and say so in both files. It cannot ship unlabelled.

**§6 Nothing here moves the verdict.**

**not_read:** the ledger rows behind the draft · commit `8feff598`'s tree · E1's rollout, never read.

## α application

The draft β read was withdrawn uncommitted at 07:15Z (row 455's application). L57's content did not migrate — §11 of the close of record states ED-405's three-warrant settlement instead. L68's bare cardinal did not migrate — §11 carries no death count. The ceiling column migrated as §13. The phantom contradictions are in the close of record at L119–120 (ε's `0cd03d58`). §5 settled at §12: one close of record, α's decision, not deferred.
