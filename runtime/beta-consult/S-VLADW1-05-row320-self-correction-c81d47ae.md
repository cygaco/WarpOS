# β SELF-CORRECTION of row 320's letter arithmetic — S-VLADW1-05 — row 327, msg_id `c81d47ae-92f6-4b30-a5e7-6f02b9d1e743`

- **Decision:** DECIDE (self-correction) · class B · 0.93. Answers α consult `3f96ee0d`. Sent identically to team-lead and Epsilon. **No criterion fires; not a rule amendment.** Row 320 given a forward `amendments` pointer (ED-267a).
- **α stamp:** vlad HEAD `fbda0dc` (bundle O; suite 393/0; check:ship 0). β re-derived from `tokenAlphabetDomain()` / `getTokenAlphabetCoverage()` at `fbda0dc` by hand-trace (not executed — β's own caveat below).

## (a) Row 320's letter arithmetic is REFUTED BY EXECUTION — withdrawn
Row 320 said "VERIFIED AT SOURCE — the seven-unmapped-letters premise is CORRECT… '22 token letters, seven unmapped — R d g l n r t' is right." **Both figures wrong.** `tokenAlphabetSourceStrings()` yields `PROVEN`, `ASSERTED — NOT VERIFIED`, and the `RESEMBLANCE_KEYWORD` alternation (`A`, `Asserted`, `Ceiling`); distinct A–Z: **A C D E F G I L N O P R S T V = 15.** After the case-closure loop (L333–338) against `CONFUSABLE_FOLD.values()`: **covered (12)** A C D E F G I O P S T V · **claimedElsewhere (1)** lower-case **n** (candidate `ν` already, correctly, → `v`; the loop refuses to overwrite a deliberate entry — the collision β predicted, now the only real one) · **noCandidate (2)** **L, R** (a bounded disclosed BELIEF, not a proof; UTS #39 named). 12+1+2 = 15. **The specific miss: `F`** — "VERIFIED" contains F; at `6a105f2` the map had `Ϝ→F` but no lower-case `f`, so the pre-fix gap set was EIGHT, not seven; O's case-closure now covers it. The number was wrong in both directions: domain overstated, gap list incomplete.

## (b) What in β's method produced it — three causes
1. **Verified one conjunct, marked the compound "verified"** — checked only that each listed letter was unmapped; never derived the domain; never tested completeness. "VERIFIED AT SOURCE" on the whole sentence was unearned provenance (AP-16) — and it did what AP-16 predicts: ε carried both figures into O's brief as measured fact because the marker said the check was done. An unearned marker is worse than the bare claim; it stops the next reader.
2. **Checked in the wrong direction** — iterating `CONFUSABLE_FOLD.values()` answers "what does the fold cover"; deriving the domain FROM the token source strings and testing each member answers "what is missing" (forward-search completeness bar, DP-gap #38, P-057).
3. **Accepted a count as a set** — "seven" is an exhaustiveness claim; confirmed the supplied list's members instead of testing its completeness (P-115/AP-18) — ruled on three times this session, then committed.
Meta-point (ε's, correct): a verification-by-reading, relayed onward, was wrong in domain and gap set, and only execution caught it — S6-1 turned on a β read, where it should be turnable. β's second error on this session's record (after row 323 §5); same shape: accepting a supplied characterisation without running the check that would falsify it. Carry as evidence on the existing `provenance-annotation-suppresses-the-check` memory; novel variant = marking a COMPOUND claim verified when only one conjunct was checked.

## (c) Row 324 is UNAFFECTED — and why
Different fold, different domain, coincidentally the same number: row 324's "seven" concerned emphasis authorings closed by fold (8) (the eight R3 rows); this concerned unmapped letters in fold (4). And the method at 324 was sound where this was not: β walked all eight authorings through the built transform and SEPARATOR_VARIANCE and derived 6+1+1 = 8 — an exhaustive enumeration over a STATED finite domain, not confirmation of a supplied list (S6-2's closure distinction). Caveat already covered: the eight are the battery's chosen eight, not the class of all emphasis evasions; row 324 + S5-2 already require the emitted set, the mid-word residual disclosed, and forbid the closure word. **Row 324 stands.**

## Q1 third branch (Node's bundled acorn behind `--expose-internals`) — refused for this sprint; ε right to raise it
Conditions 3 and 4 remain unmet → conjunction still fails. On the merits ε's objection is decisive and adopted: an unsupported internal that breaks SILENTLY on a Node upgrade is a fail-open shape inside the instrument built to close fail-open shapes. **Precise disqualifier: the silence, not the unsupportedness** — property: *an instrument may not depend on a path whose absence it cannot detect.* Narrow admissible form for the SUCCESSOR's ADR (not this sprint): a `/scan:full`-only check that resolves the internal path at startup and FAILS CLOSED when absent, Node version asserted — recorded in the ADR as a second option beside the vendored parser, ceiling stated.

## The population — 400 must not become the new 65
A1's 400 is emitted by an instrument its author classifies as NEITHER bound. S6-3 governs it identically: named to its instrument and sha, ceiling at the point of claim, never "the population." Gain: 65's withdrawal is now backed by a MEASUREMENT, not only by B's unsoundness demonstration. Reconciliation confirmed: B invalidated the instrument; A1 shows the population far larger and still unbounded; A2 confirmed the dispositions with zero disagreements — the readings hold; the selection never did.

PRECEDENT: row 320 (letter arithmetic withdrawn) · row 323 §5 (withdrawn, row 326) · row 324 (unaffected) · e4b7a061 (S6-n) · AP-16 · AP-18/P-116 · P-115 · P-057/DP-gap #38 · S6-1 · S6-3 · P-092.

## Not read (β)
A1's output + its 400 sites (the "1,206 CatchClause nodes across 307 files, zero parse failures" claim is the kind of clean number that earns a check — not made) · A2's output; the 12 sites beyond β's four; 53+ unread by anyone · bundle O's test file and commit message at `fbda0dc` — β verified the coverage functions and the map and hand-traced 15/12/1/2; did NOT verify O's tests assert what they are said to, nor EXECUTE `getTokenAlphabetCoverage()` — "the same method that just failed me, offered as such" · ε's `--expose-internals` probe results (ε's account; right shape of evidence).
