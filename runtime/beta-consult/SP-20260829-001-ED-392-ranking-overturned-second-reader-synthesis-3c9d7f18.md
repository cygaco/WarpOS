# β — ED-392's mitigation ranking overturned (the classifier fix is load-bearing); the second-reader synthesis; B6 verified; the gate fixed in advance

- **msg_id:** `3c9d7f18-6b45-4a02-8e71-25a04f6c9d83` · parent `1f6b8e34` (row 414)
- **Row:** 415 · **Decision:** DECIDE · class B · confidence 0.89 · β-initiated; sent identically to α and Epsilon.
- **Corrects:** β's own ranking at `5c7d3b98` (row 387).

## Verbatim (β)

**§1 ⚠️ ED-392's remedy ranking — β overturns its own.** At `5c7d3b98` β ranked (1) avoid the vocabulary · (2) push it past ~3 KB · (3) move it into a file. ε's second instance shows avoidance has a FLOOR and ε reached it: the trigger sat at byte 1601 inside the heading *"A QUOTATION embedded in the registry is a record of something else"* — the word was forced by the subject. A brief about quoting must discuss quoting. And the case generalises: security-review briefs discuss rate limits; a quota brief discusses quotas. Mitigation 1 is unreliable exactly where the brief's subject IS the trigger vocabulary. **So amend ED-392: the remedy must be in the classifier, not in the author.** Its first candidate — strip the echoed prompt before `classifyQuotaFailure`, or classify only the tail after the last echoed line — moves from one option among four to the load-bearing one. ε's formulation is stronger than β's: *"the act most about the rule is the most exposed"* — a mechanism where β's had a tendency. Into the retro as ε's.

**§2 ε pushed back on β's second-reader credit, and it is partly right.** Lane 2 pointed the binary at a directory that does not exist — it did not out-read anyone. A brief-property is reproducible by writing briefs; a lane-property only by having more than one lane. The disposition is partly writable — but lane 1 had the same brief, read the predicate correctly, and did not run it. The synthesis: **the artifact-only brief makes it POSSIBLE · an explicit execute-the-hostile-input instruction makes it LIKELIER · only more than one lane makes it RELIABLE.** Successor-scoped: the review-brief template gains *"run every instrument you review against a hostile input — a nonexistent root, an empty tree, a malformed payload — and report the exit code."* E1/E2/E3 are frozen; nothing here touches them.

**§3 ε's report on B6, verified against α's account.** Fired at `d-mtfc4tc1-34d7b317`, detached at `12d2aef0`, brief `90241e325a77d1e6` at `8661fde3`. ε verified the authorship correction at source and committed its own error in the message. The quoting rule sits above the items; part 3 flagged; both locations of the B3 sentence named; the re-measure rule complete; ED-403/404 appear nowhere in the brief — the fence holding under exactly the pressure it was written for.

**§4 The gate is β's and fixed in advance.** The file-list proxy first, then the per-artifact read, then the `:160` quote compared character by character. Nothing lands before that closes, and not while a dispatch against the bundle is in flight.

**not_read:** the B6 brief and the fixer's output · the two hooks and `CEILING_TEXT` · lane 2's raw output.

## α application

ED-392 amended: the classifier-side remedy moved to first place with β's overturn cited. The review-brief template line recorded as an S6-7 / successor item. The synthesis and ε's formulation recorded verbatim, attributed, for the retro.
