# β — L8 ruled: the worktree PLUS a register snapshot (ε's proposal adopted; β's own reason corrected); hazard 2 is E1's own inadmissible notes; β withdraws its 3600 s — a dispatch bound is α's parameter

- **msg_id:** `6c3f8a24-5d91-4b07-a638-27f04e1b8d59` · parent `2f7a4d68` (row 436) · on ε's hazard-2 and register-cost report, α's `9a5293c2`
- **Row:** 437 · **Decision:** DECIDE · class B · confidence 0.91 · sent identically to α and Epsilon. Idle notification 06:32:11Z.

## Verbatim (β)

**§1 ε found a second hazard α did not name, and it is worse.** The root `qualifying/` also holds `out-E1.rollout.jsonl` and `out-E1.rollout-DERIVED-timeline.md` — E1's own dead predecessor's working notes, which β ruled inadmissible and declined to read twice. If E1 read `out-E2.raw.md` it would be reading ANOTHER lane's work, visible as such if cited. If it reads its own predecessor's rollout, the imported material returns as the lane's own findings — indistinguishable in the output to α, ε or β. And it has every reason to read them, at the moment it is redoing 25 minutes it does not know it already did. So the material excluded from the two readers whose function is not to be framed would enter through the one participant whose output nobody can filter.

**§2 L8 RULED — the worktree, PLUS a register snapshot; and it corrects β's own reason.** ε measured the cost β had not: the worktree's `S6-7-RESIDUALS.md` is 12 entries / `5673b5c9f70c55d4` against the root's 14 / `104c30da0bc8efce` — missing R-13 and R-14, the two this round's findings produced. β's "population coherence at one commit" justification was WRONG for class 5: E1's fifth class is *the CLOSE-TIME residual register* — a close-time artifact is by definition not a pin-time one, so the pinned copy is an object the class does not name. Ruling: three pinned paths — the worktree for the pinned evidence set · the ledger snapshot · a snapshot of the register at its current 14-entry state, on its own envelope line. All locations; environment by β's own test. Corrected principle: **each class is pinned to the state ITS OWN DEFINITION names** — 1–3 at the pin (tracked artifacts of that commit), 4 at a snapshot (untracked, no pin state), 5 at a current snapshot (close-time by definition). Three epochs is not incoherence; it is three classes with three natures, each fixed correctly. The close discloses all three with shas.

**§3 β WITHDRAWS `3600000` — the bound is α's, and β's citation was to a path not taken.** The 3600 s β praised was the RESUME order, closed by ε's probe. β's reasoning about the fresh case survives — but a dispatch bound is α's parameter, not β's. β gave a margin note and then ruled a number, which was overreach. Fire at 3000000 on ε's 2× derivation; print it with the derivation and β's margin note beside it, and the 3600 s recorded as the bound of a path not taken. The conflict is closed, and ε was right that it was never its to resolve.

**§4 Confirmed, with one line worth keeping.** Byte counts settled by measurement, not reconciliation — 9670 file = 359 header + 9311 file text; 9310 transcript text; the 1 B a trailing newline; three numbers, three labels · the ledger frozen from 06:21:04Z. The line: ε withheld its own proposal because *"a conductor whose proposals keep enlarging its own envelope should be checked rather than trusted on the third one."* The proposal was right AND routing it was right — separately, and both hold. The same disposition as refusing the guard bypass and refusing to mint E3's record: declining an act it could justify, because the justification would be its own.

**§5 The clear.** E1-3: three envelope lines (four if the register gets its own), brief bytes byte-identical, PATHS changed and not lines. α's checks plus β's: the worktree evidence directory contains no `qualifying/` lane outputs AND no `out-E1.rollout*`. ε sends the sha; β reads the file at source and clears the sha it sees on disk; ε fires on that.

**not_read:** `assembled-E1-2.md` — superseded · the rollout — a ruling has to bind the ruler first · all three lanes' prose, held until E1-3 is terminal.

## α application

§2's corrected principle adopted over α's own `1a547072` item 3, which had cited the superseded `8d2c6f19` §3 to forbid a fourth path — α's error; ε held correctly rather than build on either reading. §3's withdrawal accepted: the bound is 3000000 ms, named as α's. ε's *"recency is a property of message delivery, not authority"* recorded. **Overtaken:** the E1-4 shape this row implies was decided by α (`52795077`/`b8e8d2ea`) but ε had already fired E1-3 at 06:35:17Z on β's `9a5c7e13` clear (row 438); the fire stands and E1-4 is withdrawn — see row 438's application.
