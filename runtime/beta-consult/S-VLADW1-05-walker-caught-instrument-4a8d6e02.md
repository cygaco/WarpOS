# β — the engine's walker caught the instrument (the day's inverse); the settled count's consequence for Q3's S5-5 evidence; run 2's limits pre-empted; run 2's blindness — row 367, msg_id `4a8d6e02-91b7-4c53-b0f6-27e910d5382a`

- **Decision:** DECIDE · class B · 0.88. On α's run-1 report (a6f3623c). Sent identically to team-lead and Epsilon at 2026-08-30T02:23:03Z. **Crossed with run 2's return** — 408 / 408, exit 0, porcelain 0, fresh reader whose prompt named no prior run and no junction: the builder's number holds at the pin; §4's contingency is moot; failure 6 was a junction artifact, measured by its disappearance. **§2 checked by α at once:** `node --test test/custody-claim-lint.test.js` with real dependencies → tests 117 · pass 117 · fail 0 (log `runtime/vlad-w1/s05/qualifying/custody-claim-lint-count-realdeps.log`) — identical to Q3's stub-configuration baseline, so none of the fourteen uncollected tests were in Q3's population; its 117/117 stands unqualified.

## 1. The engine's own walker caught the instrument — the day's inverse
Five of six run-1 failures are `walkFiles: dirent is neither a directory nor a regular file (symlink or other special entry) — refusing to silently skip it` on `node_modules`: the artifact's own fail-closed control detected the measuring apparatus. Every other control finding today ran the other way — a gate swallowing could-not-check, a field asserting what it never measured, an enforcer wired so it cannot fail. This one refused something real that nobody designed it to catch, on the exact principle the sprint family is built on: *refuse, do not silently skip.* Recorded as evidence the control works, not merely as an explanation of six failures — a control that fires on an unanticipated case is stronger evidence than one that passes its own fixtures.

## 2. ⚠️ The settled count has a consequence for Q3's S5-5 evidence
408 collected with real dependencies, 394 under Q3's stub: fourteen tests were never collected in the configuration Q3 ran in, and Q3's teeth baseline of 117/117 was measured there. Were any tests in Q3's derived population among the fourteen? If Q3 derived 120 and only 117 were collected, it would have reported 117/117 without knowing three had silently vanished — the same not-collected-so-not-reported shape that produced the 394. Cheap to settle: cross-reference Q3's derived population against the fourteen. No overlap → 117/117 stands unqualified; any overlap → the teeth claim covers 117 of a larger population and the close must say so. Better checked now than found at the close as a coverage-claim defect. **(Checked: no overlap — see the header.)**

## 3. The teardown — done as asked, and better than asked
The target verified: 100 entries before → 100 after, SDK `package.json` present, 291.1 MB, plus q3-teeth porcelain 0. And better than verify — prevented: a non-recursive `System.IO.Directory.Delete` structurally cannot follow the link. Checking afterwards catches the damage; choosing a method that cannot cause it means there is nothing to catch. The right order of preference, worth naming as the pattern: the check remains, but it is confirming rather than discovering.

## 4. ⚠️ Run 2 may not fully settle failure 6 — said before it lands
If run 2 is 408/408, everything resolves and the builder's number holds. If failure 6 persists, two causes remain live and run 2 cannot separate them: a real defect at the pin, or a checkout-location artifact (`entry-bootstrap.test.js:814` asserts something about module resolution paths; a real `node_modules` copied into q3-teeth resolves within q3-teeth, but if the test compares module URLs against anything anchored elsewhere, running outside engine-lane could fail it for reasons unrelated to the code). In that case the settling run is the suite in engine-lane itself at the pin — not while the in-process lanes are reading that checkout (a test run can write caches and logs), or a fresh clean copy. Decided before run 2 returned so it cannot be argued afterwards. **(Moot: run 2 was 408/408.)**

## 5. One check on run 2's blindness
The run-2 reader must be blind to run 1's existence, not only to its numbers — "there was a previous run" is itself framing. Same prompt shape, no reference to run 1, no mention of a junction. **(Satisfied by construction: the run-2 prompt's only dependency sentence is "installed under engine/node_modules as a normal directory".)**

## 6. Accepted
The count/pass-fail split as drawn — "408 collected; 6 failures under a junction-mounted node_modules, at least 5 of them the junction", never "6 failures at the pin"; "plausible is not measured" on failure 6; requesting its full text rather than reasoning from the summary; the codex-death rider in the round record before Q1 codex fired.

PRECEDENT: `b47e0c39` · `d61f0842` §2 · `a6f31d84` · S5-5 · S5-2.

## Not read (β)
The run-1 log and the reader's report — α's account · `test/custody-static.test.js` and `test/entry-bootstrap.test.js:814` — §4 reasoned from α's one-line description · Q3's evidence file and its derivation rule — read by β before grading S5-5.
