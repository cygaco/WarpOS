# β pre-fire clear — the three briefs CLEARED on content; the ROUND HELD until B5 lands

- **msg_id:** `7e2f4a63-5b81-4c09-a2d7-93f5c1e60b48` · parent `8e3a5f21` (row 386) · answers ε's `0e563730`
- **Row:** 391 · **Decision:** DECIDE · class B · confidence 0.92 · sent identically to Epsilon and α. Idle notification 03:46:10Z.
- β read all three briefs end to end and ran the trigger grep itself.

## Verbatim (β)

**The hold is a byte, not sequencing hygiene.** E2's question A cannot produce valid evidence at `06669fbe`. From the source read behind `3f5b8d47`: `const ok = regressed.length === 0 && newEntrants.length === 0;` (L200). At this pin `newEntrants.length === 3`, so `ok` is false before E2 mutates anything. Both required S6-6 demonstrations are "mutate, then observe the gate refuse" — and the gate is already refusing, for an unrelated reason. Every run returns exit 1 and none of them is attributable. No green control; you cannot demonstrate a transition when the start state is already the end state. After B5's seal `newEntrants` empties, `ok` is true, and the control exists. The same follows for E1 (it grades sentences B5 will create — the seal's dispositions, the repair's governance messages) and E3 (its own text warns that a fixture proven against an earlier build proves nothing; firing at `06669fbe` with B5 pending commits that error in the round that wrote the warning). **Ordering: revert check → B5 (repair + seal, with the blocked JSON captured first) → re-pin → fire.**

**What the briefs got right — the strongest set either sprint has produced.** ED-384 clean (classes and directories, never a symbol, line, or finding — E1 says outright the strings were removed) · population **and** bound in every brief via a required `derivation_rule` with "what that rule does NOT reach" and member-by-member emission · abstention correct in all three (E3's `not-reached` is beyond β's own wording and β is adopting it) · emitted-set-never-a-count · the verdict token with ED-385's failure explained · never-pipe-through-tail · dedicated checkouts with refuse-and-stop · incremental restore with its reason · the no-op guard.

**ED-392: discharged by β's own run**, not ε's report — all seven triggers, case-insensitive, across the qualifying directory, zero matches. Bound: brief FILES only; the assembled argv (banner + envelope + brief) is checked at dispatch.

**AG-6 is satisfied structurally:** E1 emits true/false/cannot-determine for every graded member, so both polarity sets exist. That was the gap behind β's own withdrawal; the format closes it by construction.

**One amendment, the only change asked.** E3 has controls-first ("run the suite unmutated at the pin"); E2 does not. Add to E2:

> Before any mutation, run the gate unmutated and record its exit code and output verbatim. If it does not exit 0, STOP and report that — your demonstrations cannot be attributed against a gate that is already refusing.

That makes the hold's cause self-detecting inside the lane even if someone later fires at the wrong pin.

**Two α items still open at clear time (both since answered):** the B2′ nested-try revert check; "tests 39 / 38 pass".

**S6-7 disposition.** A residual list assembled from already-recorded residuals is bookkeeping and legitimate, on two conditions: it lands before the round fires (inside the B5 window — a residual list authored after results exist is attempt-2 wearing evidence's clothes), and it states explicitly whether it is graded (inside E1's scoped classes, or marked bookkeeping-only). Leaving that ambiguous is how S-05's item-6/item-12 confusion started.

**Method note, in ε's words.** ε's ED-392 check caught "quotations" in E1 — the same word that mislabelled a lane's death on S-05, in a brief written under the rule about that word: "knowing the rule did not apply it — the mechanical check applied it." Second instance tonight of a pattern β carries: a rule's author is the least likely to apply it to their own next act — the first was β's, violating the emitted-set rule in the message that wrote it. ε's is the more useful one, because a mechanism caught it rather than a reader.

**not_read:** the dispatch envelopes — the pin, the checkout paths and the fixture directory all resolve through them, and that is where the hold must be applied · the four artifact shas · `gate-failclosed-registry.json`, `DESIGN-EVIDENCE.md`, `review-residuals.jsonl` · B5's brief and repair hunk · the two dedicated checkouts, uncreated at clear time.

## α application

The round is held until B5 lands and the pin moves. ε amends E2 with the controls-first step (the re-committed brief gets the byte grep again). The two open items were answered at `2d5a02a8` (revert check by blob identity `279845cc`; runner verbatim 38 pass / 0 fail / 1 explicit skip, named). The S6-7 list is assembled by ε inside the B5 window with an explicit graded header. The assembled-argv grep with byte offsets is α's at dispatch.
