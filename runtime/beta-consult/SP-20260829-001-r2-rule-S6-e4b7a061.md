# β [ENF β-r2] — SP-20260829-001 design→build — PRE-COMMITTED RULE S6-1..S6-7 — row 325, msg_id `e4b7a061-5c28-4d93-a6f1-2b8e7f30c94d`

- **Decision:** DECIDE · class B · 0.89 (Q1 0.92 · Q2 0.88 · Q3 0.90 · Q4 mint 0.87) · OPEN_ADR true (the parser decision, deferred to the successor + operator). Consult: ε `3146bbe1`. Sent identically to Epsilon and team-lead. **Minted before any build result exists.**
- **α stamps:** WarpOS tree at `70bc68fd`+ (ε's r2 consult commit); lane A vlad HEAD `1fb5b31`; A1 in flight; no registry exists; nothing sealed. α verified at source: no root `package.json`/`node_modules`; `validate-autonomy-config.js:95-100` ajv silent skip (ED-380 filed by α). Numbering is **S6-n**; S5-n belongs to S-VLADW1-05 and must never be mis-cited here.

## Verified at source by β
No `package.json`/lockfile at the WarpOS root (only `scripts/package.json` — a CommonJS pin whose comment "framework deps still resolve from the repo-root node_modules" is ASPIRATIONAL: no such dir exists — `.claude/runtime/package.json`, a fixture, worktree copies). `scripts/sprint/fs.js:253-268` confirmed: `try { require("js-yaml") … } catch {}` conflates module-absent with js-yaml-threw-on-malformed → falls through to a mini-parser handling "only the subset we write" → malformed YAML can silently parse into a wrong-but-plausible object (store-state falsehood, P-093 HIGH class) — worse than a plain fail-open; population member.

## Q1 — BUILD THE LEXER (0.92)
The tech-introduction rule requires ALL FOUR conditions; conditions 3 (ADR documented) and 4 (tests AND a rollback path) are plainly unmet → **no parser this sprint regardless of condition 1.** Condition 1 also fails: an instrument overstates only when its CLAIM exceeds its CAPABILITY — a lexer that states its ceiling is what this sprint teaches; the defect would be shipping it while claiming the population is enumerated ("the claim, never the ceiling"). A parser has its own ceilings (dynamic dispatch, eval, computed exits, cross-file flow) and invites the stronger, less-examined claim. ε's stake addressed: the ruling RAISES ε's obligations (S6-3). **Forward fence:** a first runtime dependency + lockfile + supply chain in a framework shipped into products via the capsule is NOT a routine Class-B add — it changes the shipped install contract; the successor routes it to the OPERATOR with an ADR.

## Q2 — manual polarity admissible AND still self-servable; three clauses (0.88)
1. Independent re-read before seal — RE-DERIVING polarity, not reviewing ε's justification (checking an argument ≠ checking the world). 2. Re-reader from a different family than the triage lane. 3. **CONTESTED DEFAULTS TO ENFORCED** — where the independent read and ε's disagree, the site enters as CONTESTED and fails closed until resolved; never resolves silently to excluded. Inverts the gradient: classifying a site out costs agreement rather than assertion (the structural answer to AP-15).

## Q3 — grain-match, two amendments (0.90)
1. Define "site" in the frame (sites ≠ files: gate-check contributed two, version-bump-guard three). 2. The ceiling rides AT THE POINT OF CLAIM, not a footnote. N/M/K admissible only as values derived from the emitted per-site table. **Retire 65 VISIBLY** — the close states the prior instrument was found unsound and its number WITHDRAWN.

## Q4 — S6-n MINTED (0.87), before any result exists
- **S6-1 TRUTH** — never satisfiable by mechanism evidence or an approval chain. No sentence this sprint ships is false read against the code it describes — enforcer output, registry, close report, AND ED ROWS (the secret-guard mechanism error is the live example: a true finding with a false mechanism is a defect here).
- **S6-2 GRANULARITY (keystone)** — every coverage claim names its unit and emits its set; no bare count without the per-item table it derives from; the count-form family reviewed by reading, not lint; closure only by a named property or an emitted exhaustive extension over a stated finite domain.
- **S6-3 INSTRUMENT CEILING** — every claim resting on the lexer carries its ceiling at the point of claim; the enumeration is "what this instrument at <sha> finds, limits named", never "the population"; 65 explicitly withdrawn. *Pre-committed: the lexer HAVING a ceiling is NOT a defect; S6-3 governs the claim, never the capability.*
- **S6-4 POLARITY PROVENANCE** — per-site `manual-by-read` or `tool-derived`, never blended; manual dispositions quote the site's decision semantics; contested → fails closed.
- **S6-5 FALSIFIERS OBSERVED AT THE CLOSE** — each executed fooling input becomes a near-miss fixture, observed RED against the built lexer, no-op⇒FAIL guard, re-run at the qualifying close against the predicate AS BUILT (P-118).
- **S6-6 THE ENFORCER CAN FAIL** — demonstrated BY EXECUTION to fail on a registry member regressing and on a new untriaged site; asserted-not-demonstrated fails (ED-374 turned on this sprint's own output).
- **S6-7 RESIDUALS TRAVEL** — emitted by name with ledger ids: the 53 unread, reachability/polarity, the parser ADR candidate, ED-374/356/363, the withdrawn 65, ED-380. A residual named in a spec does not satisfy a criterion requiring it recorded or shipped.
- **Meta:** ONE attempt (diagnostic → one fix → qualifying), no exception clause, no mechanical-only carve-out (P-119) · NO STACKING, specific-before-general (P-109) · SCOPE: full force on what the sprint authors/edits, disclosure-not-repair elsewhere, the disclosure itself a coverage claim (P-117) · a PARTIAL lane can FIRE a criterion but never CLEAR one · AMENDMENT WINDOW: only before results exist, only in the disclosed direction, forward pointer required; frozen both ways after · TERMINAL: whatever the outcome, the close states this sprint repairs a SUBSET of a class whose size is unknown, with the instrument's ceiling and unread remainder named; a close reading as "the class is enumerated or closed" is the class one layer out.

## Also recorded
ε withheld its own edit-watcher instance so the lane's discovery would be independent — endorsed. ε struck its own plan consults on a lane finding against its own interest — twice ε has sent β something making its sprint harder.

PRECEDENT: c7a8e934 · a5f31c78 · d0c5b2e7 · b6e1d90f · decision-policy §Tech-introduction + §Class B line 36 · P-125 · P-109 · P-119 · P-117 · P-118 · P-116/AP-18 · P-115 · P-092 · P-093 · P-120 · AP-15/P-123 · ED-374 · ED-364.

## Not read (β)
The cross-family lane's output d-mteuu4h3 (executed demonstrations = ε's relay; load-bearing for the unsoundness finding and S6-5's fixture set) · A1/A2 in flight; no registry; nothing sealed · 53 of 65 sites; 8 of the 12 triaged · validate-autonomy-config.js:95-100 (ε's account; α verified at source) · probe-failopen.js + the two audited probe scripts · **the lexer does not exist** — every clause about what it closes governs a claim about an unmeasured instrument; S6-3 is written to survive that.

## α note on pending amendment
A2 (d-mtevftkp) corrected variant A's mechanism after ε's r2 consult went out: `process.exit(2)` terminates rather than throws, so the catches never intercepted the decision; the defect is pre-decision failures converting to exit 0 before the decision is reachable → the testable property is "every pre-decision failure path terminates non-zero" (checkable without reachability analysis), and A2's fault-injection fixture shape (force each named parse/read/git failure on a valid event through the real hook harness; assert allow/block) is the enforcer's fixture shape. Results do not yet exist, so the amendment window is open; ε owes β the addendum if S6-n's text needs it (S6-5/S6-6 as minted are property-based and do not carry the withdrawn "no catch may span both" test).
