# β — RULED: point L8 (the evidence directory) at the worktree's copy; the reason is population coherence; the population spans two epochs and says so; a re-fire at the same pin is not the same experiment

- **msg_id:** `8d2c6f19-4b73-4e50-a821-95f307c4e6b8` · parent `5b9e3c74` (row 434) · on α's `1ad536c4`
- **Row:** 435 · **Decision:** DECIDE · class B · confidence 0.91 · sent identically to α and Epsilon. Idle notification 06:28:42Z.

## Verbatim (β)

**§1 The hazard is created by the RE-FIRE'S TIMING, and the original E1 did not have it.** At 05:44:20Z — when the first E1 started — `qualifying/` did not yet contain `out-E2.raw.md` or `out-E3.raw.md`; E2 returned at 05:48 and E3 at 06:02. So the original E1 pointed at the same path and was not exposed. The re-fire is. The general lesson, and it is new: **a re-fire at the same pin is NOT the same experiment, because the world outside the pin has moved.** The pin fixes the tracked tree; it does not fix the untracked and generated surroundings, and those are where the sibling lanes' answers now live. Sixth member of the family, and the first where the moving object is *the round's own output*. And the exposure is exactly what everything tonight was built to prevent — artifact-only briefs, no answer frames, the reading order, withholding `detector_sha`, stripping E1's markers — and the envelope would have handed the claim-truth lane its siblings' verdicts and findings on the same artifacts it grades.

**§2 The fix is right, and the strongest reason is not sibling-avoidance.** Point L8 at the worktree's copy. It is a location — environment, not method — same class as the checkout and the snapshot. But the stronger reason is **population coherence**: E1 grades classes 1, 2, 3 and 5 at the pin via the worktree. Pointing class 5 at the root would have E1 grading four classes at one commit and one class at HEAD — incoherent by construction, and wrong even if the siblings' outputs were not there. Sibling-avoidance is a consequence of the right fix, not its justification — a fix justified only by what it avoids invites re-litigation when the avoidance stops applying.

**§3 And the fix surfaces something that must be DISCLOSED, not smoothed.** With L8 at the worktree, E1's population spans two epochs: classes 1, 2, 3, 5 at the pin `c88aac1d`; class 4 at the ledger snapshot (06:27Z, `e345088ab5277bab…`, 375 rows) — because the ledger is untracked and therefore has no state at the pin at all. Unavoidable and not a defect — but a coverage fact, and this round's whole discipline is that coverage claims state their unit. The close says: *E1 graded four classes at the pin and one at a named later snapshot, because that class's artifact is untracked and has no pin state.* One consequence: the worktree's `S6-7-RESIDUALS.md` predates ED-409, R-14 and the enumeration clause — correct, the pin is the pin — but the close must not later cite E1's verdict on the register as a verdict on the CURRENT register. They are different objects.

**§4 What β clears, and the sequence.** Rebuild as E1-3 with the single additional envelope line; brief bytes byte-identical; envelope-only diff printed (now three lines). Then α's checks — ED-392 grep with offsets · `fs.existsSync` resolution · identity (`rev-parse` in the worktree = `c88aac1d`; snapshot sha re-read) · **and one added: confirm the worktree's evidence directory contains NO `qualifying/` lane outputs**, since that is the specific property this change buys and it should be verified rather than assumed. ε sends the new sha; β's re-clear names the sha β sees on disk; ε fires only on that.

**§5 α's alternative was offered honestly, and that is why the fix is safe to take.** *"If you rule the root stays, the close must say E1 could see E2/E3's returns, in those words."* Offering the honest version of the option not recommended is what makes the recommendation trustworthy. Record it as method, not manners.

**§6 The composition question was answered at `5b9e3c74`** — hold step 2 until E1-2 is terminal; E1's place is its original start; the exception is pre-ruled.

**not_read:** `assembled-E1-2.md` and the snapshot — α's shas; β reads E1-3 at source before clearing · all three lanes' prose, held · the worktree's evidence directory — §4 asks α to verify rather than β assuming.

## α application

E1-3 built by ε (`assembled-E1-3.md`, 9226 B, sha256 `ddb6a669dd04f3d6…`) and verified by α on every check including §4's added one — the worktree's `qualifying/` holds only `brief-E1/E2/E3` and `enforcer-blocked-at-06669fbe.json`: no lane outputs, no rollout, no `FREEZE-ORDER.md`. The two-epoch disclosure and the register-at-pin caveat (E1 grades `S6-7-RESIDUALS.md` at `5673b5c9f70c55d4`, 12 entries — never cited against `104c30da0bc8efce`, 14 entries) go into the close verbatim. ε's second hazard — E1's own predecessor's rollout notes at the root, ruled inadmissible, readable by a fresh E1 and laundered back as its own findings — recorded as the stronger of the two and closed by the same move. ε's proposal of a fourth pinned path for the register is not ordered: the register is tracked and has a pin state, which is the distinction from the ledger. The sixth family member recorded.
