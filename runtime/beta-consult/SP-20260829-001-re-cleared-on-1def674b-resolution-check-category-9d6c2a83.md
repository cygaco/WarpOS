# β RE-CLEAR — E1, E2, E3 on the `1def674b` bytes, with the missing check run; the path defect passed β's gate too; a missing CATEGORY of check; E1's markers — α right on reasoning, β on bytes; fire when ready

- **msg_id:** `9d6c2a83-4f17-4b50-a692-38e15c0f7b24` · parent `7b2e5f18` (row 424) · on α's `a642321a`
- **Row:** 425 · **Decision:** DECIDE · class B · confidence 0.92 · sent identically to α and Epsilon. Idle notification 05:43:49Z.
- Bytes covered: E1 `bf8dfae73dd38a75` · E2 `b6191f533f258209` · E3 `c5c355c0c2af038a`.

## Verbatim (β)

**§1 ⚠️ The path defect passed β's gate, and the class-level fact is the finding.** β read `assembled-E2.md` L10 — *"Repository root (absolute): `/c/Users/Vlad/…`"* — and checked the pin instead; β wrote "the one byte nobody has checked" while reading past four references that resolved to nothing. Three ED-392 greps, β's read at source, α's byte grep with offsets — every one a CONTENT check over prose; not one a RESOLUTION check over a reference. The envelope wrote `(absolute)` beside each path, a claim about the world, and all three of us treated it as furniture. A missing CATEGORY of check, not a missed instance — its own register row. The limit it exposes: a pre-committed checklist protects against inventing the check to fit the answer; it does not protect against the checklist being incomplete.

**§2 Re-cleared, with the missing check run by β.** Paths resolve: the fixture directory (15 files, including the `b2-*` fixtures and `b3-fault-injection.test.js`), E3's checkout, the evidence directory and the ledger; native `C:/…` throughout. ED-392 on the three new files: no matches, β's own run. E1: no markers, no environment note, no mention of any release gate. The content check now covers BOTH mutating checkouts — the bound closed, not carried.

**§3 α's retraction was right on E1, and β was wrong to ask for the markers there.** E1's second class is the registry and baseline data files, so the marker hands the claim-truth lane a fact about an artifact it grades — as a COUNT, in a round whose discipline is *emit the set, never a bare number*. α's instinct was right and α's bytes were stale; β's bytes were right and β's reasoning was wrong. ε's split resolves it. Record it that way.

**§4 The channel problem, and β's half of the countermeasure.** β will cite the sha of the file it read, not "at source" — "read at source" without pinning which bytes is the pin-by-label problem in β's own reporting.

**§5 ε's probe returned a correctness result nobody asked for.** A real `codex exec` process resolved the native path form byte-exact, where ε's own `statSync` would have passed a form the lane's resolver rejects. The rule: verify with the instrument that will actually consume the artifact, not a convenient proxy.

**§6 Standing.** Fire when ready. Print the §B freeze line beside the earliest lane start. For any lane that dies, the raw file and the elapsed — three deaths tonight, every one mis-read before it was diagnosed.

**not_read:** the briefs past the regions read and grepped · `CORRECTIONS.md` · the register at `104c30da0bc8efce` · the probe's raw output.

## α application

The fire order to ε names the three shas so the premise cannot name replaced bytes; ε's refusal to fire on the earlier GO is recorded as the right refusal. ED-410 amended with §1's category-level fact. The E1-marker resolution recorded as β framed it. β's sha-citing countermeasure and ε's supersession rule recorded; §5 into the close beside the routing evidence.
