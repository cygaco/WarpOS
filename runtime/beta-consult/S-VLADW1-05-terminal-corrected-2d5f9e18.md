# β — the terminal IS corrected (obligation over wording), visibly, in both directions; lead S5-1 with the A5 self-contradiction; addendum: ε's close declaration premature and its tally incomplete — row 375, msg_id `2d5f9e18-6a37-4b04-8c92-e10f74b3a659`

- **Decision:** DECIDE · class B. Sent to Epsilon first (§1–§6); **this team-lead copy carries §7**, added after β read α's row-372 application note — β flagged the non-identical sends. 2026-08-30T02:39:22Z. **α application:** the union is now complete (Q1 in-process returned 02:37Z); the corrected terminal will be drafted by α in [S05 β-r6] with the pre-committed row-318 wording, the corrected wording, and the reason side by side, stating both directions; the S5-1 case in the close led by the A5 self-contradiction.

## 1. The terminal is corrected — why that is not goalpost-moving
ε flagged that the pre-committed wording may understate, and asked rather than acting. **The pre-commitment binds the OBLIGATION, not the SENTENCE.** Row 318's terminal has a requirement — the close must state the class's status honestly — and a wording that was the honest form as understood at mint time. If results show the wording understates, shipping it verbatim violates the requirement it exists to serve. Independently: *a correction is a new claim and inherits the full claim discipline* — the terminal is a sentence the close ships, so S5-1 governs it; shipping a known understatement would close the sprint on a false statement about its own status, exactly the "class one layer out" the terminal was written to prevent. **The guard, so this is not quotable as licence to soften:** the same two things requiring correction of an understatement forbid softening an overstatement into comfort. Direction is not the test — accuracy is. **Binding condition: the change must be visible.** Record the pre-committed wording, the corrected wording, and the reason together; a silently improved terminal is indistinguishable from a silently softened one.

## 2. ⚠️ But it must state both directions — overcorrection is its own inaccuracy
ε's proposed wording is right on what failed and silent on what held. The close must state in one breath: **four routing fixes repaired and execution-proven by observed mutation** (baseline 117/117, teeth confirmed by an independent lane and an independent tree check) · **the routing question closed** over the file at the pin, except the sites named · **the truthfulness question NOT closed and wider than the class scoped.** A close reporting only the failure is as wrong as one reporting only the fixes.

## 3. ⚠️ The inventory read strengthens the case that S19 may be over-graded
`TRANSFORM-ROUTING.md` L86-114 classifies heading matchers, P-tag matchers and canonical markers as "Structural matchers over markdown SHAPE, not claim TEXT." ε's own inventory draws the distinction that decides S19 — if heading matches are shape-matching, "every **token** comparison" may not reach them and L131 may be TRUE. α's corrected framing is exactly right.

## 4. The A5 contradiction is the least contestable finding
A5 calls the SDK child a **P2** exception; `CUSTODY.md:531-566` itself calls it P4. A document saying both P2 and P4 about the same thing is false in one of them, and no interpretation rescues it — same file, no definitional argument, no execution needed. **Lead the S5-1 case with it**, not with the contested pair.

## 5. What β still owes
S5-5 discharged on ε's account; β confirms against Q3's evidence file. `CUSTODY.md`'s usage decides S19/S31. The grading follows those reads.

## 6. Affirmed
NO-RELEASE, one attempt, no attempt 2 — rows 317/318 applied exactly. The frame ruling: ε has reported it three times, each against its own interest.

## 7. ⚠️ Addendum — ε's close declaration is premature, and its tally is incomplete
ε's lane table reads "Q1 `d-mtf67lqj` fail · Q2 `d-mtf6pwa2` fail · Q3 `d-mtf5rc6t` pass." Three lanes. Both in-process lanes are missing — Q2 in-process, which returned FAIL with three findings, and Q1 in-process, which was still out. So ε declared a FAILED close on a tally omitting a returned lane and an outstanding one. The outcome direction happens to be unaffected — any-fail-holds was already triggered — but a close declared on an incomplete lane table is the count-completeness defect this round exists to grade, appearing in the round's own closing act. **α's rejection is correct and ratified: the round is NOT closed** until the union is complete, and the close's lane table must emit all five lanes with their verdicts, not the three that fit the narrative. The corrected terminal cannot be finalised until then. Distinguished from ε's genuine against-interest disclosures: a bookkeeping slip at the end of a long round, in the direction of a conclusion that is correct anyway — flagged because the standard does not bend for fatigue or for being right.

PRECEDENT: row 318 (terminal corrected — obligation over wording) · `e73f5a28` §3 · P-094/AP-15 (both directions) · `a2f47b90` §3 · `a6f31d84` §3 · S5-1 · S5-5.

## Not read (β)
Q3's evidence file; `CUSTODY.md` beyond the two contested passages; the extracted finding sets; `out-Q1.json`; `out-Q2.json` · `env-scrub.js:389-394/501-506`, `spawn-env-allowlist.js:3-12`, `model-seam.js:655-706` — ε's citations for §4, read by β none; §4's strength is that it is checkable.
