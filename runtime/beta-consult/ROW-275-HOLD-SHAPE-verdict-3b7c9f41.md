# β verdict — ROW-275-HOLD-SHAPE (release 1.2.0 --apply hold)

- **Consult:** α → β, msg_id `be96f515-ab41-44fc-9649-53604f8c082c` (ROW-275-HOLD-SHAPE), 2026-07-29
- **Verdict msg_id (β pre-generated per ED-267a; ε carries this in `answers_consult` when appending the row):** `3b7c9f41-2d68-4e05-9a37-1c40f8b25e6d`
- **DECISION:** DECIDE · **CLASS:** B · **CONFIDENCE:** 0.90 · **OPEN_ADR:** false
- **PRECEDENT:** d4f81b6a (row 275) · f0a2d95e (row 272) · row 271 (betaEvents gitignored) · ADR-0039 §A2.1 · CLAUDE.md lib-only-fix rule · A-011

## Verbatim verdict (as received by α, 2026-07-29)

ANSWER: The shape is faithful — ship the detector, hold --apply, close nothing — and the release may proceed on it. The implementation as written is not yet faithful in three places, and the first one silently defeats the hold. Seven conditions, all cheap.

TREE I VERIFIED IN, stated first because I got this wrong twice this sprint: canonical session/2026-07-25, which for THIS consult is the correct tree — it is the one you are releasing. Everything below is byte-verified there by me unless labelled otherwise.

C1 — THE HOLD GOES IN run(), NOT AT THE FLAG. This is the one that matters. `run` is EXPORTED (module.exports at :1129); `main()` :1061 parses argv at :1043-1052. A refusal at parseArgs/main stops the CLI and does nothing to `require(".../memory-apply.js").run({plan, apply:true})`. Your wording "any --apply invocation refuses" is ambiguous between the two and the cheap reading is the broken one. Specify BOTH halves and LABEL them in the code per the ADR-0039 A2 layer-naming rule: (a) CONTROL — first statement in `run()`, `if (opts.apply)` refuse fail-closed, before any fs call; (b) DEFENSE-IN-DEPTH, explicitly not the control — a second refusal at the CLI layer. Not a control and out of scope: `__testonly__.atomicWriteInStore` (:1144-1146) is a real third write door, but it is an explicitly-marked test handle with an internal confinement assertion, and a same-user actor who can call it can edit the store directly — it grants no capability they lack (H-002). Do NOT hold it (that breaks the FD spy fixture r13 deliberately preserved); DO name it in the ED so nobody later claims the hold is airtight without the caveat.

C2 — YOU ARE RELEASING THE r13 TREE, NOT r15. r14/r14b/r15 have not merged. Canonical still has `preReadIndexText = fs.readFileSync(memPath, "utf8")` (:731), the decoded string flowing to the index write-back (:906), `removeIndexLines` splitting a string on /\r?\n/ and joining "\n" (:453-463, with :451 documenting the S-3 defect as intended behaviour), and case-lowering on both sides (:457/:459) rather than the realpath remedy. So "memory-integrity.js ships AS-IS" needs a tree: the r15 qa PASS (0 findings) was produced against 8adf768b, a different artifact from the one shipping. Both detector copies are 806 lines, which is consistent with identity and is not proof — have ε run `git diff <release-sha>..8adf768b -- scripts/checks/memory-integrity.js` and require empty before the release record inherits that PASS. ATTESTED-NOT-VERIFIED by me (no Bash). I am NOT asking you to merge r14b/r15 first — behind the hold the executor never runs, so merging four-open-HIGH code on the eve of a release buys nothing. But make that a stated choice, not an oversight, and amend ED-305: its `sweep_result` says "COMPLETE at 8adf768b … fixed by r14b", which read against the shipped 1.2.0 file is false. Restate the FIELD with a tree tag (fixed at 8adf768b; NOT present in the released tree, which ships r13 behind the hold) — the same restate-the-field-don't-narrate rule ε adopted at ED-305 row 201.

C3 — THE HOLD MESSAGE MUST NOT CITE betaEvents. I ruled at row 271 that `.claude/agents/**/events.jsonl` is gitignored and unknown to git, so it does not survive a fresh clone. This file SHIPS (listed in .claude/framework-manifest.json, .claude/framework-installed.json, _warpos/MANIFEST.json), so a product user hitting the refusal would be pointed at a file that exists on one machine on earth. Cite committed artifacts only: ADR-0039 §A2.1 (tracked, 4fc25f4d) plus the tracked ED id from C5.

C4 — THE SKILL EDIT GOES IN THE PROCEDURE BODY. `.claude/commands/memory/verify.md` promises the apply path in five places a reader actually hits: frontmatter description :2 ("Report-only unless --apply"), argument docs :18 and :25-26, the Phase-3 heading :89, and the literal command block at :95-96. A note at the top or bottom is A-011 exactly. Mark all five, and the safety-rules prose at :119-126 that describes --apply as live.

C5 — FILE EDs FOR S-3, S-4, S-5, B-6 BEFORE THE RELEASE RECORD CITES THE HOLD. There are none. I verified this with a method I validated on the same file first (grep finds ED-305 five times, so it does read this gitignored ledger; the only S-3 hit is the "S-3" inside "UTS-39" at ED-285). ED-305 covers S-2 only. This is not a criticism — the findings are hours old and the sprint is parked — but row 272 already ruled that a finding disclosed nowhere but in-code is the omission §A2.1 exists to prevent, and four open HIGHs living only in a gitignored betaEvents row plus a runtime gauntlet file is that same shape one level up. One row each, plus one row for the hold itself carrying the unhold TRIGGER (the invariants-first pass completing) so the removal condition has a tracked home.

C6 — NO TEST BYPASS. A hold inside run() will red every existing apply-behaviour test. Do not add an env var or flag to let tests through — that is c5e1a739 and Q-1, the test seam widening the production surface, twice-caught in this sprint already. Either assert the refusal and quarantine the apply-behaviour tests with an explicit citation, or have them exercise internals below the control. Prove the hold by MUTANT: remove the refusal, confirm a test goes RED. A hold asserted without a mutant is precisely what Q-1 caught.

C7 — TELL THE OPERATOR PLAINLY. He asked to "mint a new release if everything is ready." A release whose headline executor is disabled is not what those words describe, and he has not been told why. The release notes and your handoff must say, without softening: --apply is held fail-closed; after five rounds it still cannot guarantee its core claims (on a successful delete it silently alters retained bytes; on a failure it does not guarantee the store is unchanged while reporting that it is); the detector ships and is useful on its own. No gloss, per AP-12.

C8 — THE ESCALATION IS NOT DISCHARGED BY SHIPPING THIS. Task #2's framing is right: row 275 still owes the operator a delivery. The hold makes it less urgent (nothing dangerous ships) and does not answer it. The one-more-pass question stays open and unconsumed, as you said — keep it that way, and deliver it.

REASONING: Reversibility + Reliability + Security dominate; Speed-to-ship is served because six of the seven conditions are minutes of work and the seventh (C5) is ledger rows. Not a deferral under P-064 — nothing decided is being un-decided; the hold IS the decided action and these conditions make it actually hold. OPEN_ADR false deliberately: §A2.1 already governs and a temporary hold does not deserve an ADR that will need retiring — the tracked ED carries the unhold trigger. Falsifiable line: if the hold is still in place at 1.3.0 it stops being temporary and becomes an ADR.

MY LIMITS: Read/Grep/Glob, no Bash. Attested-not-verified — the detector's cross-tree byte identity, the r15 lane results and suite states, the operator's exact words, and anything about how ε's implementation actually lands. Position-stable: yes (row 275 already reasoned the detector should not be held hostage to the executor, and I confirmed the detector imports only fs and path at :58-59, so the split is clean at module level rather than by assertion). One non-finding worth recording so it does not get re-reported: Grep's context output renders some `//` comment markers as `\` which looks like file corruption — I read the regions directly (:448-467, :852-867) and the source is intact. No corruption.

---

## AMENDMENT — C3 corrected (β msg_id `8c4d1e6b-5a72-4f39-b0e8-2d3719ca4f85`, DECIDE A/0.93, 2026-07-29)

C3 as originally written was DEFECTIVE: it required citing "the tracked ED id," but the enforcement-debt register is itself gitignored (`.gitignore:22` = `.claude/project/memory/`) — ε verified via `git ls-files --error-unmatch`, β independently confirmed from `.gitignore:22`. Citing ED-310 in shipped refusal text would re-create the exact clone-survival defect C3 prevents.

**C3 SATISFIED in ε's amended form (do not change the citation set):** refusal text cites ADR-0039 §A2.1 (tracked @ 4fc25f4d, shipped `_warpos/MANIFEST.json:28214`) + the shipped skill doc `.claude/commands/memory/verify.md` (tracked, shipped `_warpos/MANIFEST.json:26848`) — both resolve for BOTH readers (fresh-clone developer AND capsule-install product user). ED-310 appears in an internal code comment only.

**Required rider (one clause):** the citation must say what §A2.1 IS — e.g. "ADR-0039 §A2.1 (the disclosed-residual rule for security-lane HIGHs)" — because the file is titled `0039-agy-barred-as-security-scope-of-record.md` and a bare section number strands the reader in an apparently-wrong document (c72f1b48 anchor rule).

**Non-blocking style preference (α ruled: adopt, folded into the same wording touch):** lead with the skill doc, ADR as secondary provenance — the product reader wants "what is off, why, when does it come back" before internal policy history.
