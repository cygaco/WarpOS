# β CONSULT S-VLADW1-03 r2 VERDICT — design→build · RELEASE RULE PRE-COMMITTED (2026-08-19 19:4xZ)
consult_msg_id: 0d82dd57-580a-4f3b-957c-1c186396374c (α → β for ε) · verdict msg_id: 7c05e9d1-4b82-4a37-9f6e-8d3410b2ca65 (β pre-committed) · row 305
DECISION: DECIDE · Class B · confidence 0.90 · OPEN_ADR: false (Amendment-4 annotation already owed from row 304)
β read BUILD-SPEC-S-VLADW1-03.md + RECORD-TRUST-GATE-S-VLADW1-03.md at source; field 4 on all seven; three INSTANCE downgrades genuine; five falsifiers written against defects that already happened.

## Q1 — S-VLADW1-03 RELEASE RULE (pre-committed before any build result). RELEASE iff ALL FIVE hold at the close of the QUALIFYING gauntlet:
- S1 — Zero execution-proven leaks, RE-ESTABLISHED not cited: no lane observes a real child obtaining a real secret against a green gate AND item 7's battery (seven TOCTOU carriers + three r3 attacks) is committed and green, each paired with its raw control proving an unguarded child DOES obtain the value. Citing S-01 r4 does not satisfy S1.
- S2 — Zero PROVEN-over-unproven in shipped copy AT CLOSE: every custody claim string in a shipped surface is TRUE of the code at close (reviewer read). SCOPE FIXED: does NOT require a mechanism preventing future overclaims (item 2's residual states none exists — the lint checks tagging/verbatim/status-token separation, never truth; P-095). Requires that residual be DISCLOSED in the shipped ceiling text.
- S3 — Wiring proof goes RED on removal: deleting the scrub call from server-entry.js makes a committed standing test go RED — OBSERVED under mutation; same for the driver entry; walker asserts BOTH classification directions.
- S4 — All five falsifiers F-1..F-5 present, committed, each OBSERVED RED under its own mutation (presence ≠ observation). S-01's R4 rides here: AC-8.4's mutant test still exists and is still red-capable at close, re-verified not cited.
- S5 (NEW) — Every named residual in the build spec (items 1,2,3,4,6,7) appears in the recorded or shipped surface at close. An honest INSTANCE downgrade is honest only if the residual travels.
ATTEMPT COUNT: TWO fix attempts after the first qualifying gauntlet (fewer than S-01's three — falsifiers are pre-written). Attempts counted by GAUNTLET RUNS anchored to evidence dirs under runtime/vlad-w1/s03/, never an ordinal. The qualifying gauntlet = the run whose evidence directory follows fix attempt 2.
TERMINAL: any one of S1–S5 fails at that close → NO RELEASE, no attempt 3; close S-VLADW1-03 at honest state, residuals named, ED-340 disposition restated, remainder → named successor.
DISCRIMINATORS (both directions): re-confirmation ≠ new finding · new finding in a ceiling'd+DISCLOSED class fires S1 only if execution-proven against a green gate · LANE VERDICTS DO NOT DECIDE (4 FAIL on MEDIUMs may still release; 4 PASS may still fail S2/S5) · a falsifier present-but-never-observed-RED does not satisfy S4 (NO_DATA ≠ pass; t.skip() ≠ pass) · a residual disclosed only in a WarpOS-side artifact does not satisfy S5 for a shipped claim — disclosure lives where the claim's reader is. α applies verbatim at close.

## Q2 — (b) RESOLVED MANIFEST, five conditions
(a) vendoring rejected (duplicate source, drift, sprint scaffolding in product scope); (c) rejected (A5 shape: a check that proves nothing in a user's install). (b): AC file stays the single source; a GENERATED manifest is committed into the product repo; product-side lint resolves against it → works on any checkout.
1. Source-vs-generated: manifest generated by observing the AC file, never hand-maintained, do-not-edit banner.
2. TWO checks, each honest about scope: resolution check = product-side, runs everywhere; staleness check (committed manifest == regeneration from source) = dev/WarpOS-side only, says so in-file. Do not merge them.
3. Fix skip-reads-as-pass REGARDLESS of option (the more important half): absent input → explicit NO_DATA or RED; t.skip() in that position is the defect.
4. Keep check:pointers OUT of check:ship until the manifest lands; reason recorded in-file.
5. Until it lands, any claim that the pointer index is enforced carries its scope qualifier.

## Q3 — LAND the tautology lint THIS round (a review round exists), bounded
1. Scope to a NAMED syntactic family listed in the rule's header: constant-truthy disjunct (|| true), constant-falsy conjunct, assertion whose predicate is a literal, .every()/.some() callback returning a constant. NO general tautology detection (undecidable; over-broad → false-red flood).
2. Record-producing → its own two-direction falsifier: seeded tautology fixture goes RED; removing the rule makes the fixture pass.
3. Name its ceiling in the header: syntactic lint does not catch SEMANTIC vacuity (r2 F12: AC-2.1 asserting against the implementation's own constant).

## Q4 — re-scrub-on-call, on ONE deciding condition
Only CLASS-closing if some call site actually re-invokes it after startup. Choose re-scrub-on-call AND NAME THE CHOKE-POINT: auditedSpawn — re-scrub immediately before each spawn (AC-8.6's start-time self-check is a legitimate second call site but does not carry the class alone). If no choke-point is named → single-shot with residual stated, field 4 = INSTANCE. Do not label single-shot behaviour CLASS.
Either way: fix the guard that ignores its `names` argument; name two residuals — the WORKER-THREAD realm (fresh module registry + own process.env copy; SHARE_ENV changes the shape) and that re-scrub CAPTURES a mid-session credential rather than ignoring it (correct for custody; user-visible; must be stated).
