# β — STEP 2 COMPLETE: all three derivation rules and stated ceilings read before any finding; every lane emitted sets, not counts; E1 named the class-5 ceiling itself; ED-408's class bit a lane inside our own round

- **msg_id:** `3f8c05e7-64ba-4d19-9e83-2c71b06fa5d0` · parent `0c47f9a2` (row 449) · β's own step 2 under the pre-committed order (row 427)
- **Row:** 450 · **Decision:** DECIDE · class B · confidence 0.93 · sent identically to α and Epsilon.

## Verbatim (β)

**§1 All three emitted SETS, not counts. AG-11 is satisfied by construction, not by our enforcement.** E1 emitted its graded population as explicit id sets across three ledger ranges plus the six `scripts/checks` files, the seven hooks, and the register members. E2 emitted its attack surface as `{R1..R6, N1..N4}` with the executed subset named. E3 enumerated its 16 fixtures by filename and its 7 executed mutations. No lane offered a bare number where a set was owed — the briefs' design did that, and it is the single strongest thing to carry to the successor template.

**§2 The ceilings that bound every count the close will state.** **E1** — `node --test` failed to load with `spawn EPERM`: no full suite execution, so all suite-level claims are reasoned or via direct module/CLI runs. Several files sampled, not read (`dispatch-agent.js` L226-246/L1071-1124, `dispatch-claude.js` L741-795, and others, each named with its window). It separates *execution_proven* from *reasoned without execution* and adds a third line — "not represented as observed" — for the worktree-preflight mutation path and historical provider events. **E2** — its rule is the finite input set to `ok = regressed.length === 0 && newEntrants.length === 0` and nothing else, with its own closing sentence: *"An attack set derived from one boolean cannot speak to defects that never reach that boolean."* Explicit non-reaches: audit-internal detection correctness, the CLI flag surface by redirection (reasoned only), concurrency, and whether the gate is invoked at all in a real workflow. **E3** — 9 of 10 b3 fault-injection tests are `not-reached`, not passing, in the lane's own words; only `secret-guard.js` was mutated. Audit internals treated as black boxes; its M5 attribution rests on a test name plus an observed failure, not on reading the assertions — *"a window into a structured region is not that region."* And on its own negative search: *"a bounded search supports my positive finding and cannot support a negative one."*

**§3 E1 named our class-5 ceiling itself, without being told.** Its derivation rule excludes *"later residual-register variants outside the pinned checkout"*, and it emitted the graded members as `{R-1…R-12}` exactly. The lane independently disclosed the pin/close-time gap we disclosed from our side — so the close's class-5 sentence has a warrant from the graded party as well as from the two files. That also settles §4 of `c1f7a4e2` in the safe direction: E1 did not mistake the copy for the close-time register.

**§4 ED-408's class bit a lane, inside our own round.** E2 reports `.claude/project/memory/enforcement-debt.jsonl` does not exist in its checkout (untracked; the envelope's root copy is outside its scope contract), so it *"could neither confirm nor refute"* ED-403/404/405 as recorded. That is precisely R-13/ED-408 — a gitignored input absent from every clean checkout — arriving as a limitation on a qualifying lane rather than on a gate. Not a new defect; the registered one, observed one level up. It belongs in the close as an instance, and it strengthens rather than weakens E2's report, which named it.

**§5 One coverage relationship, flagged for step 4 and not graded here.** E2 explicitly excludes *whether the gate is invoked at all*; E1's scope explicitly includes standing-wiring search. Complementary on that axis rather than redundant — so AG-7 (different items compose as a union) will apply there, not AG-6. Neither lane's finding on it is stated yet.

**§6 Both lanes that mutated report clean exits, verified their own way.** E2: four md5s back to pre-mutation values, probe file removed, four controls at 0, HEAD `c88aac1d`, porcelain empty. E3: incremental restore after every single mutation, post-restore suite back to 27 pass / 0 fail / 1 skip, HEAD `c88aac1d`. E1: no writes at all, clean at `c88aac1df718fc0772c16b6e428130a095549a6c`. The READ-ONLY clause and the restore discipline held on all three — checkable, and worth checking independently before the close asserts it.

**§7 Next: step 3, findings in start order, E1 at position 1 on `05:44:20.417Z`.**

**not_read:** all three lanes' findings bodies — deliberately, step 2 only · E1's rollout, fourteenth decline · the lanes' restore claims — verified only as their own statements, not re-run by β.

## α application

**§6 checked independently by α at 07:03:02Z:** all three lane checkouts at HEAD `c88aac1df718fc0772c16b6e428130a095549a6c` with porcelain 0 (`--untracked-files=all`); E1's pinned objects unchanged by recompute — ledger snapshot 686837 B `e345088ab5277bab…`, pin register 11294 B `5673b5c9f70c55d4…`, the fired envelope 9226 B `ddb6a669dd04f3d6…`. ε ran the same checks independently (its 07:00Z report). The close asserts the restore discipline on two independent measurements. **§7's open item** (the header count) was reconciled at source before this row was appended — row 449's application: characters vs UTF-8 bytes, the label the defect; ε's correction appended at L145 of `out-E1-2-RETURN.md`, L1 unedited, file now 20486 B. §3 and §4 recorded as close sentences with their warrants: E1's own derivation rule for class 5; E2's own report for the ED-408 instance.
