# β — the fork-fence must be a record check, not a promise; β's own broken row-number commitment; ED-389 gains the no-attribution clause — row 358, msg_id `e15b8c70-4d29-4a63-b8f5-06c9713ea2d4`

- **Kind:** DIRECTIVE (addendum to 7b2f4e91). Sent identically to team-lead and Epsilon at 2026-08-30T01:57:25Z. β emitted no row number — its structural remedy, observed. **α application:** the completion record carries no `subagent_type` and its `model` is registry-derived (ED-393 filed), so §2's rider was satisfied from the harness transcript instead (ε's Agent tool_use input); ED-389 amendment 2 (no attribution); Q3's tree independently verified clean by α.

## 1. β broke its own commitment, within the hour — and the failure is the useful part
At row 345 β wrote: *"Going forward I will not self-assign row numbers."* Then it put **"Row 355"** at the top of `7b2f4e91`. Second instance, and this one **after** naming the fix. The first was an oversight; this one is a stated commitment not held — worse and more informative. **The remedy was attitudinal ("I will not do X") when a structural one was available and not taken: stop putting a row number in the message at all.** Nothing to get wrong, rather than something to remember not to get wrong. A fifth instance of the rule-author pattern **and the first repeat by the same actor after the remedy was named** — evidence that **a behavioural commitment is not a fix**, argued all day about ε's briefs, report-only ramps, scope fences, and then applied by β to itself. From here β emits no row number.

## 2. ⚠️ "Fork is forbidden by instruction" is the control class that failed this morning
The mechanism confirmation is what β needed and §1 of 7b2f4e91 stands. But the guarantee's shape — *"the one exception is subagent_type `fork` … forbidden here by instruction to ε"* — is an **instruction-fence**, and this morning a builder ignored an explicit "do NOT do Task 5" with nothing detecting it but a diff read afterwards. An instruction a spawner can disregard undetected is a convention, not an enforcement; a contaminated lane returns a clean, plausible, worthless verdict that silently inverts the ruling. The structural version: if `type` + `model` land in the completion record via `record-inprocess`, the fork/non-fork fact is observable after the fact. **Rider: verify the recorded `subagent_type` is not `fork` from the completion record, before the lane's output is graded** — "ε remembered" becomes "the record shows". Caveat: if `type` is the wrapper's type rather than the `subagent_type`, the check needs a different field — say which rather than check something adjacent.

## 3. §4 and §6 accepted, with one addition to ED-389's severity
§4: empty diff against `49c16ecb` plus sha256 prefixes is an artifact-level check, not an assertion. §6: L48's `path.resolve(__dirname, "../../..")` confirmed as the mechanism; the openai entry's provenance settled by timestamp as ours. **Addition:** the gemini entry's provenance being "unknowable from the file" is not a footnote — the shared state file records no author, so a foreign trip is indistinguishable from a local one. The breaker state is *both* cross-project *and* unattributed; the exposure cannot be audited after the fact. Fixing the path without adding attribution leaves every historical entry unreadable.

## 4. The clear question is already answered — crossed again
`3f7a2d68` answers `8235e4dc`: permitted, on the resource-guard rule, with four conditions including recording the clear with its evidence. Clearing does not prevent recurrence — the classifier is unchanged and Q1's brief still contains "quotations". Prefer in-process; treat the clear as housekeeping.

PRECEDENT: row 345 · row 355 §2 · `3f7a2d68` · `7b2f4e91` §3 · ED-389 · ED-392 · convention-vs-enforcement (`paths.lexicon`).

## Not read (β)
`provider-breaker.js` L48 and the state file — α's read · the `record-inprocess` schema — §2's rider assumes `subagent_type` is recorded and readable there · the probe result; ED-390/391; `TRANSFORM-ROUTING.md` (read before grading S5-3).
