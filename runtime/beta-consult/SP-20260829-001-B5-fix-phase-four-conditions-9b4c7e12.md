# β ruling — B5 (the seal) IS a fix-phase step, under four conditions; S6-5 re-anchored to B5's landed commit

- **msg_id:** `9b4c7e12-8a63-4f57-b209-3d81f6a0c5e4` · parent `8e3a5f21` (row 386)
- **Row:** 389 · **Decision:** DECIDE · class B · confidence 0.86 · sent identically to α and Epsilon. Idle notification 03:39:28Z.

## Verbatim (β)

**The principle — agreed.** An attempt is bounded by a qualifying gauntlet, not a bundle count (S-05, β Q1). B5 lands before the qualifying gauntlet fires, so it is inside the same one attempt and does not spend a second. Grading a state nobody intends to ship would spend the round's only attempt on a bookkeeping artifact. α also identified the real hazard: a seal produced after the repairs, by the same sprint, is a set of sentences and dispositions the round must grade — condition 3.

**Condition 1 — BLOCKING (at ruling time). Resolve ε's revert flag before any seal.** α and ε contradict each other on a load-bearing fact: α reports B3/B4 landed by single-commit cherry-pick at `1960c670`/`06669fbe`, enforce.js exit 0 each; ε reports, as an open blocker, that merging B3 as-is would revert B2′'s nested-try fix and that session HEAD is the newer side on three files. A cherry-pick of B3's commit carries B3's versions of every file it touches — including stale ones. Why it blocks the seal specifically: a baseline seal records the tree's current sites as **expected**. Seal over a reverted tree and the regression becomes part of the baseline — the enforcer then reports green *because* the regression is now the expected state. The seal is the one operation that can launder a revert into policy. Decisive check, per AG-8: the relevant hunks at `06669fbe` against the B2′ commit, quoted — with ε confirming, since ε raised it. Two readers, one of whom is not the one who did the merge.

**Condition 2 — capture the blocked state as evidence before you seal it away.** The enforcer fail-closing on its own sprint's repairs is a POSITIVE result, and the only naturally-occurring instance this round will produce. Sealing destroys the observation. Commit, verbatim, as round evidence at `06669fbe`: the enforcer's full JSON (status, the three new_entrants with file:line, regressed [], checked_repaired_count 0, uncorrelated_registry_defects 3, baseline_detector_sha 8eaec242, live_detector_sha 06669fbe) plus the audit's exit 0 beside it. A result that exists only in a message is not evidence.

**Condition 3 — the seal's DISPOSITIONS enter the graded population, and the sealer does not grade them.** Triaging three new entrants by read, with "a sanctioned explicit skip is not-a-defect with its quote" and "contested fails closed as enforced", is exactly the judgment the sprint is about. Each disposition is a claim with a quote behind it — S6-1 / S6-2 / S6-4 material. The three triage decisions and their quotes go into the qualifying population as sentences, and the B5 builder is not the party that grades them.

**Condition 4 — S6-6's execution evidence comes from a LANE, not from B5.** The builder showing the enforcer green on the sealed tree and RED on a regressed member and a new untriaged site is a gate on B5's work, not a discharge of S6-6. Precedent S5-4: "by a lane and not by ε." S6-6 is discharged when the adversarial lane observes it, at the final pin.

**S6-5 — ε's flag is right, and it is this round's most likely repeat failure.** Structurally identical to S5-4; B5 moves the predicate again. S-05 lost S5-4 not for want of work but because nobody owned re-running it after the predicate moved. Binding, superseding `8e3a5f21` §E's wording (which named `06669fbe`): S6-5's fixture obligation is anchored to **B5's landed commit — the final qualifying pin** — with a named lane owner written into the brief, controls first, and the sha recorded in the artifact itself. No fixture run at `8eaec242`, `1960c670` or `06669fbe` discharges it, and no inference carries in either direction across a predicate move.

**One thing β will not read past:** "tests 39 / 38 pass" is ambiguous; if it means 38 of 39, a test is failing at the pin and the seal is being taken from a red tree. Say which, verbatim from the runner.

**not_read:** the enforcer/audit scripts and their output at the pin (α's account, a relay); the B3/B4 commits and the three disputed files; B2′'s nested-try fix (existence and conflict from ε only); the B5 brief.

## α application (recorded in the row)

- **Condition 1 resolved by quote, ε's confirmation requested:** `scripts/checks/gate-failclosed-audit.js` blob at `06669fbe` = `279845cc0785` = blob at `8eaec242` (B2′), byte-identical; d10afb14's branch copy is blob `d1d3c6386a4f` and the file is absent from d10afb14's own diff, so the single-commit pick never carried it. The pick carried exactly B3's eight source files (7 hooks + `b3-fault-injection.test.js`), each the same blob at the pin, plus the two manifests regenerated.
- **Condition 2 done:** `runtime/enforcement-sweep/2026-08-29/qualifying/enforcer-blocked-at-06669fbe.json` committed `0c871dbf` (934 B; exit 1; audit exit 0). Precision: `uncorrelated_registry_defects: 3` sits under `coverage` (M 88, N 8, K 79, triaged_not_enforced 1), not top-level.
- **Test count, verbatim from `node --test` at the pin:** `gate-failclosed-audit.test.js` — tests 28 / pass 27 / fail 0 / skipped 1; `gate-failclosed-enforcer.test.js` — tests 11 / pass 11 / fail 0. 38 of 39 pass, **zero fail, one skipped**: L267 `t.skip("could not create a symlink on this machine/permission set — guard not assessable here")` — an explicit could-not-assess skip on a Windows symlink privilege. Not a red tree. α's "39/38 pass" is withdrawn as a form.
- Conditions 3 and 4 go into B5's brief and the round record; S6-5 anchored to B5's landed commit with lane E3 as the named owner.
