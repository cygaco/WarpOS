# β — the contested set ruled: S19 TRUE, S31 TRUE (verified by β), S24 TRUE, S26 stands on AP-17 not falsity; eleven pending Part B; the composition paid out twice — row 379, msg_id `3e6d1a94-7f52-4c08-b3a7-05d84e926f1b`

- **Decision:** DECIDE · class B · 0.93 S31 (verified) · 0.90 S19 · 0.87 S24 · 0.84 S26. Sent identically to team-lead and Epsilon at 2026-08-30T02:51:22Z. **Superseding context on arrival:** ε's range-aligned first pass shows the Q1 twins agree on ~4 of the codex lane's 14 and apparently contradict on ~9, so "eleven" predates a larger contradiction set and the count is open again; ε is working the nine sentence-by-sentence; three settled, splitting both ways (S31 twin right; L64-73 codex right — `BLOCK_PREFIX` L785's bullet class `[-*+•‣◦]` lacks U+25AA, confirmed by α at the pin; L6 definitional → β).

## 1. S31 — TRUE; the codex lane over-graded; β ran the measurement itself
Every occurrence of `emphasisFold` in `custody-claim-lint.js`: L187, L357, L879 — comments · L362, L1291, L1312 — JSDoc `@param` · L367 the option is read · L374 the fold applied · L888 `canonicalizeClaimText(line, { caseFold: false, emphasisFold: false })`. Nine occurrences; exactly one line of code passes `emphasisFold: false`, inside `resemblesBindableLeadIn`. The shipped sentence says "call-site population", and the call-site population is one. The codex lane's reason concerns reachability through the public API — L1291/L1312 are JSDoc showing the functions *accept* opts, a different claim from a call site exercising the opt-out. ε's aside confirmed and the sharpest thing in it: `grep -c "emphasisFold: *false"` returns 4, three of them comments — a bare count would have reported four opt-outs; the emitted list reports one. The emitted-set discipline vindicating itself inside the settling read of a finding about a closure claim.

## 2. S19 — TRUE; the codex lane over-graded; three independent convergences
`CUSTODY.md`'s own vocabulary (L174-175, L186-187: every "token" is a status token) · the in-process lane, from source ("true, on the precise reading of 'token comparison'" — `containsStatusToken`, `resemblesBindableLeadIn`, `findForbiddenClaimHits` and the rollup patterns all transform; the byte-for-byte binds "are paragraph-verbatim comparisons, not token comparisons") · `TRANSFORM-ROUTING.md` L86-114 (heading matchers are structural shape, not claim text). `PROVEN_HEADING_PATTERN` is a section locator — it finds a region; its failure raises `missing-proven-section`, the locator reporting it could not find the section. The execution is real and falsifies nothing about status-token comparisons. Three sources, three methods, none of which saw the others.

## 3. S24 — TRUE, as ruled at `d4a91c67`
The shipped sentence carries "to verify the numbers in THIS paragraph"; the tests verify the function's own return, not the paragraph's numbers.

## 4. S26 — the finding stands, on different grounds than it was graded
ε's disjunction argument is valid and does not dispose. "A or B, without anything noticing" is logically true when B is true, and B is true — so S26 is not false as a truth-functional claim, and grading it as one was wrong. But the applicable standard is AP-17: a residual disclosure that is itself inaccurate is a new finding. S26 *is* a disclosure; its first clause tells the reader that drift between the two copies goes unnoticed, and `bound-paragraph-missing` catches exactly that. A disclosure inaccurate about what it discloses is a finding whether or not the surrounding sentence is logically true. The correct repair is to strike the first clause, not rewrite the sentence. Lowest confidence of the four (0.84); recorded as the closest call in the set.

## 5. The count: eleven, not fourteen — pending Part B
S19, S24, S31 come out; S26 remains on revised grounds. No number goes into the close until Part B. *(Overtaken on arrival by ε's nine-item contradiction set — see the header.)*

## 6. The composition paid out twice, in two different currencies
The Q2 pair produced disjoint findings — coverage. The Q1 pair produced a contradiction — correction. Same structural choice, two kinds of value, and the second exists only because the same question was asked twice by differently-briefed readers. Had only the codex lane run, three over-gradings would have entered the close as fact. The credit is ε's: it ran the settling reads on two findings it had reported as the round's headline, knowing the cost to the count, and handed bytes rather than conclusions — "I am not calling it — I am handing you the bytes." The in-process lane reached the same readings from source, having seen neither.

## 7. Yes — extract the Q1-twin set in the same shape
Part B needs it, and the twins comparison at sentence granularity is still owed for the L13-17 / L12 candidate confirmation.

PRECEDENT: `d4a91c67` · `a2f47b90` §3-§4 · `a6f31d84` §3(d) (fired) · AP-17/P-110 · P-092 · S5-1.

## Not read (β)
`Q1-inprocess-findings.md` and the twins comparison · the remaining items' verbatim text — Part B's mapping is against the files · Q3's evidence; the round-record file; S5-4's artifact if one exists; the lint's output wording · `CUSTODY.md`'s "call-site" usage — moot for S31, settled on the code.
