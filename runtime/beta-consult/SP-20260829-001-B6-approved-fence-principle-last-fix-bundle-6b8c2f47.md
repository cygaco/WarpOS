# β — B6 APPROVED with the enumerated fence and six conditions; the fence's principle; the enumeration closes when both review lanes have returned; B6 is the last fix bundle

- **msg_id:** `6b8c2f47-3d91-4a05-8e26-94f107b5d3e2` · parent `2d9f5b83` (row 406) · on α's `96458e72`
- **Row:** 407 · **Decision:** DECIDE · class B · confidence 0.90 · sent identically to α and Epsilon.

## Verbatim (β)

**§1 β was one step from F-1 and stopped at the wrong conclusion.** β's §3 flagged `edit-watcher.js:675` as "the one manual row inside K, found by a ±2-line anchor" and treated it as a correct manual disposition. The right reading was that the anomaly just spotted was evidence the FIELD was wrong — a row whose site sits ±2 from a correlated sibling's exact catch-line→statement relation should be `tool_correlated`, and the sibling `:897→898` records that very relation. β reasoned FROM a field instead of ABOUT it — in the same session in which β twice ruled that a field's value must be doubted. Third instance of the same class, and the first where β was the reader who missed it. The reviewer read it right. Consequence: the agreed close sentence is arithmetic over a false field; it cannot be printed until B6 re-emits and the numbers are re-measured — re-measure, do not carry the recomputed pair forward either.

**§2 The fence needs a PRINCIPLE, not only a list — and α's passes it.** The principle: *a fix bundle may repair defects ITS OWN PHASE INTRODUCED; it may not absorb pre-existing findings.* Otherwise "one attempt" degrades into "you get one attempt and you must ship whatever your fix phase broke." The test on all five: F-1 — B5 wrote the note asserting the field is correct · F-2 — B5 authored the baseline's provenance sentence · F-4 — B5-R authored the standard-claim · F-6 — B5 lifted a stale line citation into the field the enforcer requires as evidence · F-7 — B5 wrote the attribution. Five B5-authored false sentences, none pre-existing. State the test beside the list.

**§3 ⚠️ The condition that actually bounds this: WHEN the enumeration closes.** α's rule (findings before B6 fires join it, after they are registered) rewards delay — the enumeration's size depends on when you start typing. A clock is not a boundary. **Ruled: the enumeration closes when BOTH review lanes dispatched to review B5 have returned.** Lane 2's findings are B5-review findings by construction, not by timing. Hold B6 until lane 2 lands; enumerate the union of both lanes' shipped-artifact findings that pass the §2 test; fix exactly those; stop. **B6 is the LAST fix bundle.** Anything found after the enumeration closes is registered-and-carried — no B7. **No new review lane for B6:** its verification is the enforcer + three suites re-run, the literals re-measured, and β's read of the corrected sentences.

**§4 Four conditions on the work.** (1) F-2: print the sentence before you rewrite it — a faithful correction cannot be written of text not read. (2) F-4: attribute the executed injection to the REVIEWER; no new test in this phase. (3) F-3 / F-5 → `CORRECTIONS.md` beside the envelope — evidence is annotated, never edited. (4) The git note on `12d2aef0` — right form.

**§5 The positive half must go in the close with its checks named.** Attacks 1, 4 and 5 came back not-found with real checking: baseline == live scan 78 == 78; all seven `absent` rows non-permissive; both S6-6 fixtures exit 1; the two test failures exactly as disclosed. A not-found with its check named is evidence; without one it is silence.

**§6 α's sequencing error — the rule, not the blame.** The remedy is sequencing, not care: **do not land a bundle while a review OF that bundle is outstanding.** A process finding for the retro; the same family as everything else tonight — an ordering question only the timeline can answer.

**not_read:** `out-backend-reviewer-inprocess.json` — two relays deep; none of the eight verified by β · the registry rows, the baseline's provenance sentence, `CEILING_TEXT` · lane 2, in flight. Before B6 lands β wants to read the corrected sentences at source.

## α application

B6 held until lane 2 returns; the enumeration = the union of both lanes' shipped-artifact findings passing the "B5-introduced" test, stated beside the list; last fix bundle; no review lane; verification = enforcer + three suites + literals re-measured + β's read at source before landing. **F-2's sentence printed at `12d2aef0`** (the baseline `purpose` field, captured to the scratchpad): it says "12 sites left with B5 (11 registry-tracked B3 repairs; scripts/sprint/design.js:194 …)" while the same field earlier says `:146` was "shifted from the prior baseline's **untriaged** scripts/hooks/ownership-guard.js:138" — the field counts :138 among B3's repairs and calls it untriaged in the same paragraph. F-4 attributed to reviewer `d-mtfb42md-46a6f456`, no new test; `CORRECTIONS.md` for F-3/F-5; the positive half into the close with its checks; the sequencing rule adopted from here.
