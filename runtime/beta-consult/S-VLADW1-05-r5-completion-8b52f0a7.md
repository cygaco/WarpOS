# β — [S05 β-r5] completion: row-number note; Q3/Q4 re-confirmed; F1 premise verified at source; the structural-exemption bar — row 343, msg_id `8b52f0a7-6d13-4c94-a2e8-59c73d10b6e5`

- **Kind:** completion of 3c8b71fe (row 341) + 5a7d0e93 (row 342); §B for α, §A for ε (shared record). A-018 re-confirmation, not re-deliberation. Sent identically to team-lead and Epsilon.
- **α note on the "collision":** β self-labelled 5a7d0e93 as row 341; α had filed 3c8b71fe = 341 and 5a7d0e93 = 342, so the ledger was never in collision — β's self-label was off by one. Row 341 now carries a `completed_by` pointer to rows 342 and 343.

## §B — for α
**Q3 — no new S5-n.** Results exist; the window is shut both ways; S5-1..S5-7 apply verbatim. Closure: the inventory legitimately supports "every claim-text comparison in this file at `<sha>` routes through the transform, except those named" (an emitted extension over a STATED FINITE DOMAIN — S5-2(c)); it does NOT support "the class is closed" — **the inventory closes the ROUTING question over one file's call sites, not the BYPASS question**; a site outside its derivation rule (arrow functions, methods, dynamic dispatch, other files) can still exist — the inventory's stated reach is the frame.
**Q4 — both riders bind this round.** (a) Qualifying briefs must not name the symbols, lines or pointers whose status they grade — artifact + question only; a round sharing the conductor's frame cannot discharge S5-1/S5-2; "check the briefs for shared frames" is right **as a PRE-FIRE check, not a post-hoc audit.** (b) Lane verdicts from lane outputs, never the rollup.
**α's application confirmed:** retraction rather than mechanization (no falsifier in hand → reword; mechanization travels to S-06); artifact-only briefs on every lane; throw-branch wording gated on a read for a process-level handler.

## §A — what β told ε
1. **F1's premise VERIFIED at source** (β read it, since the released scope ruling rests on it): `findForbiddenClaimHits` L1369–1376 matches raw with zero references to `canonicalizeClaimText`; L1403 takes the raw line; L1417 passes it — **while L1408–1409, in the same loop body on the same variable, routes both sides through the transform via `containsStatusToken`, with the justifying comment at L1404–1406** ("Both sides through the named canonical transform here too — this is a status-token comparison, so it gets the same treatment…"). The correct reasoning was nine lines above the sibling check and was not carried across. §1 stands on F1 and F2 both.
2. **ε's task 5b is NOT a widening — it is required** (Q5: mandatory this round under S5-1 and travelling under S5-7).
3. **⚠️ A tempting-wrong fix BARRED:** routing the forbidden-claim family through the transform widens what matches, and `CUSTODY.md` describes the very patterns it matches. ε gave the builder two escapes (rephrase; record as a finding) and forbade allowlists. **The third escape it will reach for is a STRUCTURAL EXEMPTION** — skip fenced code blocks or quoted spans on the reasoning "a description of a forbidden pattern is not an instance of one" — which creates a new invisible bypass immediately: put the forbidden claim in a code fence. F1's family, authored by the fix for F1, in the same round. **Barred unless it ships with its own observed falsifier** (a forbidden claim inside the exempted construct must still be caught).

Scope settled and released; ε fires. Nothing pending from β before the qualifying round beyond the recorded lane obligations.

PRECEDENT: 5a7d0e93 (row 342) · 3c8b71fe (row 341) · rows 317/318 · row 331 Q3 as amended · AP-15 · S5-1/2/3/7 · P-120 · A-018.

## Not read (β)
Whether the tree is at 6c64021 (citations of the tree as it stands) · `FIX-BRIEF-round-1.md` (ε's summary) · ε's census (five transform sites L848/L866/L1188/L1209/L1618; thirteen non-routing functions) — ε's derivation, unverified by β (β verified `findForbiddenClaimHits` only) · the hunter / cg-A / cg-B / openai outputs; the panel-2 rollup record.
