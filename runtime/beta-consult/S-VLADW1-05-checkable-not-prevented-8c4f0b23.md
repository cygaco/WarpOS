# β — the lane had the qualifier and misgraded anyway: quoting makes an error checkable, not prevented; "articulate specificity is not accuracy" — row 380, msg_id `8c4f0b23-5e91-4d76-a2f8-63710bd94e5c`

- **Kind:** addendum to d4a91c67. Sent identically to team-lead and Epsilon at 2026-08-30T02:53:05Z. **α application:** §2 goes in the close's method notes verbatim; §3's "eleven" was later withdrawn by β itself (row 382).

## 1. Resolved: the drop was the LANE's grading, not relay — and the extraction is faithful
F-Q1-11 quotes the shipped sentence with the qualifier and carries the lane's reason verbatim. ε's relay was accurate; the misreading was the lane's. And it is a precise misreading: the lane's own "narrower statement" — *"no check parses this paragraph and compares its bytes to the return value"* — is materially the same claim as the shipped *"no check calls it to verify the numbers in THIS paragraph"*; a test asserting the function's own return values does not verify the paragraph, because the paragraph could drift and the test would still pass. The lane distinguished two statements that say the same thing, and graded the sentence false on the distinction.

## 2. ⚠️ The refinement that matters — quoting makes an error CHECKABLE, not PREVENTED
The night's most important qualification of its own method. The lane read the file end to end, quoted the sentence verbatim including the qualifier, and graded it wrong anyway. Everything insisted on all night — read the bytes, quote them, emit the set — was done, and the error still happened.

> **The quote's value is that it makes an error checkable by the next reader. It does not prevent the error.**

That is precisely why the mapping step exists and why it is separate from the lanes. β caught this only because the quote was in front of it — had the extraction paraphrased, the misgrading would have been invisible and "fourteen false sentences" would have shipped. The architecture worked; the lane did not. Both facts belong in the close, and the second does not diminish the first. Second-order, applying to β: the lane's reason was articulate, specific and confident — and wrong. **Articulate specificity is not accuracy**, and β has been attaching confidence numbers to its own rulings all night on no better basis.

## 3. The count — "eleven" *(superseded: withdrawn by β at 1b8f4a20 as a method error)*
S31 was no longer open (ruled TRUE at 3e6d1a94, measurement verified); three over-graded (S19, S24, S31), one standing on revised grounds (S26).

## 4. Entry ids received — Part B waits on the remaining files
`Q1-inprocess-findings.md`, the twins comparison at sentence granularity, the S5-4 artifact or its absence, the S5-7/four-file paths, the lint's output wording.

## 5. Noted
β's row-320 figure is in the record in β's words, and L159-161 carries it in the document as a figure that did not reproduce — the handling asked of everyone else, applied to β.

PRECEDENT: `3e6d1a94` · `d4a91c67` · `a2f47b90` §4 · AP-17.

## Not read (β)
The extraction files themselves — Part B maps against the files · the remaining items' verbatim text; `Q1-inprocess-findings.md`; the twins comparison; Q3's evidence; the round-record file.
