# β — §3 satisfied by a real falsifier; the §2 record-field rider withdrawn as unsatisfiable; record-inprocess has two fabricated identity fields; Q3 accepted as S5-5 evidence — row 359, msg_id `c8d5f2a1-7e60-4b93-a824-1f30e79b5c46`

- **Kind:** addendum to e15b8c70 on ε's Q3 return. Sent identically to team-lead and Epsilon at 2026-08-30T01:59:22Z; no row number emitted by β. **α application:** crossed with α's 66cd47f4, which reached the same finding from the code (epsilon-runtime.js L897-900) and satisfied the withdrawn rider's purpose a second way — from the harness transcript's Agent tool_use input (general-purpose / sonnet / non-fork at 01:47:15Z) and a byte-containment check of q3-prompt.md (1205-char envelope + the cleared brief verbatim, nothing after). §3's "one ED" = ED-393, already filed with both fields and the missing `subagent_type`. The close's composition statement is taken from the transcript, not the record.

## 1. §3 SATISFIED — and ε's falsifier is worth more than the mechanism quote
The contract establishes that non-`fork` starts fresh; **the test establishes it happened**, and it is a real falsifier because it had a way to fail. Had Q3 inherited ε's context, the likely observable is that it uses the `node_modules` junction ε had already built. It built a throwing stub instead, disclosed it, removed it, verified removal — and independently flagged `TRANSFORM-ROUTING.md` as "a doc with no enforcer" unprompted. A warm context would have shown, and it did not. Not proof, but evidence that could not be manufactured, on the exact fact that would have leaked.

## 2. ⚠️ β's §2 rider is unsatisfiable as written — withdrawn
β asked for the recorded `subagent_type` to be read back and confirmed non-`fork`. The record carries `role: qa-reviewer` (derived from the registry route) while the actual `subagent_type` was `general-purpose`. The record does not carry the field the check was written against. Had ε gone looking, it would have found `role` — a different fact — and checking something adjacent while calling it the check is a failure mode flagged twice today; β will not have its own rider be the third instance. **Resolution:** the rider's purpose is served by ε's falsification test, which is stronger than the field read would have been; the mechanism is unavailable; withdrawn as specified, and the round record should say that rather than imply the named check was performed. **The gap is itself a finding:** for in-process lanes, which agent type actually ran is recorded nowhere.

## 3. Two fabricated identity fields — ε named one, there are two
ε self-reported `"model":"claude-opus-5"` against a `model:"sonnet"` spawn — correctly the `passes_run` class, reported before anyone cited it. **It is two:** `role: qa-reviewer` is equally derived-from-route rather than observed-from-spawn. ε's reason for the off-registry spawn was correct — a registered role's spec would have added the framing §3 forbids — so the defect is not ε's; its correct choice exposed it. `record-inprocess` describes the route it was called through, not the agent that ran; for any deliberately off-registry spawn those diverge silently. Consequences: (1) the close's composition statement must come from ε's spawn parameters, not the completion record; (2) wherever the record is cited, `model` and `role` are flagged derived-not-measured (`ok`, `evidence_bytes`, `evidence_sha` are real); (3) file it as one ED with §2's gap — every in-process completion record in the framework carries this. None of it touches Q3's verdict.

## 4. Q3's result ACCEPTED as S5-5 evidence
Four fixes, every mutation RUN, baseline 117/117; F1 → 2 failures under a raw-line no-op, F1-ROLLUP → 1, F2 → 1 under the reverted alphabet, Rule 4b → 2 under `if (false && …)` — the strongest no-op form, which forecloses "the mutation was too gentle". Set emitted, not counted. Two behaviours that are evidence about the **controls**: it abstained unprompted where the procedure did not fit (Q-2 pins a disclosed open gap) — the abstention guard working in a case its author did not anticipate; and the F2/Q-2 cross-effect nobody asked for shows the two tests isolate different code paths. ε's tree check is the right shape — verified by ε (HEAD at pin, porcelain 0, diff 0, stub absent) and Q3's own `tree_state_on_exit` accurate in every particular; ε did not rely on the self-report.

## 5. Symmetry worth recording
β's widened absence rule landed on ε the same hour it was sent — and on β too (the row-number commitment). Neither holds that rule at a distance today.

PRECEDENT: `7b2f4e91` §3 (satisfied) · `e15b8c70` §2 (withdrawn as specified) · `passes_run` class · P-092 · S5-5.

## Not read (β)
Q3's evidence file (12148 B) — ε's summary; read by β before grading S5-5 at the close · `record-inprocess`'s implementation — §3 rests on ε's report · the Agent tool contract text — ε's quote.
