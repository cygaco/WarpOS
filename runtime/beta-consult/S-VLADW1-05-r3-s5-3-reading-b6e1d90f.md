# β [S05 β-r3] — READING of S5-3 after bundle M — row 324, msg_id `b6e1d90f-3a5c-42e7-91d8-04f7a2c6b385`

- **Decision:** DECIDE · class B · 0.92. Answers α consult `40e013bf`. Sent identically to team-lead and Epsilon. **Explicitly NOT criterion-firing** — a pre-close reading recorded BEFORE the qualifying gauntlet (only bundle M exists; N in flight); must not be treated as leverage at the close. Rows 317/318 stand verbatim.
- **α stamp:** vlad engine-lane HEAD `3596c2d` (parent `6a105f2`) per ε's verified read of the diff (+482/−25; 377 pass / 0 fail; check:ship 0); WarpOS tree `3b432832`+. β's reads are of the working tree as it stands.

## Conflict declared first (P-112)
β wrote "seven." A property reading means β's own number being wrong does not fail the sprint; a literal reading means β's drafting error fails it. Position-swapped both ways (builder finds EIGHT → still demand the emitted set; α argues the literal binds → no, on ground 2). Position-stable. Arithmetic verified at source, not from ε's relay.

## VERDICT — S5-3 binds the PROPERTY; S5-3 is NOT failed
Property: **no shipped sentence overstates the fold's yield, and the yield is stated as an emitted set derived against the transform as built.** Grounds: (1) **pre-committed before any result** — row 320 (filed at `6e8501e5`, engine-lane clean at `6a105f2` 20:03Z, before M at `3596c2d`) said the count is contingent on the builder's replacement semantics + alphabet and that any sentence restating a moved number becomes a granularity falsehood (P-094; checkable from the row's timestamp — what separates "β read the rule" from "β moved the goalposts"). (2) **A literal reading would mandate shipping a falsehood** — "seven closed by design" is now known false (six); S5-1 (truth) and S5-2 (granularity) forbid it; the literal reading is incoherent within the rule set. (3) **The property reading is STRICTER** — under it "seven" fails AND "six" fails (a bare count is an exhaustiveness claim, AP-18); only the emitted set by name satisfies. (4) The builder's conduct is what the rule was for — refused the premise with evidence, declined to tune code to preserve seven; correcting a number to fit reality while holding the rule fixed is the opposite of goalpost-moving.

## β re-derived the decomposition at source — 6 + 1 + 1 CONFIRMED
- `SEPARATOR_VARIANCE = "[\\-:~|/\\s]*"` (L974) — tilde IS a member → `~~ASSERTED~~ — NOT VERIFIED` matches regardless of the fold: **strikethrough is red by accident** (mechanism = the separator class, not the fix).
- Fold (8) `EMPHASIS_FOLD_PATTERN = /[*_`~]+/g` replaced with a SPACE (L280, L303) → `ASS**ERTED**` → `ass erted`: **mid-word bold remains OPEN** (exactly row 320's predicted space-vs-empty divergence).
- Battery's eight R3 authorings against the built transform: bold-first, italic-both, underscore, bold-second, code-span, bold-separator fold to `asserted - not verified` → **six close by design**; mid-word open; strikethrough already red. 6+1+1 = 8 — the arithmetic closes. The original "seven" was correct for the design battery's SIMULATED fix (empty replacement) and wrong for the BUILT one — the contingency, not a sprint-introduced error.

## S5-3 is satisfied IFF bundle P ships ALL FOUR
1. The emitted set BY NAME — the six authorings the fold closes, the one already-red-for-an-independent-reason, the one open residual. Never "seven", never "six", never a bare count (AP-18).
2. The mid-word residual disclosed as OPEN, with its mechanism (space replacement splits the word) AND the reason space was chosen over empty (the `**only**place` anti-evasion in `flattenForAssertionScan`). The disclosure is itself a claim (P-110/AP-17).
3. Strikethrough must NOT be credited to the fold — state the separator-class reason; crediting it restores the overstatement the seven-not-eight bound existed to prevent.
4. No sentence claiming the fold lives in "one place" / "a single site." True form: one shared fold (inside `canonicalizeClaimText`, L303), one documented opt-out, plus separate shape-recognition uses (`EMPHASIS_RUN` steps (b)/(d), and the fourth site `BOLD_LEAD_IN`) that are a different mechanism. A one-place claim is false and fires S5-2.

## The `{emphasisFold:false}` opt-out does NOT reintroduce S4-2(c)
The fold lives INSIDE `canonicalizeClaimText` (L303); every caller receives it by default; exactly one implementation (`EMPHASIS_FOLD_PATTERN`, exported at L280 so tests can assert both sites share one alphabet); precisely one caller opts out with the reason documented (L186–191, L717–725). S4-2(c) was a fold implemented BESIDE the shared transform; this is a parameterised shared transform. ε's reported falsifier (opt-out reverted → 4 genuine failures) is the correct proof shape.

## Not settled
The fourth emphasis site (`BOLD_LEAD_IN` ~L554) scoped out "with reasoning" — β has not read the reasoning; a scope-out is a claim; it belongs in bundle P's disclosure if any shipped sentence's truth depends on it.

PRECEDENT: rows 317/318 · **row 320** (decisive) · P-094 · P-112 · P-110/AP-17 · AP-18/P-116 · P-115 · P-124 · P-130 · the G-30 amendment-window invariant (a reading fixed before results, or entailed by the rule's siblings, is not an amendment — both hold).

## Not read (β)
`engine/test/custody-claim-lint.test.js` @3596c2d — the 377/0, 86/86, 13 RF-M2 tests and 4 genuine failures are ε's relay of the builder's envelope, unverified (β verified the transform's BEHAVIOUR by reading it and deriving the eight rows; not that the tests assert what they are said to) · the `BOLD_LEAD_IN` scope-out reasoning · the `**only**place` battery row re-run · the bundle M diff as a diff (file's current state read, not the change) · whether the tree is at `3596c2d` (α's HEAD stamp is the authority) · **row 318's third residual still OPEN**: the four un-audited files unread by β and load-bearing inside S5-2's scope — bundle P task 5's sentence may not be drafted before that read.
