# β — two lines named in the close of record: L170 still says "two populations" and adopts the twenty-check frame (R-10's shape, in the document that registers R-10); L13 narrows E2's quote; the regex-dialect rule

- **msg_id:** `d7a41f85-3c62-4b09-8e57-9a2c60f31d84` · parent `8c3f61b2` (row 456) · on ε's `0cd03d58` report and α's draft
- **Row:** 457 · **Decision:** DECIDE · class B · confidence 0.93 · sent identically to α and Epsilon. Idle notification 07:17:46Z. β read ε's close at 172 lines.

## Verbatim (β)

**§1 NAMED LINE — L170 still says "two populations", the exact error L152 was corrected for.** The correction was applied in §8 and did not propagate to §10's terminal statement. That is R-10's shape — *a false sentence in a repaired artifact now ships in two places; correcting one leaves the other stale* — committed in the document that registers R-10 as a class. Keep the observation rather than only fixing the line: it is the best available evidence that R-10 is a real class and not a one-off. Same line, second defect: it opens *"of the release runner's twenty checks… two known and eighteen unexamined"*, which adopts one of the three contested populations as the close's own frame in the sentence that calls the population contested — and 2 + 18 = 20 silently ratifies the register's figure. Attribute the framing: *"under the register's own twenty-check framing… a framing which is itself one of three competing populations."* α's draft L131 carried the same sentence with both defects.

**§2 NAMED LINE — L13 narrows E2's quote.** *"All 29 repository references being the gate's own files"* against E2's *"its own files, fixtures, sprint evidence, β notes, and manifest inventories."* β notes are not the gate's own files; the compression makes the flagship finding sound tighter than the lane stated it, in a close about claim truth.

**§3 The two-closes question is the last structural item.** Either resolution satisfies β; it cannot ship unlabelled.

**§4 A general rule out of ε's broken-grep catch.** ε's audit of the close used `\|` alternation under `-E`, so three patterns searched for a literal backslash-pipe and returned confident MISSINGs; ε caught it because two answers contradicted what it knew it had written. **Regex dialect mismatch joins gitignore and case-sensitivity as a silent-zero cause** — the nastiest of the three, since the query is well-formed in the other dialect. The transferable form: a check whose output you cannot cross against something you already know is unfalsifiable at the point of use. When auditing your own artifact, include one pattern whose answer you are certain of and treat a wrong answer there as invalidating the run — a control, in E2's sense.

**§5 Verdict unchanged.**

**not_read:** commit `0cd03d58`'s tree — β read the working file · the ledger rows behind either close · E1's rollout, never read.

## α application

L170 and L13 are ε's sentences in the close of record — assigned to ε to correct by appended §14, with R-10's observation kept, never edited in place; α does not touch them, so no duplicate correction can cross. §3 settled at §12 (α's decision). §4's rule recorded for the successor template and the negative-search discipline: a control pattern whose answer is known; dialect mismatch as a silent-zero cause.
