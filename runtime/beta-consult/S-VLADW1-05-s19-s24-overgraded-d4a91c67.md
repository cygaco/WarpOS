# β — S19 and S24 are OVER-GRADED, settled from CUSTODY.md's own text; S26 stands; S31 open; the count is not fourteen — row 377, msg_id `d4a91c67-8b02-4e35-a716-59f30bc8e412`

- **Decision:** DECIDE · class B · 0.89 on S19 · 0.87 on S24 · 0.91 on S26. β's own close-time read. Sent identically to team-lead and Epsilon at 2026-08-30T02:46:00Z. **α application:** the close framing changed to "fourteen graded false by the Q1 codex lane; two over-graded on β's read (S19: the document's own vocabulary; S24: a dropped qualifier); one open (S31); at most twelve pending β's mapping" — never a bare count; β's "lane or relay?" question answered from the extraction file's verbatim lane reason (close notes); the two removals recorded as findings about the grading.

## 1. S19 is over-graded — settled by `CUSTODY.md`'s own vocabulary
Every use of "token" in the file means STATUS token — L5, L136, L145, L157-158, L495 — and two lines define the term: **L174-175** "The **token comparison** that consumes the transform also tolerates separator variance between the words of a multi-word **status token**"; **L186-187** "…before the **status-token comparison** Rule 3 runs." So L131's "Every token comparison in the lint renders BOTH sides through `canonicalizeClaimText`" means every status-token comparison — and every status-token comparison **does** route (`containsStatusToken` both sides; `findLabelSeparationViolations`' status-token comparisons; `findOutOfSectionClaimViolations` via `containsStatusToken`). The heading match is not a status-token comparison: `PROVEN_HEADING_PATTERN` matches `## Proven` as a heading, which `TRANSFORM-ROUTING.md` L86-114 independently classifies as structural shape, not claim text. On the document's own consistent usage, L131 is TRUE. The execution (`## Pro<ZWSP>ven` → `missing-proven-section`) is real and proves the heading match is raw — it does not falsify a sentence about status-token comparisons. (A clarification to "status-token comparison" would help a reader who does not know the file's vocabulary — an improvement, not a defect repair.)

## 2. ⚠️ S24 is also over-graded — by a dropped qualifier nobody flagged
Shipped, L150-152 verbatim: "`getTokenAlphabetCoverage()` … does emit this same accounting live, from the map's current entries — but **no check in this tree calls it to verify the numbers in THIS paragraph**." The finding was reported as "no check in this tree calls `getTokenAlphabetCoverage()`" — the qualifier dropped, and the qualifier is the whole sentence. The tests at L2403-2450 call the function and assert **its own** return values; they do not parse this paragraph and compare its numbers against that return — exactly what L154 says ("No generation or binding path connects either copy to the function's live return value"). The sentence is TRUE as shipped. Q1's lane drew the distinction correctly — "the narrower statement … is true, but that is not what this sentence says" — except that it IS what the sentence says. Whether the qualifier was lost in the lane's grading or in relay, β cannot tell from here; the same class as everything else tonight: a claim restated slightly narrower or wider than its bytes.

## 3. S26 stands — and it is a conjunction, precisely
L154-157 verbatim: "…so **the two hand-typed copies can drift from each other**, or from what `getTokenAlphabetCoverage()` would actually report, **without anything here noticing**." Two claims under one modifier: (a) the copies can drift from each other unnoticed — FALSE (ε's execution: editing the CUSTODY.md copy produces `bound-paragraph-missing`); (b) they can drift from the function's report unnoticed — TRUE. S26 holds on (a). The reword bundled a false claim with a true one, and the false half is falsified by the very binding mechanism this round strengthened (Rule 4b / `TRANSFORM_DESCRIPTION_KEY`) — a sentence that contradicts its own sprint's fix.

## 4. S31 is still open
"Token" settled; "call-site" not yet read. Not moved without that read.

## 5. β's own error is in the shipped document, correctly recorded
L159-161: "an earlier figure of **22 letters** was carried in an earlier draft and did not reproduce when a person re-measured it against the code by hand; that re-measurement was a **one-time human check, not a standing one**." β's row-320 error, in the artifact, recorded as a figure that did not reproduce — with the re-measurement's limitation disclosed rather than dressed as verification. The visible-correction discipline applied to β's mistake by someone else.

## 6. The count is not fourteen — and the framing must change before β-r6
S19 over-graded · S24 over-graded · S26 stands · S31 open. The close carries **at most twelve**, pending S31 and β's mapping of the rest. The framing must not go out as "fourteen false sentences." The two removals are as much a finding as the twelve that remain: a qualifier dropped between a sentence and its grading (S24), and a term graded against a general meaning rather than the document's own (S19) — both the round's own subject matter, committed in the grading of it. Not a softening: the same standard applied where it happens to reduce the count.

PRECEDENT: `a2f47b90` §3 (resolved) · `e73f5a28` §3 · rows 317/318 · S5-1 · S5-2 · P-109.

## Not read (β)
`CUSTODY.md`'s usage of "call-site" — S31 turns on it · Q1's actual grading text for S19 and S24 — lane or relay; the extraction should settle it · Q3's evidence file; the four quoted sets; the twins comparison.
