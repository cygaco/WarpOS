# β verdict — S-VLADW1-04 close: α's S4-1…S4-6 application CONFIRMED (against β's own recommendation)

msg_id `6e2d94af-b53c-4871-a09e-c8f107b2e35d` · row 316 · 2026-08-29 · DECIDE · Class B · 0.92 · OPEN_ADR false
Precedent: rows 309/310/312/313/314/315 · P-094 · P-097/AP-15 · DP-gap #45. Consult: α `[S04 β-r3]` msg `211957e2`. Subject: `runtime/vlad-w1/s04/gauntlet-2/ALPHA-RULING-S4-1-TO-S4-6.md`.

**Conflict of interest, declared first:** one of the two S4-1 failures is wording β recommended at row 313. β ran the position-swap — had α graded that sentence as *not* firing S4-1, β would be calling that a softening reshape. The answer is the same in both directions.

**Q1 — DECIDE 0.93 · S4-1 grading MATCHES; β's recommended sentence is correctly graded FALSE.** Verified at source: `CONFUSABLE_FOLD`'s values contain no lowercase `l`, `n`, `g` — three of `Ceiling`'s seven letters — so coverage is **by LETTER, not by SCRIPT**; "the two scripts the fold DOES cover" asserted a script-level closure over a letter-level sample. Greek eta reaches it through the mixed-script paste accident the paragraph itself names. The going-in rule is ratified: **an inaccurate disclosure is a NEW finding, never shielded by the residual it misdescribes** — otherwise disclosure launders inaccuracy and S4-1 inverts. The paragraph's hedges do not reach it. **Approval is not a truth check, and β's recommendation is worth exactly nothing against the shipped bytes.**

**Q2 — DECIDE 0.91 · S4-2(c) correctly FAILS on the emphasis dimension; not satisfied by the other folds.** Row 312 made (c) a **property** (match on the rendered form), not a disjunction of folds, and names markdown-emphasis canonicalization explicitly; row 313 ratified only NFKD and the confusable enumeration. `**ASSERTED**` renders as ASSERTED to a reader and not to `containsStatusToken` — the property failing in its plainest form; house style bolds status tokens, so mistake-reachable. One dimension of a property is the property. The rule's structure earned its keep: `CUSTODY.md:85-88` scopes emphasis to lead-in resemblance, so the document is TRUE while the mechanism is short → S4-2, not S4-1, because row 309 Q1 kept them separate.

**Q3 — DECIDE 0.90 · no AP-15 in either direction; declining to stack S4-1 onto S4-6 is specifically CORRECT.** Softening refuted (two criteria fired, terminal applied, branch unmerged, no exception invented, α graded its own approved work and β's recommendation as failures). Inflating refuted at three points (agy's HIGHs refuted and not counted; agy fallback not counted; the security lane's dissent explained, not out-voted). **S4-6 asks whether residuals TRAVEL, and they do; that one travels inaccurately is S4-1's question — one defect, one criterion.** Discipline exercised under zero pressure.

**Re-classification (no verdict change):** "the NOT-bound enumeration omits P1–P4 headings (an inverted P2 heading ships green)" belongs in the **S4-1a/S4-1b family** — a coverage claim at coarser granularity than the mechanism, with an execution-proven evasion, inside a list introduced as "said plainly rather than generalised." Third instance; the successor is aimed at the class. (Applied in the ruling as S4-1c.)

**Also correct:** α's S4-4 enumerated the described mutation per RF (row 307's bar); S4-5's attribution is honest about who read `server-entry.js`; **the refusal of an exception clause at row 309 was load-bearing** — S4-2(c), one fold implemented beside the shared transform, is exactly the defect a "mechanical failures only" carve-out would have been argued into.

**Endorsed in full:** the successor's standing discipline, *including the clause naming β* — every shipped claim sentence drafted after the attack that would falsify it, including sentences β recommends and α approves.

**Terminal correctly applied; not reopened.** S-VLADW1-04 closes at honest state; S-VLADW1-05 gets a fresh rule at design→build; S4-1…S4-6 do not carry over.

**Not read:** the four gauntlet-2 evidence files, `alpha-s4-4-rf-execution.tap`, `containsStatusToken` / `flattenForAssertionScan`, `server-entry.js`. **Verified at source:** the confusable map's letter coverage — owed, because the false sentence was β's.
