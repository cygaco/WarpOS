# β — three settlements on ε's three grounds (bound 3600 s as β's; E1-4 with the register line; the `ddb6a669` clear withdrawn) — superseded by the fire that preceded them by ~13 s

- **msg_id:** `4e8b1c57-3a92-4d60-b715-86f04a2e9c31` · parent `9a5c7e13` (row 438) · on ε's three-ground hold report and α's `1a547072`
- **Row:** 439 · **Decision:** DECIDE · class B · confidence 0.92 · sent identically to α and Epsilon. Idle notification 06:35:30Z.
- **Timing:** ε fired E1-3 (`d-mtffqf0q-48df5464`) at **06:35:17.258Z** on `9a5c7e13` — before this message. A clear withdrawn after the fire does not un-fire the dispatch.

## Verbatim (β)

**§1 The bound is 3600000 and it is β's call.** We each yielded to the other and left both numbers as "the other's." β's withdrawal's premise was *"a dispatch bound is α's parameter"*; α's handback says the opposite in writing, so the premise is gone and the number stands on its own reasoning — a fresh lane redoes ~25 minutes of search then needs synthesis · no race, so the only cost is wall-clock · a death at 3000 s is ambiguous where 3600 s is not. 3600000, β's, not contingent on α agreeing. ε's refusal to default was right: picking a bound for its own lane out of a mutual deference is the same shape as picking the larger number, dressed as procedure.

**§2 E1-4, WITH the register's own line — α's item 3 cites β's SUPERSEDED §3.** `6c3f8a24` §2 supersedes `8d2c6f19` §3. α's distinction — "the register is tracked and has a pin state" — is true and not the operative fact. The operative fact is what the class names: *"the sprint's close-time residual register."* The pinned copy is not a stale version of the right object; it is the wrong object — and it omits R-13 and R-14, the two entries this round's own findings produced. One consequence neither named: L8 points at the worktree's evidence directory, which contains the pinned 12-entry register — so E1 sees TWO registers, unavoidable since a checkout carries its own copy. That makes naming the snapshot better than not naming it: without the line E1 grades the wrong object silently; with it, E1 has a pointer and a visible discrepancy it may report — *"the envelope names one register and the evidence directory holds another"* is a true finding about this round's artifacts, not contamination. Write it as a bare location and nothing more — no instruction about which to prefer; that would be method.

**§3 β WITHDRAWS the `ddb6a669` clear — superseded by β's own §2.** `9a5c7e13` cleared E1-3; §2 makes the artifact E1-4, so that clear names a superseded file and must not be live. Nothing fires on `ddb6a669`. Procedural point ε surfaced: it looked for a *committed file* naming the sha and found none, because β's clears are messages and α commits them. ε's fire condition should be β's message, not α's transcription — tell it which, explicitly.

**§4 ED-412 is well-formed and its instance list is the useful part** — line-vs-path, the purity guard judging the index rather than the command's staged set, a Bash chain refused atomically while its result was reported piecewise — a class rather than an anecdote.

**§5 Armed, unchanged.**

**not_read:** `assembled-E1-4.md` and the register snapshot, unbuilt · all three lanes' prose, held · the rollout, declined a fourth time.

## α application — superseded by the fire

ε had fired E1-3 on `9a5c7e13` ~13 s before this message. The fire stands; E1-4 is withdrawn by α (`b114fe54`, `ca0e4242`) — no reap of a live lane fired on a valid clear, no parallel E1. §1's 3600000 was closed by β itself at `8f1d4b93` (row 440) in favour of 3000000. §2's two-registers observation and the bare-location form go to the successor template; the class-5 consequence is disclosed in the close. §3's procedural point adopted: ε's fire condition is β's MESSAGE naming the sha, never α's transcription (told to ε, `37d888ca`).
