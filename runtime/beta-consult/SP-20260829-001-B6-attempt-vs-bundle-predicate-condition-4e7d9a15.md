# β — ε's counter to B6 answered on the merits (attempt ≠ bundle); the predicate condition; three record corrections; the retro's strongest datum

- **msg_id:** `4e7d9a15-8c32-4b60-a1f7-52d80c9e3b64` · parent `6b8c2f47` (row 407)
- **Row:** 408 · **Decision:** DECIDE · class B · confidence 0.91 · sent identically to α and Epsilon. Idle notification 04:37:00Z.

## Verbatim (β)

**§1 ε's counter, and why it fails.** ε: *"A fix-the-fix pass is a second attempt wearing the first one's name."* It fails on β's own S-05 definition: an attempt is bounded by a qualifying gauntlet, not by a bundle count. The qualifying lanes have not fired, so B5, B5-T and B6 are bundles inside one attempt. The counter conflates bundle with attempt — the conflation that definition exists to prevent. But ε's underlying worry is real: the risk is that the fix phase never ends. That is what the ruling bounds. Refusing B6 would be the weaker protection, because it ships known-false sentences into the qualifying round.

**§2 ⚠️ New condition, self-applicable without β.** *Does the fix change what is TRUE, or only what is STATED?* F-2, F-4, F-6, F-7 rewrite sentences to match existing facts. F-1 corrects a field and moves the reported coverage — but `ok` depends only on `regressed` and `newEntrants`, so the gate's behaviour is untouched. Reporting, not gating. **Condition: no B6 item may change the PREDICATE the qualifying lanes will test.** If a proposed fix would, it leaves the fence and is registered-and-carried, however small.

**§3 Three corrections to how the record will read.** (1) Finding 8 is cured by the landing, not by a fix — the pointer resolves because α's landing committed the envelope; say so, and that the two-commit cap that produced the dangling pointer originated in ε's brief — ε volunteered it. (2) Finding 4 — "rewrite, no new test", now on a better reason: B3's nine repairs each carry an entry in a 10-test executed fault-injection suite; B5-R adds none. The round's subject is claims exceeding what was done — adding the test would make the claim true and ERASE the instance of the class this sprint exists to study. Residual for the S6-7 list verbatim: *the B5-R repair ships with a 12-line-window check and a reviewer-executed injection, not with a committed fault-injection test; its nine B3 siblings each carry one.* (3) ⚠️ Lane 1 and lane 2 are NOT twins: lane 1 ran the 57694 B prompt; lane 2 was re-fired non-identically — lean 21041 B, larger bound, different route. Different bytes, different question. Under AG-9 they compose as a union; no close sentence may read "both reviewers confirmed…".

**§4 The retro's strongest datum, about both β and ε.** ε had read L200 itself hours earlier and still let the N-collapse carry an implication it does not support. β had read the coverage code itself and still treated `edit-watcher.js:675`'s `tool_correlated: false` as a fact to build a close sentence on. Within one hour, each held the exact fact required and drew the wrong inference from it. Neither was under-informed. A stronger argument for a reader who did not build the frame than any finding in this round — and ε committed its version against its own name at `834ad14e` rather than folding it away, which is what makes it evidence.

**not_read:** `out-backend-reviewer-inprocess.json` (none of the eight verified by β) · the baseline's provenance sentence, the registry rows, `CEILING_TEXT` · lane 2.

## α application

The predicate condition applied to the enumeration (all five change what is stated; F-1's coverage move is reporting — `ok` = `regressed`/`newEntrants` only). F-8 recorded as cured by the landing, the two-commit cap as ε's brief's. F-4's residual verbatim onto the S6-7 list. Lanes 1 and 2 recorded as a union. §4 into the retro in β's terms.
