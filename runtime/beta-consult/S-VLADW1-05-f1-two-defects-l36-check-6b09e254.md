# β — Q2 in-process's evidence well-formed; F-1 may be two defects (no stacking); a possible first genuine lane contradiction on CUSTODY.md L36; S19/S31 remain contested; the union so far — row 373, msg_id `6b09e254-7d31-4a85-93c6-2f4a71d08e35`

- **Kind:** still not the grading (addendum to a2f47b90). Sent identically to team-lead and Epsilon (after 02:32:46Z; no separate idle notification). **α application:** the L36 check run on Q1 codex's committed evidence at once — a *candidate* contradiction: Q1's S08 graded a sentence spanning L36-38 TRUE, and L36 at the pin opens mid-sentence with "forbidden-phrase rules check, but no stored copy pins" — the clause F-1 contradicts; whether S08's graded sentence and F-1's cited sentence are the same item depends on the sentence boundary, so rule (d) waits on both lanes' verbatim quotes (ε's extraction). F-1 recorded as one finding with two candidate questions, mapping reserved to β. "Falsified by execution" struck from the framing.

## 1. Q2's evidence is well-formed
Each finding has a control that fires and an attack that does not, across 31 lint invocations — F-1 at 10/10 (five sentences, two positions), F-2 control exit 1 / attack exit 0, F-3 executed. The control-first discipline applied unprompted to an adversarial task, where the brief asked only for attacks. And F-2 narrows itself — "narrowed to the one non-derived bound key" — rather than claiming Rule 4b broken generally: a lane bounding its own finding.

## 2. ⚠️ F-1 may be two defects, and no stacking forces a choice
F-1 reports both that the `## Asserted` section is scanned by no forbidden-claim rule and that `CUSTODY.md` L36 says the rules check that region. Two different defects answering two different questions — "is the region scanned?" (mechanism) and "is L36 true?" (claim). NO STACKING says one defect fires one criterion: if the design intends the Asserted section scanned → the mechanism is broken and L36 is accurate-about-intent; if it intends it unscanned → the mechanism is fine and L36 is false → S5-1. That mapping is β's at the close; flagged so the finding is not recorded as firing two criteria before anyone has decided which question it answers.

## 3. ⚠️ This may be the round's first genuine lane contradiction — a concrete check
Q1 graded ~37 `CUSTODY.md` sentences. Does its set include L36? If Q1 graded L36 TRUE and Q2's F-1 shows it false — a CONTRADICTION between two lanes on the same item, and the frozen rule (d) fires: resolved by a read at source producing a quote, checkable by the other party — the first actual trigger tonight. If L36 is not in Q1's graded set — a UNION: Q2 found something Q1's population did not reach, and the quote rule does not fire. Run that check the moment the quoted per-sentence list lands: a single lookup that decides whether the close carries a resolved contradiction or a straightforward union.

## 4. S19 and S31 remain contested — the execution is not in doubt, what it falsifies is
`## Pro<U+200B>ven` → `missing-proven-section` is a real result; it proves a *heading match* is raw. Whether that makes "every token comparison" false depends on whether a heading match is a token comparison, which the inventory itself classifies as structural markdown shape, not claim text. Same for S31: `opts`-forwarding is real; whether it makes "the full call-site population" false depends on whether that phrase means exercised call sites or the reachable surface. **Keep the close's framing from hardening on "falsified by execution" for S19** until that resolves — the phrase imports the conclusion into the evidence.

## 5. Accepted
The suite wording — "independently confirmed by a blind third-party run at the pin with real dependencies (408/408, exit 0)"; ε's "the round has FAILED S5-1 and S5-2" recorded as ε's characterisation, mapping β's; Q2's token read from the raw file, sole occurrence, line 1, with `role`/`model` disclosed per ED-393.

## 6. Union so far, stated plainly
Q1 finds prose defects; Q2 finds mechanism bypasses; Q3 found the tests have teeth. Three lanes, three questions, no contradiction among them yet — §3 is the only place one could appear. The close grades the union, and the union is larger than any single lane's verdict.

PRECEDENT: `a6f31d84` §3(d) (first potential trigger) · row 369 §4 · `a2f47b90` §3 · P-109 · S5-1 · S5-2 · S5-5.

## Not read (β)
`out-Q2-inprocess.md` and `out-Q1.json` — α's and ε's reports · `CUSTODY.md` L36 — the sentence F-1 contradicts · Q2 codex and Q1 in-process (then out).
