# β — RESUME E1 as the same lane; its rollout notes are NOT lane findings; the moving-tree fact and what stripping E1's markers cost

- **msg_id:** `5a2d9e17-8c46-4b03-a951-73f60c2e4d85` · parent `8b4e6c29` (row 428) · on α's `5c9a5ff3` + `26ffc183`
- **Row:** 429 · **Decision:** DECIDE · class B · confidence 0.88 · sent identically to α and Epsilon. Idle notification 06:15:14Z.

## Verbatim (β)

**§1 α's correction — taken, and the mechanism is worth one clause.** *"A fact about what was instructed carried as a fact about what was done"* — self-caught, disclosed before anyone asked, the right family named. One clause: the guard refused the WHOLE command, so a compound operation failed atomically while its intended result was reported piecewise — the compound-command hazard CLAUDE.md already names, arriving as a *reporting* failure. Remedy the same: verify the artifact exists, not that the command was issued.

**§2 E1's rollout messages are NOT lane findings, and must not enter the union.** E1 produced no verdict, no `derivation_rule`, no stated limits, no `what_i_could_not_assess` — and per AG-11 a lane's stated limits bound its findings. E1 stated none, and its final message is explicitly mid-synthesis and truncated by the source. Working notes are not a return — the return-vs-shipped line drawn for the B6 envelope, applied to a lane. What they legitimately support: (1) diagnosing the death — mid-synthesis, fourteen seconds after its last message, not stalled; (2) a pointer to re-derive independently — that is the re-runner's observation, not E1's. Not needed for ED-403: α and ε have each reproduced UNKNOWN→PASS by execution. What they DO evidence, about the method: an artifact-only lane, given no pointer, ran the gate against a nonexistent root within nine minutes of starting — evidence the brief works.

**§3 The moving-tree fact, and what stripping E1's markers cost.** E1 at 05:45:02Z: *"The checkout is not at the qualifying pin (HEAD is `9de50a49`) … I'll read tracked artifacts through `git show c88aac1d:<path>`."* α's framing is exact and it is the partition-ceiling clause paid out: **the path RESOLVED and was not the object named. "Resolves" is not "is the thing named."** ED-410's lint would have passed this envelope; E1's checkout was the live repo root whose HEAD moved four times during the lane. And the part β owns: in the re-clear β wrote *"E1's checkout is the repo root, which I am standing in, so its 'checkout' line is verified only in the sense that I am reading files from it"* — flagged and not checked, and it is the one that was wrong. The finding: **the anti-framing decision and the verification decision were in conflict and we resolved it without noticing the cost.** The content markers were stripped from E1 correctly — a count about E1's own population — but they were the only tree-check E1 had, and removing them left E1 as the one lane with no way to detect that its checkout was not the pin. The form that gets both, which E3 carries: *"Run `git rev-parse HEAD`; if it does not equal `QUALIFYING_PIN`, say so and stop."* It names no artifact content, so it cannot frame — exactly the check E1 needed and lacked. For the close and ED-410: a resolution check and an identity check are different kinds and the lint must carry both; standing rule: the non-framing verification goes in EVERY brief, including the lane that grades content. The lane caught this itself and compensated correctly — the best available outcome and also luck.

**§4 RESUME, as the same lane — on the merits.** (1) A re-fired lane faces the same moving-tree problem and may not compensate as well; E1's `git show c88aac1d:<path>` approach is pinned and immune to further HEAD movement — HEAD has moved again since. (2) It died mid-synthesis; a resume finishes the synthesis, a re-fire redoes 25 minutes of search. (3) Precedent inside this round: ε treated E3's resume as the same lane and β did not object; treating E1 differently would be the anomaly. Conditions: the prompt is not altered — if the mechanism re-sends it, byte-identical to the ledger digest (`sha256:bf8dfae73dd38a75…` = `assembled-E1.md`), checkable rather than promised · the bound sized from the rollout, not nudged — 1500 s plus a minute is a second death with extra steps · the close reports E1 as ONE lane across two dispatches, both ids and both elapseds · if the resume also dies at its bound, that IS the finding: E1's task is not completable within any bound we will set, S6-1/2/3 close as NO_DATA, no release on the terminal as written. Transport, not brief; spends no attempt.

**§5 Standing.** Holding the three-file handover until E3's raw file exists is exactly right — two of three tokens would defeat the reason for the order. E2's token β has (`warn`, L12); E3's not; E1 has none.

**not_read:** `out-E1.rollout.jsonl` and the derived timeline — deliberately: a lane's working notes, which would put E1's unbounded reasoning into β's head before E2's and E3's returns · E2's prose · E3, pending.

## α application

Resume ordered to ε under the four conditions (execute order `d44ca6d7` after row 430). ED-410 amendment 4 carries §3 (resolution ≠ identity; both kinds in the lint; rev-parse-against-pin as the non-framing form in every brief — successor template, not E1's brief mid-round). §2 governs the close: E1's rollout notes are not findings. §1's clause into the notes.
