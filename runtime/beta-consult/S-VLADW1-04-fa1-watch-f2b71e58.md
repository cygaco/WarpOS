# β supplement — S-VLADW1-04 fix-attempt-1 watch pass: four flags (row 312, supplements rows 309/310)

msg_id `f2b71e58-4c93-4d07-a86e-51e0c3d9b874` · row 312 · 2026-08-29 · DECIDE · Class B · 0.89 · OPEN_ADR false
Precedent: rows 309/310 · class-closure rule (name the transform's closure PROPERTY, not a fold list) · "name the invariant, not the live state" · re-established-not-cited. Consult: α `[S04 β-FYI-3]` msg `405686e2-8764-4b39-84b0-4f6db1b8d795`. Rows 309 and 310 carry forward pointers to this row.

**1. J's `createRequire` ban — WIRING (agrees with α).** A ban on bare `node:child_process` outside `spawn-shim.js` already exists with implementation, test shape and comment-stripping; J adds `createRequire` as another member of that family. Widening a family is the sanctioned direction; adding one is what the non-goal catches. S-03 item 4 already put `createRequire` into `canSpawn`'s classification; J promotes it one notch. **Residual for the ban's header:** "zero executable uses on tree" is live state, not an invariant — the ban forbids a legitimate future use (ESM interop); the escape hatch is a **code-level structural exemption, never a settable suppression marker**.

**2. S4-2(d) is NOT dischargeable at gauntlet-1 (touches the rule).** The adjudication marks it DISCHARGED because the backend lane re-ran the battery against the predicate at `b9b8df3` — but bundle G changes that predicate (NFKC, default-ignorable strip, confusable fold, emphasis canonicalization). A re-run against the pre-G predicate cannot discharge a criterion evaluated at the qualifying close against the post-G predicate (row 310 FIX 3, one level over). **S4-2(d) must be RE-ESTABLISHED at the qualifying run** — by a lane, not ε, against the predicate as built then, population including bundle A's paragraph and the new ZWSP/homoglyph/emphasis variants. Strike "DISCHARGED" → "satisfied at gauntlet-1 against the pre-G predicate; owed again at the qualifying close."

**3. S4-2(c) was under-specified — β's own error.** Row 309 wrote the transform as a LIST ("case-fold + whitespace-collapse + dash-class fold"); the attack came through ZWSP and Cyrillic О, neither on the list — naming folds reads as category-enumeration and the bypass recurs one alphabet over. **Amendment (widens):** S4-2(c) is satisfied by **matching on the RENDERED FORM** — canonicalize to what a reader actually sees (NFKC, strip default-ignorables, fold confusables over the token alphabet, canonicalize markdown emphasis) and compare that. **Bundle G's transform satisfies S4-2(c) as amended and does not exceed the rule** — ratified now so nobody argues later that G overbuilt.

**4. Correctly applied, not re-litigated:** the terminal does not fire at a diagnostic run; RT-8's rollup GREENs are re-confirmations of a disclosed class; lane disagreement on S4-6 resolved by criteria not vote; S4-4 honestly unassessed with the described-vs-claimed mutation count owed at the qualifying run.

**Endorsement:** the process rule — each shipped claim drafted AFTER the attack that would falsify it, `falsification_attempts` in the envelope — attacks the drafting-order defect directly. Keep it.

**Not read, stated bare:** `CUSTODY.md:33-40` — so "RT-8 correctly stated as a CLASS" is unverified; the adjudication's quoted phrase ("a spelled-out numeral, or the word `every`, passes it") names the two INSTANCES; if the disclosure names only those, **S4-6 fails on it at the qualifying close** — a lane resolves by read. Also not read: the four gauntlet-1 lane evidence files, `custody-claim-lint.js`, the vlad tree at `b9b8df3`.

α disposition: relayed to ε as `[S04 FA1 r3]` — J header residual; "DISCHARGED" struck and S4-2(d) re-run put in the gauntlet-2 backend brief; row 312 cited in G's header sentence + spec; H makes RT-8's class-form wording explicit and the gauntlet-2 qa brief verifies by read. H/I/J cleared to fire on G's landing.
