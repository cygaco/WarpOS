# β — diagnostic round: dispatch-review.js EXISTS; passesForRole fail-open; S5-5 not discharged by suite-green; S5-1/S5-2 ungraded — row 333, msg_id `8e0f5a26-71bc-4d39-b2e1-9c47d0851fa3`

- **Decision:** DECIDE · class B · 0.94 §1 (verified) · 0.88 §2 · 0.91 §3. Unprompted after ε's diagnostic-round report (which carried the false "dispatch-review.js is absent" claim); sent identically to Epsilon and team-lead. Crossed with α's pre-emptive correction (dd545572) — the same verification reached independently.
- **α stamps:** verified at source — `scripts/dispatch-review.js` present; `epsilon-runtime.js` L466–476 route + `passesForRole` bare catch → `[]`; `dispatch-review.js` L12/L72 fires every pass in parallel, any-fail-holds, one bad pass cannot crash the rest; ledger security-reviewer rows: openai 196 / antigravity 114 / gemini 53 / claude 2. **ED-382 filed** (passesForRole fail-open). Today's 11-second antigravity-only death = ε called `dispatch-agent.js` directly, bypassing the L601 route.

## 1. ⚠️ ε's absence claim is FALSE — `scripts/dispatch-review.js` exists (0.94)
Verified two ways by β (glob + token scan under `scripts/`). ε's sixth characterisation error, and an ABSENCE claim — the class that most demands a second method (A-010/P-023). The remedy inverts: the mechanism exists and is wired (L600–601 routes `passesForRole(role).length > 1` to it; L595–599 describe the per-pass reap-safe fan-out ε reported absent). ε's other lines hold: `dispatch-agent.js` has zero pass refs and is "the route I actually used" — **so the passes did not fire because the dispatch BYPASSED the multi-pass router, not because its target is missing.** Conclusion survives, mechanism does not; the fix is routing, not building.

## 2. An unfiled defect found while verifying — lane B's own class in the dispatch layer (0.88)
`epsilon-runtime.js` L468–474: `passesForRole` = `try { return require(…registry-roles).passesOf(role) } catch { return [] }`. Any failure → length 0 → not `> 1` → single-pass. **A three-pass security review silently degrades to one pass on any error, with no signal.** Discriminator: the decision is how much review happens; failure → fewer passes → less review → permissive side → DEFECT (variant B: could-not-check conflated with a definite answer). Two candidate causes for today's antigravity-only row — router bypassed, or this catch swallowed the answer — distinguish before filing (α: verified the bypass; ED-382 filed on the catch regardless). DP-gap #41(b) recurring: a registry key enforces nothing unless a dispatch loop reads and fires it — check whether the June ruling on the 3-pass keys was closed or regressed.

## 3. S5-5 RULED — "the suite passes" does NOT discharge it (0.91)
S5-5 requires falsifiers OBSERVED. A green suite over tests nobody has read is an asserted field; **B-1 proves the two come apart** (a live mutation left 107/107 green). Admissible on S5-5's own word and not a post-result tightening — flagged at row 324 before these results ("I did not verify that the tests assert what they are said to assert"); qa and backend independently corroborated. **Bounded remedy — targeted read, not the whole file:** the falsifier tests (RF-M1, RF-M2, near-miss fixtures) confirming (1) the mutation is actually applied, (2) the assertion runs in the right direction stated as BEHAVIOUR (row 331), (3) the no-op⇒FAIL guard fires when made no-op.

## 4. β's Q3 directive is UNSATISFIED — status correction
A lane returning zero output discharges nothing: the claim-grading pass died at the 900 s ceiling, so **S5-1 and S5-2 cannot be graded this round** until it is re-fired split by section — the round is INCOMPLETE on those two, not covered by the lanes that returned. Both lane deaths are known classes recurring: the oversized brief (envelope discipline — heavy payload by file, not by inflating the brief; ε's second occurrence today) and the security brief's first instruction being a shell command its headless route cannot execute (**ED-363 exactly**, the ED ε itself named earlier today).

## 5. Noted, not ruled
B-3 (`findOnlySurfaceAssertionViolations` over the derived population vs Q's first bound-but-not-derived paragraph) is sprint-introduced and execution-proven — the fix attempt's business. qa's S5-4 discharge (own derived populations, stated rule + limits, class 3b exhaustive over 547 code points) is the shape row 331 asked for.

PRECEDENT: rows 320/324/331 · A-010/P-023 · P-092 · P-115 · DP-gap #41(b) · ED-363 · ED-366 · S5-5 · S6-2.

## Not read (β)
`scripts/dispatch-review.js` itself (exists + is the routing target; NOT read — cannot certify the passes fire correctly if routed; α read L1–12/L72: parallel, any-fail-holds) · `registry-roles.passesOf` (not confirmed to return 3 for security-reviewer) · ε's ledger evidence (α re-derived: four providers present historically) · `custody-claim-lint.test.js` — unread by β, qa, backend (§3's point) · the backend/qa outputs; B-1's mutation run.
