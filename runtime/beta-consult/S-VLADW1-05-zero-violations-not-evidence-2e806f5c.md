# β — third convergence on the rollup; the token guard must be folded in; "0 violations" is not evidence F1's fix works; the rule's-author pattern — row 349, msg_id `2e806f5c-b41d-4a97-8c53-90f1e6a7d3b8`

- **Decision:** DECIDE · class B · 0.91. Answers ε's retraction (row 347, crossed with β's 1d5f8a04 = row 348). Sent identically to team-lead and Epsilon. **Two live flags: §2 and §4** — α relayed both to ε (msg cc273554).

## 1. Third convergence on the rollup
Same lines, same conclusion; two readers read the file. β drops the inference ε asked it to drop ("a rollup that can turn two FAILs into error can turn them into anything") — **corrected form: the rollup is evidence of PARSEABILITY, not of lane findings** — it accurately reports what it could parse; what it could parse is not what the lanes said. Q4(b) stands on that ground. (ε's `surviving_verdict:null` L453 detail — defined only when exactly one family is live — accepted.)

## 2. ⚠️ The token guard crossed with ε's amendment — fold in before the final clear
A machine-readable enum invites a default: a lane that assessed nothing may emit `"verdict":"pass"` because the schema demanded a value — a conservative loss traded for a FALSE GREEN on the route BE-CQ-001 hardened against exactly that. Each brief must state: `"pass"` = an affirmative finding of correctness, never "found nothing" / "could not assess" · could-not-assess emits `"error"` + `what_i_could_not_assess` · NO_DATA is not a pass · the token is in addition to the prose. **First thing β checks when the briefs return at the post-task-5 pin.**

## 3. ε's edits accepted — and ε improved on β's blocking item
Q3 takes a dedicated checkout, refuses a shared-tree fallback, and α runs the independent post-run check. ε drew the conclusion β did not: **restore INCREMENTALLY, not batched at the end** — batched restoration is exactly what leaves a dirty tree when a lane dies at a ceiling (one did today).

## 4. ⚠️ "0 violations" is NOT evidence the F1 fix works
Routing the forbidden-claim family through the transform WIDENS what matches; a widened check returning 0 violations on the same document deserves a second look, not reassurance — equally consistent with a clean document, the routing not taking effect, or the extra matches being suppressed. **The evidence that F1 is fixed is the seeded bypass being caught** — the four U+200B injected into a forbidden claim, refused now and shown to have passed before (owed under S5-5). "Green before resuming" is a legitimate COHERENCE check that must not drift into VALIDATION — different claims.

## 5. A retro line — and it includes β
ε's self-diagnosis ("it is not that I forget the rule; it is that I do not apply it to my own prose while writing it") generalised: **a rule's author is the least likely to apply that rule to the artifact stating the rule.** Three instances today: ε's closed disjunction inside the brief that grades closure claims; ε's excerpt-frame warning placed forty lines below a frame violation; β's ledger row self-labelled from expectation in a session demanding every value be derived from its store. The excerpt-frame finding moved from reading to authorship — the frame you cannot see is the one you are writing inside; same remedy: a different reader for the rule's own text.

PRECEDENT: 1d5f8a04 (row 348) · 9dc317ba §4 (β's token guard, crossed) · 7c40e9b1 (row 346) · P-092 · P-106 · S5-5 · ED-385.

## Not read (β)
The amended briefs at 834b095f (ε's description; the real pre-fire check runs at the post-task-5 pin) · `dispatch-review.js` L453 and L165–185 (the `Promise.all` fan-out — any-fail-holds still unverified by anyone) · the builder's output; the 403/0 suite; the lint run behind "0 violations"; `FIX-BRIEF-round-1.md`.
