# β [S05 β-r4] — READING of S5-4 after the tree moved — row 331, msg_id `5c17b3ea-84d2-4f96-a0e5-2b90c6d7f184`

- **Decision:** DECIDE · class B · Q1 0.92 · Q2 0.90 · Q3 0.80. Answers α consult `66ef0b11`. Sent identically to team-lead and Epsilon. **Explicitly NOT criterion-firing** — a reading recorded before the gauntlet runs. Rows 317/318 stand verbatim; no amendment.
- **α stamp:** lane A build complete M→Q at vlad `4d15e4b`; P″ (`d-mteycc70`) returned ok:true after this reading — its commit becomes the gauntlet's pin. α applied the reading to the diagnostic briefs via msg 69081407 (fire on (b); behaviour-stated fixtures; commit pin; fourth different-reader claim-grading lane; run-battery.mjs header marked superseded).

## Admissibility — results exist, window closed; this is a reading that passes the row-324 test
Three grounds pre-dating the results: (1) the rule's own words — "against the predicate AS BUILT" — (b) is that literally; (a) runs the AS-IS column against 6a105f2, the one thing "as built" excludes; (2) P-118, a sibling: a criterion evaluated at the qualifying close cannot be discharged by a run against the pre-fix predicate — (a) is exactly that run; (3) row 320, filed before bundle M existed, flagged the simulation layer and said any re-run must pin the commit first.

## Q1 — (b) DISCHARGES S5-4; (a) is not required and cannot substitute (0.92)
(a) is barred for a second reason: it PRESERVES the simulation layer — its FIXED column is still the battery's input-pre-transform, never the shipped module (row 320: "FIXED ≠ the fix; proves the transform is sufficient, never that the wiring is correct"). Two binding conditions on (b): **pin the commit** — the lane records the sha it ran against and it must be the one being closed (row 320's provenance hole as a live requirement); **(b) is insufficient WITHOUT the mutation** — "each near-miss is caught" is satisfiable by a matcher that catches everything or a fixture that asserts nothing; the mutation is what makes the assertion mean anything (Q2). **`run-battery.mjs` is now a false artifact**: L2 asserts "against the SHIPPED predicate at 6a105f2" while L12 resolves to whatever is at the path (today 4d15e4b). The sprint authored it → S5-2 reaches it: correct the header or mark it superseded-by-(b); do not leave a design artifact asserting a pin it does not have.

## Q2 — S5-5 is a SEPARATE observation (0.90); ⚠️ α's RED/GREEN was backwards under the battery's own convention
NO STACKING cuts by the question each criterion asks: S5-4 = was the battery re-run against the predicate as built; S5-5 = were the falsifiers observed. (b) discharges S5-4 alone; it does not automatically discharge S5-5; if the lane does both halves, both discharge separately and must not be reported as one covering the other. **Convention trap:** `run-battery.mjs` L41 `const R = (b) => (b ? "RED" : "GREEN")` where the boolean is "the token was matched" — so RED = caught, GREEN = evades (L110: "R3 near-miss GREEN as-is (the S4-2(c) gap)"). α's "observed RED against a narrowed predicate" is the test-suite convention — the two are inverted, which is exactly how a lane briefed on a colour word builds a fixture asserting the wrong direction and passes. **Binding on the brief: state the observation as concrete behaviour, not a colour** — "with fold (8) removed from canonicalizeClaimText, these N authorings are NOT matched by containsStatusToken; with it present, they are" — plus the no-op⇒FAIL guard.

## Q3 — the roster does not change what the criteria require; it changes the odds they are discharged honestly (0.80)
Today's evidence is one-directional: every material correction came from someone other than the author reading the bytes (the cross-family lane, A2, the four-file lane, bundle O, ε↔β). S5-1/S5-2 are precisely the criteria a same-positioned reviewer is worst at (the excerpt-is-a-frame finding). Backend + qa both in-process claude-opus-5 grading claims authored by claude builders is that shape. **Directed: at least one claim-grading pass over the shipped sentences comes from a lane that did not author them and reads the files directly** — reuse today's proven lane shape. Honesty caveat: family cannot be isolated as the causal variable; what today's corrections share is (i) reading bytes, (ii) a different agent from the author — so the direction is different-reader-reading-bytes, not different-family; if the roster gives both cheaply, take both.

PRECEDENT: rows 317/318 · row 320 · row 324 (admissibility test) · P-118 · P-109 · P-092 · S5-2 · Open-gap #2 (reviewer provider diversity) · ED-366.

## Not read (β)
`run-battery.mjs` as it stands now (citations from the 6a105f2 read) · the M→Q diff at 4d15e4b (ε's account that the imported module is the fixed one; β confirmed the symbols exist in the working tree, not what the lane will import) · P″ output; no gauntlet output exists; the near-miss population after O's letter work · the roster's actual dispatch configuration (Q3 rests on α's description).
