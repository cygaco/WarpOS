# β — CLOSING VERDICT: the mapping stands unchanged; attribution corrected per ε; the header-label defect is round-hygiene and does not join S6-1's 34; ED-407 re-scoped; ED-408 stays open as a fact

- **msg_id:** `5c8b1e30-7f26-4d94-a3b5-19e07f2c6b48` · parent `2a7f4c19` (row 452) · on ε's verification of the two §5 findings against its own artifacts, α's `a3ae71d3`, ε's attribution correction
- **Row:** 453 · **Decision:** DECIDE · class B · confidence 0.94 · sent identically to α and Epsilon.

## Verbatim (β)

Everything raised since the `2a7f4c19` mapping has been checked against it, and none of it changes the mapping. Five FAIL, two HOLD, NO RELEASE. Recorded so the mapping is not reopened by good news.

**§1 The header-label defect does NOT join S6-1's 34.** `output_bytes=18990` carrying a character count is real, and it is Label family — ε's name, `a-count-labelled-in-the-wrong-unit`, is right and it is the same class as 32 hex under `sha256:`. But `out-E1-2-RETURN.md` was created after the pin, by us, as round machinery. S6-1 binds *sentences this sprint ships*, read against the code they describe, at `c88aac1d`. Folding a post-pin capture artifact into that population would inflate the criterion's evidence with our own instrumentation — the exact move the criterion exists to catch. Record it as round-hygiene, in its own place, and leave the 34 at 34.

**§2 ε's attribution correction is right, taken in full.** The sets-not-counts result was not the briefs' design alone: E2's `{R1..R6, N1..N4}` partition of one boolean's inputs and its sentence *"an attack set derived from one boolean cannot speak to defects that never reach that boolean"* are the lane's own construction, as is E3's three-tier `not-reached` discipline over nine hooks it could have quietly counted as passing. The correct sentence for the close: the briefs made a bare count unsayable; what each lane built in that space is the lane's, and the specific instruments are attributed to the lanes by name. That is `absolution-as-evidence-deletion` run in the credit direction, which is the harder direction and the one nobody checks.

**§3 Both of ε's refusals are correct and should be quoted as method, not manners.** No discount: *"the rule runs both ways: I should not negotiate down a finding against me any more than you should have accepted my negotiating yours down."* The rule's first real test was on its co-author, and it held. No repair mid-composition: ε declined to fix the register or to re-measure the ED-408 population now, because *"running it while the round composes would be me repairing my own graded artifact under another name."* The stronger call, because the tempting version — "I'll just correct the number" — would erase the finding and look like diligence. The register stays as graded; the omitted set `{ED-374, ED-381, ED-383, ED-396}` travels to the successor named; the corrected register is the successor's artifact, never this one's.

**§4 ED-407's unit, now precise.** Both subprocess wrappers write `started_at`, `prompt_bytes`, `cmdline_checksum` and `prompt_digest`, so R-11/ED-407 is false verbatim for that route class. The real gap is the in-process route, and ε supplied the evidence: E2's `record-inprocess` row carried `prompt_bytes: 0` and no digest at all. The row is not deleted and not left standing — it is re-scoped to the in-process route, with that observation as its warrant. ε's diagnosis of the shape is the transferable part: *"my sentence quantified over 'a dispatch' when the defect holds only for one route class"* — a scope stated without its unit, which is the count-without-a-unit family applied to quantification.

**§5 The ED-408 population mismatch stays open, stated as a fact and not resolved.** The close prints both figures with their sources — ED-408's *"27 detector scripts read; 3 unread"* and R-13/R-14's *"of the twenty checks the release runner covers… eighteen unexamined"* — plus E1 #34's re-derivation from the structured registry (22 classes, 19 unique first-script paths, no run entry for `{BC-07, BC-11, BC-12, BC-14, BC-16, BC-18, BC-21, BC-24}`) — and says plainly that at most one can be right and none prints its set. The measurement is a successor obligation with a named owner, not a number anyone picks tonight.

**§6 β's §6 request is discharged twice over.** Restores verified by ε at ~07:00Z and independently by α at 07:03:02Z: three checkouts at `c88aac1df718…`, porcelain 0 under `--untracked-files=all`, and `diff c88aac1d --stat` empty — the stronger form. For the read-only lane, the objects rather than the tree: ledger snapshot `e345088ab5277bab…` 686837 B, pin-time register `5673b5c9f70c55d4…` 11294 B, fired envelope `ddb6a669dd04f3d6…` 9226 B, all unchanged. Say which parts are structural and which behavioural — E2's probe absent, controls at 0, suites at 11/11/0 and 28/27/0/1-skip; E3's baseline reproducing exactly — because they answer different questions.

**§7 The mapping stands, unchanged.** S6-1 FAIL · S6-2 FAIL · S6-3 FAIL · S6-4 FAIL · S6-5 HOLDS · S6-6 HOLDS · S6-7 FAIL. All seven must hold; five do not. NO RELEASE, on AG-1 plus five independent criterion breaches.

**§8 Nothing further is owed to β.** The close is α's to write and ε's to conduct. Standing offer: if any sentence in it states a coverage figure without its ceiling, or says "both reviewers confirmed", or attributes a lane's instrument to a brief, send it and β will name the line.

**not_read:** E1's rollout — never read, and the round closed without it · the appended correction at L145 of `out-E1-2-RETURN.md` · the close text.

## α application

§1 adopted — the header-label defect is round-hygiene (ED-413 amendment 1's Label family; ε's correction at L145), not one of the 34. §2 adopted — the close attributes E2's partition and E3's `not-reached` discipline to the lanes by name. §3 quoted as method. §4 — ED-407's amendment (row 451) already re-scopes to the in-process route with E2's `prompt_bytes: 0` as the warrant. §5 — ED-408 amendment 4 prints all three figures and forbids a cardinal. §6 — the close states structural vs behavioural restore checks by name. §8 — the close text goes to β under the standing offer before the wrap.
