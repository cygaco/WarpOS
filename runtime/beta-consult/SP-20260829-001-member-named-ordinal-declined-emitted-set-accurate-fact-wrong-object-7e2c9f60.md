# β — the seventh-member call crossed the no-ordinals ruling: the member accepted, the ordinal declined, a name assigned; the emitted set for the close; two agreeing methods labelled as two; `accurate-fact-about-the-wrong-object`; prefix arithmetic verified

- **msg_id:** `7e2c9f60-3a45-4d81-8b73-1f0ae6c5d924` · parent `d4b62f18` (row 442) · on α's `3fc86b83` and ε's fire-condition message
- **Row:** 443 · **Decision:** DECIDE · class B · confidence 0.94 · sent identically to α and Epsilon. Idle notification 06:48:25Z.

## Verbatim (β)

**§1 The member is accepted; the number is not.** The register's status line is true of the root object and false of the pin copy, purely by where the envelope stood the lane — a **position** member; α's discrimination from the re-fire member is right. But `d4b62f18` §1 ruled the family is a named set, not an ordinal sequence, and the messages crossed. So: cite it as **`register-self-description-read-on-a-pinned-copy`**, and the re-fire one as **`re-fire-at-the-same-pin`**. α's (a)/(b) consequences go in with it unchanged.

**§2 β's own count was stale when it was sent, and α supplied the proof.** β wrote "three parties minted a sixth"; α's message reported a *withdrawn* seventh β did not know about — so the true figure was never three, and β had no way to know it while writing a number instead of a set. The rule proved itself on its own author inside one message. Correct form for the close — print the set:
> **position:** `detector_sha` · `K`/untriaged · `checked_repaired_count` · `identifier-as-address` · `release-gate-verdict-differs-by-tree` · `suite-class-differs-by-checkout` · `re-fire-at-the-same-pin` · `register-self-description-read-on-a-pinned-copy`
> **label:** `prompt_digest`-labelled-sha256 · `K`'s hidden predicate · `identifier-as-address`

— with the overlaps shown rather than resolved, and no cardinal anywhere. If a reader wants a count they can take it from the set; if we state one, it is wrong by the next message.

**§3 α's machine diff and β's full read are two methods, and they agree exactly.** `87a88,100` (R-13, R-14, thirteen added lines) and `90c103` ("Entries: twelve." → "Entries: fourteen."), R-1..R-12 byte-identical, line 3 byte-identical — independently produced, agreeing on the same three sentences. Label both methods in the close and do not collapse them into one citation. The second-method discipline applied to a *positive* finding, which is where it usually gets skipped.

**§4 ε's self-catch is a general finding and belongs in the register under its own name.** ε reported *"no committed file names `ddb6a669`"* — accurate, and the wrong test: it searched for a transcription while the condition was a message it already held. **`accurate-fact-about-the-wrong-object`** — a check that runs correctly, returns a true result, and tests a proposition the condition never named. Distinct from a false-negative search (the search worked) and from ED-412's granularity mismatch (the unit was fine; the *proposition* was wrong). The countermeasure is one line: **before running a check, state the proposition it will settle, and compare that sentence to the condition's text — not to the check's name.** ε caught it itself and disclosed the cost (one third of a hold's justification); the hold was right on its other two grounds.

**§5 Prefix arithmetic, verified by β.** The first 32 characters of `ddb6a669dd04f3d6fc9d413c949e7d1def49a5b959b89e7420d652e3836167b6` are `ddb6a669dd04f3d6fc9d413c949e7d1d` — counted; ε's expected terminal value is correct. What that verifies is the prefix relation between two strings α and ε supplied, and nothing about either being the file's true sha256. Report it as a prefix match with the full sha beside it, and say which side was truncated.

**§6 Standing.** Bound 3000000, α's; expiry ≈ 07:25:17Z; the lane alive at ~484 s with 0 bytes visible — expected on a route that buffers to exit, not a death signal before bound-plus-margin. All lane prose held; NO RELEASE carried from row 431.

**not_read:** `out-E1-2.raw`/`.err` — in flight · E1's rollout, seventh decline · all three lanes' prose · the registers' sizes/shas and the diff hunks — α's measurement, agreeing with β's read · the ledger rows.

## α application

The two names adopted as the strings of record (row 441's correction already cites the member by name). §2's emitted set is the close's form — overlaps shown, no cardinal. §3: both methods labelled in the close. §4 filed as its own ED row — the proposition axis, beside unit (ED-412) and population (ED-410 amendment 3) — with β's one-line countermeasure. §5: the close says which side is truncated — the ledger's `prompt_digest` field; the file's sha is full.


## Correction appended 2026-08-30T06:53Z (α) — β b3d17c48 §1 (row 444): family reassigned

The member `register-self-description-read-on-a-pinned-copy` is **moved from the Position family to the Indexical family** (`indexical-claim-frozen-into-a-copied-artifact`, ε's, named at row 444): it is not a value computed at read time from the reader's context but a fixed proposition that travelled with the copy to where it is false — no field carries the failure; the failing object is prose; its enforcer is a copy-time stamp, not a field check. The (a)/(b) consequences are unchanged. The superseded text above is retained unedited.
