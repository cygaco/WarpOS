# SP-20260618-002 — Retro Synthesis (ops-analyst, advisory)

E-MC-READINESS-ANALYSIS-001 (analysis-only). Conducted by Alex ε; gauntlet GREEN; epic Completed 100%.
ops-analyst (gpt-5.5) cross-cycle analysis. ε defers self-management to α+β → ADVISORY. **NB: the
Class-A "fix CLAUDE.md scan:tools line" CANNOT be applied in THIS analysis-only sprint (a CLAUDE.md edit
would violate the analysis-only invariant + fail the diff-scope gate) — it is already register R4/N-1,
routed to EXECUTION. The analysis-only discipline correctly defers it.**

## Recurring classes + proposed changes
1. **Tracker truth drift is BIDIRECTIONAL** (not just ED-056's "Missing But Required but exists"):
   under-credited-done (this epic ~15%→100%), claimed-green-but-RED, live sprint-metadata ≠ ROADMAP/reports.
   → **Class B (ADR):** `tracker-reality-drift v2` — compare declared state ↔ artifact existence ↔ executable
   check result ↔ sprint/report state. (3rd recurrence this session: E-CONTENT-DELIVERY, E-GOLDEN-FLOW, this.)
2. **HIGHEST-LEVERAGE — certified gate coverage contracts:** every GREEN gate must DECLARE the verbs/surfaces
   it covers + explicitly list EXCLUSIONS; close-gates fail when a trusted gate has no coverage declaration.
   This would have exposed A1-F4 (the sealed gate's `--status`-only blind spot) immediately.
   → **Class B (ADR; amend ADR-0006 or successor).**
3. **Analysis-only diff-scope gate is worth generalizing for every analysis/audit sprint** (`git diff
   --name-only main...HEAD ⊆ allowlist`, fail-closed; same shape as framework-purity).
   → **Class B (ADR); implement register R26 in EXECUTION.**
4. **Prose asserts an unbuilt enforcer (R4/N-1, ED-033)** — worse than an honest gap (suppresses verification).
   → **Class A** (correct CLAUDE.md to "NONE YET — ED-033") **— but EXECUTION-routed here (analysis-only).**
5. **Enforcer-claim resolver:** any `Enforcer:` claim must resolve to a hook/scan-token/script/CI-gate/ED-###;
   a missing resolver fails `/scan:full`. → **Class B (ADR).**
6. **2-source dispatch pattern (gemini tier-dead):** Claude primary for read-heavy sealed FS probes; GPT
   second-pass over the compact output for divergence/re-rank/scoped-out verbs. Gemini-absence = honest debt,
   not a reason to drop divergence. → **Class A (record as operating pattern).**

## Disposition
- Auto-apply (Class A) — but ALL the product/framework-touching ones are EXECUTION-routed (analysis-only):
  the scan:tools prose fix (R4), recording the 2-source pattern (done — see ε memory). State-file reconcile
  done in A3.
- ADR (Class B): tracker-reality-drift v2; analysis-only diff-scope gate (R26); certified-gate coverage
  contracts (the highest-leverage); enforcer-claim resolver. These are the EXECUTION/ADR seeds.
- Escalate (Class C): none from this retro (actual launch/no-launch is Class C if asked; this retro advises
  readiness mechanics only).

## ε note
Recommend α route the Class-B "certified gate coverage contracts" (highest-leverage) + "tracker-reality-drift
v2" + the "enforcer-claim resolver" to ADRs / the EXECUTION epic. The diff-scope gate (R26) is already a
register row. The stale-tracker class is a recurring-issue (3× this session) worth a standing fix, not another
one-off reconcile.
