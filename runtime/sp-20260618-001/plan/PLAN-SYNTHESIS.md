# SP-20260618-001 — Plan-Phase Synthesis (CLOSED)

**Boundary cleared:** plan→design — β DECIDE B 0.89 (EVT-sp20260618-001-plan-design-1), no autonomy gate.
**Plan consults (real GPT-5.5, gauntlet-verify PASS):** director-of-product (ok), product-lead (ok).

## Composition (locked)
- 3 backend units, backend-first, U1 lands before U2.
- Risk: HIGH (shipping-layer, value-inversion trap).
- No UI / frontend / security-hardening / copy units.

## DoP verdict (strategy)
Proceed. U1→U2 backend-first correct. Do NOT defer U2 (provenance-seeding is part of closing
the "missing parts" class). U3 = confirm-only, treat as the sprint exit gate not a build unit.
Named risk = **value inversion** (manifest-owned home that accidentally excludes from shipping)
— mitigation = ship-coverage GREEN before old-copy deletion is the binding control. HIGH risk
acceptable; opportunity cost justified (closes the parent epic's last structural gap).

## Product-Lead verdict (execution) + AC SPINE (authored)
Scope/sequence OK, U1→U2→U3, no reorder/merge. Clarification: **_warpos/BASELINE/ creation
belongs in U1's acceptance surface** unless Engineering proves an independent generator boundary.

### U1 AC spine — template ownership cutover
1. `_warpos/templates/` exists with all 9 dirs / 108 files; `framework/templates/` removed.
2. `_warpos/BASELINE/` exists and reflects the new shipping home.
3. Not-shipped allowlist narrowed ONLY for `_warpos/templates`; `_warpos/MANIFEST.json` +
   `settings/defaults.json` remain not-shipped.
4. Manifest generator + the 2 `build.js` `seeded_from` points repointed AFTER files land.
5. Hardcoded-path tests updated; no required `framework/templates` literals remain.
6. Both manifests regenerated.
7. Ship-coverage GREEN TWICE: post-repoint/pre-delete AND post-delete.
8. Positive assertion that `_warpos/templates` paths are in the shipped set.

### U2 AC spine — seed-zone provenance
1. `_requirements/` + `_docs/` seed provenance exists, generated by extending
   `populate-source.js` or a clearly-named sibling.
2. Provenance points to the new `_warpos/templates` / baseline source, not stale framework paths.
3. Fresh install seeds `_requirements/` + `_docs/` with provenance intact.
4. Re-run does not duplicate/corrupt/overwrite user-owned downstream content.

### U3 AC spine — install update-parity confirm
1. Existing update-parity / `REQUIRED_DIRS` structure-parity remains green.
2. `test-install-matrix` scenario 2 confirms post-update structure parity.
3. Fresh install AND update install both contain the same required shipped
   template/baseline/seed-zone structure.
4. No rebuild of DoD#2 ship-coverage scope beyond required confirm runs.

### Execution risk (PL)
FTUE/cold-start is the main risk. HOW: require an empty-target install fixture + an update
fixture after U2, asserting `_requirements/`, `_docs/`, `_warpos/templates/`, `_warpos/BASELINE/`
present with provenance and no stale `framework/templates` dependency.

## β HOW-corrections (binding build constraints — carried into design + build)
1. **TWO GREENs** — post-repoint-pre-delete AND post-delete + positively assert new
   `_warpos/templates` paths in the shipped set (not merely "no info_gap"). While both copies
   exist, the OLD copy can mask a mis-wired new path; re-run AFTER delete on the end-state.
2. **Carve-out PRECISION** — SCOPE the `_warpos/` not-shipped rule narrower (explicit
   `_warpos/templates/` exception), do NOT blanket-flip — else MANIFEST.json/defaults.json
   start shipping (inverse boundary regression). P-059 partition-don't-blanket.
3. **Sequence** — template files LAND at `_warpos/templates` first (U1), THEN flip `build.js`
   seeded_from, THEN regen. Flip-ahead-of-file = new dangling (zero-tolerance block).
4. **Regen BOTH manifests** (manifest/build.js + generate-framework-manifest.js) + paths.registry
   is SOURCE → run paths/build.js → verify key survived in `.claude/paths.json` AND
   `lib/paths.generated.js`. P-058.
5. **Grep ALL OLD-literal `framework/templates`** across .js/.json/.md — the 9 tests / 8 wirings
   are counts to VERIFY by forward-search (P-057 surface-form enumeration: literal path, prose,
   glob), not numbers to trust. The MISSED file is the entire bug class (anthropic→claude precedent).

## Next: DESIGN phase
- product-lead (always, block) — authors the build_spec / PRD / TRACE from the AC spine above (CLI, I dispatch).
- director-of-engineering (claude, in-process → RELAY to α) — owns the atomic-cutover contract,
  FE/BE split (here pure BE), integration-seam, the _warpos/BASELINE generator-boundary question PL raised.
- quality-lead (claude, in-process → RELAY to α; risk≥medium fires) — qa-plan, AC verified_by coverage.
- Then β at design→build (LOAD-BEARING — get DECIDE BEFORE any file moves).
