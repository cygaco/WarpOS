{"ok":true,"provider":"openai","model":"gpt-5.5","actualModel":"gpt-5.5","output":"build_spec: SP-20260618-001 DESIGN / backend-first / HIGH risk\n\nU1 — cutover: move templates home, create baseline, repoint generators, prove shippability.\nINPUTS:\n- Read/edit: `framework/templates/**`, `_warpos/templates/**`, `_warpos/BASELINE/**`\n- Read/edit: `scripts/checks/warpos-ship-coverage.js`, `scripts/generate-framework-manifest.js`, `scripts/warpos/manifest/build.js`\n- Read/edit: `framework/paths.registry.json`; generated by build: `.claude/paths.json`, `scripts/hooks/lib/paths.generated.js`, `scripts/path-lint.rules.generated.json`, `schemas/paths.schema.json`, `_requirements/03-architecture/PATH_KEYS.md`\n- Read/edit generated manifests: `.claude/framework-manifest.json`, `_warpos/MANIFEST.json`\n- Read/edit all live old-literal hits from `rg -n \"framework/templates\" scripts framework .claude -g \"*.js\" -g \"*.json\" -g \"*.md\"`\nEDITS:\n- Move `framework/templates/` to `_warpos/templates/`, preserving exactly 9 top dirs and 108 files; remove `framework/templates/`.\n- Create `_warpos/BASELINE/` in U1 unless DoE defines an independent generator before build starts.\n- In `warpos-ship-coverage.js`, replace blanket `{ prefix:\"_warpos/\" }` with narrow non-shipped entries for `_warpos/MANIFEST.json` and `_warpos/settings/defaults.json`; do not exclude `_warpos/templates/`.\n- In `generate-framework-manifest.js`, change `ASSET_DIRS` template asset from `{src:\"framework/templates\", kind:\"template\"}` to `{src:\"_warpos/templates\", kind:\"template\"}`.\n- Only after files land, in `scripts/warpos/manifest/build.js`, repoint seeded_from for decision-policy and `_requirements/<rest>` to `_warpos/templates/...` real paths; add/adjust docs comment to remove “future flip” stale note.\n- Repoint every live `framework/templates` literal discovered by rg, including hardcoded-path tests and path registry keys, to `_warpos/templates`; do not trust a precomputed count.\n- Run `node scripts/generate-framework-manifest.js`, `node scripts/warpos/manifest/build.js`, `node scripts/paths/build.js`.\nTRACE:\n- Move/create dirs → U1 AC: `_warpos/templates` exists 9/108, `framework/templates` removed, `_warpos/BASELINE` exists.\n- Ship allowlist narrowing + manifest asset repoint → U1 AC: only `_warpos/templates` shipped; `_warpos/MANIFEST.json` + defaults stay not-shipped.\n- Seeded_from flip after files land → U1 AC: no new dangling seeded_from.\n- Registry/source regen + literal sweep → U1 AC: no required old literals; both manifests and paths regenerated.\nverified_by:\n- Before deleting `framework/templates`: `node scripts/generate-framework-manifest.js && node scripts/checks/warpos-ship-coverage.js` exits 0 and `node -e` asserts `.claude/framework-manifest.json.assets.template[].src` contains `_warpos/templates/`.\n- After delete: repeat `node scripts/checks/warpos-ship-coverage.js` exit 0 plus the same positive shipped-set assertion.\n- `node scripts/generate-framework-manifest.js --check`; `node scripts/warpos/manifest/build.js --dry-run --json`; `node scripts/paths/build.js --check`.\n- Count checks: PowerShell `Get-ChildItem _warpos/templates -Directory` count 9; recursive file count 108; `Test-Path framework/templates` false; `Test-Path _warpos/BASELINE` true.\n- `rg -n \"framework/templates\" scripts framework .claude -g \"*.js\" -g \"*.json\" -g \"*.md\"` returns no live required refs.\nSCOPE GUARD:\n- Do not hand-edit `_warpos/MANIFEST.json` content except via generator output. Do not broaden `_warpos/` not-shipped. Do not touch U2 provenance logic beyond unblock stubs.\n\nU2 — seed provenance: make `_requirements/` and `_docs/` seeded content provenance-aware from `_warpos/templates`.\nINPUTS:\n- Read/edit: `scripts/warpos/views/populate-source.js` or new sibling `scripts/warpos/views/populate-seed-provenance.js`\n- Read/edit call sites that invoke populate source during fresh/update install: `scripts/warp-setup.js`, `scripts/warpos/scaffold-core.js`, `scripts/warpos/update.js` as applicable after grep.\n- Read/edit tests near existing view/install tests: `scripts/warpos/views/test-regenerate.js`, add focused sibling test if needed.\nEDITS:\n- Extend `populate-source.js` or add named sibling to seed provenance for project-owned `_requirements/**` and `_docs/**` from `_warpos/templates/**`.\n- Provenance must record source as `_warpos/templates/...`, be idempotent, and skip/keep user-modified local content without overwrite.\n- Wire fresh install and update path to invoke provenance seeding after `_warpos/templates` exists and after U1 manifest regeneration.\nTRACE:\n- Provenance generator/wiring → U2 AC: `_requirements` + `_docs` provenance generated and points to new source.\n- Idempotent skip/keep behavior → U2 AC: rerun does not duplicate, corrupt, or overwrite user content.\nverified_by:\n- Focused Node test exits 0 proving fresh fixture seeds `_requirements` + `_docs` with provenance source `_warpos/templates/...`.\n- Same test reruns seeder twice and asserts byte/content stability plus user-edited file preserved.\n- `node scripts/warpos/test-fresh-install-smoke.js` or nearest fresh-install smoke exits 0 after wiring.\nSCOPE GUARD:\n- Do not rebuild ship-coverage allowlists or manifests except if U1 generated outputs are stale. Do not change template contents except provenance metadata scaffolding required for seeding.\n\nU3 — parity confirm: update install/update confirmation gates only.\nINPUTS:\n- Read/edit: `scripts/checks/warpos-structure-parity.js`\n- Read/edit: `scripts/warpos/test-install-matrix.js`\n- Read generated/read-only for assertions: `.claude/framework-manifest.json`, `_warpos/MANIFEST.json`\nEDITS:\n- Keep `REQUIRED_DIRS` green; add required shipped structure assertions for `_warpos/templates`, `_warpos/BASELINE`, and existing required skeleton dirs where the install/update matrix validates final fixture state.\n- Extend scenario 2 `existing_install_upgrade` to assert post-update parity includes required shipped structure and no stale `framework/templates` dependency.\n- Add fresh install assertion path if not already covered by scenario 1/smoke; use existing matrix helpers, no release/capsule rebuild.\nTRACE:\n- Structure parity assertions → U3 AC: REQUIRED_DIRS stays green; fresh+update contain required shipped structure.\n- Scenario 2 extension → U3 AC: post-update parity confirmed.\nverified_by:\n- `node scripts/checks/warpos-structure-parity.js --json` exits 0.\n- `node scripts/warpos/test-install-matrix.js --scenarios 1,2 --json` exits 0 and JSON shows required `_warpos/templates` + `_warpos/BASELINE` present in fresh and update fixtures.\n- `node scripts/checks/warpos-ship-coverage.js` exits 0 as final regression.\nSCOPE GUARD:\n- Confirm only; do not perform DoD#2 release rebuild/capsule rebuild. Do not alter U1 migration mechanics or U2 provenance writer except to consume their observable outputs.","stderrBytes":4027,"cmd":"codex exec --sandbox workspace-write -c model_reasoning_effort=high -m gpt-5.5 -","role":"product-lead","specModel":"gpt-5.5"}
