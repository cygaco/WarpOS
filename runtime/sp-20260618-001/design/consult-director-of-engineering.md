# Director-of-Engineering consult — SP-20260618-001 / E-CONTENT-DELIVERY-001 (U1 cutover)
*(READ-ONLY advisory; spawned by α as the in-process relay-hand at ε's design-boundary request. Real Agent return, elapsed ~74.9s.)*

**Contract judged:** `build_spec` (precedence 70) over `MIGRATION-BRIEF` (~30); they agree.

## (a) SEQUENCE — CORRECT, with two seam refinements (not reordering)
The 8-step atomic cutover is sound and matches the brief's binding contract. The two-GREEN bracket (ship-coverage GREEN pre-delete AND post-delete) + the positive shipped-set assertion is the right shape — it catches both the carve-out regression and the orphaned-old-copy case. Refinements:
- **R1 — carve-out must be a NEGATIVE-then-POSITIVE pair, order-sensitive.** `KNOWN_NOT_SHIPPED` is evaluated by `isAllowlisted` via `p === prefix || p.startsWith(prefix)` (ship-coverage.js:197–200), first-match-by-`.find()`. Replacing the blanket `{prefix:"_warpos/"}` (line 77) with two narrow entries (`_warpos/MANIFEST.json`, `_warpos/settings/`) is correct — `_warpos/templates/**` then falls through to the hard-signal check. It is NOT under `HARD_SIGNAL_ROOTS` (line 214), so an un-shipped `_warpos/templates` path lands in `infoGaps`, which now hard-fails (line 290). Good — that is the enforcer that catches a missed generator repoint. The positive assertion on `framework-manifest.json.assets.template[].src` is the second, independent guard. Keep both.
- **R2 — the `validate.js` walk-skip seam.** `_warpos/MANIFEST.json` build + `validate.js` share `walk-skip.js`. Confirm `_warpos/templates/` is NOT in `WALK_SKIP_DIRS` (build.js:666) or it never enters the ownership manifest at all (silent zero, no RED). This is the missing seam to add to the spec's verified_by.

## (b) BASELINE — DECIDE: created INLINE in U1; content = pristine shipped-skeleton snapshot of the seed-zones. No independent generator this sprint.
Code evidence: grep of all `.js`/`.json`/`.md` finds NO `_warpos/BASELINE` generator. The sole reference is `scripts/warpos/manifest/validate.js:30` — a *deferred consumer*: "owner=project seed-drift (depends on `_warpos/BASELINE/` existing with snapshot of seeded content)." That consumer defines the contract: BASELINE is the **pristine post-install snapshot of owner=project seeded content** (the `_requirements/`+`_docs/` skeleton U2 seeds), against which `validate.js` will later diff to detect user edits vs upstream drift. So: NOT an update-diff, NOT a templates copy — a shipped-skeleton baseline. Build it inline (it has no walker today); defer the generator to when validate.js's seed-drift check is actually implemented (out of this sprint's scope). Producer = U1; consumer = a future validate.js increment. Confidence: high.
**Mind-changer:** if a `validate.js` seed-drift increment IS in this sprint's scope (it reads as deferred), BASELINE needs its own generator now, not inline.

## (c) SEAM U1→U2→U3 — owner is U1's build_spec contract; the shared invariant is the seed SOURCE path
The producer (U1) defines the new home `_warpos/templates/`; consumers (U2 seeder, U3 parity) adapt. Binding shared contract: **U2's provenance seeder MUST read its source from `_warpos/templates/...`, never `framework/templates/...`** (which U1 deletes). The spec encodes this (line 40–42) but the seam is under-named: nothing fails if U2 hardcodes the old (deleted) path. Enforcer to add — make U2's `verified_by` provenance-source assertion the seam gate, plus a negative: grep U2's seeder for `framework/templates` returns zero. U1↔U2 ordering (U1 lands first) is correctly gated.

## (d) UNDER-SPECIFIED β-correction — one gap
Four of β's five are in the spec (two-GREEN ordering, narrower carve-out, files-before-flip, regen-BOTH+verify-survival). The fifth — **forward-search-ALL-OLD-literals** — is WEAKLY specified. Spec verified_by only greps `scripts framework .claude`. My grep finds **59 hits across 24 files in `scripts/` alone**, 40+ files under `.claude/`, and **3 LIVE path-registry keys** (`framework/paths.registry.json` lines 370, 859, 994 — app-scaffold/sprint/portfolio; lines 539/1000 are `_note` prose). This is the CLAUDE.md rename-hygiene bug class (the `anthropic→claude` miss). Tighten: the post-cutover gate must assert `rg "framework/templates" -g"*.js" -g"*.json" -g"*.md"` returns zero LIVE refs repo-wide (excluding `runtime/`, test-fixtures, `.claude/worktrees/`, archived sprint `prd.md`/`changelog.md`, and the deliberate KNOWN_DANGLING reason-string), NOT just three named dirs. The 3 registry keys edited in SOURCE (`framework/paths.registry.json`) then regenerated are the load-bearing subset — name them explicitly.

## Net verdict
Sequence APPROVED with R1/R2 seam guards added; BASELINE = inline pristine seed-skeleton snapshot (generator deferred); seam contract = "consumers read `_warpos/templates`, U1 owns the move"; one β-correction (forward-search) needs a repo-wide enforcer, not a 3-dir grep. Confidence: high.

**Cited files:** `scripts/checks/warpos-ship-coverage.js` (77, 197–200, 214, 290); `scripts/warpos/manifest/validate.js` (30 — the BASELINE contract); `scripts/warpos/manifest/build.js` (201, 217 seeded_from; 666 walk-skip seam); `scripts/generate-framework-manifest.js` (277); `framework/paths.registry.json` (370, 859, 994 — the 3 live keys); `scripts/warpos/views/populate-source.js` (U2 sibling target).
