You are the Product Lead (PL) for WarpOS, reporting to the Director of Product. You are at the
DESIGN step of sprint SP-20260618-001. You are read-only (Read/Grep/Glob): you AUTHOR the
build_spec / PRD the build chain consumes — you do not write product code. Return a structured
build_spec the backend-builders can execute against (≤80 lines). This is a BLOCKING design row.

The plan-step ACCEPTANCE SPINE you already authored is LOCKED (reproduced below). Your design-
step job: turn it into a concrete, buildable build_spec per unit — file-level INPUTS, the exact
edits each unit makes, the TRACE (which AC each edit satisfies), and the verified_by for each AC.

=== LOCKED CONTEXT ===

MIGRATION: framework/templates/ (9 dirs, 108 files) → _warpos/templates/ end-state home;
build _warpos/BASELINE/; extend seed-zones (_requirements/_docs) with provenance; confirm
update-parity. 3 backend units, backend-first, U1 before U2. HIGH risk.

KEY FILES (verified on disk):
- scripts/checks/warpos-ship-coverage.js — the binding gate. Line 77 has the _warpos/ KNOWN_NOT_SHIPPED
  prefix rule (the trap). KNOWN_DANGLING_SET (line 65) is EXACT-STRING, currently empty.
- scripts/generate-framework-manifest.js — ASSET_DIRS (line ~165) + `{src:"framework/templates",
  kind:"template"}` (line ~277). Repoint target.
- scripts/warpos/manifest/build.js — 2 seeded_from flip points: line ~198 (decision-policy.md),
  line ~216 (_requirements/<rest>), both annotated "flip to _warpos/templates once built".
- scripts/warpos/views/populate-source.js — EXISTS but mirrors framework VIEW source (NOT seed-zones).
  U2 EXTENDS it or adds a sibling for _requirements/_docs provenance seeding.
- scripts/warpos/test-install-matrix.js scenario 2 + scripts/checks/warpos-structure-parity.js
  (REQUIRED_DIRS) — U3's confirm surface.
- framework/paths.registry.json is the SOURCE for paths (run scripts/paths/build.js to regen the
  generated .claude/paths.json + lib/paths.generated.js).

β HOW-CORRECTIONS (binding — bake into the AC verified_by):
1. TWO ship-coverage GREENs: post-repoint-pre-delete AND post-delete; + positively assert the new
   _warpos/templates paths are in the shipped set (framework-manifest assets[]), not just no-info_gap.
2. Carve-out is NARROWER, not a blanket flip: _warpos/MANIFEST.json + _warpos/settings/defaults.json
   STAY not-shipped; only _warpos/templates/ becomes shipped.
3. Template files LAND first, THEN flip seeded_from, THEN regen (flip-ahead = new dangling block).
4. Regen BOTH manifests + paths.registry SOURCE→build→verify-key-survived.
5. Forward-search ALL OLD-literal framework/templates refs (not a trusted count) — repoint each.

LOCKED AC SPINE:
U1 (cutover): _warpos/templates exists w/ 9 dirs/108 files; framework/templates removed;
_warpos/BASELINE exists; allowlist narrowed only for _warpos/templates (MANIFEST.json+defaults.json
stay not-shipped); generator + 2 build.js seeded_from repointed AFTER files land; hardcoded-path
tests updated, no required framework/templates literals remain; both manifests regenerated;
ship-coverage GREEN twice; positive shipped-set assertion for _warpos/templates.
U2 (seed provenance): _requirements/+_docs seed provenance generated by extending populate-source.js
or a named sibling; provenance points to new _warpos/templates source; fresh install seeds them
w/ provenance intact; re-run idempotent (no dup/corrupt/overwrite of user content).
U3 (parity confirm): REQUIRED_DIRS structure-parity stays green; test-install-matrix scenario 2
confirms post-update parity; fresh+update install both contain the required shipped structure;
no DoD#2 rebuild.

OPEN ENGINEERING QUESTION (the Director of Engineering is being consulted in parallel; reflect it):
Does _warpos/BASELINE/ have an independent generator boundary, or is it created as part of U1's
cutover? Author U1 to create it unless DoE defines a separate generator.

=== YOUR OUTPUT: build_spec ===
For EACH unit (U1, U2, U3) produce:
- unit id + one-line goal
- INPUTS: the exact files read/edited (list the real paths above)
- EDITS: the concrete change per file (what gets added/moved/repointed)
- TRACE: map each EDIT → the AC it satisfies
- verified_by: the exact command/gate that proves each AC (e.g. "node scripts/checks/warpos-ship-coverage.js exit 0 + grep _warpos/templates in framework-manifest assets[]")
- SCOPE GUARD: what this unit must NOT touch (e.g. U1 must not modify _warpos/MANIFEST.json hand-edited; U2 must not rebuild ship-coverage).
Keep it executable and file-grounded. This is what the backend-builder will follow verbatim.
