# SP-20260829-001 — CLOSE

**Verdict: NO RELEASE.** Five of seven criteria fail; two hold. Aggregated by β under AG-1…AG-14 and S6-1…S6-7 (`2a7f4c19`). Qualifying pin **`c88aac1d`**.

---

## 1. What the round found, stated in the order that matters

**The enforcer is demonstrably able to refuse.** S6-6 was discharged by execution, with a green control and a no-op guard: a regressed registry member → exit 1 naming the site; a new untriaged site → exit 1 naming the site; the decision neutered at L200 → exit 0 with detection still running, proving the decision was what was measured. The shipped suite catches that no-op.

**The sprint's claims about it are false.** E1 filed 34 false current claims across shipped code comments, the enforcer's header and coverage sentence, all seven repaired hooks' descriptions, three register sentences and eight-plus ED rows.

**⚠️ And nothing invokes it.** Two lanes found this independently, by different methods: E1 #11 searched for callers across the settings file, the hooks, `/scan:full`, the testsuite and the release gates and found **no invocation** — its only non-test caller is its own CLI; E2, out of its own rule, ran `grep -c failclosed .claude/settings.json` = **0** against **66 registered hooks**, with all 29 repository references being the gate's own files. **Union: nothing runs it today. Whether it is supposed to be wired yet is unsettled — E2 explicitly declined that normative half, and this close declines it too.**

**This sentence belongs beside the one above it, not below it.** *"The enforcer is demonstrably able to refuse"* is true and was ruled so — **but a gate that can refuse and that nothing invokes is a materially different object from a gate that can refuse.** The first version of this close stated the first half and omitted the second, and **the omission ran in the document's favour.** It was caught by an outside read, which is the only reliable way a gap that flatters its own document gets found.

**Those are different results and the second is the one that matters.** This round did not find a broken gate. It found a working gate, **not wired**, described falsely — which is the class the sprint was minted to study, occurring in the sprint's own record.

---

## 2. Criteria

| | criterion | result |
|---|---|---|
| S6-1 | TRUTH | **FAILS** — 34 false current claims across every named surface |
| S6-2 | GRANULARITY (keystone) | **FAILS** — the instrument asserts the rule and violates it |
| S6-3 | INSTRUMENT CEILING | **FAILS** — `runEnforcer()` drops `status`, `property`, `ceiling` and the site set at the point of claim |
| S6-4 | POLARITY PROVENANCE | **FAILS** — *"contested → fails closed"* is false as built |
| S6-5 | FALSIFIERS | **HOLDS**, with its ceiling |
| S6-6 | THE ENFORCER CAN FAIL | **HOLDS** — the one criterion positively discharged |
| S6-7 | RESIDUALS TRAVEL | **FAILS** — residuals required to travel did not |

**The terminal requires all seven. Five do not hold.** NO RELEASE rests on AG-1 (two lanes returned `fail`) **and** on five independent criterion breaches; either alone suffices.

**S6-2's breach is the sharpest single finding of the round.** `gate-failclosed-audit.js:64-67` ships *"No bare count anywhere without the emitted list it derives from"* — while the audit emits `files_scanned` without its set and the enforcer emits four counts without their member sets. The keystone rule, asserted by the instrument, violated by the instrument, found by an outside lane.

**One judgement β declined, recorded because it was declined rather than missed:** E1 executed three new fooling inputs at this close that are not fixtures. A plain reading of S6-5 could call that a breach; β ruled a bundle cannot fixture what a qualifying lane found after it shipped, and made them a successor obligation instead. α may overrule; it would make six fails and change nothing.

---

## 3. The lanes

| lane | dispatch | outcome | raw file |
|---|---|---|---|
| E1 claim-truth | `d-mtfdwwch-491effca` then `d-mtffqf0q-48df5464` | `fail` | `out-E1-2-RETURN.md` |
| E2 adversarial | `d-mtfek189-db4cb9a2` | `warn` | `out-E2.raw.md` |
| E3 fixtures/teeth | *(no completion record — refused)* | `fail` | `out-E3.raw.md` |

**E1 is one lane across two dispatches.** The first died at `elapsed_ms 1500238` against a 1500000 bound — mode settled (a wrapper killing its child at its own bound), **cause unavailable**: the stderr head cap held only the provider banner and our own prompt echo. The second completed at `elapsed_ms 1320846` against a 3000000 bound. E1 sits at position 1 in the reading order on its **original** `05:44:20.417Z` start; a re-fire's clock position is an artifact of the death.

**E3 has no completion record and that is correct.** `record-inprocess` refused it: the lane was spawned in-process under a role the registry declares a subprocess route. Re-running under a different `--role` would have minted a row describing a lane that did not run. **The refusal is the fail-closed behaviour this sprint exists to produce, fired by the conductor's runtime against the conductor's own dispatch.** Under AG-14 its verdict is valid from its raw output; liveness comes from the harness transcript (`agentId a34e63055bcb9f731`, two legs: spawn `06:02:33.770Z`, 20-turn ceiling, resume `06:05:20.174Z`, final `06:08:39.367Z`).

**Byte-containment is provable for E1 alone.** `prompt_bytes` **9226** equals `assembled-E1-3.md` exactly; the ledger's 32 hex `ddb6a669dd04f3d6fc9d413c949e7d1d` match **the first 32 of** `ddb6a669dd04f3d6fc9d413c949e7d1def49a5b959b89e7420d652e3836167b6` — **the ledger side is the truncated one**, a 128-bit prefix, not a sha256 match. For E2 and E3 the evidence is **consumption**, not transmission:

- **E3 — consumption-at-path**: transcript row 3 `TOOL_USE Read {"file_path":"…/assembled-E3.md"}`, row 4 `tool_result` carrying the envelope's own first lines. This shows the file was *delivered* at that path; it is not evidence the lane processed all 10693 bytes.
- **E2 — no citation exists.** Its transcript file is 0 bytes and its prompt was passed inline and never persisted. **E2's link to the cleared bytes is attested by the conductor and by nothing else.**

**Name pairing, stated rather than assumed:** `out-E1-2.raw` and `out-E1-2-RETURN.md` are the output of **`assembled-E1-3.md`**, dispatch `d-mtffqf0q-48df5464`. `assembled-E1-2.md` exists on disk and never fired.

**Every lane's restore claim was verified independently, twice, by two parties** — a lane's statement about its own cleanliness is the claim it has least incentive to test.

- **Structural**: all three checkouts at `c88aac1d`, `git status --porcelain --untracked-files=all` empty, **and `git diff c88aac1d --stat` empty**. The diff is the stronger form: porcelain says index and worktree agree, the diff says every tracked file matches the pin.
- **Behavioural** — the part porcelain cannot give: E2's created probe absent, enforcer exit 0, suites `11/11/0` and `28/27/0/1 skip`; E3's *"back to 27 pass / 0 fail / 1 skip"* reproducing exactly, plus `10/10/0`.
- **E1 is read-only, so a tree check is satisfied by inaction.** Verified instead on the objects it read: ledger snapshot `e345088ab5277bab…` and pin-time register `5673b5c9f70c55d4` both unchanged — **the population E1 graded is the population the envelope named, then and now.**

Only the behavioural half could have caught a restored-but-broken tree, which is why both are stated.

**E1's graded population spans two epochs, and the close states its unit rather than implying one.** E1 graded **classes 1, 2, 3 and 5 at the pin `c88aac1d`**, and **class 4 at a named later snapshot** — `enforcement-debt.SNAPSHOT.jsonl`, sha256 `e345088ab5277bab…`, **375 rows, all 375 parsing**, carrying ED-392…ED-411. **Class 4 has no pin state at all**: the ledger is gitignored and exists in no checkout, so there is nothing at `c88aac1d` for it to have been read from. Unavoidable, and a coverage fact rather than a defect — but a coverage claim states its unit, so: **"E1 graded the ledger as of sha `e345088a…`, 375 rows, 20 of them authored by this sprint."**

**E1 named the register-at-pin ceiling itself, unprompted.** Its derivation rule excludes *"later residual-register variants outside the pinned checkout"* and it emitted its graded members as `{R-1…R-12}` exactly. **So the class-5 gap has a second, independent warrant from the graded party** — E1 did not mistake the pin-time copy for the close-time register, and the gap below is disclosed by the lane as well as by us.

---

## 3a. E2's residue — findings belonging to E2 alone

**The seal was defeated to exit 0 over a live fail-open site.** With a genuinely permissive `catch (e) { process.exit(0); }` in the tree, appending its id to the baseline produced `status: "ok"`, exit 0.

**And the gate then asserted a false provenance sentence.** Its coverage line read *"0 of the 79 sites the detector at 12d2aef0 enumerated…"* when the detector enumerated **78** and the 79th had been typed by hand thirty seconds earlier. **That attribution is structural, not incidental:** the sentence is built at `gate-failclosed-enforcer.js` L178-179 from `M` and `detector_sha` **both read out of the baseline file**, so the gate has no path by which it could know the difference. **E2's B1 and the `detector_sha`-as-settable-label finding meet in that single line** — a claim exceeding the instrument, emitted by the instrument, with no mechanism available to it for detecting that it was doing so.

**The file carried the evidence of its own tamper and the gate never read it:** `site_count: 78` beside `site_ids.length: 79`, while `loadBaseline` checks only that `site_ids` is an array.

**Provenance validation is asymmetric:** `tool-derived` with **no quote at all** is ACCEPTED; `manual-by-read` with an empty quote is REJECTED. So the cheapest way to strip a human judgement of the evidence that made it auditable is to relabel it machine-derived.

---

## 4. The freeze ordering, printed as two values

- Aggregation rule frozen: **ledger row 386, appended `2026-08-30T03:43:05Z`**
- Earliest lane start: **`2026-08-30T05:44:20.417Z`** (E1's first dispatch)

**The rule was frozen 2 h 01 m 15 s before any lane existed.** Both values are printed so a reader can run the comparison rather than trust the claim. The mechanical enforcer remains owed (ED-397).

---

## 5. The green sentence, in four clauses

> The enforcer is green at `c88aac1d` · **because the baseline was re-emitted** at the `128cf0af`-stamped detector · with **0 of 78 sites under enforcement, 7 repaired sites guarded by the regression check, and 75 sites carrying no tool-correlated registry row** · **and `ok` is computed from `regressed` and `newEntrants` alone — it does not read `status`, `N`, `K` or the ceiling.**

Two of the three coverage figures need their real units: `K` counts sites with **no tool-correlated registry row**, not sites nobody triaged; `checked_repaired_count` counts **attempts, not checks** — verified twice, by two lanes, two mutations, one mechanism.

**And `enforce.js` at this pin is exit 0 in the working tree and exit 1 on a clean checkout of the same commit** — root-caused to one gitignored asset the installed manifest promises. Five landing commits carry *"enforce.js exit 0"*, corrected by appended git notes rather than edits. Filed as ED-409, a facet of ED-408's class.

---

## 6. What the close must not say

**Not that the round found the enforcer broken.** It found the enforcer demonstrably able to refuse and the sprint's claims about it false.

**Not that "both reviewers confirmed" anything.** The two B5 review lanes ran different prompt bytes, different bounds and different routes; they compose as a **union** under AG-9, and their most valuable result exists only in the union of their reasons — `ok` reported green while the enforcer's own coverage line declared an open defect, because `ok` does not read `N`.

**Not "the close-time register was graded by no lane"** without naming what is in the gap. The gap is exactly **three sentences**: R-13, R-14, and the count sentence (`Entries: twelve.` → `Entries: fourteen.`). Warranted two ways — β's full read of both files and the conductor's machine diff — with the convention stated so `13 added + 1 changed` and `14 added / 1 removed` do not read as a contradiction.

**Not any bare count.** Every figure in this close carries its emitted set or its unit.

### Two apparent contradictions that are not contradictions — stated before a reader finds them

**Both are pairs of true statements about different things, and both are named here so nobody files them as conflicts later.**

1. **E1 affirmed *"all seven repaired hooks are wired"* while E2 found *"the gate has no trigger."*** **Different objects:** the seven repaired hooks' registration and ordering, versus `gate-failclosed-enforcer.js` itself. **Both true.** The hooks are wired; the gate that checks them is not.
2. **E1 #10 found *"no current `defect` row"* while E2 B4 reported *"N went 0→4."*** **Different states:** the unmutated tree at the pin, versus E2's own mutation setting all sixteen rows to `defect`. **Both correct.**

This is the same move §7 makes for the disposition and baseline routes, and for the same reason: **two methods agreeing is worth more than one only if their frames are stated, and two methods appearing to disagree is worth nothing until someone checks whether they are looking at the same object.**

---

## 7. The positive half, with its checks named

A not-found with its check named is evidence; without one it is silence.

- **Absorption — not found.** The emitted baseline equals the live scan exactly (78 == 78, no padding); the departed set is 12 ids each accounted for, the arrived set exactly the three entrants.
- **Green-by-moving-the-measured-thing — not found on the disposition route.** Scope stated: dispositions never enter `ok`. Lane 2 found it **on the baseline route**, and the two answers are about two mechanisms, not a disagreement.
- **Could-not-check resolved into a pass — found**, by two lanes independently.
- **All seven `absent` rows genuinely non-permissive**; both S6-6 fixtures exit 1; the two disclosed test failures were exactly the two disclosed and no others.
- **The B5 builder refused to route around its own instrument.** It kept the honest catch-line for three rows where a window-friendly line would have looked like more repair, and disclosed the false positive by execution rather than avoiding it.
- **Lane 1's departed-set count of 12 was correct.** It was described as an over-count; the error was the reader's endpoint, one step further back. Stated as an explicit sentence rather than a silent absence — a lane mis-described in a record that then stops mentioning it has been left mis-described.
- **The reviewer-executed injection on the B5-R repair is the reviewer's**, dispatch `d-mtfb42md-46a6f456`, not the bundle's.

---

## 8. Findings against the conductor's own artifacts

Both verified at source, both stand, neither repaired.

**ED-407 is false as written.** Its claim that nothing records a dispatch's prompt sha, byte length and wall-clock is refuted by `dispatch-agent.js` and `dispatch-claude.js`, which write all three — **the same fields used two hours later to prove E1's containment.** A residual asserting a mechanism's absence, refuted mid-round by its filer using that mechanism. The real gap is the **in-process** route (`record-inprocess` wrote `prompt_bytes: 0`); the sentence quantified over "a dispatch" when the defect holds for one route class.

**The register's completeness closure over-claims.** It states that an absent residual failed one of three conditions "by construction and not by judgment". ED-374, ED-381, ED-383 and ED-396 each satisfy all three conditions and are absent. **They failed nothing; they were omitted.** The derivation rule was honest; the closure sentence claimed a property the rule did not deliver — and unlike the class-5 gap, this is a hole inside the version the lane could see.

**Neither was repaired**, because the register is a graded artifact under composition and editing it would change the object three lanes were pointed at. The successor inherits the findings with the omitted set named.

**A third defect of the conductor's, deliberately kept OUT of S6-1's 34.** The round's own capture header in `out-E1-2-RETURN.md` printed `output_bytes=18990` — a **character** count under a byte label (`a-count-labelled-in-the-wrong-unit`, the same class as 32 hex under `sha256:`). Corrected by appended note at L145 with L1 left unedited, since L1 had been cited by line. **It does not join the 34**: S6-1 binds sentences *this sprint ships*, read against the code they describe at `c88aac1d`, and that file was created after the pin as round machinery. **Folding our own instrumentation into the criterion would inflate it with evidence we manufactured** — the move the criterion exists to catch. Recorded here under round hygiene instead, so its absence from the 34 is a decision rather than an omission.

**One measurement deliberately not taken.** ⚠️ **SUPERSEDED — the sentence below undercounts, and the superseded text is retained after this marker per the round's own rule for shipped artifacts.** The correct statement: **three** populations exist for one enumeration — ED-408's *"27 read; 3 unread"*, the register's *"of the twenty checks"*, and E1's *22 classes / 19 unique first-script paths with a named no-run set*. **At most one can be right and none prints its set.** ED-408 is amended accordingly: **no cardinal until the set of check ids is emitted with, per id, the input it reads and whether it is tracked.** Running that measurement mid-composition would be the conductor repairing its own graded artifact under another name.

> *Superseded text, retained:* "ED-408's *"27 read; 3 unread"* against the register's *"of the twenty checks"* are two populations for one enumeration, at most one right, neither printing its set."

**That this close shipped a bare count that was itself wrong — in the paragraph naming a contested enumeration, in a document whose §6 forbids bare counts — is recorded rather than quietly fixed.** It is the round's subject occurring in the round's own close, caught by β's row 451 before the close was read by anyone.

---

## 9. Ceilings — no figure travels without one

- **E1** could not run `node --test` (`spawn EPERM`); files named with their sampled windows; its own three tiers, including *"not represented as observed."*
- **E2**'s rule is the finite input set to `ok = regressed.length === 0 && newEntrants.length === 0` **and nothing else** — it cannot speak to audit-internal correctness, CLI redirection, concurrency, or whether the gate is invoked at all. It **could not confirm ED-403/404/405 exist**: the ledger is gitignored and absent from its checkout, so R-13/ED-408's class landed on a qualifying lane rather than on a gate.
- **E3**'s teeth verdict covers **1 of 10** b3 fault-injection hooks; nine are `not-reached`, not passing. Its grep for un-promoted probe inputs is a bounded search: it supports a positive finding and cannot support a negative one.

---

## 10. Terminal statement

This sprint **repairs a subset of a class whose size is unknown.** The instrument's ceiling is stated at every point of claim in this document, and the unread remainder is named: 75 of 78 baseline sites carry no tool-correlated registry row; nine of ten fault-injection hooks are unverified for teeth; of the release runner's twenty checks, the number reading a gitignored input is **two known and eighteen unexamined** — and that enumeration is itself contested between two populations.

**A close reading as "the class is enumerated or closed" would be the class one layer out.** This one does not.


---

## §11 — α addendum (appended 2026-08-30T07:16Z; the close of record is this file, conducted by ε, audited by β at source — β `6b1c94ae`, row 455; α's separate draft `qualifying/QUALIFYING-CLOSE.md` was never committed and is withdrawn so that one close exists)

**S6-5's alternative reading is declined by α as well as by β.** E1 executed three new fooling inputs at this close (ASI; regex-state-blind interpolation; CR/U+2028/U+2029) that are not fixtures. A plain reading of *"each executed fooling input becomes a near-miss fixture"* could count that a sixth breach; the bundle cannot fixture what the qualifying lane found after it shipped, so the three inputs travel to the successor by name under S6-5's shape. The decline is the round's, stated by both readers, and it would not have changed the disposition.

**ED-405, settled at source with three warrants (β `4a9d7e52`, row 454).** `computeCoverage` in `scripts/checks/gate-failclosed-enforcer.js` (read in the E2 worktree at `c88aac1d`, L166–168) puts `contested` in its predicate — `(r.disposition === "defect" || r.disposition === "contested") && !r.tool_correlated` — and emits the count at L176 as `uncorrelated_registry_defects` and again in the phrasing at L181–182. ED-405's "enters no enforcer output at all" is false; E1 #31 is exact including its qualifier (such rows do not enter `N` or the verdict); E2 B2 observed the field go 2→1 when it resolved one of the two uncorrelated contested rows — arithmetic matching the predicate. α's earlier probe returned no such key because its `--json` output lacked the whole coverage block (`N` at L173 and the field at L176 are three lines apart in one object literal; an output missing both is missing the block) — a negative about the wrong object, not a negative about the field; one method returning zero is not a zero.

**The frozen rule set's own disclosed defects.** AG-13 (row 430): a verdict token is the lane's disposition of its own findings, never the round's; where the rule set does not name a token the briefs offer, that silence is a defect in the rule set, disclosed, never resolved by reading a disposition into the token after its value is known. AG-14 (row 433): a lane's verdict and its liveness come from different sources — record, then harness transcript, then the conductor's attestation labelled as such; a record that would have to be manufactured is not a record. Both found by results, in the gate's own instrument.

**The recurring defect, as three named families with overlaps shown and no cardinal** (rows 442–444; ordinals in an enumeration three parties append to collide by construction — three "sixth members" were minted in one night). *Position* — the value is computed from where the reader stands: `detector_sha` · `K`/untriaged · `checked_repaired_count` · `identifier-as-address` · `release-gate-verdict-differs-by-tree` · `suite-class-differs-by-checkout` · `re-fire-at-the-same-pin`. *Label* — the label misdescribes the value identically for every reader: `prompt_digest`-labelled-sha256 (ED-413; the remedy is the label, never the length) · `K`'s hidden predicate · `identifier-as-address` · `a-count-labelled-in-the-wrong-unit`. *Indexical* — a proposition that travels with a copy to where it is false, reachable by no field lint: `register-self-description-read-on-a-pinned-copy` (ED-415; a two-sided copy-time control, binding tools not hands). Three axes on which a check can miss: unit (ED-412), population (ED-410 amendment 3), proposition (ED-414 `accurate-fact-about-the-wrong-object`). `absolution-as-evidence-deletion` (row 446): a finding made against oneself is the round's best evidence — its mechanism arguable, its magnitude not — and the rule runs in the credit direction too.

**Provenance of this close.** No sentence in it traces to the dead first dispatch's rollout (`qualifying/out-E1.rollout.jsonl`; inadmissible, row 429; never read by β). The aggregation rule was frozen at row 386 before any lane existed; the reading order at row 427 before any output existed; the disposition at row 431 before any prose was read. Every count carries its emitted set or its ceiling; every sha is labelled with who computed it.

---

## ⚠️ SUPERSEDED — this is the EARLIER record, not the canonical close

**Canonical close: `runtime/enforcement-sweep/2026-08-29/qualifying/QUALIFYING-CLOSE.md`.**

That document is strictly more complete — it carries a ceiling-per-criterion table in which a
verdict and its bound sit in one row and cannot be quoted apart, and it incorporates the five
omissions β named against this file. **This file is retained, unedited except for this note,
as the earlier record** — the same rule this round applied to the registry's quoted comments,
to α's struck justification, and to L1 of `out-E1-2-RETURN.md`.

**Why the note rather than a deletion or a merge:** two records of one round that disagree is
this round's own subject one layer out, and a future reader holding neither author has no way
to tell which is canonical. Deleting this file would erase the corrections it carries and the
record of what was fixed when; editing it into agreement would erase the same thing more
quietly. **Naming the canonical one is the only resolution that loses nothing.**

**Verdict is identical in both and is not in dispute:** S6-1/2/3/4/7 FAIL · S6-5, S6-6 HOLD ·
**NO RELEASE**, on AG-1 plus five independent criterion breaches.


---

## §12 — the close of record, decided (α, appended 2026-08-30T07:18Z; α's decision, not deferred)

**The §"SUPERSEDED" marker above (L191, ε's `1e3b4e2f`) is itself superseded; its text is retained unedited.** It names `qualifying/QUALIFYING-CLOSE.md` as canonical. That file was α's draft, never committed, and was withdrawn and deleted from the working tree at 07:15Z — before the marker landed — because two closes are the two-lane shape by another name. At the same minutes ε was yielding its close to α's; α was yielding α's to ε's. A close does not get to be "the other's call" (β `8f1d4b93`: a withdrawal conditional on the other party's response is a negotiation; ε: recency is a property of message delivery, not authority).

**Decision: this file — `runtime/enforcement-sweep/2026-08-29/CLOSE-SP-20260829-001.md` — is the close of record for SP-20260829-001**, on the ground that decides it: it is the version that was committed (`8feff598` → `4bef3cba` → `0cd03d58` → `1e3b4e2f`), conducted by ε, read at source by β (`6b1c94ae`, row 455) with its five omissions filled (L13–15, L68–70, L119–120, L78–82, L150), and carries α's addendum (§11). No other close exists. Nothing in §1–§11 changes: NO RELEASE; five criteria FAIL, two HOLD; the map as ruled at rows 452–453.

The one sentence in the L191 section worth keeping stands on its own: the record is read in order, and a later section can correct an earlier one only by appending, never by editing. This section does that to the one above it.


## §13 — the criterion table, verdict and ceiling in one row (α, appended 2026-08-30T07:19Z; the one property of the withdrawn draft worth keeping, carried here so that no second file is needed)

| Criterion | Verdict | Warrant (lane finding, read by β from the lane's own raw file) | Ceiling attached |
|---|---|---|---|
| S6-1 Truth | **FAIL** | E1: 34 execution-proven or reasoned false current claims across shipped comments (`gate-failclosed-audit.js` :23-25, :116-120, :117-120, :64-67), the enforcer's header and coverage sentence, all seven repaired hooks' descriptions, three register sentences, eight-plus ED rows | E1 could not run `node --test` (`spawn EPERM`); its enforcer/audit findings are direct runs, its suite-level claims are reasoned; sampled windows named (`dispatch-agent.js` L226-246/L1071-1124, `dispatch-claude.js` L741-795) |
| S6-2 Granularity (keystone) | **FAIL** | E1 #4: `gate-failclosed-audit.js:64-67` ships *"No bare count anywhere without the emitted list it derives from"* and breaks it in its own output — `files_scanned` without the scanned set; the enforcer's baseline/registry/repaired/untriaged counts without member sets (verified at source by α, L64–67 vs L631/L662). Compounded by #25 and by the three-population mismatch (§5 / L152) | same |
| S6-3 Instrument ceiling | **FAIL** | E1 #9: `runEnforcer()` drops `status`, `property`, `ceiling` and the exhaustive site set at the point of claim; #24 and #30 assert the opposite in the register and the ledger; #2/#3 are claims past the lexer's ceiling (interpolation neutralised; newlines preserved — both false in its own behaviour) | same |
| S6-4 Polarity provenance | **FAIL** | E2 B2: a `contested` row resolved to `not-a-defect` with its quote deleted → exit 0, `uncorrelated_registry_defects` 2→1 silently; loader validation asymmetric (`tool-derived` with no quote ACCEPTED; `manual-by-read` with empty quote REJECTED); E2 B4 + E1 #31: no disposition value makes the gate refuse | E2's attack set is the finite input set to `ok = regressed.length === 0 && newEntrants.length === 0` and cannot speak to anything that never reaches that boolean |
| S6-5 Falsifiers observed at the close | **HOLDS** | E3: all 16 fixtures git-tracked at the pin, each `fn`/`fp` carrying a live `oldNaiveDetect` discrimination re-proven against the current build every run; M1/M2: each fixture has teeth only against the opposing mutation direction | E3: *"a bounded search supports my positive finding and cannot support a negative one."* Successor obligation: E1's three new executed fooling inputs (ASI; regex-state-blind interpolation; CR/U+2028/U+2029) are not fixtures — the alternative reading (a sixth fail) was available and declined by β and by α: the bundle cannot fixture what the qualifying lane found after it shipped |
| S6-6 The enforcer can fail | **HOLDS** — discharged by execution | E2: A1 in-window regression → exit 1 naming `dependency-admission-guard.js:33`; A2 fresh untriaged site → exit 1 naming `e2-new-entrant-probe.js:7`; the no-op guard (`const ok = true` at L200) → exit 0 with `new_entrants` still populated, so A1/A2 measured the decision, not the detection; the shipped suite catches the no-op (`gate-failclosed-enforcer.test.js` → exit 1, fail 2). Beside it: a regression moved outside the disclosed 12-line window was caught only by the new-entrant path. And beside that (§1, L13): nothing invokes this gate today | E3's teeth verdict covers 1 of 10 b3 hooks; nine are `not-reached`, never "passing" |
| S6-7 Residuals travel | **FAIL** | E1 #25: the register's completeness closure is false with a named omitted set `{ED-374 amendment L312, ED-381 L309/L316, ED-383 L311/L313, ED-396 L337/L339}` — each open, predating assembly, required to travel, absent; plus R-1's reporting sentence (#24) and R-11 (#26) false; plus the class-5 gap by position (L83) | same as S6-1 |

All seven must HOLD; five do not. **NO RELEASE** — AG-1 on two lanes' own tokens (E3 `fail` L11, E1 `fail` L2; E2 `warn` L12) plus five independent criterion breaches. Per AG-13 no token is the round's disposition; the criteria are. This table restates §1–§10; where any cell and the body differ, the body's cited line governs and the difference is a defect to be filed, not resolved by reading.
