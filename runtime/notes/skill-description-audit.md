# Skill description audit

Generated by `scripts/skill-description-audit.js` (re-runnable; deterministic).

## Summary

- Scanned: 182
- ok: 18
- flag-tag-missing only: 161
- critical (description-missing or -ambiguous): 3

## Per-skill verdicts

| Slug | Verdict | Description (first 100 chars) |
|---|---|---|
| `agents:list` | flag-tag-missing | Enumerate every agent spec by mode and role. |
| `agents:test` | flag-tag-missing | Smoke-dispatch one agent role (or all non-claude roles) with a tiny ping prompt. |
| `beta:integrate` | flag-tag-missing | Apply validated recommendations from beta mining into the judgment model |
| `beta:mine` | flag-tag-missing | Mine patterns from user behavior — prompts, decisions, skill chains, evolution cycles |
| `bootstrap:lastmile` | flag-description-ambiguous / flag-tag-missing | Prototype → monetizable product. Drives the 'last mile': readiness audit → launch plan → roadmap/spr |
| `bootstrap:ponder` | flag-tag-missing | Exploratory pondering of a project — surface tensions, patterns, JTBD drift, and one forcing questio |
| `bootstrap:spinup` | flag-tag-missing | From 'just WarpOS' to something on screen — one in-project command: intent (guided brief, or --clone |
| `check:all` | ok | [deprecated alias → /scan:full] Run every scan in parallel — a full system scan. Superseded by /scan |
| `check:framework-purity` | ok | [deprecated alias → /scan:framework-purity] Refuse product-content leaks in canonical. Superseded by |
| `check:framework-views-fresh` | ok | [deprecated alias → /scan:framework-views-fresh] Verify .claude views are byte-identical regeneratio |
| `check:install` | ok | [deprecated alias → /scan:install] Verify a fresh WarpOS install. Superseded by /scan:install in the |
| `commit:both` | flag-tag-missing | [deprecated alias → /commit:land] Commit locally then push — superseded by /commit:land, which also  |
| `commit:land` | flag-tag-missing | Land the working branch — commit locally, push the branch, then merge it into the repo's default bra |
| `commit:local` | flag-tag-missing | Stage and commit changes locally — smart message, no push |
| `commit:remote` | flag-tag-missing | Push current branch to remote — with safety checks |
| `content:contra` | flag-tag-missing | Create a Contra portfolio post with carousel images — write copy, design slides, render PNGs |
| `content:linkedin` | flag-tag-missing | Create a LinkedIn post with carousel images — write copy, design slides, render PNGs |
| `discover:orphaned` | flag-tag-missing | Discover orphaned work — find every deferred, forgotten, or abandoned task across NEXT.md, runtime n |
| `discover:systems` | flag-tag-missing | Multi-angle system discovery — find every system in a project by intersecting 6 discovery lenses, su |
| `docs:catalog` | flag-tag-missing | Enumerate reference docs under _docs/ and paths.reference, with title/size/mtime. |
| `enforcement:list` | flag-tag-missing | List open enforcement-debt entries — policies/conventions without an automated enforcer |
| `enforcement:log` | flag-tag-missing | Record a policy/convention that has no automated enforcer — appends to paths.enforcementDebt |
| `etc:author` | flag-tag-missing | Author or refine a skill/prompt in standard format, producing a sibling eval-pack for evaluation |
| `etc:eval` | flag-tag-missing | Evaluate a skill or prompt artifact against its eval-pack, emitting a validated decision_record |
| `events:query` | flag-tag-missing | Query the events log by type, time range, or regex match. |
| `events:tail` | flag-tag-missing | Tail the events log — last N events with timestamp, type, and message. |
| `fav:list` | flag-tag-missing | Browse all saved favorite moments, grouped by category |
| `fav:search` | ok | Search saved favorite moments by keyword across category, title, and notes — find one specific momen |
| `fix:deep` | flag-tag-missing | Deep fix — Full diagnostic with automatic framework selection, 5 solutions, root cause analysis, and |
| `fix:fast` | flag-tag-missing | Quick fix — Direct Investigation, no formal framework. Read error, find cause, fix it, verify. |
| `growth:ad-images` | flag-tag-missing | Turn an angle into native-ad image prompts (scene-first, no text/logo/product, --ar) and render them |
| `growth:ad-video` | flag-tag-missing | Turn an angle into a video ad (swipe→script→storyboard→image-to-video). Generation via Higgsfield/Kl |
| `growth:advertorial` | flag-tag-missing | Write a long-form advertorial (pre-sell editorial) from a message brief — research → foundational do |
| `growth:angles` | flag-tag-missing | Mine untapped marketing angles from real customer voice (Amazon/Reddit/forums) — ≥3 evidence-backed  |
| `growth:iterate` | flag-tag-missing | Iterate a winning creative/message against a conversion/engagement scalar — thin wrapper over karpat |
| `growth:landing-page` | flag-tag-missing | Build a converting landing page from a conversion brief — conversion-hierarchy, scaffold component l |
| `growth:message-brief` | flag-tag-missing | Distill the single winning message (the spine artifact) from an audience dossier + angles — contrast |
| `growth:product-finder` | flag-tag-missing | Find validated high-margin products for paid traffic — EQ-scored (Product×Ads×Funnel×LTV), SCALE/TES |
| `hooks:add` | flag-tag-missing | Design and create a new hook from a description |
| `hooks:disable` | ok | Temporarily disable a hook by moving it from settings.json into a `_disabled_hooks` section, with a  |
| `hooks:friction` | flag-tag-missing | Analyze friction points — find patterns that suggest missing hooks |
| `hooks:test` | flag-tag-missing | Test all hooks with synthetic payloads and measure execution time |
| `issues:list` | flag-tag-missing | List recurring system issues — bugs/regressions in the agent framework, hooks, skills, .claude/, scr |
| `issues:log` | flag-tag-missing | Record a new instance of a recurring system issue — appends to recurring-issues.jsonl, dedupes by ti |
| `issues:resolve` | flag-tag-missing | Mark a recurring system issue resolved with a permanent fix summary |
| `karpathy:integrate` | flag-tag-missing | Review a completed /karpathy:run and merge its winning artifact(s) into main — only command that tou |
| `karpathy:run` | flag-tag-missing | Karpathy autoresearch loop — plan a closed-loop experiment, review, then run autonomously in an isol |
| `karpathy:status` | flag-tag-missing | Read-only status dashboard for an active or completed /karpathy:run. Shows score curve, flag counts, |
| `learn:deep` | flag-tag-missing | Deep learning — extracts from conversation + event log + oneshot retro files in parallel, deduplicat |
| `learn:ingest` | flag-tag-missing | Ingest external knowledge from files, links, or YouTube videos and apply learnings to the system |
| `learn:integrate` | flag-tag-missing | Learning integrator — promote validated high-score learnings into actual system enforcement (hooks,  |
| `linters:run` | flag-tag-missing | Run every project linter (path-lint, lint-*, npm lint:*) and aggregate pass/fail. |
| `manifest:migrate` | flag-tag-missing | Migrate the manifest to a target WarpOS version. Dry-run by default; --apply to write. |
| `manifest:show` | flag-tag-missing | Print .claude/manifest.json (pretty by default, --json for compact). |
| `manifest:validate` | ok | Validate the current .claude/manifest.json against the v1 manifest schema and report any drift, miss |
| `maps:all` | flag-tag-missing | Registry of all maps — shows every map, its source, last updated, and staleness |
| `maps:architecture` | flag-tag-missing | App structure — routes, components, libs, how they connect |
| `maps:enforcements` | flag-tag-missing | Enforcement coverage — hooks, gates, gap analysis, open/closed gaps |
| `maps:hooks` | flag-tag-missing | Hook wiring diagram — events, matchers, scripts, execution order |
| `maps:memory` | flag-tag-missing | Memory store relationships — who reads/writes each store, entry counts |
| `maps:skills` | flag-tag-missing | Skill dependency graph — namespaces, cross-references, data flow |
| `maps:steps` | flag-tag-missing | Regenerate step tables in canonical docs from _requirements/00-canonical/STEPS.json — closes the las |
| `maps:systems` | ok | Render the systems manifest as a dependency graph — visualize which systems depend on which, their s |
| `maps:tools` | flag-tag-missing | Tool registry — skills, hooks, external CLIs, API services, npm scripts, platform tools |
| `mode:adhoc` | flag-tag-missing | Enter adhoc team mode — Alpha + Beta + Gamma for collaborative feature development |
| `mode:oneshot` | flag-tag-missing | Initiate a oneshot build — launch Delta as standalone orchestrator for full skeleton runs |
| `mode:solo` | flag-tag-missing | Enter solo mode — just Alpha and the user, no agent team |
| `oneshot:improve` | flag-tag-missing | Update preflight passes based on gaps discovered during runs. Modifies the check skills themselves. |
| `oneshot:preflight` | flag-tag-missing | Pre-run preflight — branch creation + skeleton gut + 7-pass verification audit. Default = full setup |
| `oneshot:retro` | flag-tag-missing | Post-run retrospective — context + git log + code diffs + cross-run analysis, all 9 categories. Defa |
| `oneshot:start` | flag-tag-missing | Lightweight kickoff — verify ready-state and hand off to Delta. Does NOT run setup or destructive wo |
| `paths:add` | flag-tag-missing | Guided flow for adding a paths registry key. |
| `paths:convert` | flag-tag-missing | Guided flow for converting hardcoded literals to paths.* tokens. |
| `paths:coverage` | ok | Report on documentation coverage for the paths registry — which path keys are documented in PATH_KEY |
| `paths:doctor` | flag-tag-missing | Validate path registry, generated artifacts, and path lint rules. |
| `paths:explain` | ok | Explain one paths registry key — show its resolved on-disk path, owner, kind, deprecation status, an |
| `paths:rename` | flag-tag-missing | Guided flow for renaming a paths registry key. |
| `permissions:authorized` | flag-tag-missing | Operator authorization — durably allow a blocked action by adding a scoped permissions.allow rule fr |
| `playbook:add` | flag-tag-missing | Append a play to the Playbook (.claude/project/reference/playbook.md) — a named, example-anchored op |
| `portfolio:list` | flag-tag-missing | List all registered portfolio products — slug, path, WarpOS version, last commit, dirty count, curre |
| `portfolio:new` | flag-tag-missing | Scaffold a new product repo (sibling to WarpOS) with the framework installed and committed, then reg |
| `portfolio:open` | flag-tag-missing | Open a registered portfolio product — print its path and a cd hint, or spawn a new terminal window w |
| `portfolio:register` | flag-tag-missing | Register an existing local repo as a portfolio product in ~/.warpos/portfolio.json. |
| `portfolio:run` | flag-tag-missing | Run a skill against another portfolio product in a fresh Claude subprocess — never retargets the cur |
| `portfolio:spinup` | flag-tag-missing | From WarpOS, run the idea→on-screen on-ramp against a registered product: dispatches /bootstrap:spin |
| `portfolio:status` | flag-tag-missing | Portfolio dashboard — per-product WarpOS version, last commit, dirty count, current sprint, GitHub r |
| `portfolio:sync` | flag-tag-missing | Run /warp:update across every registered portfolio product sequentially. No fail-fast — failures cap |
| `qa:audit` | flag-tag-missing | Active full-codebase QA audit — systematically walks all 7 failure-mode personas |
| `qa:check` | flag-tag-missing | Passive QA scan on recent git diff changes — checks for 7 failure-mode signatures |
| `reasoning:log` | flag-tag-missing | Log a reasoning episode — record what framework was used, why, and what happened |
| `reasoning:run` | flag-tag-missing | Reason through a problem or decision — auto-detects quick triage vs deep deliberation |
| `reasoning:score` | flag-tag-missing | Score fix quality (0-4) and retroactively reclassify old fixes when new evidence appears |
| `redteam:full` | flag-tag-missing | Full red team audit — 11 personas across deterministic scanning + LLM reasoning. Finds auth bypasses |
| `redteam:scan` | flag-tag-missing | Quick red team scan — deterministic tools only (deps, routes, CVEs, secrets, config). Fast, no LLM r |
| `research:deep` | flag-tag-missing | Real deep research — Gemini Thinking writes the brief, then OpenAI Deep Research API + Gemini Deep R |
| `research:simple` | flag-tag-missing | Deep research pipeline — queries Claude, ChatGPT (Codex), and Gemini in parallel, saves reports, syn |
| `roadmap:add` | flag-tag-missing | Append a new entry to ROADMAP.md — picks section, formats consistently, preserves existing content |
| `roadmap:cleanup` | flag-tag-missing | Audit ROADMAP.md — detect completed items, stale entries, duplicates, hidden urgencies; propose a cl |
| `roadmap:create` | flag-description-ambiguous / flag-tag-missing | Bootstrap a product ROADMAP.md from the inputs a project actually has — prefers _requirements/00-can |
| `roadmap:ideas` | flag-tag-missing | Predict candidate roadmap entries across four evidence lenses (3 each = 12 ideas) — whole-roadmap, l |
| `roadmap:next` | flag-tag-missing | The 1-idea alternative to /roadmap:ideas — the single highest-leverage next roadmap entry (the role- |
| `roadmap:prioritize` | flag-tag-missing | Role-aware roadmap prioritization — runs /roadmap:cleanup first, then consults the Product Lead (sin |
| `scan:ac-coverage` | flag-tag-missing | Read-only audit of acceptance-criteria.md verified_by:- linkage across active sprints. |
| `scan:adhoc-team-hygiene` | flag-tag-missing | Read-only probe for adhoc-team accretion — flags teams whose members carry a -N de-dup suffix or a s |
| `scan:architecture` | flag-tag-missing | Architecture integrity — do the layers connect? agent system, cross-layer seams, documentation healt |
| `scan:coherence` | flag-tag-missing | Run the WarpOS system coherence graph across 15 drift types. |
| `scan:design-system` | flag-tag-missing | Design system compliance check - scans UI code for raw colors, raw primitives, missing design docs,  |
| `scan:dispatch-routing-parity` | flag-tag-missing | Assert the role→provider routing tables agree across providers.js, catalog.js, and the dispatch guid |
| `scan:environment` | flag-tag-missing | Environment readiness and tooling quality — fast go/no-go or deep audit |
| `scan:etc-harness` | flag-tag-missing | Audit the /etc authoring+eval harness — fail-closed enforcer that rejects an invented authoring form |
| `scan:framework-purity` | flag-tag-missing | Refuse product-content leaks in canonical — scans for client slugs, maintainer abs paths, root-level |
| `scan:framework-views-fresh` | flag-tag-missing | Verify .claude/commands and .claude/agents are byte-identical regenerations of their _warpos/ source |
| `scan:full` | flag-tag-missing | Run every scan in parallel — a full system scan across project health, governance, and WarpOS distri |
| `scan:ingest-firewall` | flag-tag-missing | Audit the ingest stores (_docs/research, _docs/imports, _docs/briefs, _docs/clones) for un-firewalle |
| `scan:install` | flag-tag-missing | Verify a fresh WarpOS install — manifest, paths, agents, hooks, version, settings. |
| `scan:issues` | flag-tag-missing | Pattern-mine events.jsonl for repeat audit-block signatures — surface candidates for /issues:log |
| `scan:node-procs` | flag-tag-missing | Read-only diagnostic — list Node processes on the host with PID, start-time, working-set KB, and com |
| `scan:patterns` | flag-tag-missing | Cross-run intelligence and automation proposals — diagnose recurring patterns or propose prevention |
| `scan:privacy` | flag-tag-missing | Pre-publish scan for personal data — credentials, emails, homedir paths, runtime files tracked by gi |
| `scan:references` | flag-tag-missing | Cross-file reference integrity — broken links, orphans, stale SPEC_GRAPH edges |
| `scan:regressions` | flag-tag-missing | Run the regression-seed suite — the 26 recurring bug classes from the 0.17.0 spec, made runnable. Re |
| `scan:requirements` | flag-tag-missing | Specification consistency, coverage, and drift — static audit, change-driven propagation check, or p |
| `scan:roadmap-trace` | flag-tag-missing | Assert every done/retrospected/released sprint has BOTH a Sprints-table ledger row AND a Shipped nar |
| `scan:role-parity` | flag-tag-missing | The one check that owns role parity across the org map, the dispatch catalog, and team-guard — fail- |
| `scan:scaffold-coverage` | flag-tag-missing | Verify the WarpOS app scaffold (Next+Tailwind v4+shadcn/ui+Radix+Lucide) is complete and coherent —  |
| `scan:sprint-beta-honesty` | flag-tag-missing | Audits Beta consultation honesty across post-cutoff /sprint:full runs (missing consults, placeholder |
| `scan:system` | flag-tag-missing | System inventory — enumerate every active WarpOS system, diff against manifest, report drift and gap |
| `scan:timeline` | flag-tag-missing | Reconstruct a build timeline from transaction, event, and provider logs. |
| `scan:version-coherence` | flag-tag-missing | Verify version + schema-label coherence — product version agrees across ALL manifests (incl. the one |
| `scan:warpos-applied-migrations` | flag-tag-missing | Detect already-applied WarpOS migration scripts left on disk in consumer projects |
| `scan:warpos-capsule-resolvable` | flag-tag-missing | Verify the capsule for /warp:update --to <v> is resolvable from REPO_ROOT, sibling clones, manifest. |
| `scan:warpos-install-baseline` | flag-tag-missing | Verify a WarpOS install baseline exists (.claude/framework-installed.json present, installedVersion  |
| `scan:warpos-manifest-coverage` | flag-tag-missing | Verify every on-disk path is enumerated in _warpos/MANIFEST.json — catches "added framework content, |
| `scan:warpos-manifest-honesty` | flag-tag-missing | Verify framework-installed.json reflects actual disk state (no missing files, no hash drift) |
| `scan:warpos-migration-coverage` | ok | Verify every breaking change in a WarpOS release ships with a corresponding migration script under f |
| `scan:warpos-migration-presence` | flag-tag-missing | Verify every migration listed in capsule release.json#migrations[] exists in the source tree before  |
| `scan:warpos-path-resolution` | flag-tag-missing | Verify every paths.json key points to an existing path (skip generated/ephemeral keys) |
| `scan:warpos-ship-coverage` | flag-tag-missing | Verify every framework-owned path under the consumer-essential roots is actually shipped (enumerated |
| `scan:warpos-staleness` | ok | Detect drift between the installed WarpOS version on disk and the latest canonical version, flagging |
| `scan:warpos-structure-parity` | flag-tag-missing | Verify installed framework has the structural skeleton dirs canonical declares |
| `scan:warpos-tracked-transients` | flag-tag-missing | Catch transient state accidentally committed (.warpos/, qa-*.png, runtime/qa-*/, etc.) |
| `scan:warpos-version-quorum` | flag-tag-missing | Verify version.json, .claude/framework-manifest.json, .claude/framework-installed.json, and install. |
| `session:checkpoint` | ok | Force an immediate session checkpoint save — captures conversation context and tool activity that gi |
| `session:dump` | flag-tag-missing | Write a prescriptive handoff to DUMP.md at project root — context, verbatim payloads, dispatch instr |
| `session:handoff` | flag-tag-missing | Generate a rich AI-analyzed handoff document (replaces /handoff) |
| `session:history` | ok | Browse past session handoff summaries from the handoffs directory — useful for tracking what happene |
| `session:read` | flag-tag-missing | Read the cross-session inbox — see what other Alex sessions have been doing |
| `session:recap` | flag-tag-missing | Catch up on the last N turns of this session — what you asked, what I did, what's still pending |
| `session:resume` | flag-tag-missing | Load and display the last handoff or checkpoint — use after /clear or cold start |
| `session:takenotes` | flag-tag-missing | Append a timestamped note to a per-topic file under runtime/notes/ |
| `session:turbo` | flag-tag-missing | Session speed mode — pre-authorize a batch of high-impact actions (permissions.allow) AND switch the |
| `session:write` | flag-tag-missing | Post a message to the cross-session inbox so other Alex sessions can see it. Default is fully automa |
| `skills:cleanup` | flag-tag-missing | Audit all skills for dead weight, duplicates, broken references, and namespace issues — then clean u |
| `skills:create` | flag-tag-missing | Create a new skill from a description — supports simple, multi-phase, and parallel workflows |
| `skills:delete` | ok | Remove a skill from .claude/commands with a backup, so it can be restored if the deletion turns out  |
| `skills:edit` | ok | Edit the body or frontmatter of an existing skill under .claude/commands — guided flow that preserve |
| `sleep:deep` | flag-tag-missing | Full sleep cycle — all 6 phases: NREM consolidation, cleanup, replay, REM dreaming, repair, growth ( |
| `sleep:quick` | flag-tag-missing | Light nap — NREM consolidation + glymphatic cleanup only (~5 min) |
| `sprint:cost-gate` | flag-tag-missing | Toggle the /sprint:full cost-estimate halt on or off — turn off the heuristic spend gate when an ope |
| `sprint:design` | flag-tag-missing | Turn an approved Plan Contract into PRD, stories, COPY, INPUTS, TRACE, acceptance criteria, QA, red- |
| `sprint:execute` | flag-tag-missing | Execute the sprint via Ralph-style plan/act/test/review/record/checkpoint loops per ticket, with cra |
| `sprint:full` | flag-tag-missing | Single-invocation execution of the full sprint pipeline (plan→design→execute→release-prep→retro) und |
| `sprint:plan` | flag-tag-missing | Turn a brief plain-language request into a structured sprint plan and durable Plan Contract. Evidenc |
| `sprint:release` | flag-tag-missing | Prepare and execute a sprint release — final checks, approval, deploy gate, release notes, rollback  |
| `sprint:retrospective` | flag-tag-missing | Synthesize a post-sprint retrospective from tracker artifacts — outcomes, friction, action items. Id |
| `sprint:status` | ok | Read-only status view of every live sprint — shows id, lane, status, phase, last checkpoint, and the |
| `turbo` | flag-tag-missing | [alias → /session:turbo] Pre-authorize a session batch of high-impact actions via permissions.allow  |
| `ui:review` | flag-tag-missing | Design system compliance audit — read-only check of components against the project's design-system d |
| `warp:check` | flag-tag-missing | Compare your WarpOS installation against the latest version — find stale, new, and missing items |
| `warp:deprecate` | flag-tag-missing | Create a guarded WarpOS deprecation proposal for an agent, skill, hook, path, requirement, pattern,  |
| `warp:diff` | flag-tag-missing | Diff canonical WarpOS against an installed product — version/staleness, framework-file drift (stale  |
| `warp:doctor` | flag-tag-missing | Unified WarpOS diagnostic — runs every health check in one place. Like /warp:health but full-coverag |
| `warp:flag` | flag-tag-missing | Flag a WarpOS framework/tooling gap from a downstream product — append a structured, canonical-consu |
| `warp:health` | flag-tag-missing | Verify WarpOS installation — checks every system, reports green/yellow/red with plain-English fixes |
| `warp:md` | flag-description-ambiguous / flag-tag-missing | Tune CLAUDE.md with project-specific context — refresh the auto-generated project block from PROJECT |
| `warp:reconcile` | flag-tag-missing | Reconcile downstream-flagged WarpOS gaps into canonical — discover every product's WARPOS.md, verify |
| `warp:release` | flag-tag-missing | Drive a full WarpOS release of the canonical clone from this product repo — promote, bump, regen, bu |
| `warp:setup` | flag-tag-missing | Set up WarpOS end-to-end — clone, install, merge CLAUDE.md, restart, verify. Safe to re-run; auto-de |
| `warp:sync` | ok | Legacy alias for /warp:update that forwards to the canonical update flow so older references and mus |
| `warp:tour` | flag-tag-missing | Guided introduction to WarpOS — explains everything in simple language, no jargon |
| `warp:uninstall` | flag-tag-missing | Completely remove WarpOS from a project — restores pre-install state from backup |
| `warp:update` | flag-tag-missing | Update WarpOS in this project to a target release. Default = latest. Default mode = dry-run; pass -- |

## Critical follow-ups

These skills MUST be fixed inline this sprint per AC-7.2 (zero criticals at sprint end).

- `bootstrap:lastmile` — flag-description-ambiguous, flag-tag-missing
- `roadmap:create` — flag-description-ambiguous, flag-tag-missing
- `warp:md` — flag-description-ambiguous, flag-tag-missing

