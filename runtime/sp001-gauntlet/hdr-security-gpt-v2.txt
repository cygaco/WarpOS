You are the SECURITY REVIEWER — the MANDATORY GPT SECOND PASS (a Gemini pass already ran independently and returned PASS; do NOT assume it caught everything — re-derive yourself). Sprint SP-20260614-001 (R-2 founders readiness panel, net-new product-facing surface). BINDING security verdict on the CURRENT code below (post-fix commit 05e37832). Binding — orchestrator CANNOT override a FAIL. Be adversarial.

THREAT MODEL — attack these surfaces; confirm each holds or FAIL:

1. PATH TRAVERSAL — guides/[ref]/page.tsx.tmpl. [ref] is attacker-controlled. MUST: separator-strip BEFORE validate; allowlist-validate against registry-referenceable basenames BEFORE any disk read; non-allowlisted/traversal -> 404 with NO disk read. Attack: encoded dots/%2e, null bytes, double-strip evasion, .md double-extension, "."/".."/empty. NOTE: a prior QA finding observed that because separators are stripped FIRST, a traversal-shaped ref whose basename IS allowlisted (../../DEV_SETUP_GUIDE.md) resolves to the allowlisted basename rather than 404. Assess the SECURITY impact: is this a real vuln (can it read outside _content?) or defended-in-depth (it can only ever resolve to a registry-allowlisted basename inside _content)? Confirm fail-closed when registry unreadable.

2. AUTH GATE — readiness/actions.ts.tmpl + guides/[ref] + config.ts.tmpl (the trust root, included below). Toggle action + both routes gate on resolveAdminActor() + !actor.allowed (default-deny, founder allowlist). The NEW resolveOwnerClass derives owner_class SERVER-SIDE from the producer (no longer trusts client FormData) — verify this closes the prior privilege issue (client cannot relabel a sprint-work/waiver gate as owner-action) AND introduces no new issue (e.g. require() of a producer path — is the path attacker-influenced? it uses process.cwd() constants — confirm). Prod dev-email fallback fails closed (NODE_ENV check). HMAC + timingSafeEqual. Confirm: no auth bypass, errors surfaced not swallowed.

3. FILE-WRITE SAFETY — writeback.ts.tmpl. Server-action write to FOUNDERS_CHECKLIST.md. MUST: patch-on-current re-read (no TOCTOU clobber); atomic tmp+renameSync; fail-closed unknown id (NO write, NO tmp); preserve non-target bytes. Attack: id injection via a label containing a fake id= token (is ID_TOKEN anchored safely to line start?), tmp leak on failure, path injection via id, an id matching multiple lines.

4. BRAND/INFO BOUNDARY — no product-facing "WarpOS" in shipped guide bodies/panel DOM; warpos/readiness/v1 schema id machine-layer only (never visible DOM). Assess whether the strip-to-view (toReadinessView) + the brand-leak-scan visibleFromTsx stripping could be evaded so a real leak passes.

5. GENERAL: markdown rendered as escaped <pre> text (no dangerouslySetInnerHTML -> no injection), no secrets in code, server-only modules not leaking into the client bundle.

CONTEXT: .tmpl = scaffold templates (no tsc). config.ts.tmpl is included below as the auth trust root.

Output a ReviewResult: VERDICT (PASS or FAIL), then findings (severity blocker/major/minor, file:line, attack + why it works or why defended). If PASS, state each of the 5 surfaces was checked and holds. Ground every finding in the code below.

===== CURRENT CODE UNDER REVIEW (post-fix 05e37832) =====
