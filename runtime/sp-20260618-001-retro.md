# SP-20260618-001 — Retro Synthesis (ops-analyst, advisory)

E-CONTENT-DELIVERY-001 templates-migration. Conducted by Alex ε; gauntlet GREEN; epic → Completed 100%.
ops-analyst (gpt-5.5) cross-cycle analysis. ε defers self-management to α+β, so this is ADVISORY —
the Class A/B/C proposals are recommendations for α, not auto-applied by ε.

## Recurring classes + proposed changes

1. **Silent Dispatch Boundary Lies** (the dominant pain — 5 failure modes this sprint):
   requested-timeout ≠ effective-timeout, background death with no terminal signal, outer wrappers as
   hidden ceilings, "commit incrementally" unenforced.
   - **Class B (ADR):** a Dispatch Runtime Contract — every dispatch records `requested_bound_ms`,
     `effective_bound_ms`, active clamp source, a START heartbeat before spawn, periodic lease updates,
     an END/death record, and savepoint evidence. A no-record / stale-lease = a machine-detected death,
     not an operator wait.
   - **Class A:** fail-loud warning when `DISPATCH_BUILDER_TIMEOUT_MS > 540000` without
     `WARPOS_DISPATCH_BACKGROUND=1` (the silent-clamp this sprint).
   - HIGHEST-LEVERAGE change. Maps to the (parked) E-DISPATCH-PERFECT-001 epic.

2. **Provider Readiness Assumed Too Late:** gemini account-tier ineligibility discovered inside a
   binding gauntlet lane.
   - **Class B:** provider-readiness preflight before the gauntlet (probe each {role,provider,model}
     via the same CLI path, classify auth/tier/quota/model errors, select sanctioned fallback before
     lane dispatch).
   - **Class A:** add `IneligibleTierError / Antigravity` to provider RCA signatures + provider-smoke.
     (Already logged ED-060.)

3. **Green Gates That Don't Prove Their Claim:** the gauntlet caught a real path-traversal that units +
   ship-coverage all passed green.
   - **Class A:** the seed-provenance path-traversal regression fixture — DONE this sprint (security-fix
     0c802292, test 9→14).
   - **Class B (ADR):** make "a negative lie-fixture is required for every binding gate" a release-gate
     standard.

4. **Verify-Don't-Inherit:** the 12-day-stale epic tracker was wrong twice (populate-source.js
   "absent"; DoD#4 "unconfirmed").
   - **Class A:** extend tracker-reality-drift to run/verify DoD-named artifacts; require sprint-close to
     ground claims against disk + git + actual gate output.

5. **Stale Lead State / Destructive Watchdog Risk:** α acted on ~40-min-old state and nearly reverted
   live completed work (would have destroyed U3 + the security fix). ε held with live commit-chain
   evidence + a protective tag.
   - **Class A:** watchdogs MUST refresh HEAD + commit-chain + active checkpoints + dispatch leases +
     protective tags immediately before ANY revert/rollback recommendation. No revert from inherited state.

## Disposition
- Auto-apply candidates (Class A): loud timeout-clamp warning; gemini tier RCA signature; the
  path-traversal fixture (done); tracker-reality DoD-artifact expansion; watchdog current-state precheck.
- ADRs (Class B): the Dispatch Runtime Contract; provider-readiness/fallback policy; the
  negative-fixture-per-binding-gate standard.
- Escalate (Class C): none. (Restoring gemini via Antigravity is an operator/provider-account action.)

## ε note
ε does not auto-apply these (defers to α+β). The 3 dispatch-infra failure modes are captured in
runtime/sp-20260618-001/REAP-FIX-NOTE.md; the gemini gap is ED-060; the capsule-drift + BASELINE-debt
are in runtime/sp-20260618-001/HANDOFF-AND-WAIVER.md. Recommend α route the Class B Dispatch Runtime
Contract to E-DISPATCH-PERFECT-001 (the parked dispatch-reliability epic this is direct evidence for).
