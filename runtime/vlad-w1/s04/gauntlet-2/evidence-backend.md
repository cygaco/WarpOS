# GAUNTLET-2 (QUALIFYING) — lane `backend-reviewer` — VERBATIM RETURN

Commit under review `6a105f2`. In-process Agent spawn, synchronous, model opus. Conductor verified
`git status --porcelain -- engine/` EMPTY immediately after the lane returned.

Preamble as returned: "Tree clean, commit verified. Emitting my lane verdict."

```json
{
  "lane": "backend-reviewer",
  "verdict": "FAIL",
  "commit_reviewed": "6a105f2",
  "worktree_clean_at_finish": true,
  "s_criteria": {
    "S4-1": "FAILS",
    "S4-2": "FAILS",
    "S4-3": "holds",
    "S4-4": "holds",
    "S4-5": "holds",
    "S4-6": "cannot-assess"
  },
  "criteria_reasoning": {
    "S4-1": "FAILS on one newly-authored sentence. CUSTODY.md:40-43 states 'two escapes REMAIN, named here rather than left to be discovered later' and names exactly two (an unemphasised lead-in; a Cherokee/Armenian homoglyph). A third class is execution-proven: an ATX-heading-prefixed lead-in, which IS bolded and IS Latin-spelled, is silently skipped. resemblesBindableLeadIn('#### **Ceiling - x') returns null (SKIPPED) for '#', '##', '###', '####', '#####', and likewise for '|', '<p>', '<!-- c -->', '\"', '(', '[', 'Note: ' and '\\u2022'. Confirmed end-to-end against the real gate, not just the pure function: a CUSTODY.md carrying '#### **Ceiling - the battery probe claim.** Body sentence.' under P3 exits 0 with 'OK - ... 0 violations', while the byte-identical paragraph without the '####' exits 1 as unbound-paragraph. The sentence is NEW at this commit (git diff b9b8df3..6a105f2 -- engine/CUSTODY.md, line +47); its own preceding paragraph (CUSTODY.md:34-38) concedes the PREVIOUS version was false for exactly this reason ('naming two escapes. Three shapes ... shipped green anyway'). The fix replaced a false two-escape count with another false two-escape count. The anti-staleness pin at custody-claim-lint.test.js:1337 pins only the two named escapes, so nothing goes red. Established by reviewer read of shipped prose against the code, not by mechanism evidence. Aggravating: CUSTODY.md item (2) (lines 55-61) argues at length that count-form exhaustiveness claims are the failure mode and that widening matchers against them is refused - and bundle L1 dropped a different count from a different paragraph for exactly that reason, while leaving this one standing and false.",
    "S4-2": "FAILS on (c). (a) HOLDS - all seven R1 near-miss authorings RED and both em-dash controls RED, each row observed individually; controls fired FIRST and fired the correct DISTINCT rule (unbound-paragraph) versus the near-misses (unbindable-paragraph-shape), so the row is valid rather than trivially red. My first run was invalid and is reported rather than hidden: the probe body lacked a closing '**', so every row refused on lead-in shape; re-run with a properly closed lead-in. (b) HOLDS - observed by mutating the REAL source, not by reading the test title: replacing the resemblance call in extractBindableParagraphs with a bare skip turned 'RF-3 (lever)', 'RF-1: all seven NEAR-MISS...' and 'G-3: every markdown emphasis / block-prefix...' RED (75 tests, 72 pass, 3 fail, exit 1). Restored byte-exactly; suite back to 366/0. (c) FAILS - judged as a PROPERTY of the rendered form, not against a category list. Row 312 names markdown-emphasis canonicalization as one of the four dimensions; canonicalizeClaimText implements NONE of it. Emphasis canonicalization exists twice in the file (resemblesBindableLeadIn steps (b)/(d), and flattenForAssertionScan) but neither is on the status-token path, which runs the bare transform via containsStatusToken. Forms that render identically compare UNEQUAL: '**ASSERTED** - NOT VERIFIED', '*ASSERTED* - *NOT VERIFIED*', 'ASS**ERTED** - NOT VERIFIED', '_ASSERTED_ - NOT VERIFIED', 'ASSERTED - NOT **VERIFIED**' and 'ASSERTED&mdash;NOT VERIFIED' all ship GREEN planted in the Proven section, while the exact token and '**ASSERTED - NOT VERIFIED**' go RED. Verified through the real CLI: exit 0, 'OK - ... 0 violations'. Not a disclosed residual - CUSTODY.md:85-88 scopes markdown canonicalization to lead-in resemblance only, so the token-side gap is nowhere disclosed, and it is outside both disclosed residuals (comma separator; Cyrillic/Greek fold enumeration). The document's own house style for this token is bold (CUSTODY.md:280 'Status: **ASSERTED - NOT VERIFIED**'), so moving the bold to close around one word is a one-keystroke variant of the canonical authoring. (d) DISCHARGED by this lane - see findings and the battery report; controls first, controls fired.",
    "S4-3": "Holds. check:custody exit 0, reporting 15 bindable paragraphs derived from the document matched against 15 canonical copies. I checked that green means what it should rather than trusting it: RF-4 drives a realistic drift over EVERY derived paragraph (not a sample), carries a per-paragraph no-op guard, and asserts paragraph-not-verbatim naming the specific id; the reverse direction is observed RED at custody-claim-lint.test.js:717 under a genuine deletion of A8's line-wrapped document copy (with a guard against the wrap/unwrap no-op), firing bound-paragraph-missing. No shipped claim string diverges from its canonical copy at close. Recorded caveat: a green bind is compatible with both copies being wrong together - that is where my S4-1 finding sits, but 'two escapes REMAIN' lives in the preamble, which CUSTODY.md:13-15 explicitly lists as NOT bound, so it is not an S4-3 divergence.",
    "S4-4": "Holds. Read per-description, not by pass-total. RF-1: two control lead-ins plus RF1_NEAR_MISS_LEAD_INS holding literally seven entries - described count equals claimed count; both tests observed RED under my source revert. RF-3: a lever that re-implements the pre-fix derivation and asserts it goes green on all seven where the shipped one goes red, with an equality assertion on the count so the lever cannot silently stop pinning; observed RED under my revert. RF-4: both directions observed RED (15-paragraph sweep, and the A8 deletion). RF-5: builds a real mutant wrapper copy, guards that the mutation actually changed the text AND that the mutated copy really calls the caller's args.map, rewires relative imports to absolute src/ URLs with its own guard so an unloadable copy cannot masquerade as a RED, then runs a real child and reads observed argv. RF-6: locates the absorption loop and asserts it matches EXACTLY ONCE so a moved target fails loudly, guards the no-op, writes a mutant module, runs it in a child, and asserts a behavioural difference. RF-7: genuinely RUNTIME - real child process, a rebased CONTROL copy run first so the rebase cannot be what silences the mutant, mutant asserted to still start (server-started present) and to still CONTAIN the function definition (so text/AST inspection would stay green), redness asserted on the ABSENCE of the custody-selfcheck record, plus a section proving the check is not constant-true. Every mutant carries the no-op-implies-FAIL guard. No mutant found that fails to mutate, and none whose predicate cannot fail. One naming weakness, filed LOW and not firing the criterion: custody-claim-lint.test.js:1028 is titled 'RF-4 ATOMICITY, the other direction' but never observes a rule go RED - it asserts a proxy (the drifted canonical copy is not findable in the document). The real observation for that direction is a differently-named test 300 lines earlier.",
    "S4-5": "Holds, contributing not deciding. check:pointers exits 1 (RED BY DESIGN, not treated as a defect) and reports 34/48 resolved, 14 unresolved; selfcheck-runs-on-user-machine is NOT among the 14 RED entries, so the pointer resolves rather than classifying missing-name. RF-7 is RED at runtime by construction and by execution (see S4-4). The either-or sub-clause is satisfied by the FIRST option, not by silence: driver/host-free-driver.js:88-115 gives the driver entry's scrub call a runtime-observable consequence - it reads process.env immediately after the call, before any dynamic import below it and therefore before model-seam.js's own scrub could cover for it, and throws at load if a credential-shaped name survives. The header also states honestly what was true BEFORE (a neutered call was still fully scrubbed by a later module, so no end-state probe could tell the versions apart; what differed was a window nothing observed).",
    "S4-6": "cannot-assess - residual placement across build-spec items 1-7 is the prose lane's scope and I did not audit it. What I did verify inside my own scope: the class-form residual DOES ship as a Ceiling under P3 in strong, actionable form (CUSTODY.md:251-266, bound in BOUND_PARAGRAPHS and pinned by a test asserting it sits between the P3 and P4 headings); RT-2's NBSP tolerance is disclosed on the shipped surface (CUSTODY.md:95-96) and its control fires correctly (canonicalizeClaimText folds NBSP, asserted at custody-claim-lint.test.js:986); RT-8's rollup blindness is disclosed in CLASS form (CUSTODY.md:47-61) and I re-confirmed by execution that 'four of four claims verified' and 'every custody control verified' still ship GREEN while both controls ('4/4 claims verified', 'all custody controls verified') fire RED - matching the disclosure, so a re-confirmation and not a new finding."
  },
  "findings": [
    {
      "id": "F-1",
      "severity": "HIGH",
      "criterion": "S4-2",
      "file": "engine/scripts/checks/custody-claim-lint.js:244-255 (canonicalizeClaimText) and :954-956 (containsStatusToken)",
      "claim": "The rendered-form property fails for markdown emphasis on the status-token comparison: '**ASSERTED** - NOT VERIFIED' renders as the Asserted status token but compares unequal, so it ships GREEN inside the Proven section.",
      "execution_proven": true,
      "evidence": "containsStatusToken(x, 'ASSERTED \\u2014 NOT VERIFIED') -> RED for 'ASSERTED \\u2014 NOT VERIFIED' and '**ASSERTED \\u2014 NOT VERIFIED**'; GREEN for '**ASSERTED** \\u2014 NOT VERIFIED', '*ASSERTED* \\u2014 *NOT VERIFIED*', 'ASS**ERTED** \\u2014 NOT VERIFIED', '_ASSERTED_ \\u2014 NOT VERIFIED', 'ASSERTED \\u2014 NOT **VERIFIED**', 'ASSERTED&mdash;NOT VERIFIED'. End-to-end through the real gate: `node scripts/checks/custody-claim-lint.js <probe>` where the probe is the shipped CUSTODY.md with 'Status: **ASSERTED** \\u2014 NOT VERIFIED' inserted after line 266 (inside the Proven section, under P3) -> EXIT=0, 'custody-claim-lint: OK - 4 Proven claim(s) checked, 15 bindable paragraph(s) derived ... 0 violations.' The control probe (plain '**Ceiling \\u2014 ...' paragraph) on the same tree -> EXIT=1, unbound-paragraph. Baseline probe (unmodified doc) -> EXIT=0. The transform runs NFKD, default-ignorables, combining marks, confusables, dash class, whitespace and case - and no emphasis fold; the two emphasis folds that exist in this file (resemblesBindableLeadIn steps (b)/(d) and flattenForAssertionScan) are on other code paths.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-2",
      "severity": "HIGH",
      "criterion": "S4-1",
      "file": "engine/CUSTODY.md:40-43",
      "claim": "The newly-authored sentence 'two escapes REMAIN, named here rather than left to be discovered later' is false of the code at close - at least a third escape class exists (a bolded, Latin-spelled, ATX-heading-prefixed lead-in), and it is not the disclosed unemphasised-lead-in escape nor the disclosed Cherokee/Armenian homoglyph escape.",
      "execution_proven": true,
      "evidence": "resemblesBindableLeadIn returns null (SILENTLY SKIPPED) for '# **Ceiling \\u2014 x', '## **Ceiling \\u2014 x', '### **Ceiling \\u2014 x', '#### **Ceiling \\u2014 x', '##### **Ceiling \\u2014 x', '| **Ceiling \\u2014 x', '<p>**Ceiling \\u2014 x', '<!-- c --> **Ceiling \\u2014 x', '\"**Ceiling \\u2014 x', '(**Ceiling \\u2014 x', '[**Ceiling \\u2014 x', 'Note: **Ceiling \\u2014 x', '\\u2022 **Ceiling \\u2014 x' - while returning CANDIDATE for '**Ceiling \\u2014 x', '- **Ceiling \\u2014 x', '> **Ceiling \\u2014 x'. Full-gate confirmation: CUSTODY.md carrying '#### **Ceiling \\u2014 the battery probe claim.** Body sentence.' under P3 -> EXIT=0, '0 violations'; the byte-identical paragraph without the '####' -> EXIT=1, 'RED custody-claim-lint/unbound-paragraph: ceiling paragraph \"Ceiling \\u2014 the battery probe claim.\" (line 268) has NO canonical copy in BOUND_PARAGRAPHS'. The sentence is new at this commit: `git diff b9b8df3..6a105f2 -- engine/CUSTODY.md` shows '+But \"resembles\" is ITSELF a predicate, and two escapes REMAIN, named here rather than left to be discovered'. BLOCK_PREFIX (custody-claim-lint.js:612) strips only [-*+], \\d+[.)], > and whitespace; the function's own contract three lines below claims 'any line whose first token, after stripping markdown shape, is one of those keywords is a CANDIDATE' - a comment stronger than the code beneath it. The anti-staleness pin (custody-claim-lint.test.js:1337-1360) pins exactly the two named escapes, so a third cannot make anything go red.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-3",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/test/custody-claim-lint.test.js:1028",
      "claim": "The test titled 'RF-4 ATOMICITY, the other direction: a canonical copy that drifts from the document is RED' never observes a rule go RED - it asserts a proxy (that the drifted canonical text is not findable in the document).",
      "execution_proven": true,
      "evidence": "The test body calls findBoundParagraphPresenceViolations only on REAL_CUSTODY (asserting it is EMPTY, i.e. green) and then asserts !normalizeWhitespace(REAL_CUSTODY).includes(normalizeWhitespace(canonicalDrift)). No violation is ever observed firing in this test. The genuine RED observation for this direction exists at line 717 ('Rule 4b: DELETING a bound paragraph outright is RED'), which does fire bound-paragraph-missing naming A8. Coverage is present; only the naming is misleading, so this does not fire S4-4.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-4",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/scripts/checks/custody-claim-lint.js:938-943 (statusTokenPattern)",
      "claim": "The generated status-token pattern carries no word boundaries, so it can match across word interiors - a latent false-RED source in the over-folding direction.",
      "execution_proven": false,
      "evidence": "words.map(escapeRegex).join('[\\\\-:~|/\\\\s]*') produces /asserted[-:~|/\\s]*not[-:~|/\\s]*verified/ with no \\b anchors, so a hypothetical 'unasserted notverified...' substring would match. Reasoned from the source, not run against a real document, and the shipped CUSTODY.md is green - filed as a code-quality note in the over-folding direction the brief asked me to watch, not as a criterion finding.",
      "new_or_reconfirmation": "new"
    }
  ],
  "going_in_items_adjudicated": [
    { "item": 1, "judgement": "CONFIRMED and it recurred. The organising finding held for where I spent my read: the mechanisms in this fix attempt are strong (RF-1/3/4/5/6/7 all carry real mutants with real guards; the refuse-not-skip fix genuinely closes all seven original near-misses; bundle J's ban is structurally exempted with no suppression marker; bundle I's annotation is load-bearing and names its enforcer). What failed again is a SENTENCE - F-2 - and it failed in the same shape as its own predecessor, which the same paragraph documents as having been false for the same reason. Weighting my read toward prose paid off exactly as the item predicted." },
    { "item": 2, "judgement": "Accepted and acted on. I did not reason from excerpt windows: every claim I filed was re-run against the full function or the full gate, and I re-ran my own first R1 battery when I noticed its probe body lacked a closing '**' and every row was therefore refusing for the wrong reason. files_i_could_not_see is filled honestly below and my findings are scoped to what I actually read." },
    { "item": 3, "judgement": "Re-confirmations, checked against the code rather than accepted as present. Confusable fold IS an enumeration over Cyrillic+Greek and is disclosed as such; Cherokee/Armenian evade, pinned by the anti-staleness test. Separator variance IS closed except the comma; I re-ran 'ASSERTED, NOT VERIFIED' -> GREEN, matching the disclosure. NBSP tolerance real and disclosed. RT-8 rollup blindness real and disclosed in CLASS form; 'four of four claims verified' and 'every custody control verified' both GREEN while both controls fire RED. All four disclosures are TRUE, in CLASS form, and on the shipped surface - so all four are re-confirmations and none is a new finding. F-1 is NOT covered by any of them: it is an emphasis gap on the token comparison, and CUSTODY.md:85-88 scopes markdown canonicalization to lead-in resemblance only." },
    { "item": 4, "judgement": "Not filed as a defect and not touched. I confirmed the prose describing it is accurate on the mechanical points within my scope: spawn-env-allowlist.js scans the fixture clean (check:custody exit 0, 34 files scanned, 0 violations), and the P2 half-(b) Ceiling correctly describes it as scanned clean while launching a real child. Bundle J's ban does not over-refuse it into a false RED." },
    { "item": 5, "judgement": "Noted; nothing for me to adjudicate in code. The self-disclosed flag concerns a merge-guard interaction, not the artifact. I found no evidence of a reshaped command or a suppression marker anywhere in the change set - bundle J in particular explicitly refuses to add one (spawn-env-allowlist.js:275-283: 'There is DELIBERATELY NO SUPPRESSION MARKER'), which is the right call and is implemented as an early structural return on the wrapper path." }
  ],
  "regressions_from_the_fix_attempt": [
    "F-2 is a REGRESSION INTRODUCED BY THE FIX ATTEMPT. The sentence 'two escapes REMAIN' does not exist at b9b8df3; bundle G/L1 authored it at 6a105f2 to replace a predecessor sentence that was false for precisely the same reason (an incomplete count of escapes). The fix corrected the instance and re-committed the class, in the same paragraph that narrates the previous failure.",
    "F-1 is a gap in bundle G's own headline deliverable rather than a pre-existing defect: bundle G added emphasis canonicalization to the resemblance test and did not add it to the status-token comparison, which is the comparison S4-2(c) governs. The two sides were explicitly identified as sharing one defect (the code comment at resemblesBindableLeadIn step (0) says 'The status-token comparison and this resemblance test share one defect, so they must share one transform') - the shared transform was applied, but emphasis was implemented outside it, so only one side got it.",
    "No functional/runtime regression found: suite 366 pass / 0 fail / 0 skipped / 0 todo at exit 0, check:ship exit 0, check:custody exit 0, all re-verified after my mutations were restored.",
    "Bundle I: no regression. The Array.isArray gate annotation (src/spawn-shim.js:230-236 and :314-321) is in a form a future cleanup would respect - it states non-subsumption in BOTH directions with concrete counterexamples and names its enforcer (RF-I2). It is duplicated at both gates, so deleting either one meets the warning.",
    "Bundle J: no regression and no over-refusal. CREATE_REQUIRE_PATTERN is tested against comment-stripped content, test/ is outside the default scan, and the exemption is an early return on relPosix === 'src/spawn-shim.js' rather than a settable marker."
  ],
  "files_i_could_not_see": [
    "engine/test/spawn-shim.test.js - read only the RF-5 mutant-construction region plus greps; the RF-5a/b/c assertion bodies were not read in full",
    "engine/test/env-scrub-capture.test.js - read only the RF-6 mutant region plus greps",
    "engine/src/env-scrub.js - header comments and greps only; the executable body was not read line by line, so I did not independently verify the coercion is once rather than once-per-path",
    "engine/src/server-entry.js - greps and the RF-7-adjacent region only; runCustodySelfCheck's definition body was not read",
    "engine/test/custody-runtime.test.js - lines 140-380 only",
    "engine/scripts/checks/no-held-secret-in-surface.js, no-secret-on-outbound.js, branding-identity-enforcer.js, a5-wiring-presence.js, no-tautological-assertions.js, verified-by-resolver.js and scripts/checks/lib/* - not read; only their green exit codes observed",
    "engine/package.json, engine/ac-pointer-manifest.json, engine/port-references.json - not read",
    "engine/test/ - a5-wiring, branding, cancel, credential-custody-decoy, driver, entry-bootstrap, env-scrub, journal, lifecycle, mcp-surface, model-seam, no-tautological-assertions, permission, port-refs, quota, receipt, seam-boundary, ship-set, status, verified-by-resolver tests were not opened at all",
    "engine/test/custody-claim-lint.test.js - read lines 700-1060 and 1228-1380 of ~1560",
    "The prose-lane surfaces: P1-P4 body prose, the A5 commentary paragraphs, and RT-8's rollup residual wording were read only incidentally"
  ],
  "what_i_could_not_assess": [
    "S4-6 in full - I did not audit field-4 residuals for build-spec items 1-7 against the surface where each claim's reader is. A lane that reads the residual ledger against the shipped surface still owes this.",
    "S4-1 beyond the resemblance-ceiling paragraph - I did NOT verify the P1-P4 body prose, the A5 commentary, A6's SDK var list against the installed SDK, A7's rotation semantics against src/env-scrub.js's body, or A8's walker description against the walker. Any could carry a false sentence I did not look for, and CUSTODY.md:13-18 itself flags P1-P4 body prose as previously carrying 'three flat falsehoods'.",
    "Whether F-2's escape classes are exhaustive - I found at least thirteen skipped shapes but made no attempt to enumerate the class, deliberately: the point is that the count-form claim is false, not that I know the true count.",
    "The cost/performance question about bundle G's transform on large documents - not measured.",
    "Whether the 15 bindable paragraphs the lint derives is the complete set a reader would consider bindable - by construction it cannot be, given F-2, but I did not quantify it.",
    "RF-5 and RF-6 were judged from their mutant-construction discipline plus a green suite - I did not myself mutate the underlying source for those two the way I did for RF-1/RF-3."
  ]
}
```
