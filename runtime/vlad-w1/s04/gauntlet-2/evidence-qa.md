# GAUNTLET-2 (QUALIFYING) — lane `qa-reviewer` — VERBATIM RETURN

Commit under review `6a105f2`. In-process Agent spawn, synchronous, model opus. Worktree clean at finish
(conductor verified `git status --porcelain -- engine/` EMPTY immediately after the lane returned).

Preamble as returned: "Worktree clean at `6a105f2`. All probes ran against scratchpad copies — zero mutation of the tree."

```json
{
  "lane": "qa-reviewer",
  "verdict": "FAIL",
  "commit_reviewed": "6a105f2",
  "worktree_clean_at_finish": true,
  "s_criteria": {
    "S4-1": "FAILS",
    "S4-2": "cannot-assess",
    "S4-3": "holds",
    "S4-4": "holds",
    "S4-5": "cannot-assess",
    "S4-6": "holds"
  },
  "criteria_reasoning": {
    "S4-1": "FAILS on F-1, established by READ of the code and confirmed by execution against the real gate, never by a green mechanism. I read canonicalizeClaimText (scripts/checks/custody-claim-lint.js:244-255) and CONFUSABLE_FOLD (:213-233). The fold is a 56-entry character map. I enumerated its value set: it reaches only 36 of the 52 Latin skeleton letters — L R U b d f g h k l m n r t u z have NO fold entry in either script. Three shipped sentences describe this fold as covering Cyrillic and Greek as SCRIPTS, and all three are false of that code. The worst is that the keyword 'Ceiling' itself uses l, n and g, none of which the map reaches — so the narrowest statement of the fold's own scope ('the Latin letters used by this file's status tokens and resemblance keywords') is false too. The consequence is not cosmetic: a Ceiling lead-in spelled with Cyrillic U+04CF, Greek U+03B7 or Greek U+03C4 is returned as candidate=null by resemblesBindableLeadIn — SILENTLY SKIPPED, not refused — so an unbound claim paragraph ships with exit 0. That is the exact fail-open class bundle G was written to close; G closed it for the mapped letters and left it open for unmapped letters inside the same two scripts, while the prose says the two scripts are closed. Secondary truth defects F-2 (the 'two escapes REMAIN' enumeration is now false — a third escape class exists, unnamed and unpinned) and F-3 (the 'what is NOT bound, said plainly rather than generalised' enumeration omits the P1-P4 clause headings and the section preambles; inverting P2's heading into a flat falsehood ships GREEN). Per going-in item 3's own rule — 'a disclosure that is inaccurate, instance-form, or on the wrong surface is a NEW S4-1/S4-6 finding, not a re-confirmation' — F-1 is NEW, because the disclosed residual is 'scripts OUTSIDE the fold evade' and what I proved is that scripts INSIDE it evade. Positive controls fired: the plain-Latin authoring went RED (exit 1) and the mapped-Cyrillic authoring went RED (exit 1), so the run proves something about the near-misses.",
    "S4-2": "Not assigned to my lane for (a), (b) or (d), and I did not run the near-miss battery — S4-2(d) is owed by the lane whose section says so, and my lane section does not. On (c) I did read the mechanism and judge the PROPERTY as row 312 amended it: canonicalizeClaimText matches on the RENDERED FORM via NFKD compatibility decomposition, deletion of default-ignorable code points by Unicode property, deletion of combining marks, a confusable fold over the token alphabet, the dash class by \\p{Pd} property, whitespace collapse and optional case fold; resemblesBindableLeadIn additionally canonicalizes markdown block prefixes and emphasis runs before the keyword test. That is a property-based rendered-form transform, not an enumeration of the four observed variants, so (c) holds on my read. I record deliberately that F-1 is NOT an S4-2(c) failure: row 312 itself names 'confusable fold over the token alphabet' as part of the required property, and the enumeration is intrinsic to that fold. What F-1 breaks is the PROSE describing the enumeration's extent, which is S4-1's domain. Conflating the two would be inflation.",
    "S4-3": "Holds. The real lint against the real file exits 0 reporting '15 bindable paragraph(s) derived from the document and matched against 15 canonical copies byte-for-byte', so at close no shipped claim string diverges from its canonical copy. RF-4 is present in BOTH directions as described mutations — test/custody-claim-lint.test.js:1002 ('editing ANY shipped claim paragraph without moving its canonical copy is RED, for every paragraph in the document') and :1028 ('a canonical copy that drifts from the document is RED'). I independently observed the atomicity failure fire: mutating the bound P3 Ceiling paragraph produced exit 1 with custody-claim-lint/unbound-paragraph naming the paragraph and line. git diff b9b8df3..6a105f2 -- engine/test/ removed no test title and removed no assertion.",
    "S4-4": "Holds on the per-description count, read the way the discriminator demands rather than from the suite total. Reading test descriptions individually, all six of the required falsifier set are DESCRIBED as mutations: RF-1 (custody-claim-lint.test.js:794 controls-first, :811 all seven near-miss authorings, :832 no-over-refusal), RF-3 (:876 reverting the refusal to a bare continue), RF-4 (:1002 and :1028, both directions), RF-5 (spawn-shim.test.js:888 / :989 / :1065 as a/b/c), RF-6 (env-scrub-capture.test.js:254 plus :280 'RF-6 mutant proof: reverting ONLY the absorption loop ... destroys the mid-session value'), RF-7 (custody-runtime.test.js:235, 'deleting that invocation goes RED at RUNTIME (RF-7)'). Six described, six claimed — the counts agree. RF-2 is correctly ABSENT from the set. The no-op guard is present and widespread, with a real oracle: 'B2 no-op guard: this mutation must ACTUALLY change the text — a mutation that does not mutate proves nothing, and silently passes' appears on every text mutant I sampled (custody-claim-lint.test.js:139,151,165,193,204,218,220,262,273,287,289,307,318,336,347,358,397,418,430,442,454). No t.skip and no .todo were introduced by G-L, and the suite reports 0 skipped / 0 todo. I did independently re-observe RF-1's shape go RED under my own mutation. I did NOT independently re-run RF-5, RF-6 or RF-7 under mutation — see what_i_could_not_assess.",
    "S4-5": "Not my lane to decide, and I did not run the runtime RF-7 removal. What I did verify traceably: runCustodySelfCheck is defined at src/server-entry.js:415 and invoked at src/server-entry.js:456, one invocation; and the pointer RESOLVES — I ran scripts/checks/verified-by-resolver.js myself (exit 1, RED by design) and read its per-pointer output: selfcheck-runs-on-user-machine appears NOWHERE among the unresolved pointers it names, so it is no longer missing-name. I did not assess the either-or sub-clause about the driver entry's scrub, nor RF-7 going RED at runtime on removal of the invocation.",
    "S4-6": "Holds, and beta's explicitly-open question RESOLVES IN FAVOUR — the RT-8 disclosure names the CLASS, not the two instances. It is on the shipped surface (CUSTODY.md, item (2) of the header, lines 47-61) and carries all three required elements verbatim: (i) the named lexical family — 'The rollup rule matches a named lexical family: digit-form counts, and the word `all`.'; (ii) no detection of equivalents — 'It does not detect semantically equivalent rollups in ANY other wording'; (iii) no enumeration closes it and review is the control — 'no enumeration of wordings will close this, because the family of equivalent phrasings is unbounded. A rollup claim must be reviewed, not linted: human review is the actual control, and the linter never was that control.' It then explicitly REFUSES instance form: 'The residual is stated as the CLASS it is, deliberately WITHOUT a list of the wordings that happen to slip past today. Naming the instances we happened to observe would itself be a false disclosure: it reads as an enumerable gap when the gap is unbounded, and it invites the one repair that makes matters worse.' So the row-309 failure mode does not obtain. RT-2's NBSP tolerance also travels to the shipped surface where its reader is — CUSTODY.md:95-96, 'That transform's whitespace fold also tolerates a non-breaking space, which is not line-wrap whitespace. Named, not repaired: narrowing it would invite false REDs' — and is repeated at the fold's own definition in the shipped lint (fold (6), custody-claim-lint.js:149-154). The class-form residual ships as a Ceiling paragraph under P3 (CUSTODY.md:251-266) in strong actionable form, states that AC-8.6's landing does NOT make that control an exception, and gives the actionable reason ('Executed against an environment holding no credential it returns ok:true, so its green is equally consistent with a correct scrub, with a scrub that is ABSENT, and with a machine that never held a credential at all'); I confirmed it is bound, since mutating it goes RED. SCOPING, stated rather than left silent: I could not enumerate field-4 of build-spec items 1-7 because the build spec is not in my read scope, so 'every field-4 residual for items 1-7' is confirmed only for the residuals I could reach from the shipped surface. I also record a reading I decline to force: going-in item 3 says an inaccurate disclosure is 'a NEW S4-1/S4-6 finding', and F-1 is an inaccurate disclosure. I judge that F-1's defect is CONTENT, not TRAVEL — the confusable residual is on the correct shipped surface — so I fire S4-1 and not S4-6. If alpha reads item 3 as firing both, F-1 supports that reading; I did not stack it, because stacking a finding onto a second criterion to reinforce a terminal is the reshape the brief forbids in the inflating direction."
  },
  "findings": [
    {
      "id": "F-1",
      "severity": "HIGH",
      "criterion": "S4-1",
      "file": "engine/CUSTODY.md:76-83 (and CUSTODY.md:41-43; scripts/checks/custody-claim-lint.js:199-206)",
      "claim": "The shipped prose says the confusable fold covers Cyrillic and Greek as SCRIPTS and that the surviving evasion class is reachable only by a deliberate hand; in fact the fold is a 56-entry map reaching 36 of 52 Latin letters, and Cyrillic and Greek homoglyphs of the unmapped letters ship an unbound claim paragraph GREEN.",
      "execution_proven": true,
      "evidence": "VERBATIM SHIPPED PROSE, CUSTODY.md:76-83: 'the confusable fold is an ENUMERATION over Cyrillic and Greek, because Unicode exposes no confusable property to lean on. Armenian, Cherokee, Coptic, Deseret and Lisu lookalikes therefore still evade it; a Cherokee and an Armenian spelling of the `Ceiling` keyword were each observed GREEN. The two scripts the fold DOES cover are the two a homoglyph reaches BY MISTAKE — a paste from mixed-script text, an editor substitution, an identifier copied out of a document that was never all-Latin — whereas a keyword spelled in Armenian, Cherokee, Coptic, Deseret or Lisu is essentially only reachable by someone doing it ON PURPOSE. Read the split that way: the fold is calibrated against accident, and what remains is a class reachable by a deliberate hand'. CUSTODY.md:41-43: 'the homoglyph fold is an enumeration over Cyrillic and Greek only, so a keyword spelled with a Cherokee or Armenian lookalike still passes'. Shipped enforcer comment, custody-claim-lint.js:199-206: 'Covers the Cyrillic and Greek homoglyphs of the Latin letters used by this file's status tokens and resemblance keywords' ... 'this fold closes exactly the two scripts listed and no others'. WHAT THE CODE DOES — read of CONFUSABLE_FOLD (custody-claim-lint.js:213-233), 56 entries; Latin skeleton letters actually reached: ABCDEFGHIJKMNOPQSTVWXYZaceijopqsvwxy. Letters with NO entry in EITHER script: L R U b d f g h k l m n r t u z. The keyword 'Ceiling' needs l, n, g — none is reachable; 'Asserted' needs r, t, d — none is reachable. UNIT EXECUTION (node, importing the real module): resemblesBindableLeadIn('**Ceiling — x') => 'Ceiling'; ('**C<U+0435>iling — x') => 'Ceiling'; ('**Cei<U+04CF>ing — x') => null; ('**Ceili<U+03B7>g — x') => null; ('**Asser<U+03C4>ed — x') => null. canonical rendering is identity on the evading chars: '**Cei\\u04CFing - x', '**Ceili\\u03B7g - x', '**Asser\\u03C4ed - x'. END-TO-END AGAINST THE REAL GATE (node scripts/checks/custody-claim-lint.js <copy>, one new unbound Ceiling paragraph inserted, em dash, column 1) — CONTROLS FIRST: plain Latin => EXIT=1, 'RED custody-claim-lint/unbound-paragraph: ceiling paragraph ... has NO canonical copy in BOUND_PARAGRAPHS'; Cyrillic U+0435 (IN the map) => EXIT=1, 'RED custody-claim-lint/unbindable-paragraph-shape: line 268 RESEMBLES a bindable paragraph lead-in (keyword \"Ceiling\") ... Refused by name rather than skipped'. THEN THE PROBES: Cyrillic PALOCHKA U+04CF => EXIT=0, 'custody-claim-lint: OK — 4 Proven claim(s) checked, 15 bindable paragraph(s) derived ... 0 violations.'; Greek ETA U+03B7 => EXIT=0, same OK line; Greek TAU U+03C4 in 'Asserted' => EXIT=0, same OK line. Controls fired, so the run proves something. The calibration sentence is the load-bearing falsity: Greek ETA is reached by 'a paste from mixed-script text' — the accident vector the paragraph itself names — so the mistake-reachable class is NOT closed and the remainder is NOT attacker-only. Bundle L1 authored the calibration wording and bundle G authored the 'exactly the two scripts listed and no others' comment, so this is prose the fix attempt itself wrote.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-2",
      "severity": "MEDIUM",
      "criterion": "S4-1",
      "file": "engine/CUSTODY.md:40-44",
      "claim": "The header states that exactly two escapes remain from the resemblance check and that both are pinned by an anti-staleness test, but a third escape class exists — an in-script Cyrillic/Greek homoglyph of an unmapped letter — which is named nowhere and pinned by nothing.",
      "execution_proven": true,
      "evidence": "VERBATIM, CUSTODY.md:40-44: 'But \"resembles\" is ITSELF a predicate, and two escapes REMAIN, named here rather than left to be discovered later: a lead-in carrying NO markdown emphasis at all is still invisible to it, and the homoglyph fold is an enumeration over Cyrillic and Greek only, so a keyword spelled with a Cherokee or Armenian lookalike still passes. Both escapes are held by a residual test that FAILS if either is ever closed, so this disclosure cannot go stale without something going red.' The residual test is test/custody-claim-lint.test.js:1337-1359, titled 'G-3 DISCLOSED RESIDUAL (pinned, not merely claimed): an UNEMPHASISED lead-in, and a homoglyph from a script outside the Cyrillic/Greek fold, are STILL invisible'. Its oracle is real (assert.equal(resemblesBindableLeadIn(variant), null)) and its failure message is correctly ordered ('update CUSTODY.md's header FIRST ... then update this assertion'), so it is not a no-op. But its population is exactly two out-of-script variants — Cherokee U+13CF and Armenian U+0578 — and its own message repeats the false premise, 'the \"Cyrillic and Greek only\" ceiling ... is now STALE'. The third escape proven under F-1 (Cyrillic U+04CF, Greek U+03B7, Greek U+03C4, all EXIT=0 against the real gate) is therefore not among the 'two escapes' the sentence enumerates, and nothing goes red for it. The clause 'this disclosure cannot go stale without something going red' is false in the direction that matters: the disclosure is already inaccurate and the whole suite is green (366 pass / 0 fail).",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-3",
      "severity": "MEDIUM",
      "criterion": "S4-1",
      "file": "engine/CUSTODY.md:12-19",
      "claim": "The what-is/is-NOT-bound block promises to say plainly what is not bound and then enumerates, but the enumeration omits the P1-P4 clause HEADINGS (which carry substantive claim sentences) and the '## Proven' / '## Asserted' section preambles.",
      "execution_proven": true,
      "evidence": "VERBATIM, CUSTODY.md:12-19: 'What is NOT bound byte-for-byte, said plainly rather than generalised: prose that is neither an Asserted nor a Ceiling paragraph — this preamble, the three numbered limits-of-this-checker paragraphs below (including (3)'s description of `canonicalizeClaimText` itself), the P1–P4 status/enforcer/proof-scope lines, the P1–P4 BODY PROSE ..., A1's `Live measurement` follow-on paragraph, and the commentary paragraphs around A5'. Headings and section preambles appear nowhere in that list. EXECUTION against the real gate, each mutant its own run: (a) P1 BODY PROSE mutated to a flat falsehood ('Scans every file on the machine, at runtime, continuously, and proves absence in general.') => EXIT=0 — this one is CORRECTLY disclosed, and confirms the body-prose half of the disclosure is TRUE; (b) P3's Ceiling mutated to its inversion ('this package fully verifies that all its controls are invoked in YOUR install') => EXIT=1, RED custody-claim-lint/unbound-paragraph — the user-relied disclosure inversion IS caught, so the predecessor's exact failure is closed; (c) P2's HEADING replaced with a flat falsehood far stronger than anything the document proves — '### P2 — Every child process launched by this package is proven to carry no credential of any kind, by exhaustive runtime verification' => EXIT=0, 'OK — 4 Proven claim(s) checked ... 0 violations'; (d) the '## Proven' section preamble mutated to 'Each item below has been independently audited and certified complete by a third party' => EXIT=0. MITIGATION I record rather than omit: the sentence's leading class clause, 'prose that is neither an Asserted nor a Ceiling paragraph', IS true and does cover headings and preambles. The defect is in the enumeration the sentence commits to when it says 'said plainly rather than generalised'. I grade this MEDIUM for that reason and do not treat it as independently criterion-firing.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-4",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/CUSTODY.md:66-67",
      "claim": "The transform paragraph enumerates the dash fold's inputs as \\p{Pd} plus the mathematical minus sign, but the code also folds U+30FC PROLONGED SOUND MARK, which is named only in an internal comment.",
      "execution_proven": true,
      "evidence": "CUSTODY.md:66-67 lists, in order, 'the Unicode dash class `\\p{Pd}` plus the mathematical minus sign folded to a single hyphen'. The code at custody-claim-lint.js:183 is DASH_CLASS_PATTERN = /[\\p{Pd}\\u2212\\u30FC]/gu — three inputs, not two. The internal JSDoc at :178-182 does name it ('plus U+30FC PROLONGED SOUND MARK — visually a horizontal bar but categorised Lm, so like U+2212 it must be named separately'), but the shipped user-facing paragraph does not. Graded LOW deliberately: the direction is OVER-coverage, so the fold catches more than the prose advertises and no control is overstated. Recorded because the paragraph is an ordered enumeration of the transform's steps and K's bundle exists to make that enumeration match the code.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-5",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/CUSTODY.md:47",
      "claim": "The rollup residual describes the matched family as 'digit-form counts, and the word `all`', but the worded branch requires a three-part collocation, so the description overstates what the rule catches.",
      "execution_proven": false,
      "evidence": "CUSTODY.md:47 reads 'The rollup rule matches a named lexical family: digit-form counts, and the word `all`.' The code is WORDED_ROLLUP_PATTERN = /\\ball\\b[^.\\n]{0,40}\\b(?:custody\\s+)?(?:controls?|claims?)\\b[^.\\n]{0,40}\\bverified\\b/i (custody-claim-lint.js:982) — 'all' alone never fires; it needs 'all' then controls|claims within 40 chars then 'verified' within 40 more. AGGREGATE_COUNT_PATTERN (:975) is likewise a collocation, not bare digits. Graded LOW and explicitly NOT criterion-firing: the surrounding paragraph immediately and correctly states that the rule detects no other wording, that no enumeration closes the class, and that human review is the control, so a reader is not left over-trusting the mechanism. Reasoned from the source, not executed.",
      "new_or_reconfirmation": "new"
    }
  ],
  "going_in_items_adjudicated": [
    { "item": 1, "judgement": "CONFIRMED, and it directed my read. I spent my budget on shipped SENTENCES rather than on re-probing mechanisms, and the decisive finding (F-1) is again a false sentence rather than a broken mechanism — the transform mechanism does exactly what row 312 asked; the paragraphs describing its extent do not. The pattern the item names held for a seventh instance." },
    { "item": 2, "judgement": "ACCEPTED and acted on. I did not reason F-1 from an excerpt window: I imported the real module, enumerated the real CONFUSABLE_FOLD value set, and then ran the real CLI end to end with controls first. Both controls fired RED before I read any probe as GREEN. files_i_could_not_see is filled honestly below and none of my findings depends on a region I could not open." },
    { "item": 3, "judgement": "PARTLY OVERTURNED — this is my central disagreement. Three of the four disclosed residuals check out against the code: RT-2's NBSP tolerance is true and on the shipped surface; the separator/comma residual is true; RT-8's rollup blindness is true and, decisively, in CLASS form. But the fourth — 'the transform's confusable fold is an ENUMERATION over a named alphabet set, and scripts outside it evade' — is NOT what the shipped disclosure says and not what the code does. The disclosure says the fold covers the two scripts and that only OUT-of-script lookalikes evade; the code lets IN-script Cyrillic and Greek lookalikes of unmapped letters evade, execution-proven GREEN. By this item's own rule — a residual counts as a re-confirmation ONLY if its shipped disclosure is TRUE and in CLASS form — that disclosure is inaccurate, so F-1 is a NEW finding, not a re-confirmation. Disclosure did not buy silence here, and checking the disclosure against the code rather than accepting that it exists is what surfaced it." },
    { "item": 4, "judgement": "ACCEPTED. I did not file the EXPECTED-BYPASS witness as a defect and did not touch it. I did judge the prose describing it: CUSTODY.md:143-161 calls it 'not a conceivable route but an EXECUTION-PROVEN one', names the commit ff6d483 and the fixture path, and explicitly re-points off the now-closed aliased-requirer example with the reason given. That prose is accurate to the fixture's described role and correctly says placeholder, not credential. No finding." },
    { "item": 5, "judgement": "ACCEPTED as recorded, no finding. The self-disclosed process flag is outside my scope contract (WarpOS is read-only to me and I did not open it). The behaviour described is the correct shape, and I note only that I could not independently verify the 83-ref figure because that tree is outside my read scope." }
  ],
  "regressions_from_the_fix_attempt": [
    "F-1: bundle L1's calibration wording (CUSTODY.md:78-83) is a NEW false sentence authored by the fix attempt — it asserts the mistake-reachable class is closed when a Greek homoglyph reached by the paragraph's own named accident vector ('a paste from mixed-script text') evades with exit 0.",
    "F-1: bundle G's own enforcer comment ('this fold closes exactly the two scripts listed and no others', custody-claim-lint.js:205-206) is a NEW false sentence on a SHIPPED file, authored alongside the widening it describes.",
    "F-2: the 'two escapes REMAIN' enumeration and its companion 'this disclosure cannot go stale without something going red' were carried forward and restated while a third escape class was created-visible by the same bundle's own widening, and neither the prose nor the G-3 residual pin covers it.",
    "No regression in the test surface: git diff b9b8df3..6a105f2 -- engine/test/ removed zero test titles and zero assertions, and introduced zero t.skip / .todo.",
    "No regression in count discipline: bundle H's count removal holds, and I re-derived every count-shaped claim that remains rather than trusting it — A1's '109 transitive packages' matches package-lock.json, A1's 'two complete HTTP server stacks' matches, the opts.cwd/opts.stdio Ceiling's 'only two occurrences' matches exactly (src/spawn-shim.js:548, :550), and A6's four SDK vars are indeed absent from SECRET_SHAPES.",
    "No regression in H's class removal: every survivor of 'the only' / 'nowhere else' / 'sole' / 'exclusively' is either the lint's own sanctioned reword for a different class or an unrelated non-disclosure use."
  ],
  "files_i_could_not_see": [
    "The S-VLADW1-04 build spec and its field-4 residual entries for items 1-7 — outside my scope contract, so S4-6's 'every field-4 residual in items 1-7' is confirmed by me only for residuals reachable from the shipped surface.",
    "The round record / gauntlet artifacts for this build other than my own lane brief.",
    "beta rows 309, 310 and 312 in full text — I worked from the brief's restatement of them.",
    "Everything under WarpOS except my own brief — so I could not verify going-in item 5's 83 pre-existing broken .claude/ doc refs.",
    "engine/test/entry-bootstrap.test.js — targeted greps only, not read in full.",
    "The predecessor sprint's RT-numbered record-trust gate and its artifacts."
  ],
  "what_i_could_not_assess": [
    "S4-2(a), (b) and (d): I did not run the near-miss battery. My probes exercised RF-1's shape incidentally but that is NOT a discharge of S4-2(d) and must not be counted as one.",
    "S4-4 completeness: I did NOT independently mutate the product code and re-observe RED for RF-5, RF-6 or RF-7. For those three I am relying on the tests' own described mutations, which is weaker than observation. Only RF-1's and RF-4's REDs did I reproduce myself.",
    "S4-5 in full: I did NOT verify RF-7 goes RED at RUNTIME rather than at text/AST level on removal of the invocation, and I did NOT assess the either-or sub-clause about the driver entry's scrub.",
    "S4-6 for build-spec items 1-7: could not enumerate field-4. The items 1-7 enumeration is genuinely unassessed by me and should not be read as a pass.",
    "The P3 preload Ceiling's claims about entry-bootstrap.test.js: four sub-claims verified by targeted grep; I did NOT read the file's ceiling note in full to judge whether the --import usage is genuinely 'a live demonstration'.",
    "Whether a Cyrillic or Greek homoglyph exists for every one of the 16 unreached Latin letters. I proved three concrete evasions, which is sufficient to falsify the shipped sentences; I did not characterise the full size of the undisclosed escape class.",
    "The non-CUSTODY shipped files' prose: src/env-scrub.js, src/model-seam.js, driver/host-free-driver.js and src/server-entry.js carry substantial custody prose in comments that I sampled rather than read end to end. A false sentence could be sitting in those regions and I would not have seen it."
  ]
}
```
