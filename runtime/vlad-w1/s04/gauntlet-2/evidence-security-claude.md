# GAUNTLET-2 (QUALIFYING) — lane `security_claude_hunter` — VERBATIM RETURN

Commit under review `6a105f2`. In-process Agent spawn, synchronous, model opus. Conductor verified
`git status --porcelain -- engine/` EMPTY immediately after the lane returned.

Preamble as returned: "Tree verified clean, commit `6a105f2`, scratch artifacts removed. Assembling the verdict."
Key artifacts it named: `custody-claim-lint.js` `canonicalizeClaimText` (line 244) omits the emphasis fold that
`flattenForAssertionScan` (line 1356) already performs one function away; `src/spawn-shim.js` RESIDUAL(LOW)
header block, lines 133-149.

```json
{
  "lane": "security_claude_hunter",
  "verdict": "FAIL",
  "commit_reviewed": "6a105f2",
  "worktree_clean_at_finish": true,
  "s_criteria": {
    "S4-1": "holds",
    "S4-2": "FAILS",
    "S4-3": "holds",
    "S4-4": "holds",
    "S4-5": "holds",
    "S4-6": "cannot-assess"
  },
  "criteria_reasoning": {
    "S4-1": "Established by READER READ of shipped prose against code, never by a green gate. Verified TRUE: (a) the P2 half-(b) Ceiling — both entries hold EXACTLY ONE static import (grep returns exactly one line each: ./bootstrap.js and ../src/bootstrap.js), src/bootstrap.js is a single-specifier re-export of env-scrub.js, and initCredentialCustody(CREDENTIAL_ENV_NAMES) is the GENUINE first statement in both (server-entry.js:107, driver:128); (b) the node: builtins Ceiling — every node: builtin in either entry is reached by dynamic await import of a string literal, never a static import, exactly as claimed; (c) the argv Ceiling — auditedSpawn Check 3 does refuse a secret-shaped argv value (execution-proven, see F-3 evidence); (d) the opts.cwd/stdio Ceiling — 'the only two occurrences of either identifier in this function are those pass-through lines' is TRUE (spawn-shim.js:548,550), and I re-observed the cwd pass-through with a real child; (e) the AC-8.6 Ceiling — runCustodySelfCheck is defined (server-entry.js:415) and invoked ONCE from startServer (456), and the driver does spawn that file as a child (driver:340 auditedSpawn(process.execPath,[SERVER_ENTRY])), so 'a shipped path' is true; (f) the class-form Ceiling — runCustodySelfCheck genuinely reads an OUTCOME (hasOwnProperty over env, 415-436), so 'it verifies NEITHER the presence nor the passing of any control' is accurate; (g) the preload Ceiling — package.json#files is [src/, scripts/, driver/, test/credential-custody-decoy.test.js, CUSTODY.md], so 'ships exactly one test' is TRUE; (h) all NINE commit SHAs cited in bound paragraphs resolve to real commits with consistent subjects (ff6d483, e120edc, 0f50457, 8b6993e, 5ecda24, b27ad76, 0732cd8, 514c2d9, 55fc6a3); (i) the J witness prose — 'is scanned clean by this enforcer' is TRUE by execution (scanSpawnSurface over the fixture dir: ok:true, 1 file, 0 violations) and 'the aliased-requirer route has since been closed' is TRUE (the J-ban-works fixture fires create-require-banned); (j) the createRequire residual sentence ('occurred in the scanned tree ONLY inside comments, in src/model-seam.js and strip-comments.js') is TRUE given the scanner's own SELF_REL_PATH exclusion — I nearly filed this as false from an incomplete window and the self-exclusion at line 934 refutes it; (k) the CUSTODY.md transform disclosure (lines 63-96) is TRUE and, critically, CORRECTLY SCOPED — it says markdown canonicalization applies 'before a lead-in is tested for resemblance' and describes the token comparison separately as separator-variance only, so it does NOT falsely claim the emphasis fold my F-1 defeats. ONE false sentence found: F-2, spawn-shim.js's own worked example of its split-secret residual, refuted by execution. I mark S4-1 'holds' rather than FAILS because F-2 understates the control (fail-safe direction) and the CLASS claim around it remains true; alpha should weigh F-2 against the rule text, since the rule says 'every custody claim string ... is TRUE' without a fail-safe exception.",
    "S4-2": "FAILS on (c). (a) DISCHARGED: I re-ran RF-1's population myself — CONTROLS FIRST, both em-dash authorings went RED as unbindable-paragraph-shape, then all SEVEN R1 near-miss authorings went RED (en-dash, hyphen, U+2212 minus, colon, two-space indent, '- ' list prefix, '> ' blockquote prefix). 21/21 RED including 12 further bundle-G variants (emphasis-closed, emphasis+colon, ZWSP-in-keyword, Cyrillic-e, nested '- > ', ordered '1. ', underscore emphasis, NBSP separator, A9 en-dash, A9 ZWSP, fullwidth colon, 'Asserted' keyword). (b) RF-3 is present and committed as an explicit lever and passes in the green suite. (c) FAILS — judged as a PROPERTY per row 312, not against any fold list. The rendered form is matched on 26 of 27 dimensions I drove (NFKD, default-ignorable ZWSP/ZWNJ/soft-hyphen/BOM/word-joiner, combining marks, Cyrillic+Greek confusables, fullwidth, mathematical alphanumerics, the whole \\p{Pd} dash class + U+2212, NBSP, and the full separator-variance family incl. the EMPTY run) — but NOT on the markdown-emphasis dimension, which row 312 names explicitly as one of the four components of the property. See F-1: a status-token near-miss ships GREEN with CLI exit 0. (d) DISCHARGED BY THIS LANE, not by epsilon, against the predicate AS BUILT at 6a105f2; controls fired first, so the run is informative rather than 'cannot-assess'; population included bundle A's class-form paragraph SHAPE plus ZWSP/homoglyph/emphasis/prefix variants as the standing obligation requires.",
    "S4-3": "Holds on what is observable at close. Executed: node scripts/checks/custody-claim-lint.js CUSTODY.md exits 0 reporting '15 bindable paragraph(s) derived from the document and matched against 15 canonical copies byte-for-byte' — so NO shipped claim string diverges from its canonical copy at close, which is the criterion's second half. RF-4 is present in BOTH directions as committed test nodes and both pass in the 366-green suite. I also proved the derivation direction independently: every paragraph I injected produced an unbound/unbindable RED, so nothing new ships unbound. What I could NOT verify is the process half — that RF-4 fired 'inside the owning bundle's own run, not at gauntlet' — that is a claim about bundle-run history I have no artifact for.",
    "S4-4": "Holds. All six of the required set are present and committed, and the suite is green at this commit (366 pass / 0 fail / 0 skipped / 0 todo, exit 0 — no NO_DATA, no t.skip()). Per the discriminator I read per-test descriptions rather than trusting the pass total: RF-1 (three nodes — controls-first, all-seven-near-misses, plus a NO-OVER-REFUSAL node), RF-3 (explicit lever), RF-4 (both directions), RF-5a/5b/5c (5a and 5b each state 'proven RED against a real MUTANT'; 5c is honestly self-labelled CHARACTERIZATION, not a lever — so RF-5's observed-RED comes from 5a/5b, not from 5c), RF-6 (plus a separate explicit 'RF-6 mutant proof ... load-bearing rather than decorative' node), RF-7 (read in full: it executes a REAL child process against the shipped entry, then a mutated copy, and — this is the no-op guard — runs a CONTROL COPY carrying the specifier rebase and nothing else first, so a silenced mutant cannot be attributed to the rebase). RF-2 is correctly ABSENT from this set. I independently re-derived RF-1/RF-3's subject matter with my own battery and RF-5/RF-6's with my own live-child door probe rather than trusting the descriptions.",
    "S4-5": "Holds, including the either-or. (1) Invoked from the product-layer entry path: runCustodySelfCheck() is called once from startServer() (server-entry.js:456), startServer runs under the isMain block (514), and the driver launches that file as a real child through auditedSpawn (driver:340) — so it is a shipped path, capped at one invocation and one named test. (2) RF-7 is RED AT RUNTIME, not text/AST: the test spawns the mutated entry as an actual child and asserts the custody-selfcheck record is ABSENT from its stderr while the server still starts — 'red because the check did not RUN, not because a string or an AST node went missing' — and it plants every credential-shaped name in the child env so the ok:true report is non-vacuous. (3) check:pointers exits 1 BY DESIGN with 14 unresolved, and selfcheck-runs-on-user-machine is NOT among them — I read all 14 RED lines and every one names a different node. So it resolves rather than missing-NAME. (4) THE SUB-CLAUSE: the FIRST arm is satisfied, not by silence — the driver entry's scrub gained a real runtime-observable consequence at driver/host-free-driver.js:129-141, which re-reads process.env immediately after its own initCredentialCustody() call and THROWS ('Refusing to continue — this process must not reach its dependency graph, or spawn a child, with a credential still live in its environment') if any credential-shaped name survived. That is a runtime consequence, not a text/AST assertion, and it carries a named standing enforcer.",
    "S4-6": "cannot-assess, honestly — and this is a read-scope limit, not a pass. I CAN attest the half I could see: both design-phase findings deliberately NOT fixed appear at close on the SHIPPED surface in CLASS form — RT-2's NBSP tolerance at CUSTODY.md:95-96, and RT-8's rollup blindness at CUSTODY.md:47-54 stated as THE CLASS with an explicit refusal to enumerate instances, which is the strong, actionable form the criterion asks for. The class-form residual does ship as a Ceiling paragraph under P3 and is bound. Bundle L2's count-form exhaustiveness disclosure is on the shipped surface too (CUSTODY.md:58-59), in class form. What I CANNOT assess: 'every field-4 residual in build-spec items 1-7' — I never had the build spec, so I cannot enumerate that population or confirm each member travelled. Marking this holds on two-thirds of the evidence would be exactly the rounding the run forbids."
  },
  "findings": [
    {
      "id": "F-1",
      "severity": "HIGH",
      "criterion": "S4-2",
      "file": "engine/scripts/checks/custody-claim-lint.js:244",
      "claim": "The named canonical transform does NOT canonicalize markdown emphasis, so an Asserted status token with emphasis applied to ONE half of it sits in the ## Proven section and the lint exits 0 — the Proven/Asserted conflation Rule 3 exists to prevent, shipping green.",
      "execution_proven": true,
      "evidence": "Injected `### P1 custody — **ASSERTED** — NOT VERIFIED` into the ## Proven section of a copy of the shipped CUSTODY.md and ran the real CLI: 'custody-claim-lint: OK — 5 Proven claim(s) checked, 15 bindable paragraph(s) ... 0 violations.' / CLI EXIT=0. Control, same CLI on the unmodified shipped doc: '4 Proven claim(s) checked ... 0 violations' / EXIT=0 — so the injected line was COUNTED as a fifth Proven claim (it is P1-tagged, satisfying Rule 1) while Rule 3 never saw the Asserted token. Library-level, three further shapes ship fully GREEN: 'ASSERTED — **NOT VERIFIED**', '`ASSERTED` — NOT VERIFIED', and 'ASSERTED **—** NOT VERIFIED'. Mechanism: canonicalizeClaimText applies folds NFKD / default-ignorable / combining-mark / confusable / dash-class / whitespace / case, but never touches * _ `; SEPARATOR_VARIANCE is [\\-:~|/\\s]*, which does not admit *, so the rendered form '**asserted** - not verified' does not match the token pattern. THIS IS NOT WITHIN ANY DISCLOSED CEILING: CUSTODY.md:89-91 names comma, semicolon and period as the deliberately-excluded separators and names Armenian/Cherokee/Coptic/Deseret/Lisu as the surviving homoglyphs; emphasis interposed inside a multi-word token is disclosed nowhere. WHICH CLAIM THIS IS: this proves THE CONTROL IS DEFEATABLE — it does NOT prove the package leaks a credential; no credential is involved and no shipped file currently carries the conflation. But the defeated control is the one S4-2(c) governs, and the target document IS the shipped surface, so unlike a bypass needing a non-shipping probe file, the conflated text would ship inside CUSTODY.md itself. MISTAKE-REACHABLE, NOT ATTACKER-ONLY: the shipped document's own authoring convention bolds status tokens — CUSTODY.md:107 is '**PROVEN** — the claim is bounded by...' and CUSTODY.md:280 is 'Status: **ASSERTED — NOT VERIFIED**.' (the whole-token bolding at 280 IS caught; bolding one half is the natural neighbouring authoring and is not). The codebase already owns the missing fold one function away: flattenForAssertionScan (line 1356) does canonicalizeClaimText(text).replace(/[*_`~]+/g, ' ') for the only-surface rule, with a doc comment reasoning explicitly about '**only**place' evading — so emphasis evasion was understood and simply not wired into the transform every status-token comparison runs.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-2",
      "severity": "MEDIUM",
      "criterion": "S4-1",
      "file": "engine/src/spawn-shim.js:133-149",
      "claim": "The shipped RESIDUAL(LOW) block's own worked example of the split-secret bypass is false: the half it offers as 'separately-benign' is itself refused by Check 2, so the example it gives does NOT pass every check unrefused.",
      "execution_proven": true,
      "evidence": "The header states: 'A secret split across TWO separately-benign-named env values — cut before the shape matcher's own recognisable prefix, e.g. PART_A=\"sk-ant-\" + PART_B=\"<rest of the real key>\" — matches no single shape on either half, so it passes every check above unrefused.' Executed that exact env against the real wrapper with a labelled placeholder: auditedSpawn(node, [...], {env:{PATH, PART_A:'sk-ant-', PART_B:'PLACEHOLDER-NOT-A-REAL-KEY-0000'}}) was REFUSED — 'spawn-shim: refusing to spawn ... its env carries a secret-SHAPED VALUE under key(s) not on the denylisted-name list'. Cause: the api-key shape is ^sk-ant-(?!oat); buildSecretSearchPattern strips the ^ anchor, and at the position after 'sk-ant-' the negative lookahead (?!oat) SUCCEEDS against end-of-string, so PART_A alone matches. The CLASS claim (a secret split so that neither half matches any shape) remains true and the residual is real; what is false is the specific 'e.g.' the shipped comment offers as its instance, which the code actually catches. Direction of the error is FAIL-SAFE — it understates the control — so this is not a leak, and I am not inflating it to a HIGH. It is filed because src/ SHIPS (package.json#files) and this is a sentence about a control on a shipped surface, which is exactly the class S4-1 governs and the class every one of gauntlet-1's six HIGHs belonged to. Fix is to re-cut the example (e.g. PART_A='sk-' + PART_B='ant-...'), not to change the mechanism.",
      "new_or_reconfirmation": "new"
    },
    {
      "id": "F-3",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/scripts/checks/spawn-env-allowlist.js:284",
      "claim": "Bundle J's createRequire ban is defeated by a computed member key on node:module — a CONTROL defeat that is already disclosed as a class by the enforcer's own header, and that reaches no shipped path.",
      "execution_proven": true,
      "evidence": "findRawLaunchViolations(\"import * as m from 'node:module';\\nconst f = m['create'+'Require'];\\nconst r = f(import.meta.url);\\n\", 'scripts/y.js') returned [] — zero violations. Controls: the direct form fires (['spawn-env-allowlist/create-require-banned']) and so does the committed J-ban-works fixture, so the ban itself works on the route it names. RE-CONFIRMATION, NOT NEW: the enforcer's own ceiling block already states 'COMPUTED MEMBER ACCESS ... invisible to it BY CONSTRUCTION — not by oversight, and not fixable by adding patterns' and 'the ban CLOSES ONE ROUTE, NOT THE CLASS'. Per the going-in item 3 test, I checked the disclosure against the code rather than accepting that it exists: it is accurate, in CLASS form, and on a shipped surface. THIS PROVES THE CONTROL IS DEFEATABLE, NOT THAT THE PACKAGE LEAKS — my probe string is not in the committed tree, and grep -rn 'process\\.binding' src/ driver/ scripts/ CUSTODY.md finds the reflective route ONLY inside prose/disclosure — zero executable uses in any shipped file. Per my brief's question 'is there a route that is both open AND reaches a shipped path?': no, I did not find one.",
      "new_or_reconfirmation": "re-confirmation"
    },
    {
      "id": "F-4",
      "severity": "LOW",
      "criterion": "none",
      "file": "engine/src/spawn-shim.js:548-550",
      "claim": "opts.cwd still passes through auditedSpawn unscanned into a real child — re-confirmed by execution, and the shipped Ceiling describing it is accurate.",
      "execution_proven": true,
      "evidence": "auditedSpawn(node, [...], {env:{PATH}, cwd:'C:/Windows'}) launched and the child reported {\"cwd\":\"C:\\\\Windows\"} while every refusal check stayed green. The bound Ceiling paragraph describes this correctly, including its scope sentence, and its factual claim that 'the only two occurrences of either identifier in this function are those pass-through lines' is TRUE at 6a105f2. Filed as a re-confirmation with an accurate, class-form, shipped disclosure — it does not fire a criterion.",
      "new_or_reconfirmation": "re-confirmation"
    }
  ],
  "going_in_items_adjudicated": [
    { "item": 1, "judgement": "CONFIRMED and it correctly predicted where my budget paid off. I attacked the mechanisms hard and they held: 12 live-child attack shapes against auditedSpawn produced ZERO leaks. The two defects I found are of the shape this item names — F-1 is a control that does not do what its family of sibling controls does, and F-2 is a false SENTENCE about a control whose mechanism is actually stronger than written. I did NOT find the args door reopened by bundle I." },
    { "item": 2, "judgement": "CONFIRMED as a live hazard, and it nearly caught me twice. (a) The D1 narrowing sentence is TRUE as written — I re-ran the exact refuted premise: Object.create(Array.prototype) with a counting index getter was refused by the B1 Array.isArray gate BEFORE D1, and its index-2 getter was invoked 0 times, so the container was never inspected at all. The LOAD-BEARING annotation on the B1 gate is accurate. (b) I drafted a finding that spawn-env-allowlist.js's 'createRequire occurs only inside comments' sentence was false, having found it in executable code at lines 284 and 356 of that same file — the refuting code was the SELF_REL_PATH self-exclusion at line 934, 650 lines outside my window. I read the whole file before filing and withdrew it. I have filled files_i_could_not_see honestly." },
    { "item": 3, "judgement": "Each disclosure checked against the code rather than accepted. The confusable fold IS an enumeration over Cyrillic+Greek only and scripts outside it DO evade — verified: Armenian U+0555 and Cherokee U+13AA both evaded while Cyrillic and Greek folded correctly. The comma separator IS deliberately open — verified: comma, semicolon and period all evaded, matching CUSTODY.md:89-91. RT-2's NBSP tolerance is open and its control fires correctly — verified: NBSP folded (i.e. tolerated) as disclosed. All three disclosures are TRUE, in CLASS form, and on the shipped surface, so all three are genuine re-confirmations that buy silence. RT-8's rollup blindness likewise. NOTE THE ASYMMETRY THAT PRODUCED F-1: the emphasis dimension is the one member of this family that is NEITHER closed NOR disclosed, which is why it is a new finding and these four are not." },
    { "item": 4, "judgement": "Respected — not filed as a defect, not fixed, not touched. I did judge the prose describing it, as instructed, and it is TRUE: 'is scanned clean by this enforcer' verified by execution, and the re-pointing rationale is sound because the superseded aliased-requirer route genuinely now goes RED. I attacked AROUND it as directed and found no open route reaching a shipped path." },
    { "item": 5, "judgement": "Noted as recorded; nothing in my scope contradicts it. The disclosed handling is the correct posture rather than tunneling, and self-flagging it is worth more than the flag costs. Outside my lane's evidence reach — I have no artifact from that merge-guard run and did not attempt to reconstruct one." }
  ],
  "regressions_from_the_fix_attempt": [
    "none found — I looked specifically for widening introduced by G-L. Bundle G's transform is strictly stronger than what it replaced: I drove 27 rendered-form dimensions and 26 were caught, including every dimension bundle G claims to have added. F-1 is a gap bundle G did NOT close, not one it opened.",
    "Bundle I's change to how env-scrub derives its swept population introduced no widening reachable from spawn-shim: 12 live-child attack shapes (Object.create(Array.prototype) with counting getter, Array subclass with hostile map, Proxy args, plain array with own hostile map plus stateful toString, Proxy env hiding a key from for..in, secret on the env prototype chain, own-enumerable __proto__ key, case-variant denylist name with an unshaped value, secret-shaped value used as a key name, non-string env value, secret embedded mid-string, secret as argv) produced ZERO decoy deliveries — 9 refused, 3 launched clean. The stateful toString() was called exactly ONCE (no TOCTOU re-read) and the refused-container getters were invoked ZERO times.",
    "Bundle J's ban fires on its intended route and does not over-refuse the wrapper; the standing witness is unaffected, as designed."
  ],
  "files_i_could_not_see": [
    "The S-VLADW1-04 build spec — I never had items 1-7 or their field-4 residuals, which is the sole reason S4-6 is cannot-assess rather than a verdict.",
    "beta rows 309, 310 and 312 in full text — I worked from the brief's restatement of the release rule only. My S4-2(c) judgement rests on the brief's quoted property list, which names markdown-emphasis canonicalization as one of its four components; if row 312's full text scopes that component differently, F-1's criterion attribution should be re-examined (the mechanism observation itself stands regardless).",
    "ADR-0041 upstream text — so I checked ASSERTED_PARAGRAPHS A1-A4 for internal consistency and presence, not against their true upstream source.",
    "engine/CUSTODY.md read selectively, not line-by-line: I read lines 40-101 in full, all 15 canonical bound-paragraph copies via BOUND_PARAGRAPHS, and targeted greps for status tokens, residual keywords and commit SHAs. A false sentence in a stretch I did not open would not have been caught by me.",
    "Test BODIES of RF-1, RF-3, RF-4, RF-5a/b/c, RF-6 and RF-I1/I2 — I read their per-test descriptions and read RF-7's body in full. I compensated by independently re-deriving RF-1/RF-3's subject matter with my own battery and RF-5/RF-6's with my own live-child probe, but I did not verify each body's assertions directly.",
    "node_modules/@anthropic-ai/claude-agent-sdk source — so A6's claim that four SDK credential vars are unclassified is unverified by me.",
    "The 14 unresolved check:pointers entries beyond reading their RED lines — I confirmed selfcheck-runs-on-user-machine is absent from that list but did not adjudicate whether the other 14 are all deliberate."
  ],
  "what_i_could_not_assess": [
    "S4-6's build-spec half, stated plainly rather than left silent: I could not enumerate the field-4 residuals of items 1-7, so I cannot say whether each travelled to the surface where its reader is. I verified only the two named design-phase findings (RT-2, RT-8) plus L2's count-form disclosure.",
    "S4-3's PROCESS half — that RF-4 fired inside the owning bundle's own run rather than at gauntlet. I can only attest the end state and that RF-4 exists in both directions and passes.",
    "Whether F-1 is reachable by any CURRENTLY SHIPPED text: it is not — the shipped CUSTODY.md is green and correctly authored. F-1 is a defeat of the control that would police a future edit, proven by injection, not an existing false claim in the document.",
    "The split-secret residual's true reachability boundary: my probe disproved the shipped EXAMPLE (F-2) but I did not construct a valid instance of the residual, so I neither confirmed nor refuted the residual's underlying CLASS claim by execution.",
    "The SDK's own subprocess handling of a credential once handed to it — outside this package and outside my reach, as A1/A5 already state.",
    "Whether auditedSpawn's opts.stdio pass-through can carry a secret: I execution-proved the cwd half of that Ceiling and reasoned about stdio only.",
    "Cross-lane reconciliation: F-1 is a finding no other lane may have filed. Per rule of engagement 3 that does not make it wrong, but it also has not been independently confirmed, and the conductor should reconcile it against my files_i_could_not_see before grading."
  ]
}
```
