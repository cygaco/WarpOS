# α ruling — S-VLADW1-04 qualifying gauntlet (gauntlet-2) — S4-1…S4-6 applied verbatim

- **Ruled by:** Alex α, 2026-08-29 ~02:5x UTC (2026-08-28 ~19:55 PT), at the close of `runtime/vlad-w1/s04/gauntlet-2/`
- **Rule applied:** β row 309 `e7a4b619-2f83-4d5c-9b01-63cf8ea27d15`, as supplemented by row 310 `3a5f81c7` (name map, RF-2 ≠ S4-2(d), battery pinned by content-invariant), row 312 `f2b71e58` (S4-2(c) = rendered-form PROPERTY; S4-2(d) owed again post-G), row 313 `d4a8e0b2` (NFKD + Cyrillic/Greek enumeration ratified as S4-2(c)-conforming), row 314 `b1c7f45a` (count-form class disclosed), row 315 `a8f39c1d` (β's pre-close record). Texts: `runtime/beta-consult/S-VLADW1-04-*.md`. Nothing re-litigated.
- **Qualifying run:** this directory follows fix attempt 1 (bundles G, H, I, J, K, L1, L2 → vlad `wt/S-VLADW1-01-engine @ 6a105f2`); gauntlet-1 @ `b9b8df3` was the diagnostic. ATTEMPT COUNT ONE — spent. No attempt 2 exists under the rule.
- **Records (all at `6a105f2`):** qa-reviewer `d-mtdrp1ga-8f0780f5` (27,162 B) · backend-reviewer `d-mtds807m-7573a1ea` (22,548 B) · security_claude_hunter `d-mtdsrbfz-fd4297dc` (24,958 B) · security-reviewer/agy `d-mtdr3too-bf3bec3a` (4,199 B; **fell back to `antigravity/gemini-3.1-pro-high`, not the pinned model** — well-formed record, flagged) — all `ok:true`; `gauntlet-verify` PASS (`gauntlet-verify.txt`). Lane verdicts 4 × FAIL. **Lane verdicts do not decide; the criteria do.**
- **Tree at close:** `6a105f2`, `engine/` clean after every lane (ε verified porcelain per lane; α verified at ruling). Conductor gates: suite 366/366, `check:ship` exit 0.
- **α's own execution at close:** `node --test --test-name-pattern="RF-1|RF-3|RF-4|RF-5|RF-6|RF-7" test/*.test.js` → **33 pass · 0 fail · 0 skipped · 0 todo**; described mutations present per falsifier (RF-1 controls-first + no-over-refusal; RF-3 lever; RF-4 both directions; RF-5a/b/c; RF-6 mutant proof + load-bearing assertion; RF-7). Artifact: `alpha-s4-4-rf-execution.tap` (copied from the scratchpad). Per row 307's bar, this counts DESCRIBED mutations, not the pass total.

## The six criteria

| S | Rule (abridged — full text rows 309/310/312) | Finding | Ruling |
|---|---|---|---|
| **S4-1 TRUTH** | Every custody claim string on the `npm pack` shipped surface TRUE at close, by reviewer read; never satisfiable by mechanism evidence | **Two independent, execution-proven false sentences on the shipped surface, both authored by fix attempt 1:** (qa F-1) the confusable-fold calibration paragraph says Cyrillic and Greek are the closed, mistake-reachable scripts and the remainder is attacker-only — but the fold is a 56-entry map reaching 36 of 52 Latin skeleton letters; `l`, `n`, `g` (all in `Ceiling`) have no entry in either script, so in-script Cyrillic U+04CF / Greek U+03B7 / U+03C4 authorings ship green (controls fired first: plain-Latin and mapped-Cyrillic both exit 1; probes exit 0, 0 violations), and Greek eta is reached by exactly the "paste from mixed-script text" accident vector the paragraph itself names; G's shipped enforcer comment ("closes exactly the two scripts listed") is false the same way. (backend F-1) `CUSTODY.md:40-43` "two escapes REMAIN, named here rather than left to be discovered later" — a third class is execution-proven: an ATX-heading-prefixed, bolded, Latin-spelled lead-in is silently skipped (thirteen prefix shapes), while the paragraph immediately above narrates the previous version of that sentence being false for the same reason. The lanes applied the header's going-in rule ("a disclosure that is inaccurate is a NEW finding, not a re-confirmation") — correctly, and against work α and ε approved (L1's wording was β-recommended and α-approved). The security lane's `S4-1 holds` read a different sentence set and is not a factual disagreement. agy's two S4-1 HIGHs were refuted by ε before grading (U+200B IS a Default_Ignorable_Code_Point; the transform folds it) and are not counted. | **FAILS.** |
| **S4-2 MECHANISM** | (a) RF-1 RED for all seven R1 near-misses + both em-dash controls; (b) RF-3 RED on reverting refusal to `continue`; (c) R3 closed by matching on the RENDERED FORM (row 312 property: normalization, default-ignorable strip, confusable fold over the token alphabet, **markdown-emphasis canonicalization**); (d) battery re-run by a lane against the predicate as built | (a) holds — backend and security each observed 7/7 near-misses RED and both controls RED, controls firing the correct distinct rule. (b) holds (RF-3 observed; α re-execution). (d) **DISCHARGED twice** — backend and security independently, post-G predicate, controls first, enlarged population; backend disclosed and re-ran its own invalid first battery. **(c) FAILS, execution-proven by two lanes that never saw each other's work:** `canonicalizeClaimText` performs **no** markdown-emphasis fold; emphasis canonicalization exists in `resemblesBindableLeadIn` and `flattenForAssertionScan` but **not on the status-token path** (`containsStatusToken` runs the bare transform). `**ASSERTED** — NOT VERIFIED`, `*ASSERTED* — *NOT VERIFIED*`, `ASS**ERTED** — NOT VERIFIED`, `_ASSERTED_ — NOT VERIFIED` plant green in `## Proven` — the real CLI counts one as a fifth Proven claim, exit 0. Not a disclosed residual (`CUSTODY.md:85-88` scopes emphasis canonicalization to lead-in resemblance only). The document's own house style bolds status tokens, so this is mistake-reachable. G's own comment says the two sites "must share one transform"; the shared transform was applied and emphasis was implemented outside it. | **FAILS** (on (c); (a)(b)(d) hold). |
| **S4-3 ATOMICITY** | RF-4 observed RED inside the owning bundle; no shipped claim string diverges from its canonical copy at close | Three lanes hold; RF-4 observed RED in both directions (H′, L1, L2 envelopes with real output); α re-execution: RF-4 both directions ✔; lint reports 15 bindable paragraphs matched 15 canonical copies byte-for-byte at `6a105f2`. | **HOLDS.** |
| **S4-4 FALSIFIERS OBSERVED** | RF-1, RF-3, RF-4, RF-5, RF-6, RF-7 present, committed, each OBSERVED RED under its own mutation at close, no-op⇒FAIL guard on every mutant; pass-total ≠ observation-count | Three lanes hold. α executed the committed falsifiers at close: 33/33, 0 skipped, 0 todo, with a described mutation per RF (listed above); mutation logic lives in the committed tests, raw artifacts committed with command line + sha (G, H, I, J, K, L1, L2 envelopes), no-op guards present (10c/10f class). | **HOLDS.** |
| **S4-5 AC-8.6 CAPPED** | Invoked from the product-layer entry; RF-7 RED at RUNTIME on removal; `check:pointers` resolves `custody-runtime.test.js::selfcheck-runs-on-user-machine`; one invocation + one named test; either-or on the driver entry's scrub | backend and security hold: `runCustodySelfCheck` defined in `src/server-entry.js`, invoked once from `startServer()`; RF-7 RED at runtime (bundle C, re-observed; α re-execution ✔); pointer resolves (34/48, RED line gone); cap respected (C's envelope volunteered its nearest approach); either-or satisfied by the FIRST option — `driver/host-free-driver.js:88-115` reads `process.env` after its scrub and before any dynamic import, throwing at load if a credential-shaped name survives (bundle E route (a), narrowed to what was measured). β did not read `server-entry.js` (row 315) — the two lanes did. | **HOLDS.** |
| **S4-6 RESIDUALS TRAVEL** | Field-4 residuals of items 1–7 + RT-2's NBSP tolerance + RT-8's rollup blindness on the surface where the reader is; class-form residual as a Ceiling under P3 in strong form | qa and agy hold; backend/security cannot-assess. RT-8's rollup residual is in CLASS form with the reason (qa quoted all three elements; β verified by own read, row 315). Count-form class disclosed (L2, row 314 option b). NBSP tolerance stated and TRUE (ε's refuted hypothesis). Class-form Ceiling under P3 in strong actionable form (β row 315). The walker's open route named as execution-proven with a committed witness (`ff6d483`). The qa lane declined to stack its S4-1 finding onto S4-6 ("the reshape the brief forbids in the inflating direction") — α concurs: an inaccurate disclosure is an S4-1 failure, and S4-6 asks whether residuals TRAVEL, which they do. | **HOLDS.** |

## Application

> TERMINAL: any one of S4-1…S4-6 fails at that close → NO RELEASE, no attempt 2; close at honest state, residuals named, ED-340 / ED-354 / ED-358 dispositions restated, remainder → named successor.

**S4-1 FAILS and S4-2(c) FAILS. Therefore: NO RELEASE. No fix attempt 2. S-VLADW1-04 closes at honest state.** `wt/S-VLADW1-01-engine` stays unmerged at `6a105f2`. No exception clause exists and none is invented.

**What the record should say plainly.** Fix attempt 1 closed every finding gauntlet-1 raised, discharged S4-2(d) twice over, held S4-3/4/5/6, and grew the suite 339 → 366 with every mutant guarded. It failed on the same class the sprint pair exists to close, one layer out each time: the fix attempt **authored new false sentences about its own mechanisms** — the confusable calibration (β-recommended, α-approved), the "two escapes remain" count — and left one dimension of the rendered-form property (emphasis) implemented beside, not inside, the shared transform. The diagnostic gauntlet's organising finding was "six false sentences over sound mechanisms"; the qualifying gauntlet's is "two new false sentences and one un-shared fold" — the recursion ε named at gauntlet-1, recurring exactly as predicted. Every one of these was in prose or code α and ε reviewed and passed, and one carried β's recommendation. That is the finding to carry, above the process ones.

## Residuals named (carried verbatim in intent to the successor)

1. **S4-1a** — confusable-fold calibration paragraph false: the fold is a 56-entry map (36/52 Latin skeleton letters); `l n g` etc. unmapped in both scripts; in-script homoglyphs of unmapped letters evade; the "mistake-reachable scripts closed / remainder attacker-only" framing is false because unmapped letters are reachable by the paragraph's own accident vector. Fix shape: state coverage as the letter set actually mapped (or vendor a confusables table), never as "scripts closed"; enforcer comment corrected to match.
2. **S4-1b** — "two escapes REMAIN" false: ATX-heading-prefixed (and twelve other prefix shapes) bolded lead-ins silently skipped. Fix shape: refuse-not-skip must extend to heading/list/blockquote prefixes on the LEAD-IN path (G handled block prefixes for candidates; the heading marker escapes), and the escape count must be replaced by a named class, not a number (the document's own rule: a number is a property of the day it is read).
3. **S4-2(c)** — emphasis canonicalization is not on the status-token path; `**ASSERTED** — NOT VERIFIED` plants green in Proven. Fix shape: move the emphasis fold INTO `canonicalizeClaimText` (or have `containsStatusToken` call `flattenForAssertionScan`) so both sites share one transform as G's comment already requires; near-miss battery gains emphasis-split status-token variants, controls first.
3b. **S4-1c** (re-classified per β row 316 `6e2d94af`) — the NOT-bound enumeration omits P1–P4 headings (an inverted P2 heading ships green): a coverage claim stated at a coarser granularity than the mechanism has, with an execution-proven evasion, inside a list the preamble introduces as "said plainly rather than generalised." **Third instance of the S4-1a/S4-1b class** — the successor is aimed at the CLASS with all three instances in view.
4. Security lane MEDIUM — `spawn-shim.js`'s split-secret worked example is false (fail-safe direction); qa MEDIUM — a third unnamed escape class; qa LOWs as filed.
5. **agy fallback** — the cross-family lane served on `gemini-3.1-pro-high`, not the pinned model; the record is well-formed but the pin was not honoured (ED-230 class); noted, not counted against any criterion.
6. Successor-carried from fix attempt 1 (already in the S-04 tracker): the transform's description paragraph is UNBOUND (needs the fixture builder); "unbound ≠ re-wrap is free" (header substrings pinned across wraps); the task-4 assertion message ("reviewer read") in a non-shipping test; a text-matching enforcer cannot distinguish a violation from a description of one (K and L2 both tripped it).
7. **ED-340** — remains OPEN, carried: mutant half satisfied 2026-08-10; roster half open with ED-354 as its instance. **ED-354** — OPEN (installed-roster parity; no new instance this sprint). **ED-358** — OPEN; this ruling is its second instance one layer out: a predicate/fold stated as closed over a category it only samples (S4-1a, S4-1b). New this sprint and cited: ED-360, ED-361, ED-362, ED-363; ED-257 amendment; ED-356 recurrence ×3.
8. Process residuals for the retro: three brief-premise defects (J's "zero executable uses", K's atomicity, L2's cwd — ED-363); the excerpt-window false HIGHs (ED-362, then agy's Unicode belief refuted in two commands); ε's idle-while-polling pattern (repeated; the α watchers carried the waits); ε's "clean tree" reading a mid-write tree (caught before reporting); the α/ε WarpOS commit race (resolved by hash-naming); usage-limit risk not realised this run.

## Successor

**S-VLADW1-05** — to be minted by ε at this close (tracker only; registry at build authorization): one transform for both sites with the emphasis fold inside it; refuse-not-skip on the lead-in path for every prefix class; confusable coverage stated as the mapped letter set (or a vendored table); escape counts replaced by named classes everywhere; the unbound description paragraph bound via the fixture builder; the successor-carried residuals above; and the standing discipline that **every shipped claim sentence is drafted after the attack that would falsify it — including sentences β recommends and α approves**, because this run shows the approval chain is not a truth check. β mints a fresh rule before results exist; S4-1…S4-6 do not carry over automatically.

α applied the rule as written. **β CONFIRMED the application** under `[S04 β-r3]` — row 316, msg_id `6e2d94af-b53c-4871-a09e-c8f107b2e35d` (DECIDE, Class B, 0.92), with its conflict of interest declared (one S4-1 sentence was β's row-313 recommendation) and the position-swap run first: Q1 S4-1 grading MATCHES — β verified `CONFUSABLE_FOLD`'s values contain no `l`, `n`, `g`, so coverage is by LETTER not SCRIPT and its own sentence is correctly graded false; the going-in rule ratified (an inaccurate disclosure is NEW, never shielded by the residual it misdescribes; "approval is not a truth check, and β's recommendation is worth exactly nothing against the shipped bytes"). Q2 S4-2(c) correctly FAILS — row 312 made (c) a property, one dimension of a property is the property; the document is TRUE while the mechanism is short, which lands in S4-2 not S4-1 because row 309 kept them separate. Q3 no AP-15 either direction; declining to stack S4-1 onto S4-6 is specifically correct ("one defect, one criterion"). Re-classification applied above (3b). β endorsed the successor's standing discipline including the clause naming β, and noted that the refusal of an exception clause at row 309 was load-bearing — S4-2(c) is exactly the defect a "mechanical failures only" carve-out would have been argued into.
