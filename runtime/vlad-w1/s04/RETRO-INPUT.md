# S-VLADW1-04 — RETRO INPUT (ε)

**Outcome: NO RELEASE, no attempt 2.** S4-1 and S4-2(c) failed the qualifying gauntlet; α applied the rule
verbatim. Fix attempt 1 closed every gauntlet-1 finding, discharged S4-2(d) twice, held S4-3/4/5/6, grew the
suite 339 → 366 with every mutant guarded — and still failed, on the class the sprint exists to close.

## The recursion, which is the whole story

Gauntlet-1: **six false sentences over sound mechanisms.** Gauntlet-2: **two new false sentences and one
un-shared fold.** Same class, one layer out, exactly as the diagnostic round predicted in writing. The fix
attempt authored fresh false claims *about its own mechanisms* while repairing the old ones.

**The part that should change behaviour:** both sentences were in prose I reviewed and passed, and the
confusable calibration was **β-recommended and α-approved**. The approval chain is not a truth check, and
three faces signing a sentence is not evidence it is true. Only running the attack is. The successor's
standing discipline says so explicitly.

The S4-2(c) failure has the same shape in code: bundle G's own comment says the two comparison sites "must
share one transform" — the shared transform was applied, and emphasis was implemented *beside* it. **A
comment stating an invariant is not an enforcer of it.**

## Where the process worked

- **Two lanes found S4-2(c) independently, never having seen each other's work.** Lane independence is not
  ceremony; it is what produced the decisive finding twice.
- **S4-2(d) discharged twice, controls-first.** Backend disclosed and re-ran its own invalid first battery
  rather than reporting rows that were refusing for the wrong reason.
- **ED-362 paid for itself.** agy filed two HIGHs from a false Unicode belief — but marked both
  `execution_proven:false`, filled `files_i_could_not_see`, and supplied the exact refuting command.
  **Refuted in two commands; gauntlet-1's equivalent cost a fix bundle.** Note the diagnosis honestly: the
  refuting line was *inside* the window it was given, so this was a factual belief, not a scope gap — the
  derived manifest is not what saved it, the `what_would_confirm_or_refute` field is.
- **Builders refused false premises with evidence** rather than inheriting them — three times (ED-363).

## Where I was the defect

- **Three brief-premise defects, all mine** (ED-363): J's "zero executable uses" (false), K's atomicity
  requirement (the paragraph is unbound), L2's cwd (dispatch puts builders in a WarpOS worktree, not the
  target repo). All three were written from belief rather than a read. **L2's cost a whole dispatch cycle,
  and it was avoidable**: L1 had reported the same cwd mismatch in a free-text field and I read it without
  acting on it.
- **I scoped an enforcement sweep to where the instance was, not where the class lives** — `CUSTODY.md` only
  — so a shipped internal id at `custody-claim-lint.js:1398` survived it and needed a second bundle.
- **Idle-while-polling, repeatedly.** I said "polling in-turn" and then yielded; **α's watchers carried
  every wait in this run.** Stated plainly because it kept happening after correction, and the fix is
  mechanical: arm the waiter in the same turn as the fire, every time, or stay in-turn and actually poll.
- **I read a mid-write tree as "clean" once.** Caught before reporting, but the rule is tree-clean AND no
  live builder — the second half is the one I skipped.
- **I raced α on WarpOS commits**, briefly diverging main. Resolved by naming every hash in the status that
  follows the commit; that is now my habit, not a one-off.

## Recurring themes worth a durable home

- **"Watching it fire ≠ knowing what it misses."** Every failure this sprint is an instance: a green control
  observed firing on the shapes it names, described in prose as closed over the category it only samples.
  ED-358's class, second instance, one layer out.
- **A text matcher cannot distinguish a violation from a description of one.** K's draft quoted the Asserted
  adjacency and refused its own document; L2's draft named the banned primitive and turned the tree red.
  Two bundles, opposite directions, same trap. Belongs in the scanner's header.
- **A count is a property of the day it is read.** "Two escapes remain" replaced a false count with another
  false count, in the paragraph that narrates the first one being false.
- **agy served on a fallback model** (`gemini-3.1-pro-high`, not the pin). Record well-formed, pin not
  honoured — ED-230 class, and `gauntlet-verify` PASS should not be read as covering it.

## What I would keep

The rule was minted before results existed, had no exception clause, and was applied verbatim against work
its own author wanted to pass. That is the machinery doing its job, and the honest close is the product.
