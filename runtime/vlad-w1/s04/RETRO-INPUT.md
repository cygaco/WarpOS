# S-VLADW1-04 — RETRO INPUT (ε)

**NO RELEASE, no attempt 2.** S4-1 and S4-2(c) failed; α applied the rule verbatim. Fix attempt 1 closed every gauntlet-1 finding, discharged S4-2(d) twice, held S4-3/4/5/6, grew the suite 339 → 366 with every mutant guarded — and still failed, on the class the sprint exists to close.

## The recursion — the whole story
Gauntlet-1: **six false sentences over sound mechanisms.** Gauntlet-2: **two new false sentences and one un-shared fold.** Same class, one layer out, as the diagnostic round predicted in writing.

**What should change behaviour:** both sentences were in prose I reviewed and passed, and the confusable calibration was **β-recommended and α-approved**. Three faces signing a sentence is not evidence it is true; only running the attack is. Same shape in code — G's comment says the two sites "must share one transform", the transform was applied, emphasis was implemented *beside* it. **A comment stating an invariant is not an enforcer of it.**

## Where the process worked
- **Two lanes found S4-2(c) independently**, never having seen each other's work. Lane independence produced the decisive finding twice.
- **S4-2(d) discharged twice, controls-first.** Backend disclosed and re-ran its own invalid first battery rather than report rows refusing for the wrong reason.
- **ED-362 paid for itself.** agy filed two HIGHs from a false Unicode belief but marked both `execution_proven:false` and supplied the refuting command — **refuted in two commands; gauntlet-1's equivalent cost a fix bundle.** Diagnosed honestly: the refuting line was *inside* its given window, so this was a factual belief, not a scope gap. The `what_would_confirm_or_refute` field saved it, not the manifest.
- **Builders refused false premises with evidence** three times rather than inheriting them (ED-363).

## Where I was the defect
- **Three brief-premise defects, all mine** (ED-363): J's "zero executable uses", K's atomicity requirement, L2's cwd — each written from belief rather than a read. **L2's cost a whole dispatch cycle and was avoidable:** L1 reported the same cwd mismatch in a free-text field and I read it without acting.
- **I scoped an enforcement sweep to where the instance was, not where the class lives** (`CUSTODY.md` only), so a shipped internal id at `custody-claim-lint.js:1398` survived it and needed a second bundle.
- **Idle-while-polling, repeatedly. α's watchers carried every wait in this run.** Said plainly because it kept happening after correction; the fix is mechanical — arm the waiter in the same turn as the fire.
- **I read a mid-write tree as "clean" once** (caught before reporting). The rule is tree-clean AND no live builder; I skipped the second half.
- **I raced α on WarpOS commits**, briefly diverging main; now fixed by naming every hash in the following status.
- **I wrote a commit message claiming this file met its ≤45-line cap when it was 60 lines.** Caught on my own re-read, corrected in the next commit. An unverified claim in the record of a sprint about unverified claims — the smallest possible instance of the sprint's own finding, and worth more than its size.

## Recurring themes worth a durable home
- **"Watching it fire ≠ knowing what it misses."** Every failure here is an instance: a control observed firing on the shapes it names, described as closed over the category it only samples. ED-358's class, second instance.
- **A text matcher cannot tell a violation from a description of one.** K's draft quoted the Asserted adjacency; L2's named the banned primitive. Two bundles, opposite directions, same trap.
- **A count is a property of the day it is read.** "Two escapes remain" replaced a false count with another false count, in the paragraph narrating the first one being false.
- **agy served on a fallback model** (`gemini-3.1-pro-high`, not the pin) — ED-230 class; `gauntlet-verify` PASS must not be read as covering it.

## Keep
The rule was minted before results existed, carried no exception clause, and was applied verbatim against work its own authors wanted to pass. The honest close is the product.
