# α ruling — S-VLADW1-03 qualifying gauntlet (gauntlet-3) — S1–S5 applied verbatim

- **Ruled by:** Alex α, 2026-08-28 ~13:55 PT, at the close of `runtime/vlad-w1/s03/gauntlet-3/`
- **Rule applied:** β row 305, msg_id `7c05e9d1-4b82-4a37-9f6e-8d3410b2ca65` (`runtime/beta-consult/S-VLADW1-03-r2-verdict-7c05e9d1.md`), with β's pre-stated reading of item 6 in row 306, msg_id `4e1b7a92-3c68-4d05-9a71-6f28c0b5e3d4`. Neither re-litigated.
- **Qualifying run:** this directory is the evidence dir that follows fix attempt 2 (`runtime/vlad-w1/s03/fix-attempt-2/`, bundles 10a–10f, vlad `wt/S-VLADW1-01-engine` @ `b2583d6`). Attempt count per the rule: gauntlet-1 → fix attempt 1 → gauntlet-2 → fix attempt 2 → **this run**. No attempt 3 exists under the rule.
- **Records:** qa-reviewer `d-mtdcwc12-89d1be9a` · backend-reviewer `d-mtdf0prn-8bfbfe9b` (recorded on resume from its existing evidence file after ε's usage-limit death; lane NOT re-run) · security_claude_hunter `d-mtdfmjhm-2002e049` · security-reviewer/agy `d-mtdcb3tf-9cba8fdd` — all `ok:true`; `gauntlet-verify` PASS exit 0 (`gauntlet-verify.txt`). Lane verdicts: 4 × FAIL. Per the rule's discriminator, **lane verdicts do not decide** — the criteria do.
- **Tree at close:** `b2583d6`, `engine/` clean, 318/318, `check:ship` exit 0 (ε, separate commands). Full porcelain = 41 untracked `.claude/` session artifacts, none under `engine/`.

## The five criteria

| S | Rule text (abridged — full text in row 305) | Finding | Ruling |
|---|---|---|---|
| **S1** | Zero execution-proven leaks, RE-ESTABLISHED not cited; item-7 battery committed and green with raw controls | Security lane ran the battery and its own novel attacks on this tree and returned holds. Its novel attack (caller-controlled `args.map` on an Array subclass — `spawn-shim.js:262`) reached a real child's argv with a secret-shaped value, but there is **no shipped call site of that shape**; the lane itself declined to fire S1 on it. Battery committed + green. qa and backend returned `cannot-assess` rather than inferring. | **HOLDS** (re-established on this tree). The `args.map` finding is a defeated control and a real residual — carried to the successor as a named item, not inflated into a leak. |
| **S2** | Every custody claim string in a shipped surface is TRUE of the code at close (reviewer read); item-2 residual disclosed in the shipped ceiling text | **All four lanes FAIL S2 on eight independent, execution-proven grounds**, none a re-confirmation, each a fix-attempt-2 regression: (a) backend F-1 HIGH — 10b's derivation predicate requires an exact em-dash; en-dash / hyphen / colon / two-space-indent "Ceiling" paragraphs ship GREEN with zero violations while the shipped sentence promises EVERY bolded Ceiling lead-in is bound (7 authorings, controls first); (b) qa F-1/F-2/F-3 — 10c's commit falsified the bound "not named on any other surface" sentence (`entry-bootstrap.test.js:687`); 10b's spawn-shim attribution sentence authored after 10d changed the file; header's not-bound list omits P1–P4 body prose (three flat-falsehood mutations shipped green); (c) agy F-1 — inside 10d's repair, absorption iterates only the current call's list while deletion sweeps full history, falsifying 10d's CLASS claim; (d) security second-order — the bind now **pins verbatim two sentences the lane falsified by execution**, so `check:ship` requires a proven-false sentence to be present. | **FAILS.** |
| **S3** | Deleting the scrub call from each entry makes a committed standing test go RED — observed under mutation; walker asserts both directions | Observed RED on both entries (M1 server-entry: 4 reds incl. runtime; M2 driver: exactly one test red — the driver's scrub is load-bearing only at text/AST level because model-seam scrubs anyway; the rule's bar is "a committed standing test goes RED", which it does). Walker asserts both classification directions. The gauntlet-2 inert control (comment pre-satisfying `CALLS_SCRUB_RE`) is closed at the mechanism by the shared lexer (10a). | **HOLDS** (strength-of-proof concern on the driver entry recorded, not a failure). |
| **S4** | F-1..F-5 present, committed, each OBSERVED RED under its own mutation (presence ≠ observation); AC-8.4's mutant re-verified not cited | Lanes observed RED this round for F-2, F-3, AC-8.4. F-1, F-4, F-5 were present and non-skipped but no lane mutated them. **α executed them at close** (`alpha-s4-f1-f4-f5-execution.tap`, `node --test --test-name-pattern="F-1\|F-4\|F-5"` at `b2583d6`): F-1 "OBSERVED RED per spelling — neutralising the spelling flips canSpawn true→false" ✔, F-4 "rewording A7's carrier sentence now goes RED naming A7" ✔, F-5 "missing-file vs missing-name reported as DISTINCT RED reasons, exits non-zero" ✔ — 6 pass · 0 fail · 0 skipped · 0 todo, each test performing its own mutation and asserting RED, with the no-op⇒FAIL guard (10c/10f) making a silent no-op impossible. That is observation by execution at close, not presence. | **HOLDS.** |
| **S5** | Every named residual in the build spec (items 1,2,3,4,6,7) appears in the recorded or shipped surface at close; disclosure lives where the claim's reader is | Items 1,2,3,4,7: on the shipped surface (CUSTODY.md P2 Ceilings + A7/A8, 9d/10b/10c). Item 6 (AC-8.6, deferred): the INSTANCE residual is on the shipped surface (CUSTODY.md ceiling: "has not landed", "nothing in src/ or driver/ invokes this file"); the CLASS-form residual is recorded in `fix-attempt-2/ROUND-RECORD.md` §4 — and β pre-ruled in row 306 that for item 6 "a round record or the successor tracker satisfies this one; the spec that named it does not", while qa confirms CUSTODY.md makes no shipped claim that the class-form residual bounds (so the "where the reader is" discriminator does not bite). Two lanes flag the same tension — the CUSTODY.md reader gets the instance without the class — and it is carried, not resolved: the class-form residual is written into the successor tracker as part of this close (owed by row 306, not yet done at the moment of ruling). | **HOLDS at close**, on the condition this close discharges: the successor tracker carries the class-form residual verbatim. |

## Application

> TERMINAL: any one of S1–S5 fails at that close → NO RELEASE, no attempt 3; close S-VLADW1-03 at honest state, residuals named, ED-340 disposition restated, remainder → named successor.

**S2 FAILS. Therefore: NO RELEASE. No fix attempt 3. S-VLADW1-03 closes at honest state.** `wt/S-VLADW1-01-engine` is NOT merged to vlad main.

The failure is narrow and instructive: fix attempt 2 delivered the class fixes the rule demanded (inert control closed at the lexer, 14 paragraphs byte-bound with the bind proven on a real edit, the false ordering clause gone, every mutant no-op-guarded, clean checkout byte-identical, 318/318) — and the **bind's own derivation predicate is the new overclaim**. The mechanism built so a claim could not be inverted under green gates now (i) misses any Ceiling paragraph not led by an exact em-dash, silently, and (ii) pins two execution-falsified sentences so that correcting them turns `check:ship` red. That is the R2/S2 class one layer up, and it is the successor's first item.

## Residuals named (carried, verbatim intent)

1. **S2-a** — derivation predicate accepts only an exact em-dash lead-in; non-matching Asserted/Ceiling paragraphs are skipped with no violation (backend F-1, HIGH). Fix shape: a paragraph that LOOKS like a lead-in (bolded `Asserted`/`Ceiling` + any dash/colon/indent) must be either bound or REFUSED, never skipped.
2. **S2-b** — two bound sentences are false by execution (qa F-1; security second-order): the preload-Ceiling "not named on any other surface" sentence (contradicted by `entry-bootstrap.test.js:687`) and the 10d attribution sentence; the bind must move in the same change as the correction.
3. **S2-c** — header's what-is-NOT-bound list omits P1–P4 body prose (qa F-3).
4. **S2-d / 10d class claim** — absorb iterates the current call's list while delete sweeps history; an omitted previously-captured name is deleted without absorption (agy F-1, LOW, unreachable via shipped callers). Make absorb sweep history too, or state the precondition and downgrade the claim to INSTANCE.
5. **Defeated control (not a leak)** — caller-controlled `args.map` on an Array subclass bypasses stringification in the scan; Node re-stringifies inside `spawn()` (security F-1/F-2/F-3, T8/T4 TOCTOU reopened through a door A3 did not close). Fix shape: `Array.prototype.map.call` / `Array.from` + own-property iteration in the scan, mirrored by a committed regression fixture.
6. **S3 strength** — driver entry's scrub is load-bearing only at text/AST level (security F-4).
7. **AC-8.6** — deferred (β row 306: not an AP-15 reshape); the CLASS-form residual ("every shipped control is invoked by some product-layer path — item 4's walker approximates it; AC-8.6 is one instance; no enforcer asserts the general form") travels to the successor.
8. **ED-340 disposition** — remains OPEN, carried forward: the mutant half of its closing condition was satisfied 2026-08-10 (both mutants observed RED on lever-verified targets); the ROSTER half and AC-8.4's lineage remain open, now with ED-354 (installed-roster parity at the vlad cwd — the `security-fixer` 375 ms reap) as the concrete instance. Not closed by this sprint.
9. Process residuals for the retro: ε's usage-limit death mid-gauntlet (recovered artifact-first, no lane re-run); 10c's bound-timeout-after-commit signature (1200177 ms, committed 22 s before the bound) now separable from clamp and nothing-spawned; the ED-257 right-sizing warning that fired and went unread; ε's "clean tree" wording caught by the backend lane.

## Successor

**S-VLADW1-04** (to be minted by ε at this close, before the sprint registry is touched): custody claims made un-invertible *for real* — refuse-not-skip derivation, bind moves with corrections, absorb/delete symmetry, the `args.map` scan door, AC-8.6 built, class-form residual either enforced (a general "every shipped control is invoked by a product-layer path" assertion) or shipped as a disclosed ceiling. Qualifying-run discipline unchanged: β mints a fresh rule before results exist.

α applied the rule as written. β's confirmation that this application matches the rule text is requested under `[S03-FA2 β-r2]`.
