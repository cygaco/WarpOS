# CLOSE — S-VLADW1-05 — "one transform, both sites" — CLOSED UNRELEASED, 2026-08-30

**Disposition:** NO-RELEASE at the one attempt (rows 317/318). No attempt 2. The vlad engine branch `wt/S-VLADW1-01-engine` stays unmerged to vlad main; the qualifying pin is `417147d`. Successor **S-VLADW1-06** minted tracker-only. β's grading: rows 383–385 (msg 7c1e4d92, 9f4b2a17, 4d8e9c05); the pre-committed checklist: row 344 (4e17c8d2).

## The terminal — visible correction (β 2d5f9e18 §1-§2; f2a08d51 (a) A1/A2; 4d8e9c05 §8)
**Pre-committed (row 318, 7b3e6d21, verbatim):** "this sprint fixes three known instances of a class whose size is unknown, with the four un-audited surfaces named and carried to the successor; a close reading as 'the class is closed' is the class one layer out."

**Corrected:** S-VLADW1-05 repaired four routing sites in `custody-claim-lint.js` at 417147d and proved each repair by observed mutation — baseline 117/117, every protecting test fails when its mechanism is removed, confirmed by an independent lane (Q3, d-mtf5rc6t) and an independent tree check, with the package suite independently confirmed 408/408 at the pin by a blind third-party run with real dependencies. One closure claim this round made holds: L83-90 ("it is closed now" — the backtick/tilde lead-in class), agreed TRUE by two differently-briefed claim-truth lanes and by β's own read of `EMPHASIS_FOLD_PATTERN`/`EMPHASIS_RUN`, with the twin's qualification that `BOLD_LEAD_IN` is a third, separately disclosed notion of emphasis. The ROUTING question is closed over that one file at that commit, except the sites `TRANSFORM-ROUTING.md` names as deliberately verbatim — and the routing guard (RF-M2) enforces exactly the two sites it names; nothing enforces routing for an unnamed site, and the inventory that names the rest has no enforcer (ED-394). The TRUTHFULNESS question is NOT closed and is wider than the class this sprint scoped: five qualifying lanes (two claim-truth, two adversarial, one teeth; two model families; identical artifact-only briefs) returned four fail / one pass; the sprint's own document carries sentences graded false by independent readers and mapped by β under no-stacking — for truth (S5-1): L132-137, the transform's ordered enumeration, which omits fold (8) and `DASH_CLASS_PATTERN`'s U+30FC; and the A5 heading, which names P2 where `spawn-env-allowlist.js`, `model-seam.js` and this document's own L531-566 name P4 — bound verbatim by `custody-claim-lint.js:1073`, a mechanism enforcing a falsehood; for frame (S5-2(b)): L7-11 · L12 · L12-17 · L64-73 · L80-83 · L115-116 · L123-127 · L171-174 · L34-36's clause "which the structural and forbidden-phrase rules check" · L50-58 · L154-157 · L218-219 — including the sentence this sprint corrected (L12-17, false again, both claim-truth lanes agreeing from the bytes), the sentences the correction itself introduced or re-labelled (L12-17 · L50-58 · L80-83 · L154-157), and the four-file selection criterion (L218-219) that generalises one lane's disclosure to every lane. Claims one lane graded false that β's read of the document's own vocabulary and qualifiers RESOLVED AS TRUE reduced the set (S19, S24, S31, L6), while the mapping ADDED a clause no lane graded as a sentence (L34-36). The lint accepts plainly-worded false custody claims outside the phrase family it hardens — a designed ceiling the document discloses at L116 and L241, not a defect. Three instances were fixed; the class is demonstrably larger than three and its size remains unknown; its members surfaced here are carried to S-VLADW1-06 as an emitted list, not a count. No release; one attempt; no attempt 2.

**Reason for the correction:** the results showed the pre-committed wording understated (the class measured larger than three; the corrected sentence found false again by two independent lanes) and was silent on what held (four execution-proven repairs; the routing closure; one held closure claim). Accuracy, not direction: the same rule forbids softening an overstatement.

## The five lanes — verdicts only (the lanes do not adjudicate; β maps)
| lane | route / id | token (read from the lane's raw file) | coverage limits in the lane's own words |
|---|---|---|---|
| Q1 claim-truth | codex `d-mtf67lqj-8cad4634` | fail | surface read end to end; ≤7-min envelope line stated-and-exceeded (831585 ms) |
| Q1 claim-truth | in-process `d-mtf79ol1-93a9e0e4` (general-purpose/opus per the harness transcript) | fail | full test file 117/117 + eight mutants run; sampled regions named; cannot-assess: A1–A4 vs ADR-0041 |
| Q2 adversarial | codex `d-mtf6pwa2-69d952e6` | fail | lint SAMPLED; test file unassessable (`spawn EPERM`, a codex-sandbox limit); budget stated-and-exceeded (530600 ms) |
| Q2 adversarial | in-process `d-mtf6zdid-a5ed5f0c` (general-purpose/opus) | fail | 31 lint invocations, every finding with a firing control and a non-firing attack |
| Q3 teeth | in-process `d-mtf5rc6t-08cc5b00` (general-purpose/sonnet) | pass | population = the changed test file's new/flipped test() blocks (117 collected with real deps too); four population-adjacent files not run |
Record caveat (ED-393): the in-process records' `role`/`model` fields are registry-derived; identity is from the harness transcript. Aggregation was frozen before any verdict existed (β a6f31d84): any-fail-holds; abstention never a pass; contradictions on one sentence settled by a read at source with a checkable quote, sentence granularity.

## The union — adjudicated per sentence; no count
- **Confirmed by both Q1 lanes (same sentence, both false):** L12 · L12-17 (the sentence corrected this round; codex's quote carries the document's own "CORRECTED (S-VLADW1-05, this round):" marker) · L123-127 (three distinct defects across the lanes; fires S5-2(b) once) · L132-137.
- **Settled codex-right (the twin under-graded; α-confirmed at the pin):** L7-11 · L64-73 · L115-116 · L171-174 — class-level claims over character-level mechanisms (`BLOCK_PREFIX` L784-785 `[-*+•‣◦]`; `BOLD_LEAD_IN` L678 requires a leading `**`; `AGGREGATE_COUNT_PATTERN` L1335; `WORDED_ROLLUP_PATTERN` L1343).
- **Single-lane codex FALSE, corroborated by the diagnostic qa lane's execution:** L80-83 (EVADES rows P10/P11, P17, P29 — the same three grammars).
- **Settled twin-right (codex over-graded):** L188-192 / S31 (one code call site, L888; "call site" = an invocation location throughout the document; three independent lines).
- **Resolved TRUE by β, out of the false set:** L131-133 / S19 (the document's own "token" = status token; a section locator is not a token comparison) · L150-152 / S24 (the qualifier "to verify the numbers in THIS paragraph" — the lane quoted it and misread it; true on either reading of "check") · L6 (the document's own "on every run" idiom; the shipped invocation carries no argument).
- **Agreement, both TRUE — a closure claim that holds:** L83-90.
- **Stands on AP-17 (an inaccurate disclosure), not on falsity:** L154-157 / S26 — a disjunction true on its second disjunct; repair = strike the first clause.
- **The four-file selection criterion, L218-219 (β 9f4b2a17 / 4d8e9c05 §3):** "EVERY GAUNTLET-2 LANE SAMPLED RATHER THAN READ END TO END" is false in both directions against the four S-04 gauntlet-2 lanes' own fields (security-claude verified named lines and sampled nothing; security-agy never opened two of the files; only qa named all four; the round record worked three lanes where four exist — accurate on qa, incomplete on the population); "Gauntlet-2" is also an unanchored deictic — the repair's second requirement. Repair: the emitted per-lane facts, anchored to S-VLADW1-04. The rest of that disclosure block is the best disclosure writing in the document and must not be touched.
- **Q2 codex:** F-Q2C-1a (plainly-worded false custody claims accepted at exit 0 in scanned regions — execution against a designed ceiling; no criterion) · F-Q2C-1b (L320-322 is the document's authoring norm, not lint behaviour; no criterion) · F-Q2C-2 (the A5 bind — S5-1).
- **Q2 in-process:** F-Q2I-1 — the sentence half (L34-36's scanning clause) fires S5-2(b); the mechanism half (the `## Asserted` region scanned by no forbidden-claim rule; the span skipped by construction at L1583) is a lint defect → S-06 · F-Q2I-2 (Rule 4b's block-boundary closure falls to a duplicated paragraph — `locateBoundParagraphSpan` → `content.match`, first occurrence; falsifies L50-58's "CLOSED THIS ROUND (Task 4), execution-proven" — S5-2(b)) · F-Q2I-3 (the `caseFold:false` consequence is omitted, not misstated → S-06).
- **Not defects:** the lint's "N Proven claim(s) checked" report (β 5a1e93c7 / 7c1e4d92 §1, read at L387/L388/L457-481/L2054-2060: an action that occurred) · the plain-ASCII acceptances as a lint bypass (a disclosed designed ceiling).

## Criteria (β, rows 383–385; NO STACKING — one defect fires the most specific criterion naming its question)
- **S5-1 (truth) — FAILS ×2:** L132-137 · F-Q2C-2 (the A5 heading + the verbatim bind at L1073).
- **S5-2(b) (frame) — FAILS ×12:** L7-11 · L12 · L12-17 · L64-73 · L80-83 · L115-116 · L123-127 · L171-174 · L34-36's scanning clause · L50-58 · L154-157 · L218-219. In β's words: *the sprint failed, twelve times over, on the criterion written because I did it once* (S5-2(b) was minted on β's own S4-1a frame failure).
- **S5-3 (the routing/yield property) — HOLDS** at the criterion's own scope: RF-M2 (test L2039/L2044/L2059) is a real wiring guard with a control node and a lever; RF-M1, RF-N1, RF-O1 present and passing at the pin; the inventory-backed form suffices — no exception clause is owed in CUSTODY.md because, by the S19 ruling, the shipped sentence is already true on the document's own vocabulary. Ceiling, in its strong form: RF-M2 guards exactly the two named sites by a source-text assertion; nothing enforces routing for an unnamed site (how B-2 was possible); the inventory that names the rest has no enforcer — ED-394.
- **S5-4 — NOT DISCHARGED at the release pin.** An artifact exists — `runtime/vlad-w1/s05/gauntlet-1/out-qa-reviewer.md` (d-mteznt8t, at 6c64021) discharged it *there* (re-derived, mutation-differentiated, no-op⇒FAIL guard proven both sides) — but (1) its populations are class-derived near-miss authorings, not "every newly authored or edited paragraph" (S5-4 verbatim), and (2) it is pinned before the one fix attempt, which widened `EMPHASIS_RUN` (L838) — its EVADES rows P34/P35/P36 are stale; no monotonicity argument carries. ε declined to manufacture a discharge after the results existed. A named gap, not a pass. (Both "no artifact" statements — α's by artifact-name shape, ε's by directory — were claims about bounded searches; β's "no lane performed S5-4's work" was withdrawn.)
- **S5-5 — DISCHARGED** (Q3: every mutation run, baseline 117/117, unaffected by the stub — the file collects 117 with real deps; the qa lane's B-1 flag answered by Q3).
- **S5-6 (atomicity) — HOLDS** at the mechanism's grain: `git log 6c64021..417147d` = exactly one commit; each build bundle landed lint exit 0 (the byte-for-byte check is the enforcer); the executed baseline at the pin: 15 bindable paragraphs matched against 16 canonical copies, 3 carrier-note sentences bound, 0 violations. Over the bound set only; says nothing about S5-1.
- **S5-7 (residuals travel) — DISCHARGED:** item 6 — ROUND-RECORD.md L1310-1372 (nine residuals by name with id/location, the enforcement debt by id, the S5-4 gap; no count) with two corrections applied (entry 8 → graded, fires S5-2(b); one span citation L216-227); item 12 — discharged in the artifact (CUSTODY.md L216-227: criterion, completeness caveat, the capped grep with method, 14-of-16 as a ceiling, two files as a lower bound, four named misses at L240-248), the record pointing at it (L1373-1395) — with the L218-219 sentence inside it graded false.

## Method notes (the retro's input — `RETRO-INPUT.md` carries each with its row)
An executed result is evidence; a finding only against a claim · the quote makes an error checkable, not prevented; articulate specificity is not accuracy · neither lane is the better lane; the wrong-unit error at three layers · the duplicate-lane composition paid out three ways (coverage, correction, calibration) · the brief is the variable · sharing a task is not sharing an answer; sentence granularity · a correction is a new claim · a verdict and its reason are separable · a mechanism claim not read at source is provisional whoever relayed it (measured: seven β withdrawals, all unverified relays) · an absence claim is a claim about the search unless the search is unbounded · identity from the harness, not the record · a control that refuses the unanticipated · 0% envelope compliance · resource guards clearable on evidence, adjudicating guards never · the pre-commitment binds the obligation, not the sentence.

## Gaps carried by name
S5-4 undischarged at the pin · the four population-adjacent test files not run under the mutations · the four un-audited surfaces (row 318) · TRANSFORM-ROUTING.md without an enforcer (ED-394) · the `## Asserted` scanning mechanism · F-Q2I-3's omitted consequence.

## Commit-message corrections (S5-1 reaches commit messages)
`417147d` — git note (the builder's "mutated"→"unmutated" self-disclosure) · `69143de7`, `e533f7f9` — git notes (three-lane close declared with two lanes omitted; corrected record at 68c7fefb / 0a3fd916; ε's later `-f` overwrite recovered and disclosed in the notes — ED-395).

## Successor
S-VLADW1-06 minted tracker-only from `runtime/vlad-w1/s06/plan-payload.json` via `add-sprint.js` + `plan.js` + `routing.js record`, manifests regenerated (ED-365; never hand-flipped YAML, ED-359). No build authorisation is implied by the mint.
