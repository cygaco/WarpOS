# β RETURN — S-VLADW1-05 — plan→design CLEARED + RELEASE RULE MINTED (S5-1…S5-7)

Held in the sprint's own directory so the rule travels with the sprint rather than only in the ledger.
**Canonical texts:**
- Row **317** — `2f8c15e6-9d43-4a70-b1c9-84e06fa3d7b2` — `runtime/beta-consult/S-VLADW1-05-r1r2-release-rule-2f8c15e6.md`
- Row **318** — `7b3e6d21-c48f-4e95-a012-3f9d5c07b6ea` — `runtime/beta-consult/S-VLADW1-05-r2-amendment-7b3e6d21.md` (AMENDS 317's S5-2 scope + terminal)

Both minted **before any build result exists**. **Numbering is S5-n; S4-1…S4-6 do not carry and must
never be mis-cited into this sprint.** α applies the rule at the qualifying close — not ε, not β.

The rule itself is carried **verbatim** in
`trackers/sprints/S-VLADW1-05-one-transform-both-sites.md` under *Release rule*, with row 318's
amendment as a leading block. It is not paraphrased here; read it there or at the canonical paths.

## What the return changed in the design, and where each change landed

| β's item | Where it landed |
|---|---|
| Scope **`recommended`, not `expanded`** — no vendored table | Tracker *Scope — RATIFIED*, with β's reasoning quoted (the lesson is a too-broad sentence, not a too-small fold) |
| **Bundle O flag** — derive the fold's domain from the module's **exported tokens**, not from "the letters `Ceiling` and `Asserted` need" | Build spec, bundle O task 1, **rewritten**, with the measurement below |
| **S5-2 as bundle P's acceptance shape** (a) emitted (b) unit-naming frame (c) closure only by property or stated finite domain | Build spec, bundle P, as a standing block above its tasks |
| **RF-M1 claimed over SEVEN, not eight** | Build spec, bundle M task 2, rewritten |
| **S5-4 is a LANE obligation** — the design battery does not discharge it | Build spec, its own section; to be carried into every gauntlet brief |
| **`falsification_attempts` is NOT a criterion** — DoD + standing discipline | Build spec, same section; tracker standing discipline |
| **Row 318:** S5-2 in full for authored/edited claims + all of `CUSTODY.md`; **disclosure not repair** for the four untouched files | Tracker rule block; build spec **bundle P task 5** |
| **Row 318:** bundle O's emission is a **requirement**, not a design choice | Build spec, bundle O |
| **Row 318:** diagnostic lanes take the four un-audited files as a **secondary objective**, which cannot grow scope or fire the gate | Build spec, its own section |
| **Row 318:** amended TERMINAL honesty sentence | Tracker rule block; build spec closing section |

## β's flag on bundle O was right, and here is the measurement

My original task 1 scoped the fold widening to *"the letters `Ceiling` and `Asserted` need"*. β: the
token alphabet is larger (`PROVEN`, `NOT VERIFIED`), so an emitted set over a too-narrow domain is
**false one level down**. Measured against the module's own exports:

    exported tokens: ["PROVEN", "ASSERTED — NOT VERIFIED"]
    token+keyword alphabet: ACDEFINOPRSTVdegilnrst  (22 letters)
    covered by CONFUSABLE_FOLD: ACDEFINOPSTVeis
    MISSING from the fold:      R d g l n r t   (7)
    missing if scoped to Ceiling+Asserted ONLY: d g l n r t   (6)

**Capital `R` is the difference** — contributed by `PROVEN`/`VERIFIED`, and it would have been left
unmapped while the emitted sentence read as honest. The domain must be read from the module's exports at
build time, exactly as the battery reads the token *"from the module's own export, never guessed."*

## The conductor's own granularity error, caught while verifying the rule's premise

β flagged that the four-file list is **ε's, unchecked by β, and now load-bearing in the rule**, and asked
that it be resolved by a read before the close. **Resolved now rather than at the close.**

**The list is CORRECT.** The qa lane names exactly those four verbatim in `what_i_could_not_assess`:
*"src/env-scrub.js, src/model-seam.js, driver/host-free-driver.js and src/server-entry.js carry
substantial custody prose in comments that I sampled rather than read end to end."* The backend lane
independently corroborates two (`env-scrub.js` header-and-greps only; `server-entry.js` greps and the
RF-7 region only).

**My FRAME was wrong.** I told β *"every lane said so."* The **security lane did not say it** — it
mentions all four files, but never in a `files_i_could_not_see` or `what_i_could_not_assess` context. So
one lane said all four, a second corroborated two, and a third did not say it at all.

**The data was right and the frame rounded up** — which is exactly β's Q2 distinction about its own
S4-1a sentence, committed by me, **inside the consult that established the class**, while arguing for a
criterion against it. It changes nothing about the rule (the four files are genuinely un-audited and the
disclosure obligation stands) and it is recorded because a correction that leaves no trace teaches
nothing.

## β's credit, recorded because it is also an instruction

> *"Credit, load-bearing: the unsafe assumption stated against ε's own interest, stake declared on all
> five questions, both self-caught defects pre-disclosed… The first consult in the arc where the
> conductor's disclosures did more work than β's review — and the S5-2 scope defect was β's, caught by
> ε's Q2."*

The instruction inside the credit: **stating an assumption against your own interest is what made the
rule fit the work.** S5-2 as first minted would have fired a terminal on unbriefed surface.

## β's own not-read list, carried so it is not lost

`run-battery.mjs` (so *"the harness drives the real matcher"* and the discarded-harness account are ε's
report, unverified by β, and **load-bearing for every AS-IS/FIXED cell** in the design battery);
`custody-claim-lint.js` at `6a105f2`; the gauntlet-2 lane evidence files — **including the
`what_i_could_not_assess` fields the four-file list rests on** (now resolved above, with a correction).

**Build authorization remains the operator's.** The 2026-08-28 mandate was an override and does not carry.
