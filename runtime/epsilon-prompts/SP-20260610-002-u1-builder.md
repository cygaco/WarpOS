# BUILD UNIT u1 — SP-20260610-002 tickets T-287/T-288/T-289 (registry pins + parity enforcement)

You are a build-chain builder working in an ISOLATED WORKTREE (your cwd, branch build/SP-20260610-002-u1). Edit ONLY inside this worktree. Commit your work to the current branch when done. Do NOT push.

## Context
Canonical WarpOS. The role-registry (`.claude/agents/_org/role-registry.json`) is the declared single routing source (ADR-0008). Three verified bugs (doogle WG-2/WG-5, reproduced in canonical 2026-06-10):
1. Six role specs carry `model: inherit` → in-process Agent-tool spawns silently inherit the session model, bypassing registry routing.
2. `president/beta.md` pins `claude-sonnet-4-6` while the registry maps beta → `claude-opus-4-8`.
3. `dispatch-contract.json` class_derivation first-match `{tier:lead}→manager` forces shape `in-process-agent` for design-lead, but the registry routes design-lead `openai/gpt-5.5` subprocess (`dispatch_path: subprocess-consult`). Shape-based enforcers read contradictory declarations.

## TICKET T-287 — pin the specs
Files: `.claude/agents/engineering/director-of-engineering.md`, `growth/copy-lead.md`, `president/cabinet.md`, `president/ops-analyst.md`, `product/director-of-product.md`, `product/product-lead.md`, `president/beta.md`, `_system/frontmatter-guide.md`.
1. FIRST check whether `_warpos/agents/` (or any view-regen source for `.claude/agents/`) exists in this repo. If a source layer exists, edit the SOURCE and regenerate the view (keep byte-identical); if not (canonical .claude/agents IS the source), edit in place.
2. For each of the 6 `model: inherit` specs: read the role's entry in role-registry.json. If `provider` is `claude` (or absent), set frontmatter `model:` to the registry's `model` value. If `provider` is openai/gemini, set frontmatter `model: claude-opus-4-8` (the claude-fallback spawn model — these roles are subprocess-routed; the frontmatter only governs Agent-tool/claude-fallback spawns).
3. `president/beta.md`: change `model: claude-sonnet-4-6` → `model: claude-opus-4-8` (registry value).
4. `_system/frontmatter-guide.md`: it currently documents `model: inherit` as a valid value — rewrite that guidance: `inherit` is FORBIDDEN for registry-routed roles (roles present in role-registry.json); the registry is the routing SoT; frontmatter model must be the registry model (claude roles) or the claude-fallback pin (cross-provider roles).
5. Verify: `grep -rn "^model: inherit" .claude/agents --include="*.md"` → only hits (if any) must be non-registry agents; ideally zero.

## TICKET T-288 — role-parity-scan model check
File: `scripts/checks/role-parity-scan.js` (read it fully first; follow its existing structure/idioms; it has a test `scripts/checks/role-parity.test.js`).
Add a check: for every role in role-registry.json that has a `spec` path, parse the spec's frontmatter `model:`; FAIL (non-zero exit, clear message naming role+spec+expected) when:
- frontmatter model is `inherit`, OR
- registry provider is claude/absent AND frontmatter model ≠ registry model.
(Cross-provider roles: any concrete model is acceptable, `inherit` is not.)
Add planted-violation coverage to role-parity.test.js following its existing fixture idiom: one planted inherit, one planted wrong-model-on-claude-role, plus a green case. Keep ALL existing tests passing.

## TICKET T-289 — dispatch-contract derivation rule + shape-vs-route parity
Files: `.claude/agents/_org/dispatch-contract.json`, `scripts/checks/role-parity-scan.js` (+ test).
1. In `class_derivation.rules`, insert BEFORE the `{"when":{"tier":"lead"},"class":"manager"}` rule a rule matching cross-provider leads — e.g. `{"when":{"tier":"lead","provider":"openai"},"class":"cross_provider_consult_lead"}` (and gemini analog if any gemini lead exists; check the registry). Then add the matching `role_classes` entry: allowed_shapes `["subprocess-cross-provider"]` (mirror the shape vocabulary used elsewhere in the file — read `shapes` first and reuse the EXACT shape id used for cross-provider subprocess dispatch), forbidden `["api"]`, coverage fields mirroring the existing cross_provider_reviewer class. Preserve every existing rule order otherwise; this is a first-match ordered list.
2. Extend role-parity-scan.js: derive each registry role's class via the contract's class_derivation (first-match, same semantics as the contract `_doc` describes) and FAIL when the derived class's allowed_shapes cannot express the registry route (e.g. registry provider openai/dispatch_path subprocess but derived shapes are in-process-only). At minimum the design-lead contradiction must be caught by a planted fixture: revert-style fixture where the cross-provider-lead rule is absent → scan fails; with the rule → green.
3. Sanity: `node -e` derive design-lead → subprocess class; product-lead/quality-lead (claude leads) still derive `manager`.

## Gates (run before committing)
- `node scripts/checks/role-parity-scan.js` → exit 0 on the repo as fixed.
- `node scripts/checks/role-parity.test.js` → all green including new planted cases.
- `node scripts/checks/dispatch-routing-parity.js` → still green.
- If a views-fresh check exists (`scripts/checks/*views-fresh*`), run it → green.

## Output contract
Commit all changes to branch build/SP-20260610-002-u1 with message "fix(SP-20260610-002): registry model pins + parity enforcement (T-287/288/289, doogle WG-2/WG-5)". End your final message with the JSON envelope:
```json
{"status":"built","feature":"laneb-u1","branch":"build/SP-20260610-002-u1","files_modified":[...],"commit_sha":"...","tests_green":true,"notes":"..."}
```
