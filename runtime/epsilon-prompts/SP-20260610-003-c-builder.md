# BUILD UNIT C — SP-20260610-003 tickets T-297/T-298 (sprint-pipeline truth)

You are a build-chain builder in an ISOLATED WORKTREE (your cwd, branch build/SP-20260610-003-c). Edit ONLY inside this worktree; commit to the current branch; do NOT push. Do NOT touch: scripts/sprint/release.js, scripts/generate-framework-manifest.js, scripts/checks/warpos-install-baseline.js (a sibling lane owns those), scripts/research/** (the other unit owns that).

## Context (all verified reproducing 2026-06-10)
- doogle WG-3: in sprint mode, /mode:sprint promises "ε conducts the lifecycle ... really dispatches", but the `/sprint:full` skill body (.claude/commands/sprint/full.md) instructs the OPPOSITE — α constructs the Plan Contract, fills requirement templates, mints tickets, and drives the Ralph loop itself (`grep -c epsilon .claude/commands/sprint/full.md` → 0). The engine's ε-conduct exists but is opt-in (scripts/sprint/full.js:178-179: `epsilon: process.env.WARPOS_EPSILON_RUNTIME === "on", epsilonDispatch: false`). The org roster is silently bypassed on the default path. ALSO: `emitPhaseConsults` emits `manager_consult` telemetry on BOTH paths while only --epsilon-dispatch writes real per-agent completion records — consult-presence checks can read green while no manager ever ran.
- doogle WG-7 (re-reproduced LIVE today in canonical): the Phase-2 design scaffold mints a PRD with a fixed-size R-list while generated stories/trace reference more R-ids than the PRD defines — traceability born broken; req-format-guard waives it as "legacy".
- almanac AL-W-006: scripts/sprint/status.js:55-100 field schema differs from the current.yaml schema (missing crash_recovery, ralph, reports); checkpoint.js:83 returns `existing` without schema validation.

## TICKET T-297 — sprint:full org-era + epsilon-dispatch default + design-transition enforcer
1. `scripts/sprint/full.js`: make `epsilonDispatch` (and `epsilon`) DEFAULT ON when the LIVE MODE IS SPRINT — detect via `scripts/hooks/lib/mode.js`'s `isSprint()` (the S-LC-01 mode-lifecycle-registry mechanism; read lib/mode.js first; do NOT hardcode a mode literal or read mode.json directly). Explicit CLI flags / env always win over the default. Non-sprint modes: behavior byte-identical to today. Add planted tests to the existing engine test file (scripts/sprint/test-sprint-full.js or the test idiom you find): sprint mode → defaults ON; solo/adhoc → unchanged; explicit flag wins.
2. `.claude/commands/sprint/full.md` skill rewrite (org-era): Phase 2 (design) routes through ε's hook-point roster (product-lead/design-lead/quality-lead/copy-lead/director-of-engineering author the requirement bundle; α relays in-process Agent-tool dispatches when ε is a teammate per ED-041 — language must AGREE with .claude/commands/mode/sprint.md and .claude/agents/president/epsilon.md, read both first); ticket minting executed by the conductor on product-lead's story→ticket mapping; Step 3 Ralph loop conducted with real dispatch records. Keep the engine-seam facts true (full.js halts/resume contract unchanged). Mention the sprint-mode default flip. Do NOT delete the halt/resume/recovery reference sections — update them.
3. Design-transition enforcer (β: fail-closed shape, wire REPORT-ONLY): when the design phase completes under ε-conduct (epsilonDispatch active), full.js's design→designed transition must verify roster completion records exist on the ledger (.claude/runtime/dispatch-completions.jsonl) for the dispatched design roles; if requirement artifacts changed but NO roster records exist, emit a loud `design_without_roster` warning event (report-only this sprint — log via the engine's existing event emitter; include a TODO marker naming the blocking ramp). Scope: only newly scaffolded sprints under ε-conduct; legacy/non-ε paths untouched.

## TICKET T-298 — R-id single-sourcing + trace-integrity + schema align
1. Find where the Phase-2 scaffold derives the PRD R-list vs stories/trace R-references (scripts/sprint/design.js + framework/templates/sprint/requirements/*). Make BOTH derive from the SAME source — the plan contract's requirement_areas — sized dynamically (N requirement areas → R-1..R-N everywhere). A sprint with >3 requirement areas must scaffold consistent sets.
2. Trace-integrity check: every R-id cited in granular-stories.md/trace.md MUST be defined in prd.md — FAIL (not legacy-waive) for newly scaffolded sprints. Wire into the natural owner (scan:requirements check or req-format-guard — read both, follow the existing idiom; report-only wiring is fine if the owner is a guard that would block existing sprints, but the scaffold-time path must be strict).
3. AL-W-006: align `scripts/sprint/status.js` field schema with the real current.yaml schema (crash_recovery, ralph, reports fields — read scripts/sprint/add-sprint.js's skeleton as the SoT) and make `checkpoint.js` validate/normalize `existing` against the schema instead of returning it blind. Keep all existing sprint tests green.

## Gates (run before committing; set CLAUDE_PROJECT_DIR to this worktree root)
- The engine test suite you extended (all green incl. your planted cases).
- `node scripts/sprint/status.js` (or its test) — green on a real sprint dir.
- A scaffold smoke: if design.js has a dry-run/test path, prove a >3-area contract scaffolds consistent R-ids.

## Output contract
Commit with message "fix(SP-20260610-003): sprint-pipeline truth — epsilon-dispatch default + R-id single-source + schema align (T-297/298, WG-3/WG-7/AL-W-006)". End with the JSON envelope:
{"status":"built","feature":"lanecd-c","branch":"build/SP-20260610-003-c","files_modified":[...],"commit_sha":"...","tests_green":true,"notes":"..."}
