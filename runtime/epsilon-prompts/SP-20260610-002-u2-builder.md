# BUILD UNIT u2 — SP-20260610-002 tickets T-290/T-291 (epsilon conduct docs + liveness enforcer)

You are a build-chain builder working in an ISOLATED WORKTREE (your cwd, branch build/SP-20260610-002-u2). Edit ONLY inside this worktree. Commit to the current branch when done. Do NOT push. Do NOT touch: any file under `.claude/agents/` EXCEPT `president/epsilon.md` and `_system/guides/agent-dispatch-guide.md`; do NOT touch role-parity-scan.js or dispatch-contract.json (a sibling builder owns those).

## Context
Verified bugs (doogle WG-4/WG-6, reproduced in canonical 2026-06-10):
- WG-4: `.claude/agents/president/epsilon.md` documents an in-process Agent-tool conduct loop that a TEAMMATE-spawned ε cannot execute — the harness refuses the Agent tool inside subagents (ED-041). `/mode:sprint` (the mode skill) was already fixed and carries the correct "Who may call the Agent tool (ED-041)" language; epsilon.md and the dispatch guide were not. The operator RATIFIED (doogle 2026-06-09) subprocess dispatch as the sanctioned teammate-ε route: `claude -p --agent <role>` for non-build Claude roles, `node scripts/dispatch-claude.js` for build-chain, `node scripts/dispatch-agent.js` cross-provider.
- WG-6 (recurring ×3): teammates wake ONLY on incoming SendMessage; a teammate's own background subprocess completing does NOT re-wake it. ε's heartbeat/circuit-breaker section (epsilon.md ~§109-120) runs only when ε IS the session. A teammate-ε that goes idle "waiting for returns" waits FOREVER (observed 25-min stall; the teammate even believed "the harness re-wakes me" — the belief is part of the failure mode).

## TICKET T-290 — epsilon.md + agent-dispatch-guide.md
1. `.claude/agents/president/epsilon.md`:
   a. Find the conduct-loop / dispatch section. Add (or rewrite the stale part into) a clearly-marked "Conduct routes by spawn context (ED-041)" subsection: when ε is the TOP-LEVEL session face (α wearing ε), the Agent tool is available for the in-process roster; when ε is a TEAMMATE (spawned via Agent into a team), the Agent tool is UNAVAILABLE — the SANCTIONED routes are subprocess-only (claude -p --agent for non-build Claude roles; dispatch-claude.js for build-chain; dispatch-agent.js cross-provider), returns captured to files + recorded via `record-inprocess --evidence` where applicable. Operator-ratified 2026-06-09.
   b. Add a "STARTUP ROUTE SELF-CHECK" instruction: at spawn, ε determines its context (teammate vs session) and states in its readiness report which conduct route is active — so the doc's promise is verified at spawn, not assumed.
   c. Add a "TEAMMATE STALL RULES (WG-6)" block: (1) NEVER go idle while a background subprocess is outstanding — the wake event cannot arrive; the belief that the harness re-wakes a teammate is FALSE; (2) dispatch subprocesses BLOCKING/foreground (bounded by DISPATCH_BUILDER_TIMEOUT_MS) and record completions in the SAME turn; (3) before any unavoidable wait/idle point, report concrete state (what's outstanding, where evidence will land) to the team lead so the lead can watchdog; (4) the lead's recovery: idle ≠ dead — SendMessage wakes an idle teammate.
   d. Annotate the existing Heartbeat/Circuit-Breaker section: it applies ONLY when ε is the session (δ-style standalone); it does NOT run for teammate-ε (that's what the stall rules + the epsilon-liveness check cover).
2. `.claude/agents/_system/guides/agent-dispatch-guide.md`: add a compact "§ Teammate-ε conduct routes (ED-041)" section (place it near §9 in-process Agent dispatch; follow the guide's heading/numbering style — read the file first) with the same sanctioned-route table + a pointer to epsilon.md for the stall rules. Keep it tight (≤30 lines).
Consistency: language must agree with `.claude/commands/mode/sprint.md`'s ED-041 section (read it first; do not contradict it).

## TICKET T-291 — epsilon-liveness fail-closed check
New file: `scripts/checks/epsilon-liveness.js` (+ `scripts/checks/epsilon-liveness.test.js`).
Purpose: make a stalled conductor SELF-DETECTING — evidence files exist but no matching ledger record after N minutes = loud failure.
Design (follow the idioms of existing scripts/checks/* — read one like `scripts/checks/warpos-install-baseline.js` or `mode-lifecycle-registry.js` for shape):
- Inputs: an evidence dir (default `.claude/runtime/epsilon-prompts/` and/or `runtime/epsilon-prompts/` — detect which exists; also accept `--evidence-dir`), the completion ledger `.claude/runtime/dispatch-completions.jsonl`, threshold `--stale-minutes <N>` default 10, `--now <ISO>` override for deterministic tests.
- Logic: for each `*.return.txt` / evidence file older than N minutes (mtime or embedded timestamp), look for a ledger record referencing it (by filename or evidence_sha or sprint+role+step within a window after the file's mtime). Missing record → report `epsilon-stalled` finding. ANY finding → exit 1 (fail-closed) with a clear message listing the orphaned evidence. Empty/missing dirs → exit 0 with "nothing to check" (not an error). Malformed ledger line → count it, warn, do NOT crash (but a fully unreadable ledger WITH evidence present = exit 1, fail-closed — a gate must not green on a lying input).
- Test: deterministic fixture (temp dir; planted evidence file with old mtime via fs.utimesSync + a ledger with/without the matching record; use --now). No wall-clock sleeps. Cases: green (record present), red (orphaned evidence), green (no evidence), red (unreadable ledger + evidence).
- Wire REPORT-ONLY into the scan suite: read `.claude/commands/scan/full.md` to see how checks are registered (e.g. a checks list/table) and add epsilon-liveness there in the same idiom, marked report-only. If wiring requires touching files a sibling builder owns (role-parity-scan.js, dispatch-contract.json) — STOP and note it instead; the scan/full.md skill file itself is yours to edit.
Do NOT regen manifests (the orchestrator does that at merge).

## Gates (run before committing)
- `node scripts/checks/epsilon-liveness.js` on the real repo → exit 0 (no stale evidence expected) or a TRUE finding (report what it found).
- `node scripts/checks/epsilon-liveness.test.js` → all cases green.

## Output contract
Commit to branch build/SP-20260610-002-u2 with message "fix(SP-20260610-002): epsilon conduct routes + stall rules + liveness check (T-290/291, doogle WG-4/WG-6)". End with the JSON envelope:
```json
{"status":"built","feature":"laneb-u2","branch":"build/SP-20260610-002-u2","files_modified":[...],"commit_sha":"...","tests_green":true,"notes":"..."}
```
