# BUILD UNIT D — SP-20260610-003 ticket T-299 (research:deep runnability)

You are a build-chain builder in an ISOLATED WORKTREE (your cwd, branch build/SP-20260610-003-d). Edit ONLY inside this worktree; commit to the current branch; do NOT push. Your surfaces: scripts/research/** (new dir ok) + .claude/commands/research/deep.md ONLY. Do NOT touch scripts/sprint/**, scripts/checks/**, .claude/agents/**.

## Context (masterconsole MC-WG-2 + MC-WG-3, verified reproducing 2026-06-10)
.claude/commands/research/deep.md is structurally UNRUNNABLE under the Claude Code harness security model: it instructs foreground `sleep 15/90/60` bash polling (blocked / wasteful), `node -e` inline scripts that writeFileSync (classifier-blocked), and sub-agent file-writes. Its Phase 0 checks API-key MODEL ACCESS (curl /v1/models) but never CREDIT — a depleted key passes Phase 0 then dies async mid-pipeline with insufficient_quota/429.

## TICKET T-299
1. **Read .claude/commands/research/deep.md FULLY first** — it defines the real pipeline (OpenAI Deep Research API + Gemini Deep Research/Interactions API + Claude multi-round legs, brief-writing, synthesis). Your job is to RE-HOUSE the orchestration, not redesign it.
2. **NEW `scripts/research/deep-run.js`** — standalone node runner owning everything the harness blocks:
   - All async polling INTERNAL (setTimeout/await loops inside node — no bash sleep).
   - All fs-writes inside the script (runtime/research/<slug>/... outputs, same paths the skill used).
   - Key loading per the existing wrappers' idiom: use scripts/dispatch/auth-resolver.js for SOURCE resolution where applicable; .env.local / ~/.gemini/.env reads mirroring scripts/hooks/lib/providers.js — NEVER print key values.
   - The API calls themselves are ALLOWED here (deep-research is the whitelisted API exception per the dispatch guide §1).
   - CLI: `node scripts/research/deep-run.js "<question>" [--providers openai,gemini] [--out <dir>] [--skip-phase0]` + `--help`. Structured progress lines to stdout; exit non-zero on total failure; partial-provider failure = continue with the rest + clear summary.
   - **Phase 0 quota probe (MC-WG-3):** before submitting any deep-research job, make a TINY billable call per enabled provider (cheapest model, max_tokens<=5) and classify the result: ok | insufficient_quota | auth_failed | model_unavailable. A failed provider is SKIPPED UP FRONT with an actionable message (e.g. "openai: insufficient_quota — top up credits or pass --providers gemini"). Probe cost is sub-cent; note it in --help.
   - Claude's leg stays in the skill (it uses the harness Agent tool + WebSearch which a node script cannot reach) — the script handles the openai+gemini legs and emits a manifest the skill's Claude leg + synthesis step consume.
3. **Rewrite .claude/commands/research/deep.md as a THIN WRAPPER:** Phase 0+openai+gemini legs = "run `node scripts/research/deep-run.js ...` (background ok — the harness re-wakes the top-level session)"; keep the Claude leg + final synthesis as skill-body steps reading the script's manifest. The rewritten skill must contain NO `sleep <N>` and NO `node -e` write patterns (grep yourself to prove it). Preserve the skill's frontmatter contract and overall phase numbering where sensible.
4. **Test:** `scripts/research/deep-run.test.js` — no live API calls: inject/stub the HTTP layer (design the script with an injectable fetch/transport seam). Cases: --help exit 0; Phase 0 classifies a stubbed 429-insufficient_quota → provider skipped with actionable message; stubbed happy-path poll loop completes and writes the manifest; total-failure exits non-zero. Plus the self-grep: deep.md contains no blocked patterns.

## Gates (run before committing)
- `node scripts/research/deep-run.js --help` → exit 0.
- `node scripts/research/deep-run.test.js` → all green, zero live API calls.
- `grep -nE "sleep [0-9]+" .claude/commands/research/deep.md` → no matches; same for `node -e .*writeFileSync`.

## Output contract
Commit with message "feat(SP-20260610-003): research:deep standalone runner + quota probe (T-299, MC-WG-2/3)". End with the JSON envelope:
{"status":"built","feature":"lanecd-d","branch":"build/SP-20260610-003-d","files_modified":[...],"commit_sha":"...","tests_green":true,"notes":"..."}
