# BUILD UNIT — SP-20260610-005 tickets T-300/T-301 (E-DISPATCH-INTEGRITY F-1+F-3: coverage-honesty)

You are a build-chain builder in an ISOLATED WORKTREE (your cwd, branch build/SP-20260610-005-u1). Edit ONLY inside this worktree; commit to the current branch; do NOT push. Do NOT touch scripts/dispatch/dispatch-skill.js or scripts/dispatch/safe-spawn.js (a sibling sprint owns those).

## Context
WarpOS's recurring fake-green class: a sprint phase can read "covered" from telemetry alone. The epic (E-DISPATCH-INTEGRITY-001) root causes:
- RC-2 "sprint theater": `scripts/sprint/full.js#emitPhaseConsults` emits `manager_consult` records on BOTH the legacy and ε paths; only --epsilon-dispatch writes real per-agent completion records. Coverage scans that check consult-record PRESENCE read green while no manager ever ran.
- RC-4/G4 "historic green" (the T3 incident): `gauntlet-verify` matched ANY historic ok:true completion record — a lane that never ran in THIS sprint was greened by an old record.

## TICKET T-300 (F-1) — record-backed coverage predicate
Files: `scripts/checks/sprint-manager-consult.js` (+ `test-sprint-manager-consult.js`), `scripts/checks/sprint-hook-coverage.js` (+ `test-sprint-hook-coverage.js`). READ each fully first — follow their existing structure, fixture idiom, and output format.
1. For each consult/hook-point the scan currently counts as covered via a telemetry record, ALSO require a backing **ok:true completion record** on the dispatch ledger (`.claude/runtime/dispatch-completions.jsonl` — resolve via the checks' existing path idiom, paths lib if they use it) **correlated to the same sprint** (sprint_id field; fall back to the record's role+step+time-window if that's what the ledger offers — inspect real ledger lines first and match on what's actually stamped).
2. **Legacy cutoff (epic risk mitigation):** sprints whose records/artifacts date before `2026-06-10` keep the OLD predicate (telemetry-only acceptable) — historic sprints must NOT go retroactively red. Implement as a named constant `RECORD_BACKED_CUTOFF = "2026-06-10"` with a comment citing E-DISPATCH-INTEGRITY-001 F-1. Use each scan's existing notion of the sprint's date (registry created_at or artifact dates — whatever it already reads; do not invent a new source).
3. Planted fixtures BOTH ways: post-cutoff sprint with telemetry-only consult → RED; same with backing ok:true record → GREEN; pre-cutoff telemetry-only → GREEN (legacy). Keep all existing test cases green.

## TICKET T-301 (F-3) — gauntlet-verify correlation + whole-ledger refusal
Files: `scripts/dispatch/gauntlet-verify.js` (+ `gauntlet-verify.test.js`). READ fully first.
1. Make verification CORRELATED: a record counts only if it matches the sprint under verification (`--sprint <id>` matching the record's sprint_id stamp) AND falls in a bounded window (`--since <ISO>` or an automatic window derived from the sprint's start). If records lack a sprint_id field today, correlate on the window + role and ALSO stamp-check: prefer requiring whichever of sprint_id/run_id IS stamped by dispatch-agent.js/dispatch-claude.js records (inspect a real ledger line; dispatch records carry run_id/dispatch_id + start_time).
2. REFUSE unbounded verification: invoking gauntlet-verify with neither a sprint correlation nor a window exits NON-ZERO with a usage error explaining why (a historic ok:true must never green a never-ran lane — cite F-3). No silent default-to-everything.
3. CALLER AUDIT (mandatory): `grep -rn "gauntlet-verify" scripts/ .claude/commands/ .claude/agents/` — update every live caller (e.g. scripts/sprint/epsilon-runtime.js, sprint close paths, agent specs' instructions) to pass the correlation args so nothing breaks at integration. List the callers you updated in your envelope notes.
4. Planted fixture: a ledger containing ONLY a historic ok:true record (older than the window / different sprint) must FAIL verification for the current sprint; a correlated fresh record passes. Keep existing tests green.

## Gates (run before committing; CLAUDE_PROJECT_DIR=this worktree root)
- node scripts/checks/test-sprint-manager-consult.js → green incl. new cases
- node scripts/checks/test-sprint-hook-coverage.js → green incl. new cases
- node scripts/dispatch/gauntlet-verify.test.js → green incl. historic-green planted failure
- The real scans against the repo: `node scripts/checks/sprint-manager-consult.js` + `sprint-hook-coverage.js` — must NOT retroactively red historic sprints (legacy cutoff working). If they red on a REAL post-cutoff gap, report it honestly in the envelope (do not paper over).

## Output contract
Commit with message "feat(SP-20260610-005): F-1 record-backed coverage + F-3 gauntlet-verify correlation (E-DISPATCH-INTEGRITY-001)". End with the JSON envelope:
{"status":"built","feature":"f1f3","branch":"build/SP-20260610-005-u1","files_modified":[...],"commit_sha":"...","tests_green":true,"callers_updated":[...],"notes":"..."}
