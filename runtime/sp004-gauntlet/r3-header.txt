===== GAUNTLET ROUND 3 — verify the round-2 fixes are COMPLETE =====
Rounds 1-2 found real fail-open + signing holes; the authors applied two unified fixes. VERIFY each R2 finding
is fully closed AND look for any NEW regression. Do NOT re-raise a finding that is now genuinely closed.
ROUND-2 findings + the claimed fix (in the diff below):
- SR-R2-001 (CRIT, ok/fallback unsigned): attest-signing.js SIGNED_FIELDS now INCLUDES ok + fallback (+ verdict).
  A signed FAIL/fallback record edited to passing invalidates the sig. VERIFY: are ALL fields the gates read
  now signed? Any decision field still unsigned?
- SR-R2-002 (panel reader no sig): dispatch-review.js applyPanelGate now takes ctx.requireSignature (ON in the
  live call at the isSecurityPanelRole path) and rejects a lane record failing verifyRecord. VERIFY the live wiring.
- SR-R2-003/BE-R2-002 (legacy verdict-flip): the legacy multi-set fallback in verifyRecord was REMOVED; it is
  single-set now. A record signed under an older field set fails-closed. VERIFY no flip loophole remains.
- BE-R2-001 (permissive top-level default): isCanonicalPresidentRole (STRICT exact: normalized === 'alexalpha'
  or 'alpha') validates top_level_human_default; the permissive isPresidentIdentity is used ONLY to BLOCK
  dispatched workers. VERIFY 'president-worker' etc. are rejected as a default AND all aliases still blocked as workers.
- R2-ED220-DISPATCH-OPEN: a bogus top_level_human_default is now rejected in validateRoleBindingValues (GLOBAL),
  so a dispatched worker against a corrupt control also fail-closes. VERIFY.
Return the SAME strict JSON verdict. Below is the round-1 directive, then CONTEXT, then the UPDATED DIFF.
