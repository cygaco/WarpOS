===== GAUNTLET ROUND 6 — CONFIRM the same-session reader consolidation is COMPLETE (β teeth #2) =====
This is the family-matched CONFIRM of the same-session-reader consolidation (β DIRECTIVE). The diff below is
SCOPED to the consolidation surface (the helper, the guard, and every converted same-session reader).
ROUND-5 findings + fixes (verify closed):
- SR-R5-001 (missed reader): scripts/skills-test.js now routes its ok:true backing check through
  isVerifiedLivenessRecord; the guard's SCAN_DIRS is broadened to ALL of scripts/ (recursive) so a reader in
  scripts/ root is no longer missed.
- SR-R5-002 (per-FILE token hollow): the guard is now PER-READ — each raw record-.ok predicate must have
  verification within a ±12-line window, OR carry a reviewed `liveness-verified:` code pragma (a per-LINE
  annotation with a reason, NOT a settable per-record field). Legit shape-only/false-positive cases are
  annotated (gauntlet-verify isWellFormedOkRecord = shape, verified in verifyGauntlet; coverage-gate phantom
  guard = rejects-unsigned-never-trusts; cert-attest spawn-result .ok = a subprocess exit not a ledger record).
The STRUCTURAL guard is the recurrence-stopper: a NEW same-session reader that trusts field-only ok:true FAILS
it. CONFIRM: (1) does every converted same-session reader verify? (2) does the guard actually catch a new
unverified same-session reader now (per-read, all-scripts)? (3) are the `liveness-verified:` pragmas each on a
GENUINE shape-only/non-ledger case (not hiding a real trust-unsigned read)?
RULED OUT OF SCOPE (do NOT re-raise as a fix): the cross-session sealed-capsule ceiling (ED-232, α/β-ruled
honest ceiling); a request for full AST/dataflow instead of the regex+window guard (that is the tracked ED-229
AST-upgrade — a named residual, this guard is a bounded structural heuristic by design). Return the strict JSON verdict.
Below is the round-1 directive, then CONTEXT, then the SCOPED consolidation DIFF.
