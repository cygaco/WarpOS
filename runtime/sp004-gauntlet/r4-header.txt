===== GAUNTLET ROUND 4 — verify the round-3 fixes are COMPLETE =====
Rounds 1-3 hardened the binding + signing; findings are CONVERGING (each round more specific). VERIFY the
round-3 fixes and look for any REMAINING real crit/high hole. Do NOT re-raise closed findings.
ROUND-3 findings + the claimed fix (in the diff):
- SR-R3-002/SR-R2-001-INCOMPLETE (CRIT, unsigned correlation): attest-signing.js SIGNED_FIELDS now includes
  sprint_id + started_at (in addition to ok/fallback/verdict). VERIFY every field a liveness reader trusts
  (gauntlet-verify: role, ok, provider, completed_at/started_at, sprint_id, fallback) is now signed.
- SR-R3-001 (high, third reader): test-sealed-capsule-gate.js verifyTyped now defaults requireSignature:true.
  The live verifyGauntlet readers are: gauntlet-verify CLI (default on), applyPanelGate (live-on), verifyTyped
  (default on). VERIFY no OTHER live liveness reader omits signature verification.
- BE-R3-001 (high, alias miss): role-resolver.js isPresidentIdentity now maps Greek α/Α→alpha and matches bare
  'alex' + 'alpha' exact (+ alexalpha/president substrings). VERIFY bare 'Alex'/'α' are blocked as a worker role
  AND no false-positive on legit roles (alexa-connector, alpha-tester, alexander).
Return the SAME strict JSON verdict. Below is the round-1 directive, then CONTEXT, then the UPDATED DIFF.
