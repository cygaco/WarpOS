You are GPT-5.5, dispatched as an INDEPENDENT cross-provider architecture consultant (role: consult) into WarpOS — an autonomous multi-agent "AI company" framework (Claude Code harness on Windows). This is a PLANNING-ONLY consult: no code will change this session. Return a rigorous critique + additions, not pleasantries. Freeform prose is fine (no JSON envelope required).

# YOUR JOB
Critique and harden the plan below for the TOP roadmap epic (E-SYSTEM-ORG-001: agent-system organization / source-of-truth cleanup) plus a few related items. Specifically:
1. Find blast-radius we will miss (files/consumers that reference the things we plan to move/rename/delete).
2. Pressure-test the sequencing and the risky decisions.
3. Improve the CLI-vs-API enforcement design and the "don't ignore env keys" gate.
4. Flag anything that could break dispatch, the oneshot store, the paths registry, or downstream products.
Be concrete. Name mechanisms (hooks, scans, tests). Prefer "here is the failure mode + the enforcer" over generic advice.

# CONTEXT: the agent system (post ADR-0007)
- Keystone: .claude/agents/_org/role-registry.json — 33 roles, single source of truth (home/sub_home/dispatchable_by = the org reporting structure). Routers + enforcers READ from it.
- Department tree (the CLEAN, correct shape): .claude/agents/{president,product,engineering,growth,_system,_org,_principles}/
  - president/ = the 5 Alex "faces": alpha (run/orchestrate), beta (check/judgment, read-only), gamma (adhoc deliver), delta (oneshot deliver), epsilon (sprint deliver).
  - product/ (Director ζ), engineering/ (Director η, orchestrator), growth/ (Director ι), each with leads + worker pods (builder/reviewer/fixer).
  - _system/ = mode-agnostic infra AGENT SPECS: learner.md (between-cycle learning), stub-scaffold.md (skeleton stub regen).
- Dispatch: cross-provider review agents run via CLIs. node scripts/dispatch-agent.js <role> <prompt-file> → runProvider() in scripts/hooks/lib/providers.js shells to codex exec (OpenAI/gpt-5.5), gemini -p (Gemini), or native claude. Build-chain Claude roles go via node scripts/dispatch-claude.js (bounded, reap-safe). The dispatch-route-guard PreToolUse hook BLOCKS raw codex exec/gemini -p/"cat|provider"/raw claude -p --agent <build-role>.

# THE MESS WE ARE CLEANING UP (the "system files/folders")
There are THREE dotted "system" locations under .claude/agents, plus a duplicate of the most important doc:

(A) .claude/agents/.system.md  (1434 lines) — the OLD pre-ADR-0007 monolithic "Agent System — Operational Specification" ("Dark Factory", a downstream product's examples, retired 8-role model: Auditor/Evaluator/Compliance/QA/Fix Agent, old heartbeat schema, old store schema, old gauntlet). Mostly superseded by role-registry.json + ADR-0007, BUT still contains operational logic (parallel gauntlet, snapshot-diff, circuit breaker, escalation ladder) that delta/oneshot may still lean on. frontmatter-guide.md points to it as "top-level agent system spec".

(B) .claude/agents/.system/  (folder) — guides/agent-dispatch-guide.md (STALE ORPHAN, 243 ln, 2026-04-28, pre-ADR-0007: old role names, dead models gemini-2.5-flash/gpt-5.4-mini, even claims gemini-3.1-pro-preview is a "ghost 404" which now contradicts the live registry where it is the real default), guides/oneshot-token-guide.md (Delta context-budget guide, still useful), frontmatter-guide.md (frontmatter authoring ref; somewhat stale role names/models but useful).

(C) .claude/agents/president/_system/  (folder) — FAR more load-bearing: oneshot/store.json (the live oneshot state machine), policy/decision-policy.md, policy/current-stage.md, policy/adr/0000..0010 + INDEX.md (all 10 ADRs), beta/ (judgment model + source data), adhoc/protocol.md, lexicon.md. ALL referenced via the paths registry (paths.decisionPolicy, paths.currentStage, paths.adrIndex, paths.policy, paths.oneshotStore, paths.betaEvents, etc.). dispatch-agent.js has a literal fallback to president/_system/oneshot/store.json.

THE PROVEN FORCING CASE: agent-dispatch-guide.md exists TWICE and has DRIFTED:
- .claude/project/reference/agent-dispatch-guide.md (197 ln, CURRENT, post-ADR-0007; this is the one paths.agentDispatchGuide + the session-start.js banner + CLAUDE.md point to).
- .claude/agents/.system/guides/agent-dispatch-guide.md (243 ln, STALE ORPHAN in the "intuitive" agent-tree home).
Both owner=framework → BOTH ship downstream. The maintained canonical is in the UNINTUITIVE place; a stale copy squats in the INTUITIVE place. scan:references can't catch it (both exist = not a broken ref; it's content drift). The stale copy ALSO holds genuinely-useful operational detail the canonical lacks (headless env-key setup table, concurrency caps, prompt-assembly/inlining rules, worktree patterns) — so it's "merge then delete", not "just delete".

# OPERATOR DIRECTIVES (this session)
1. De-dot: reconcile the contents of .claude/agents/.system/ AND the .claude/agents/.system.md file INTO .claude/agents/_system/. Avoid file/folder names starting with "." (the operator's hunch: dot-prefixed dirs get silently skipped by the agent's own reading).
2. The agent-dispatch-guide.md is "perhaps the most important document in our entire agent system" but is either not referenced or its rules are IGNORED during dispatch. The 3 recurring failures it should prevent:
   (i)  skipping parts of the gauntlet (running fewer reviewers than required, or recording "coverage" with no real dispatch behind it),
   (ii) dispatching agents via API when we should use the CLI,
   (iii) dispatching agents INTO the orchestrator's context (in-process Agent tool) instead of as bash subprocesses.
3. Add clear PROSE + ENFORCEMENTS for what uses CLI vs API and when. Operator's belief (which we VALIDATED in code): only the deep-research pipeline and any GPT-Pro dispatches use the API; ALL other agent dispatch uses the CLI. Validate/critique this structure.
4. .env* problems: (a) when an API exists, the agent wrongly assumes to use the API instead of CLI dispatch; (b) when doing an API dispatch, the agent FLAT-OUT IGNORES the env keys (declares them "missing" without opening the .env file — keys live in .env.local for OpenAI, ~/.gemini/.env for Gemini).
5. Find the FULL blast radius of every proposed change, with resolution. Provide a checklist against every problem above.

# THE PROPOSED PLAN (critique this)
S-1 AUDIT (analysis-first, no changes): enumerate every duplicate-basename / drifted / orphaned / mispointed canonical across .claude/agents/** + .claude/project/reference/**; for each name the ONE canonical + disposition (merge→delete / repoint / remove). Freeze the inventory.
S-2 Dispatch-guide consolidation: ONE canonical agent-dispatch-guide.md = current 197-ln content + folded-in still-useful bits from the orphan (env-key/auth table + concurrency caps + prompt-assembly + worktree patterns), de-staled to current roles/models. DECISION TO MAKE: canonical home = move INTO the agent tree (.claude/agents/_system/guides/agent-dispatch-guide.md, matching operator intuition + epic framing) and repoint paths.agentDispatchGuide + session-start.js banner + CLAUDE.md, OR keep at .claude/project/reference/ and just delete the orphan. Recommend with reasoning + blast radius of each.
S-3 De-dot the agents folder: fold .system/ contents + .system.md into _system/ (probably _system/guides/ for the docs, _system/agent-system-spec.md for the de-staled .system.md), with NO name starting with ".". CRITICAL CONCERN: _system/ currently holds dispatchable AGENT SPECS (learner.md, stub-scaffold.md) resolved by frontmatter name: — dropping non-frontmatter reference docs directly into _system/ could confuse the spec enumerator (findAgentSpec DFS in dispatch-agent.js + scan:role-parity). Also cli.js reportedly skips by startsWith(".") not visible-"_". And: should president/_system/ ALSO be de-dotted now (bigger blast radius: paths registry + oneshot store + ADRs), or deferred? Recommend.
S-4 Extend scan:cutover-completeness to also fail on duplicate-basename drift.
S-5 (operator-scoped, defer) maintainer canonical scrub + framework-boundary closure.
S-6 ENFORCER: a duplicate-basename-drift detector (two shipped framework .md files, same basename, drifted content — the gap scan:references misses) wired into /scan:full + release gates.
PLUS (new items beyond the epic):
N-1 Make the dispatch guide ACTUALLY consulted/binding: it's currently "mandatory read" prose for gamma/delta + a session-start nudge, yet ignored. What mechanism makes ignoring it self-detecting? (telemetry that a phase recorded coverage with no backing ok:true dispatch record; a gate that fails when reviewer roster < required set; etc.)
N-2 CLI-vs-API policy doc + enforcer: a guard that flags a raw API call (curl/SDK/node fetch to api.openai.com etc.) for a task that should use CLI dispatch; and a "where the keys live + verify-before-declaring-unavailable" grounding rule.
N-3 The "ignored env keys" grounding gate (verify .env.local / ~/.gemini/.env before declaring a key missing).

# DELIVERABLES FROM YOU
A) Blast-radius additions: concrete files/consumers/patterns we likely missed (think paths.registry.json, framework-manifest.json / framework-installed.json ship lists, _warpos/MANIFEST.json, install.ps1, hooks that grep agent paths, scan:* suite, downstream products that already received the dup, the DFS spec enumerator, role-parity, cli.js, session-start.js, CLAUDE.md, frontmatter-guide cross-refs, ADR INDEX).
B) Sequencing critique: what must come before what; what is safe to parallelize; what should be deferred.
C) The S-2 canonical-home decision: your recommendation + why.
D) The S-3 president/_system de-dot: now or defer? + the spec-enumerator collision mitigation.
E) Enforcement design for N-1/N-2/N-3: the smallest mechanisms that make each failure self-detecting, fail-closed, and hard to bypass.
F) Anything we got wrong or any higher-leverage move we're missing.
Keep it tight and concrete.
