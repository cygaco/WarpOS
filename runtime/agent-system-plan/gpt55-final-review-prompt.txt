You are GPT-5.5, dispatched (role: consult) as the FINAL independent reviewer of a planning document before it is committed and handed to a fresh execution session. You reviewed an earlier draft already (you caught the legitimate-duplicate-basename trap, the defer-president/.system call, and the dispatch-api wrapper — all adopted). This is the final pass with deep-research now folded in. Return a rigorous, concrete critique — NOT pleasantries. Freeform prose.

# WHAT THIS PLAN IS
WarpOS = an autonomous multi-agent "AI company" framework (Claude Code harness, Windows, Node). The plan's NORTH STAR (operator reframe): "What is the best DISPATCH SHAPE for each skill and each agent, throughout the OS?" Skills and agents are ONE problem at two granularities.

Dispatch-shape menu: inline (Alpha does it) · in-process Agent tool (Claude subagent, dumps full output into orchestrator) · Claude bash-subprocess (bounded reap-safe `dispatch-claude.js`) · cross-provider CLI (`dispatch-agent.js` → codex/gemini) · direct API (deep-research + GPT-Pro only). Decision rule: inline if light/conversation-dependent; else subprocess if it RUNS + PAYS + is GOOD; API only when no CLI; in-process Agent only for small Claude returns; cross-provider for independent review.

# PLAN STRUCTURE (what's in the doc)
- §0 audit: the agent-system mess — THREE dotted dirs (.system.md relic 1434ln + .system/ + president/.system/) + a duplicated/drifted dispatch-guide (canonical at project/reference 197ln vs stale orphan .system/guides 243ln); 8 specs cite the stale .system.md as their "role definition".
- E-SYSTEM-ORG-001 (TOP, #1 workstream): S-1 audit (done) · S-2 consolidate the ONE dispatch-guide (move into agents tree, repoint paths/banner/CLAUDE.md, delete dup) · S-3 de-dot .system/ + .system.md into _system/ (DEFER president/.system — too load-bearing: oneshot store, decision-policy, 10 ADRs, beta model) · S-6 duplicate-doc-drift enforcer (scoped: legit dupes builder.md/fixer.md/reviewer.md/protocol.md must NOT false-positive) · S-7 role renames (learner→ops-analyst @ President's-office; stub-scaffold→skeleton-builder @ engineering; consult/advisor→cabinet @ President's-office) · S-8 file-usage-path trace (auto-loaded vs referenced vs orphaned) · S-9 stale Dispatch-Console GUI refresh (read from role-registry).
- Dispatch integrity: N-1 make the dispatch guide BINDING (dispatch-plan.jsonl + run_id + coverage-gate: a phase is "covered" only with a backing ok:true record — kills "sprint theater"); N-2 CLI-vs-API policy + provider-api-policy scan + a single dispatch-api.js wrapper; N-3 shared auth-resolver (override-keyfile→env→.env.local→.env→~/.gemini/.env→OAuth; verify-works-not-just-present; BOM-safe writes; keep sources in sync).
- Tool-use reliability: empirically tested — the Grep tool's `glob` with a leading-dir segment or brace-list + a `path` arg silently returns 0 (false negative, reproduced); Edit fails when old_string is reconstructed from memory. Enforcement triad: CLAUDE.md ## Tool Use doctrine + tool-use-guard PreToolUse hook + scan:tools self-test.
- CLAUDE.md ↔ alpha.md: CLAUDE.md is the only ALWAYS-LOADED doc; alpha.md is not auto-loaded; the dispatch pointer must live in CLAUDE.md + propagate downstream.
- P5 mandate: ISOLATED TESTING FOR EVERYTHING — every enforcer ships a sealed fixture + a planted-violation case that MUST fail (no false-greens).
- §13 Skill Execution Routing ("Alpha as god-dispatcher"): classify skills (execution: subprocess|inline|inline-required) → dispatch-skill.js (bounded, capture-to-file, lean envelope, run_id) → the EARN-IT ladder: §13.6 does-it-RUN (smoke test, agents:test analog) + §13.7 does-it-SAVE-tokens (A/B orchestrator cost) AND is-it-GOOD (independent cross-provider judge), fail-closed. §13.3 research:deep dispatch security (key handling, indirect prompt injection, sandboxing). Feasibility VERIFIED against the working portfolio/dispatch.js (which proved the mechanism + exposed that it lacks reap-safety).
- Launch Console (own epic): a founder launch-readiness checklist + per-checkbox agent-verifier, sourced from _guides + risk tiers; the AI-verified go/no-go. GPT-Pro library = FULL-scope adoption (operator's call).
- §14 Phase-0 PRESTEP (gates ALL execution, at RUNTIME): per-item feasibility probe + correct-tool blast-radius re-confirm + isolated end-to-end dry-run in a sealed clone + fail-closed gate.
- §15 planning principles (17, operator tracking).

# §16 — THE DEEP-RESEARCH HARDENING (o3, just folded in — REVIEW THIS HARDEST)
CORRECTS/RISKS: (1) shell:false is NOT injection-proof on Windows (CVE-2024-27980) — Node auto-shells .cmd/.bat; our wrappers call .cmd shims → fix via pinned Node + explicit `cmd.exe /c <abs>.cmd`. (2) PATH/PATHEXT hijack on bare-name spawn → absolute paths. (3) safe-spawn ≠ safe ARGUMENTS → input-gate must allowlist subcommands/flags, not just refuse metachars. (4) `export $(grep .env.local | xargs)` executes `KEY=$(cmd)` on load → parse env in-code, never shell xargs. (5) stdout pipe backpressure hangs both processes → must DRAIN stdout, not just timeout.
ADDS: Windows Job Objects (KILL_ON_JOB_CLOSE) so orphaned children don't burn paid API when the orchestrator dies; taskkill /T /F tree-kill; output secret-scanner/redactor; per-task/session token+$ budget circuit-breaker (denial-of-wallet, OWASP LLM10); exp-backoff + sub-published-limit concurrency; reject UNC/abs-exe paths; force UTF-8/normalize CRLF; AppContainer/restricted-token + network-egress allowlist for code-executing/untrusted-web sub-agents.

# YOUR FINAL REVIEW — answer concisely
A. GAPS / WRONG / RISKY: anything still missing, mis-scoped, or dangerous — especially in §16 (are any of the 7 top actions wrong, over-engineered for a Windows single-operator Node tool, or in the wrong order?), N-1's coverage-gate, and the §13.6/§13.7 earn-it ladder.
B. SEQUENCING: is "agent-system org cleanup first, then dispatch-integrity, then skill-routing, then Launch Console" right? What MUST precede what? What's safe to parallelize? Anything that should move earlier because it's load-bearing for the rest?
C. EXECUTION-READY?: is the Phase-0 runtime gate (feasibility + isolated dry-run) sufficient to start safely, or is there a precondition it's missing?
D. THE ONE THING: the single highest-leverage thing this plan still doesn't have.
E. OVER-ENGINEERING CHECK: what here is gold-plating a single-operator tool that should be cut or deferred to keep momentum?
Be specific and concrete. This goes into the final synthesis verbatim-adjacent.