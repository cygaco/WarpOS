# Hardening Changes — 2026-07-17 (pre-approval ledger)

One line per change, plain language, with the problem it solves. This is the ELI5 list for the operator's FINAL approval. **No plan phase was executed; SP-001 stays parked; nothing below reverses a ratified decision.**

## Fixes to things that were wrong
1. **Backfilled debt entries ED-203 through ED-207 into the ledger.** Problem: our tracker said these five follow-ups were "logged" — they never were. The bookkeeping was lying; now the ledger matches what the tracker claims. (Each entry is marked as a backfill so nobody mistakes it for an original.)
2. **Reworded "the 44 session-lifecycle hooks" in Phase 4.** Problem: the exact number can't be reproduced from disk today (the registry drifted to 67 entries / 75 wired calls). A stale hard number invites false confidence; the rule now names the hook *class* and treats counts as drifting.
3. **Annotated the plan's ED-205 reference.** Problem: the plan cited a ledger entry that didn't exist (see #1); the citation is now real and says why.

## Additions from reading the packet docs firsthand (α, all 18 now read)
4. **Added a one-sentence Definition of Done for 1.0** (from the packet's Charter): a clean install must get from idea to shipped-and-learned *without chat memory, stale trackers, Alpha heroics, or unverified claims*. Problem: the plan had phases but no single test for "1.0 is actually done."
5. **Pointed Phase 3's lease design and Phase 1's routing fix at the packet's ready-made schemas** (lease types one_shot/wave/phase/session; role = role_id + provider + runtime). Problem: builders would waste time re-deriving designs that already exist and were already vetted.
6. **Added a tracker-fidelity probe to Phase 3** — a check that compares what the tracker CLAIMS against what git/disk actually shows. Problem: our trackers have gone stale silently before (5 recurrences in one June day; we nearly rebuilt finished work). The current validator only checks the tracker against itself, not against reality. Two packet docs independently demanded this; the original harvest missed it.
7. **Scoped the conformance checklist to kernel-only, explicitly.** Problem: the packet checklists mix kernel items with product-layer packs we deliberately dropped (founder panel, webapp security). Without a written IN/OUT line, the dropped packs creep back in through the checklist.
8. **Rewrote every phase exit as named runnable commands with expected outcomes** (gates G0.1–G4.6, now binding in the plan). Problem: prose exits like "the panel runs end-to-end" can't be tested — "done" becomes an opinion. Now each phase ends on commands that pass or the phase isn't over.
9. **Confined the "top-level session defaults to Alpha" rule to Claude-side boot files only.** Problem: the packet's own AGENTS.md template puts that rule in the *neutral* file every worker reads — which would let any worker talk itself into Alpha authority. The neutral handbook keeps binding rules 1–4; rule 5 lives only where the human-facing session boots.
10. **Reconciled the result-status vocabulary.** Problem: the packet template has 7 result statuses, the plan has 5 — two conflicting vocabularies would fork the schema at build time. Ruling: 5 terminal states; things like "timeout" and "quota exhausted" become failure-REASON codes (from the packet's failure taxonomy), not extra states.

## Discrepancy resolutions (ε-conducted probes, all 3 closed)
11. **agy lane: the audit was right — the lane is DOWN, and our tracker was wrong to say it ran.** Problem: the flip-close note "3-lab panel attested live" was config-echo (the settings SAY agy, but zero agy dispatches ever actually ran — a live probe gets blocked before dispatch). The plan now says so, and the tracker gets a stale-marker at next reconcile.
12. **⚑ NEW DECISION FOR YOU (came out of #11): what does "the security panel" mean for 1.0?** Problem: the ratified Phase-1 exit says "full 3-lab panel end-to-end," but the third lab (Google/Antigravity) needs a real migration, not a quick fix. My recommendation: timebox that migration inside Phase 1; if it doesn't prove out, 1.0 binds on the 2-family panel (GPT+Claude) with the Google lane as a dated optional extra — so a dead lane can't freeze every build. You decide at final approval.
13. **gpt-5.6-terra is real and proven live** — a genuine dispatch record exists (408 seconds, real output, no fallback). Problem solved: the plan's claim is now evidence-backed, not hearsay.
14. **The 3 old test failures don't block this plan — but one is real.** Two are stale test fixtures complaining about debts that were since fixed (the checks themselves pass). The third (a file-count baseline, 122 expected vs 138 on disk) is REAL drift from the v1 rebuild that needs reconciling — queued for after your approval, not dismissed. *(Wording sharpened per sol: "harmless" overstated the third one.)*

## β checkpoint folds (verdict: DECIDE, confidence 0.88 + 0.90 refinement — "proceed"; logged to the beta events ledger)
15. **Honesty note on ED-205 (β's top catch): backfilling the ledger entry is NOT the same as verifying the bug.** Problem: we confirmed the *entry* was missing and restored it, but nobody has re-proven the underlying `--provider` defect still exists — it's inherited from the flip session. The gate is now "reproduce FIRST, then fix — or correct the record if it's a phantom." **RESULT: the probe landed — the behavior is real but INTENTIONAL** (documented in the code; an explicit flag combination exists for exactly this case). ED-205 reclassified from "bug to fix" to "intended behavior, keep a regression guard" — nothing to build. The honesty chain worked end-to-end: inherited claim → reproduced → correctly reclassified instead of blindly "fixed."
16. **Four gate tightenings from β folded:** (a) the Phase-0 conformance runner's report-only mode now names its debt entry and the phase where it turns binding — no silent report-only; (b) the authority-leak fixture must plant the *default-binding rule* text, not just "you are Alpha" — the sneakier leak is the one tested; (c) one line added explaining who checks the checkers before Phase 4 exists (self/CI, closed retroactively by Phase 4's self-modification test); (d) the Phase-1 lane ruling now presents both options with β's 2-family floor as the guaranteed fallback (Option A degrades into Option B).

17. **The lane ruling is now enforceable, not aspirational (β second verdict, DECIDE 0.88).** Problem: "timebox it" and "sunset-dated" were intent without mechanism — the exact aspirational-vs-enforced gap this plan closes. Now: the timebox auto-expires into the 2-family bind (no second decision), "proven" means a real dispatch record (config echoes don't count — that's what fooled the tracker), an enforcer reads the sunset date and yells when it passes, a machine-readable required-vs-optional lane manifest feeds both gates, and the lane contract gets its own ADR. Plus the honest framing: 2 families already preserve cross-provider security diversity — the 3rd lane buys breadth, not the core property.

## Process notes (not plan changes)
- **Go/no-go header (for your decision):** every change in this ledger is a plan-document or bookkeeping edit — ZERO runtime/product code touched, no phase executed, SP-001 still parked, all fully git-revertable.
- Claim sweep: 15 claim groups verified against disk/git — 13 confirmed, 1 refuted (→ change #1), 1 partial (→ change #2). Report: `evidence/hardening-20260717/verify-claims.md` (committed).
- α rulings detail: `evidence/hardening-20260717/alpha-eyeball-rulings.md` (committed). *(Links repointed from untracked runtime copies to the committed evidence tree — sol's catch.)*

## sol-reharden folds (verdict: "architecture right, no resequencing — NOT execution-ready as-is"; all dispositions PROPOSED pending your approval)
18. **The trusted gatekeeper got a concrete definition** (sol amendment 1, adopted into Phase 4 scope). Problem: the plan said "a trusted layer integrates checked work" but never said WHO holds the merge keys and HOW checks can't be poisoned. Now: one protected branch, ONE integration identity with its own isolated credential, a sealed (content-addressed) checker bundle including its helpers and config, and merges that atomically verify they're landing on exactly the state that was checked.
19. **"The worker said it succeeded" can never authorize a merge** (sol amendment 2, adopted at the Phase 3/4 seam). Problem: the worker's result report is untrusted by definition, and a forged or replayed "success" could slip through. Now a separate TRUSTED acceptance record — binding the exact work order, code state, checker version, and evidence — is the only thing that authorizes integration.
20. **Portability gets proven at the helm, not the leaf** (sol amendment 3, adopted as Phase 4's exit proof). Problem: the old exit could pass by having Claude dispatch one Codex worker — proving a Codex LEAF works, not that a non-Claude AI can RUN the company. Now the exit is a clean-install, black-box run driven FROM each certified helm, zero skips, negative fixtures mandatory.
21. **Ten "gate passes while the property fails" holes pre-plugged** (sol Q2, folded as binding gate addenda). Problem: each gate as written had at least one honest-looking pass that didn't prove the property (detect-after-bypass, self-attesting telemetry, checker poisoning via config, lease races, debt-waivable invariants…). Each named path now has an explicit counter-requirement baked into the gate spec before anything is built.
21a. **β cross-check on the sol folds (DECIDE 0.87): one scope-guard + one landing rule.** Problem: sol's "credential isolation" would quietly re-import containment work the plan explicitly defers from 1.0 — the trust root is adopted as merge-authority only, with credential isolation staying named at the out-of-scope boundary (the honest promise stays honest). And each of the ten false-green closures must land as a named test that goes RED on the loophole — provable, not prose. β also credited sol for completing β's own lane rule: an optional lane that's ABSENT doesn't block, but an optional lane that RAN and FAILED still counts (both halves now in the ruling).
22. **The lane choice got clean semantics** (sol Q3, folded into the ⚑ ruling): two frozen named security profiles — you ratify the 2-family floor now; the 3rd lane is a PROMOTION attempt, never a silent downgrade of a "3-lab" claim; and if 2-family is chosen, every stale "3-lab" claim in the repo gets explicitly superseded (no lying config left standing).
