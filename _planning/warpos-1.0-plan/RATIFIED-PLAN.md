# WarpOS 1.0 — Ratified Kernel Plan (2026-07-17)

**Status: RATIFIED by the operator 2026-07-17 (5 explicit confirmations, this session). The NEXT session is a PLAN-HARDENING session, NOT execution (operator directive at wrap, 2026-07-17). Read this doc with `verify-don't-inherit` (re-verify live claims against disk/git before building).**

## NEXT-SESSION DIRECTIVE — plan hardening (operator-ordered procedure, in order)
1. **Run through the whole plan** — every phase, every ruling, every evidence file — challenging each claim against disk/git (verify-don't-inherit).
2. **Harden it**: tighten scope/AC per phase, resolve the known discrepancies (§ below), turn phase exits into testable gates, and eyeball any packet doc not yet read firsthand by α (02, 03, 07, 09–15, 17, 18, templates — the 5 keystones 04/05/06/08/16 WERE read firsthand).
3. **Consult gpt-5.6-sol again on the HARDENED plan** ("sol-reharden"): cabinet role, advisory-only/change-nothing, via `node scripts/dispatch-agent.js cabinet <prompt-file>` — MUST run with `WARPOS_DISPATCH_BACKGROUND=1` and a detached/background shell or the 540s foreground clamp kills the ultra consult deterministically (proven this session: first attempt died at exactly 540373ms; re-run with the env succeeded). `beta-consult.js --out` crashes on absolute paths (I-3) — cabinet route unaffected.
4. **Then STOP and get the operator's FINAL approval** with an ELI5 summary of EVERY single change and the problem(s) it solves — one line per change, plain language. No execution until that approval.

### Everything the hardening session needs (all in-repo, committed)
- **This plan** (`RATIFIED-PLAN.md`) — phases, 5 ratified decisions, α rulings (marked "α ruling"), deliberate-drop ledger.
- **The ORIGINAL packet, verbatim**: `packet-original/` (18 docs + 5 templates, copied from the operator's zip 2026-07-17).
- **Evidence**: `evidence/packet-analysis.md` (doc-by-doc verdicts) · `evidence/enforcement-audit.md` (per-provider hooks reality) · `evidence/runtime-hygiene.md` (mess inventory) · `evidence/cabinet-consult-gpt56sol-ultra.md` (first sol consult, full text) · `evidence/sp-20260717-001-gauntlet-findings.md` (SP-001 RED fix-brief + tooling findings I-1/I-2/I-3).
- **Parked work**: SP-20260717-001 branches `sprint/SP-20260717-001-builder` @ `00040620` (worktree `../WarpOS-wt/SP-20260717-001-builder`) + `sprint/SP-20260717-001-runtime-retention` @ `67d7900b` — HELD unmerged, gauntlet RED, fix-brief above. Do NOT merge or delete during hardening.
- **Session-decision trail (2026-07-17)**: operator ratified — consultant's phase order (after ELI5) · keep Fable-5 judge + defensive prompt reframe (NOT a judge swap, NOT excluding security review) · operator voice = ELI5-default/depth-when-it-matters, audience-scoped · SP-001 hold + fold amendments · packet coverage gaps 03+07 added, drops (02/09/10/11/12) acknowledged.
- **Live tooling facts the hardening session will hit**: harness Agent-tool spawns fail on non-Claude registry pins (β `gpt-5.6-sol`, product-lead `gpt-5.6-terra` — override with a Claude `model` param at spawn, as done this session with Beta2) · `gpt-5.6-terra` IS live via codex CLI (I-1) · provider-id `antigravity` vs tool-id `agy` contract mismatch (I-2) · turbo re-grants that WIDEN need `--attest` with operator provenance.

## Provenance
- Input: the operator's 18-doc "warpos_1_0_finish_packet" (generated 2026-06-28, ~3 weeks stale vs HEAD `c3dab137`).
- Analysis: 3 audits (this session, agents) — `evidence/packet-analysis.md`, `evidence/enforcement-audit.md`, `evidence/runtime-hygiene.md`.
- Second opinion: cabinet consult, **gpt-5.6-sol @ ultra** (real CLI dispatch, `dispatch_id d-mroak7ue` predecessor + successful re-run; advisory-only) — `evidence/cabinet-consult-gpt56sol-ultra.md`. Its core amendment ("false-green portability / undefined trust boundary is the #1 risk; contract first, adapter last") was ACCEPTED.
- Operator kernel lens (the yardstick for all scope decisions): (a) interoperability — any provider at the helm, (b) enforcements that work with every provider, (c) model routing, (d) the sprint development cycle. Release ceremony and product-layer guardrails are OUT of kernel scope (route to bootstrap:lastmile).

## The 5 ratified decisions
1. **Sequencing = the consultant's phase order** (contract → routing truth → identity → WorkOrder/Envelope → enforcement adapter). Confirmed explicitly after ELI5.
2. **Security panel: KEEP Fable-5 as judge; REFRAME the prompts defensively.** Do NOT exclude WarpOS from security review; do NOT swap the judge yet. First determine whether panel prompts unnecessarily ask the judge to reproduce operational exploit material, and reframe (structured bounded findings from hunters; judge adjudicates evidence, not raw exploit text). Fail-closed conditions apply regardless: judge refusal / malformed verdict / missing evidence → BLOCKED-INCONCLUSIVE, never PASS; no silent judge fallback. If reframing fails to stop the trips, revisit the judge-seat swap as a follow-up decision WITH the operator.
3. **Operator voice: ELI5-default, depth-when-it-matters.** Rule text: "Lead every operator-facing answer with a short plain-language outcome; include technical detail when consequential, safety-relevant, or requested." Scoped by AUDIENCE (`operator` / `agent` / `artifact`), NOT by "top-level session". Lives in the portable runtime contract, projected only into helm bindings — NEVER in ambient provider-neutral files (AGENTS.md auto-loads into codex runs; CLAUDE.md auto-loads into worktree builders).
4. **SP-20260717-001 (runtime retention): HOLD unmerged.** Branch parked; **gauntlet returned RED (all 3 lanes BINDING FAIL — real defects incl. 3 CRIT: TOCTOU delete, untrusted deletion root, unwired enforcer). Full fix-brief with stable IDs: `evidence/sp-20260717-001-gauntlet-findings.md` — fold it WITH the consult amendments in one fix-cycle, re-gauntlet, then merge.** Amendments to fold: ≥2 rotation generations (one `.1` is too destructive); per-sink retention CLASSES instead of a uniform 20k-line cap (diagnostics vs operational evidence vs security records vs semantic memory); a non-racy pruning host (NOT bare SessionStart — many sessions start concurrently; use a lock or single-writer); keep items that are recent OR among the newest N, PLUS anything referenced by an active/resumable sprint; rotation must not silently break readers (reader tests).
5. **Packet disposition** (doc-by-doc detail in `evidence/packet-analysis.md`): extract MECHANISMS after reconciliation — never ratify stale packet docs verbatim. ADOPT-as-amended: 04+16 (merged into ONE runtime contract), 05 (scoped-down compiler), 06 (adapted onto existing ledger), 13/15 (as the executable conformance matrix), 17 (as a sunset-dated scope rule), 01's bootstrap function only (for hosts that can't auto-discover repo instructions). REJECT: 01-as-driver, 08 greenfield dispatch rewrite, 09 packs-as-second-manifest, 10/11 in-kernel.

## Execution phases (next session onward)

### Phase 0 — Kernel contract + trust boundary (small, define-before-build)
One merged 04/16 **Top-Level Runtime Contract** doc: any provider may propose/orchestrate work, but a provider-independent TRUSTED layer owns capability grants, protected mutation, verification, and integration into main. Includes a one-page **ADR ratifying packet 03's "durable company, ephemeral executors"** doctrine (company = durable on-disk state: tracker/registry/ledgers; models = interchangeable executors) — already de-facto true, never ratified. Includes: role-binding precedence graph (validated WorkOrder/CLI binding → explicit top-level runtime binding → UNBOUND fail-closed; repo prose can never manufacture a binding); provider × capability × helm-level support matrix; minimal WorkOrder field set; conformance fixtures harvested from packet 13/15; log/evidence retention classes (feeds SP-001 amendments). Every policy in it names its enforcer or logs enforcement debt at write-time.

### Phase 1 — Routing + security truth (the live bugs)
- **Harness-spawn model rule**: harness Agent-tool spawns must ALWAYS resolve to a Claude model regardless of the logical role's registry pin. Hit TWICE live this session: β spawn failed on `gpt-5.6-sol` pin; product-lead spawn failed on `gpt-5.6-terra` pin (Epsilon's SP-001 design phase). Separate logical role routing from invocation-channel capability.
- **`gpt-5.6-terra` RECONCILED (ε, this session): the model is LIVE via codex CLI but unavailable via the harness Agent-tool/API route** — same class as the harness-spawn rule above, not a phantom id. Also fix: **I-2** provider-id `antigravity` vs tool-id `agy` mismatch (security-reviewer trips dispatch-contract) and **I-3** `beta-consult.js --out` crashes on absolute paths (`path.join(ROOT,outArg)`).
- **ED-205**: `--provider` override serves provider-DEFAULT model instead of spec `provider_model`. *(Hardening note 2026-07-17: the flip session DECLARED EDs 203–207 but never wrote them to the ledger — the verify sweep caught it; all five were backfilled to `paths.enforcementDebt` during hardening, so this citation is now real.)*
- **agy lane reconciliation — RESOLVED 2026-07-17 (ε probe): the AUDIT was correct; DUMP/TRACKER were stale.** The lane is DOWN/unwired (ED-060 stands): the dispatch ledger holds ZERO agy/`gemini-3.1-pro-high` records ever; the 2026-07-16 gemini rows are all `ok:false` reaps; `providers.js` carries no agy invocation syntax; a live probe was contract-BLOCKED (`antigravity` ∉ `[codex,gemini,agy]` — reproduces I-2 exactly). The flip-close "attested live" was config-intent echo, not liveness — the very config-echo-attestation defect class the flip gauntlet itself caught. The gauntlet is 2-family (GPT+Claude) today. **Consequence: the third lane is a real Antigravity MIGRATION (agy headless contract unproven), not a reconcile — see the flagged Phase-1 scope ruling in the HARDENING LAYER.** TRACKER's flip-close 3-lab claim needs a stale-marker at next reconcile.
- **Security-prompt reframe** (ratified decision #2) + fail-closed judge conditions + a small canary corpus (legitimate dual-use finding / malicious repo instruction / unsupported claim / hunter disagreement / missing evidence).
- Record ACTUAL effective provider/model in dispatch evidence (extends the WG-26 discipline).
- Exit: the security panel runs end-to-end GREEN across all CONTRACTED lanes, and a missing contracted lane or judge failure BLOCKS correctly. *(Hardening 2026-07-17: "3-lab" is conditional — the agy lane is DOWN [ED-060] and needs a real migration, so the contract must name which lanes are REQUIRED. Flagged operator ruling in HARDENING LAYER: timeboxed in-phase agy migration, else 2-family binding + agy sunset-dated.)*

### Phase 2 — Identity + host portability
- Explicit role bindings (Phase 0's precedence graph implemented) — **α ruling 2026-07-17 (splits the packet vs consult):** DISPATCHED workers = UNBOUND fail-closed, never default to President (consult is right there); the TOP-LEVEL human-facing session DEFAULTS to Alpha (packet 04's binding order #5 is right there — a blanket no-default rule would leave the operator's own session unbound). The conditional "you are Alpha unless bound" CLAUDE.md phrasing is acceptable only as a TRANSITION.
- Provider-neutral canonical instruction source + per-provider projections (scoped-down packet 05); deterministic generation + drift check (same regen-both-manifests discipline).
- **Authority-pollution scan**, not just identity words: unconditional permissions/approval/merge/deploy authority or operator-audience in any ambient neutral surface; scan the EFFECTIVE instruction graph (imports, agent specs, shims, generated files, stale worktrees, handoff prompts).
- Generic bootstrap harvested from packet 01 (for chat/API hosts).
- cwd/sandbox behavior per provider gets TESTS before any "neutral cwd" change (changing cwd moves codex's sandbox root — don't adopt blind). Reviewers get read-only evidence; builders get exactly one isolated worktree as write root.
- Operator-voice directive lands here (ratified decision #3) as a small item.
- Evidence for current leak: builders inherit "You are Alex — the President" via worktree CLAUDE.md; codex reviewers slurp the 14KB AGENTS.md router from cwd. Root AGENTS.md does NOT declare "you are Alpha" (catastrophic form already avoided).

### Phase 3 — WorkOrder / ResultEnvelope
Adapt packet 06 onto the EXISTING dispatch ledger (dispatch-completions + dispatch-record-fields + gauntlet-verify) — no greenfield. Minimum schema: schema version, correlation id, effective role/provider/model, immutable base commit + result tree hash (NOT "worktree base = live_head"; enforce freshness separately), allowed capabilities+paths, retry lineage, evidence refs, terminal states {success, partial, blocked, failed, cancelled}. **α ruling (splits consult):** KEEP the packet's prompt-size floor as a cheap tripwire (WG-10 hollow-prompt bug was real) AND add the consult's required-semantics validation (objective, authority, base, scope, acceptance checks, evidence, outputs) — belt and suspenders, not either/or. Also adopt packet 06's WorkOrder split heuristics (one role/objective/lane, 2–7 child tasks, ≤8 expected files, split-when list) as authoring guidance. **Harvest packet 08 verbatim as checklists (not code):** its failure-classification taxonomy (incl. `model_unavailable` — it predicted the 2026-07-17 harness-spawn bug class — plus auth_missing, worktree_base_stale) and its reaper signal-ranking (never reap from process absence; 8 ranked evidence signals) become Phase 1/3 conformance fixtures. Worktree-base: declare an explicit immutable base commit (consult) AND assert freshness against the integration head for dependent builders (packet 08's real point) — both, not either. Wire ED-069 (started-row) + ED-070 (quota field) into ALL dispatch writers as one coherent change; fold teammate-stall-rules.md back into epsilon.md + agent-dispatch-guide.md (ED-071). **Harvest packet 07's two live mechanisms:** sprint **leases** (a sprint claims a lock so two sessions can't conduct the same SP-id) and a **do-not-reopen ledger** (settled decisions a resumed session must not re-litigate — protects ratified plans like this one). Optional small residual from 08: a `dispatch doctor --json` diagnostic.

**Deliberately dropped from 1.0 (operator-acknowledged 2026-07-17):** packet 02 Charter (content already lives in CLAUDE.md/registry), 09 packs-as-layer (revisit post-1.0 as a generated distribution view), 10/11 webapp-baseline + founder-panel packs (product-layer — route to bootstrap:lastmile, unqueued), 12 stream-split reframe (observability core already built).

### Phase 4 — Trusted enforcement adapter (LAST — depends on Phase 3's contract)
Architecture: `untrusted provider/worktree → exact proposed tree + ResultEnvelope → PINNED trusted checker → integration of that exact checked tree`. The authoritative gate validates exact base+tree hashes, executes checks from a pinned copy OUTSIDE the candidate tree (candidate edits to enforcers/CLAUDE.md/AGENTS.md/shims are untrusted changes), fails closed on missing/crashed/timed-out checks, merges precisely the validated object (no check/merge TOCTOU), and is the only route into main. Shared check library consumed three ways: Claude hooks (early prevention), git pre-commit (fast feedback — explicitly NOT the authority boundary; bypassable), trusted controller (authoritative). Portable runtime invariants (started/completed/died/timed-out/quota/approval/resume) move from Claude lifecycle hooks into the provider-neutral dispatch control plane. Do NOT mirror the session-lifecycle hook suite (~44 of ~68 per evidence/enforcement-audit.md's categorization — counts drift with the registry [67 entries / 75 wired invocations at 2026-07-17 recount]; the CLASS is what's binding, not the number). Test bypasses + checker self-modification explicitly. Honest promise: provider-independent ARTIFACT ACCEPTANCE + INTEGRATION (full arbitrary-helm containment additionally needs OS sandboxing/credential minimization — out of 1.0 scope, say so).

## Ratification baseline (what ALREADY changed, this session — 2026-07-17)
- Merged worktrees `WarpOS-wt/SP-20260716-001-dispatch` + `WarpOS-wt-auth-SP20260716-002` REMOVED, branches deleted (merged-only `-d`); their uncommitted runtime scratch salvaged to `runtime/worktree-salvage-20260717/` (6.9MB — do not delete; possible cited evidence).
- Stray-log/handoff bulk sweep NOT performed (classifier-blocked; deliberately not tunneled) — lands via SP-001's tested retention script post-merge.
- SP-20260717-001 built + committed on `sprint/SP-20260717-001-runtime-retention` (+ builder branch `sprint/SP-20260717-001-builder` @ `00040620`, worktree `../WarpOS-wt/SP-20260717-001-builder`), gauntlet run, HELD unmerged per ratified decision #4.
- Nothing merged to main this session. Turbo granted 4h (expires ~05:32Z).

## Known discrepancies — ALL RESOLVED 2026-07-17 (ε-conducted probes; full report `evidence/hardening-20260717/discrepancies.md`)
1. **agy lane: AUDIT CORRECT, DUMP/TRACKER STALE.** Lane DOWN/unwired (ED-060). Zero agy ledger records ever; live probe contract-BLOCKED (reproduces I-2). "Attested live 2026-07-16" was config-echo, not liveness. Gauntlet is 2-family today. → Phase-1 scope ruling flagged for the operator (HARDENING LAYER).
2. **`gpt-5.6-terra`: REAL + LIVE-VERIFIED** by ledger record `d-mrob0i1p` (qa-reviewer / openai / gpt-5.6-terra, exit 0, 408s, 65KB prompt → 3455B out, `fallback:false`, tool_id codex, 2026-07-17). No re-probe needed.
3. **3 pre-existing test failures: all still fail, all flip-independent — none block 1.0.** Sharper: only `assert-warpos-templates-shipped` is substantive (122 expected vs 138 on disk — WarpOS-v1 WIP baseline-delta to reconcile); `cutover-completeness.test.js` + `duplicate-doc-drift.test.js` are stale-fixture false-REDs (their standalone enforcers PASS; the integration assertions expect debts that were since resolved). Cheap post-approval cleanup: refresh the two stale fixtures, reconcile the templates baseline.

---

# HARDENING LAYER — 2026-07-17 (session 2, operator-directed pre-approval pass)

**Provenance:** executed per this plan's NEXT-SESSION DIRECTIVE. (1) Claim sweep: 15 claim groups verified against disk/git — 13 CONFIRMED, 1 REFUTED, 1 PARTIAL (`evidence/hardening-20260717/verify-claims.md`). (2) α firsthand eyeball COMPLETE: docs 02, 03, 07, 09–15, 17, 18 + all 5 templates read by α — **all 18 packet docs now α-read**; every ratified disposition SURVIVED (`evidence/hardening-20260717/alpha-eyeball-rulings.md`). (3) Discrepancy probes ε-conducted — all 3 RESOLVED (`evidence/hardening-20260717/discrepancies.md`). (4) sol-reharden + operator ELI5 approval follow. **Nothing below changes a ratified decision — all deltas are additive tightenings; the ONE new decision surfaced (Phase-1 lane contract) is ⚑-flagged for the operator.**

## Verification outcomes folded
- **REFUTED → FIXED:** EDs 203–207 were declared at flip close but never written to the ledger. Backfilled 2026-07-17 (marked as backfills). Lesson institutionalized: G0.1 makes policy-without-ledger-entry a lint FAIL in the kernel contract.
- **PARTIAL → REWORDED:** "44 session-lifecycle hooks" now cites the audit's categorization with drift-proof wording (Phase 4 text updated).
- I-2 confirmed at `role-registry.json:57` vs `dispatch-contract.json:121`; I-3 confirmed + reproduced live at `beta-consult.js:255` (`path.join(ROOT, outFile)` — fix is `path.resolve`, plus the uncaught-throw exit path).

## α-eyeball hardening deltas (H-1…H-6)
- **H-1** Plan DoD preamble = packet 02's acceptance sentence: *1.0 is done when a clean installed product moves idea→canon→roadmap→sprint→build→gauntlet→launch-readiness→release→retro→learning-promotion without relying on chat memory, stale trackers, manual Alpha heroics, or unverified agent claims.*
- **H-2** Phase 3's lease schema cites packet 03 §Persistence-policy (one_shot/wave/phase/session) as seed; Phase 1's routing fix cites 03's role_id/provider/runtime triple.
- **H-3** Phase 3 gains a small item: **tracker-fidelity probe** — tracker/sprint-state claims vs git/disk ground truth, wired into /scan:full (ED-056 recurrence class; independently demanded by packets 07 AND 17).
- **H-4** The 13/15 conformance matrix carries an explicit kernel-scope IN/OUT line (product-pack gates → lastmile) so dropped packs can't smuggle back in.
- **H-5** All phase exits rewritten as **named runnable commands + expected outcomes** (packet 14's shape) — see gate set below.
- **H-6** (a) Binding-order rule #5 ("top-level default = alpha") lives ONLY in helm bindings, never neutral AGENTS.md — the packet's own AGENTS.md.template violates this; fix at projection time. (b) Envelope vocabulary reconciled: 5 terminal states {success, partial, blocked, failed, cancelled} + `failure_reason` codes from packet 08's taxonomy (timeout/quota_exhausted/provider_unavailable/model_unavailable/auth_missing/worktree_base_stale are CLASSES, not states).

## Testable phase-exit gates (binding; supersede the prose "Exit:" lines above)
Full gate set with per-gate detail: `evidence/hardening-20260717/hardened-gates-draft.md`; the binding version below is THE exit criteria. Report-only is a stated exception with an ED entry, never a silent default. A gate that cannot run = the phase is not exitable. **Bootstrap trust-ordering (β catch):** Phase 0–3 gates execute as self/CI checks — the TRUSTED checker only exists at Phase 4; G4.1's self-modification test closes that loop retroactively by re-validating gate dependencies from the pinned checker. **β checkpoint on this layer: DECIDE B/0.88 + refinement 0.90 (logged to beta events 2026-07-17); its 5 HOW-tightenings are folded below.**

**Phase 0:** G0.1 contract-lint (build-in-phase; every policy block names `Enforcer:` or an ED — missing = FAIL) · G0.2 ADR committed + `/scan:references` clean · G0.3 conformance matrix seeded kernel-scope-only, runner executes (report-only through Phase 2 under an ED named at P0 exit; BINDING from Phase-3 exit — never a silent default) · G0.4 H-1 DoD preamble present.
**Phase 1:** G1.1 harness-spawn model rule test (non-Claude pin resolves to Claude on Agent-tool channel; CLI pin untouched) · G1.2 `dispatch-contract.js validate` exit 0 with I-2 reconciled · G1.3 I-3 absolute-path regression test · G1.4 ED-205 verify-THEN-fix (β catch: the defect was inherited, never reproduced — REPRODUCE first; if it reproduces, fix + regression test that `--provider` honors spec `provider_model`; if no-repro, correct ED-205 and keep the regression guard — never a silent pass on a phantom). **RESOLVED 2026-07-17 (ε code-trace): CONDITIONAL — the behavior reproduces but is CORRECT-BY-DESIGN** (spec `provider_model` is native-provider-bound; dropping it on a `--provider` override is intentional and documented at dispatch-agent.js:742-746; `--provider X --model Y` is the built-in explicit path). ED-205 reclassified in the ledger; G1.4 reduces to a regression guard on the intended semantics + one doc line · G1.5 security-panel lane contract satisfied per the operator's Phase-1 scope ruling (below) — a REQUIRED lane proves liveness with a real ledger record · G1.6 security canary corpus (5 cases) + kill-one-CONTRACTED-lane→BLOCKED negative test, no silent judge fallback · G1.7 effective-model attestation self-check green · Exit: security panel end-to-end GREEN across all contracted lanes on a real target.

**⚑ FLAGGED OPERATOR RULING (Phase-1 scope — surfaced by the agy probe; β concurs 0.90 the old gate was FALSE-GREEN):** the ratified "3-lab panel" exit silently assumed the agy lane worked; it does not (ED-060 — zero records ever across 503 ledger entries, no providers.js agy syntax, headless contract unproven, real migration required). A binding gate demanding GREEN from a structurally-down lane is exactly the false-green class this hardening exists to kill — the gate is restated to CONTRACTED lanes either way. One choice for final approval:
- **Option A (α recommendation):** timebox the Antigravity migration INSIDE Phase 1 (fix I-2 + providers.js agy syntax + one real ledger record); if the headless contract isn't proven at timebox end, auto-fall-back to Option B. Preserves the operator's interoperability bet at bounded cost; risk = the migration is partly operator-owned (ED-060) and may be blocked.
- **Option B (β-recommended default, the guaranteed floor):** 1.0 binds on the 2-family panel (GPT+Claude) NOW; agy becomes a NAMED, sunset-dated follow-up (ED-060 stays open, dated). Honest, ships the kernel, kill-one-lane→BLOCKED still meaningful with 2 contracted lanes.
Either option kills the false-green; A ⊃ B (A degrades into B). **β HOW-mechanics (second verdict DECIDE 0.88, OPEN_ADR — folded so the ruling is enforceable, not intent):** (1) Option A's timebox is CONCRETE — hard bound set at approval (default proposal: 1 builder-day / 3 attempts), proven-condition is MACHINE-CHECKABLE = one real agy ledger record through dispatch-agent.js with `fallback:false` (config echoes do NOT count — the exact trap that fooled the tracker), and the 2-family bind fires AUTOMATICALLY on expiry (never a second decision; never let a kernel phase-exit hang on the externally-blocked, operator-owned ED-060 migration). (2) The sunset date gets an ENFORCER: a /scan:full check reads ED-060's sunset date and fails loudly once it passes unresolved (wire agy or formally drop it — no silent lapse). (3) New Phase-1 deliverable: a machine-readable **lane manifest** (`required:[gpt,claude], optional:[agy]`) consumed by BOTH G1.5 and G1.6 — without it the kill-one-lane test can't distinguish "required lane down = BLOCK" from "optional lane down = proceed". (4) The lane contract (required/optional semantics + fallback + sunset trigger) is ratified via its own **Phase-1 ADR**. Framing for the choice: the security floor HOLDS at 2-family — cross-provider diversity (two model families) is preserved; the third lane buys breadth/redundancy, not the core property. (5) **sol refinement (folded into the ruling's shape):** ship the contract as TWO immutable, versioned assurance profiles — `panel-2family` (GPT+Claude required) and `panel-3lab` (all three required, every missing lane blocks). The operator ratifies `panel-2family` NOW as the 1.0 floor; Option A becomes an attempt to PROMOTE to `panel-3lab` (timebox expiry retains the already-ratified floor — promotion semantics, never silent degradation of a 3-lab claim); an agy proof moves it optional→required; an optional lane that actually RUNS and returns a binding FAIL still binds (absence ≠ waiver); no fallback ever counts as another lab; and choosing the 2-family floor requires EXPLICIT supersession of ADR-0016, the AGENTS/registry 3-lab claims, and the hard-coded `security-pass-count` enforcement — no stale 3-lab claim left standing anywhere. Decide at final approval.
**Phase 2:** G2.1 binding-precedence fixtures (unbound worker fail-closed; planted repo-prose "you are Alpha" cannot bind) · G2.2 projection regen → `git diff --exit-code` clean · G2.3 authority-pollution scan over the EFFECTIVE instruction graph, zero ambient authority + H-6a placement asserted — fixture must PLANT rule-#5-STYLE text ("default top-level human-facing role = alpha") in a neutral file and prove it's caught, not just the bare "you are Alpha" identity string (β catch: the default-binding leak class is the one that matters) · G2.4 President-leak closed in worktree instruction sets; codex reviewers stop slurping the full router · G2.5 per-provider cwd/sandbox tests BEFORE any cwd change merges · G2.6 operator-voice projected helm-only (scan-asserted).
**Phase 3:** G3.1 schema suite: 5 terminal states + failure_reason codes (H-6b) · G3.2 prompt-size floor AND required-semantics both enforced; hollow-prompt fixture fails closed · G3.3 ED-069 started-row + ED-070 quota field wired in ALL dispatch writers as one change; regression green · G3.4 leases + do-not-reopen live (two-sessions-same-SP fixture BLOCKS; resumed-session re-litigation surfaces the ledger; this plan = first do-not-reopen entry) · G3.5 tracker-fidelity probe wired into /scan:full (H-3) · G3.6 ED-071 fold-back done · G3.7 immutable base commit AND freshness assertion; stale-base fixture red→green · G3.8 packet-08 reaper-ranking fixtures (process-absence-only reap REFUSED).
**Phase 4:** G4.1 pinned checker evaluates from OUTSIDE the candidate tree (self-modifying candidate flagged) · G4.2 base+tree hash validated; check→merge mutation fixture REFUSES · G4.3 fail-closed on missing/crashed/timed-out checks (3 fixtures) · G4.4 bypass-around-controller detected loudly; pre-commit documented as non-authority · G4.5 one full WorkOrder→Envelope→checked-integration pass on a NON-Claude lane with portable invariants in the dispatch control plane · G4.6 honest-promise stated + checker self-modification test · **1.0 DoD:** H-1 sentence demonstrated by a conformance-matrix run.

## sol-reharden verdict (2026-07-17) + α dispositions — PROPOSED, pending operator FINAL approval
Verbatim verdict: `evidence/hardening-20260717/cabinet-sol-reharden-verdict.md` (real dispatch: codex exec, gpt-5.6-sol @ ultra, exit 0, ok:true, 17.5KB out; ledger record on dispatch-completions). Headline: **"no phase resequencing needed; the architecture is right; NOT execution-ready as-is"** — Q1 trust boundary PARTIAL (the operational trust ROOT is undefined), Q2 TEN false-green paths through the G-gates, Q3 lane ruling conditionally right (wants profile semantics), Q4 all six H-deltas stand with tightenings, Q5 three ranked amendments. α dispositions (nothing reverses a ratified decision; ALL pending operator approval):
1. **sol-A1 trust root → ADOPT into Phase 4 scope, WITH a β scope-guard:** protected canonical ref; a SOLE integration principal; content-addressed pinned checker BUNDLE (helpers/deps/config/executable-resolution included) with controlled promotion; acceptance verdict bound to WorkOrder + check-suite version + evidence + destination ref; integration by atomic compare-and-swap. This is the concrete definition Phase 4's "trusted enforcement adapter" was missing. **⚠️ Scope-guard (β cross-check):** A1 is adopted as ARTIFACT-ACCEPTANCE + INTEGRATION authority only — full credential ISOLATION stays NAMED at the honest-promise out-of-scope boundary (OS sandboxing / credential minimization, deferred from 1.0) and is NOT a Phase-4 exit requirement; otherwise A1 silently re-imports the deferred containment work and the honest promise stops being honest.
2. **sol-A2 AcceptanceRecord → ADOPT at the Phase 3/4 seam:** the ResultEnvelope stays an UNTRUSTED execution report; a separate TRUSTED AcceptanceRecord (WorkOrder digest, exact base/tree/target ref, checker+policy digests, evidence digests, effective route/fallback, integration receipt) is what authorizes integration — provider-authored `success` never does. Completes H-6b.
3. **sol-A3 aggregate helm runner → ADOPT-AS-AMENDED as Phase 4's exit proof (supersedes G4.5):** one fail-closed aggregate runner executing a clean-install black-box flow from EVERY CONTRACTED top-level helm + the selected security profile — exact commands, mandatory negative fixtures, zero skips, current-run evidence. "Contracted helms" = those the Phase-0 support matrix certifies (leaf-lane success ≠ helm portability). "Amended" concretely (β): the runner aggregates over REQUIRED lanes fail-closed and honors the profile's optional-lane semantics — absent optional lane ≠ block; an optional lane that ran and FAILED binds.
4. **Q3 profiles → ADOPTED** into the ⚑ ruling (see the ⚑ block: panel-2family/panel-3lab, promotion-not-degradation, optional-FAIL-still-binds, explicit ADR-0016 supersession).
5. **Q4 H-tightenings → ADOPT as amendments:** H-1 is a lifecycle/evidence-flow proof, NOT a re-import of product launch ceremony into kernel scope; H-2 lease schema += owner, TTL, renewal, atomic acquisition, fencing tokens; H-3 += field-level ground-truth authority map + consistent-snapshot semantics, mismatches BINDING at Phase-3 exit; H-4 IN/OUT manifest is versioned and candidate-IMMUTABLE (a candidate tree can never edit away a difficult check); H-5 acknowledged honestly — the G-set is the gates' SPEC; each phase's FIRST deliverable materializes its gates as exact commands + exit codes + fixture IDs + assertion counts; H-6a restated: the default-binding rule lives in TRUSTED provider-specific HELM bindings (one per supported helm — not "Claude-only", which conflicts with any-provider-at-the-helm); H-6b superseded by sol-A2.

### Gate addenda (sol Q2 — the ten false-green paths; binding when each gate is materialized)
*Landing shape (β): each addendum lands as a NAMED negative-test fixture that goes RED on its false-green path — provable closure, never prose.*
- **G4.4:** prevention, not detection — the protected ref REFUSES un-brokered merges (sole-principal CAS); "we detected the bypass afterwards" = gate FAIL.
- **G4.1/G4.6:** "outside the candidate tree" means outside the candidate's WRITABLE DOMAIN — checker helpers, dependencies, config, executable resolution, and the next-run checker are all inside the pinned content-addressed bundle; add a checker-poisoning fixture.
- **G4.2/G3.7:** the acceptance verdict binds the DESTINATION ref; integration is compare-and-swap; a head-advanced-after-check fixture must REFUSE (no check→merge target drift).
- **G4.5:** superseded by sol-A3 — helm-level proof, invariants (died/timed-out/quota/approval/resume) proven per contracted helm, never one happy path.
- **G1.5/G1.7:** attestation must correlate SAME-RUN to the actual executable return — sanitized invocation digest, code SHA, panel profile, evidence digest; wrapper telemetry alone can self-attest and does not count.
- **G3.5:** at Phase-3 exit, ZERO unexplained tracker/disk mismatches is BINDING (report-only allowed only before that).
- **G0.1/G0.3:** enforcer references must RESOLVE (named script exists AND runs), cited EDs must exist in the ledger, mandatory fixture counts are NONZERO, core invariants are non-debt-waivable — `Enforcer: nonexistent-script` is a lint FAIL.
- **G2.2:** determinism ≠ fidelity — add semantic-fidelity fixtures (canonical rule X MUST appear in projection Y); a generator that consistently omits a rule still fails.
- **G3.4:** lease acquisition is ATOMIC with fencing tokens; add a SIMULTANEOUS-acquisition race fixture (sequential two-session fixture misses it); do-not-reopen requires explicit SUPERSESSION to override, not advisory surfacing.
- **G4.3:** default-deny — malformed, stale, partial, unknown, and silently-skipped REQUIRED checks all → BLOCKED (fixtures each), not only missing/crashed/timed-out.
