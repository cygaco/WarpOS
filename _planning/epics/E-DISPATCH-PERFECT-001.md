# Epic Plan: Perfecting Agent Dispatch

> **Epic ID:** `E-DISPATCH-PERFECT-001`
> **Epic tracker (source of truth for state):** [../../trackers/epics/E-DISPATCH-PERFECT-001-perfecting-agent-dispatch.md](../../trackers/epics/E-DISPATCH-PERFECT-001-perfecting-agent-dispatch.md)
> **Status:** Planned — this document is the durable plan artifact; `TRACKER.md` outranks it for state.
> **Generated by:** `/epic:plan` (scripts/epic/plan.js) on 2026-06-17.
> **Tracker linkage:** `trackers/epics/E-DISPATCH-PERFECT-001-perfecting-agent-dispatch.md` ⇄ `_planning/epics/E-DISPATCH-PERFECT-001.md` (round-trip).

## 1. Goal
Make the WarpOS agent-dispatch system correct end-to-end: the right model+provider+effort for every role, genuine cross-provider review, product-agnostic + canon-parameterized agents, a simplified (verified) dispatch hierarchy, and machine enforcement for all of it -- built on this session's RE-VERIFIED premises, not inherited ones.

## 2. Background
This session re-verified the dispatch foundations and found: (1) the Opus-4.8-max model chain is already complete + enforced (model-chain exit 0, role-parity clean, 35 spec pins) -- foundation, no rebuild; (2) ED-041 ('in-process roster is alpha-only / Agent-not-available-inside-subagents') is FALSIFIED on the current harness via live probe (teammate-epsilon spawned a director, a lead, and a depth-3 nest cleanly; the only block was the WarpOS scope-contract-guard policy gate); (3) many silent-death 'reaps' are legitimate LONG tasks exceeding the harness time budget, which Agent-tool spawns bypass (5/5 alive vs 0/4 CLI). The DISPATCH-ERRORS.md census (40 incidents / 8 weeks, families A-H + redesigns R1-R7) is the evidence base. Operator direction: more gruntwork on codex, smartest-orchestrate / different-provider-review / dumber-doers, 3-provider security review, GPT for the product-leadership chain, fix model-catalog freshness, and genericize agents off the jobzooka origin product.

## 3. Scope
Model/provider/effort routing across the full roster (catalog.js + role-registry.json + dispatch-contract.json + spec frontmatter + the model-router panel); the model-catalog freshness mechanism; the 3-provider security review (declared + FIRED + enforced); the GPT product-leadership chain + the director class-derivation rule; the codex-doer build shape; product-agnostic + canon-parameterized agent genericization + a widened framework-purity enforcer; and the (now-small) dispatch-hierarchy simplification post-ED-041. Re-verification of the DISPATCH-ERRORS.md load-bearing premises (verify-don't-inherit).

### Out of scope
Rebuilding the already-complete Opus-4.8-max chain; the full E-DISPATCH-SHAPE-001 ADR-0013 enforce-flip repair (its own epic -- a W3 dependency, not duplicated here); per-product canon authoring (each product supplies its own canon); a full model-router PANEL visual overhaul beyond registry-derived-rows + scrapped-role cleanup.

## 4. Acceptance criteria
- Live: node scripts/checks/model-chain.js exits 0 with the frontmatter-effort dimension covered; node scripts/models/check.js flags the stale snapshots + shows ages; gpt-5.3-codex absent from the catalog.
- Live: a real 3-provider security review produces three provider-stamped passes verified by the pass-count enforcer (or the debt is logged + visible at /scan:full).
- Live: role-parity + model-chain green with the GPT product chain + the director rule; a GPT director actually dispatches via subprocess-cross-provider.
- Live: teammate-epsilon conducts the in-process roster directly with scopeContract, end-to-end, on a real sprint phase.
- Each workstream lands with a planted-violation fixture + mutation check; each new enforcer is registered as a standing regression class.

<!-- ac-categories:checklist (auto, S-LC-11) -->
### Enforcement-criteria coverage — 20 categories (PLAN §11 / S-LC-11)

Every category below needs AC + a **proof** — a planted fixture that must fail, a
real record (run_id + elapsed/bytes), a green/blocked exit code, or a captured
event sequence (PLAN §11 proof standard). Replace each `proof: TODO` with the
real proof; a bare `proof: TODO` stub is "named but unproven" and is FLAGGED
(report-only) by `/scan:ac-coverage --categories`. Single source for this list:
`scripts/sprint/ac-categories.js` (20 categories).

- [ ] **correct mode selection** — proof: TODO
- [ ] **correct mode switching** — proof: TODO
- [ ] **correct team teardown** — proof: TODO
- [ ] **correct team creation** — proof: TODO
- [ ] **correct team verification** — proof: TODO
- [ ] **correct lifecycle-hook firing** — proof: TODO
- [ ] **correct hook ordering** — proof: TODO
- [ ] **correct agent dispatch** — proof: TODO
- [ ] **correct sprint/epic binding** — proof: TODO
- [ ] **correct tracker linkage** — proof: TODO
- [ ] **correct planning-artifact persistence** — proof: TODO
- [ ] **correct provider readiness** — proof: TODO
- [ ] **correct safety gates** — proof: TODO
- [ ] **correct test strategy** — proof: TODO
- [ ] **correct fixture/holdout coverage** — proof: TODO
- [ ] **correct review requirements** — proof: TODO
- [ ] **correct completion proof** — proof: TODO
- [ ] **correct user-approval points** — proof: TODO
- [ ] **correct learning/persistence capture** — proof: TODO
- [ ] **correct blast-radius analysis** — proof: TODO
<!-- /ac-categories:checklist -->

## 5. Sprint candidates
- **S-DP-W0** — Planned — Model hygiene (SHIP NOW): check.js freshness 30->14d + honest age + regression test; remove gpt-5.3-codex (catalog+cache+route.md); frontmatter effort pins (beta xhigh, gamma/delta high); ED-058 frontmatter-blind-spot + scrapped-role coverage; panel registry-derived rows
- **S-DP-W1** — Planned — 3-provider security review (SHIP NOW): registry third_pass + enforcers read it + WIRE THE FIRING (reviewer.md Protocol + security-lead.md) + named pass-count enforcer -- the beta DECIDE 0.88 conditions a-d
- **S-DP-W2** — Planned — GPT product-leadership chain (SHIP NOW, pending beta class-derivation verify): product-lead/design-lead registry edits + director-of-product/growth class-derivation rule + fallback:claude on all GPT judgment roles
- **S-DP-W3** — Planned — Codex-doer migration (DEFERRED -- depends on E-DISPATCH-SHAPE-001 ADR-0013 repair): subprocess-codex build shape + doers->gpt-5.4 / fixers,skeleton->gpt-5.4-mini + security-builder/fixer stay Claude + coupled reviewer rebalance (FE->gemini, BE/QA->claude)
- **S-DP-W4** — Planned — Product-agnostic + canon-based agent genericization (PARALLEL): sweep jobzooka vocab from ~24 specs + parameterize product-specific checks from canon + widen framework-purity to catch domain vocab
- **S-DP-W5** — Planned — Dispatch-hierarchy simplification post-ED-041 (NEAR-TERM): epsilon summons the in-process roster directly + supply scopeContract + ADR retiring ED-041 + the spawn-hand-stays-with-the-conductor invariant

## 6. Dependency map
- W3 -> E-DISPATCH-SHAPE-001 ADR-0013 enforce-flip repair (hard gate).
- W1, W2, W5 -> role-registry.json + dispatch-contract.json (shared keystones; foreground-serial).
- W0, W4 -> independent (no shared-keystone contention).

## 7. Risk map
- HIGH: registry-key-without-firing (W1) = declarative lie -> mitigate with beta conditions a-d landing together.
- HIGH: codex build shape on a mid-revert ADR-0013 gate (W3) -> mitigate by gating W3 on the repair.
- MED: director class-derivation edits a security-class invariant -> mitigate with beta verify + cross-provider plan review.
- MED: codex headless reliability -> mitigate with reap-safety + completion records in the new shape, ramp not big-bang.
- LOW-MED: genericization drops a real check -> mitigate by keeping generic personas + canon-parameterizing only the product-specific ones.

## 8. Required agents / modes / team / lifecycle hooks
- **Required agents:** alpha (orchestrator/spawn-hand), epsilon (conductor), beta (boundary judgment); the engineering pods (frontend/backend/security lead+builder+reviewer+fixer) + qa-reviewer for the gauntlet; cross-provider reviewers (GPT-5.5 / Gemini-3.1-pro); cabinet for cross-provider PLAN review (catches enforcement overclaims before code).
- **Required modes:** sprint (full lifecycle) for the build waves; adhoc for surgical keystone edits (role-registry / dispatch-contract).
- **Required persistent-team behavior:** Small persistent core alpha+epsilon+beta; coordinators summoned on demand (ED-041 unlocked direct epsilon->roster spawn); spawn-hand stays with the conductor; no standing pods (workers are on-demand, isolation is the feature).
- **Required lifecycle hooks:** scan:model-chain (ED-058), scan:role-parity, scan:dispatch-routing-parity, scan:framework-purity, gauntlet-verify, dispatch-contract validate, scan:full; the mode:init:gate.

## 9. Tracker linkage
- Epic tracker: [../../trackers/epics/E-DISPATCH-PERFECT-001-perfecting-agent-dispatch.md](../../trackers/epics/E-DISPATCH-PERFECT-001-perfecting-agent-dispatch.md) — authoritative state.
- ROADMAP § Epics entry: pending α integration.
- TRACKER header pointer: pending α integration.

## 10. Execution-phase gates + human approval points
- Cross-provider gauntlet (GPT-5.5 + Gemini) GREEN on every blast-sensitive change (dispatch-contract / role-registry / enforcers / security path).
- beta boundary consults at the four phase boundaries, front-loaded (per ED-030).
- model-chain + role-parity + both manifests + trackers 20/20 green before each commit.
- Planted-violation fixture + mutation check on every new enforcer (no hollow ladders); register each as a standing regression class.

### Human approval points
- Epic ID + title (E-DISPATCH-PERFECT-001 / 'Perfecting Agent Dispatch') -- this approval.
- The codex-doer architecture (W3) before building the subprocess-codex shape (Class-C, after the ADR-0013 repair).
- Whether W4 genericization stays in-epic or splits into its own epic.
- Any scope-reduction of currently-allowed dispatch behavior (beta >=0.95 floor).

## 11. Blast-radius analysis
- role-registry.json (the keystone -- provider/model/effort/passes for the roster) + dispatch-contract.json (class-derivation + shapes, mid-flight with ADR-0013) + catalog.js + providers.js (live dispatch resolution) + ~35 agent-spec frontmatter pins.
- The security review path (reviewer.md + security-lead.md + provider-trace) -- a security invariant.
- ~24 agent specs carrying jobzooka vocabulary + framework-purity.js.
- The model-router panel (dispatch.js gui) + the research cache (runtime/models-research).
- scripts/checks/model-chain.js + role-parity-scan.js (enforcers extended for third_pass + frontmatter effort).

## Enforcer

Per planning principle #7 (every plan names its enforcer). The epic's named enforcers (on disk, all already referenced in §8/§10/§11 above):
- **`scripts/checks/model-chain.js`** (ED-058) — the roster model/provider/effort/passes routing gate; extended for `third_pass` + frontmatter effort.
- **`scripts/checks/role-parity-scan.js`** — the role↔spec bijection + frontmatter-pin parity gate.
- **`scripts/dispatch/dispatch-contract.js validate`** — the class↔shape permission matrix (wired into `/scan:full`).
- **`scripts/checks/framework-purity.js`** — the widened product-agnostic / canon-parameterization purity gate (W4).
- **the security pass-count enforcer** (`security-pass-count`) — proves the 3-provider security review FIRED (declared + fired + enforced), or logs the debt visible at `/scan:full`.
- **`gauntlet-verify`** — the cross-provider gauntlet completion-record liveness gate over every blast-sensitive change (dispatch-contract / role-registry / enforcers / security path).
- **Per-workstream planted-violation fixture + mutation check**, each registered as a standing regression class (the no-hollow-ladders rule, CLAUDE.md policy-hygiene) — one per new enforcer.

---
_End of plan artifact for E-DISPATCH-PERFECT-001. State lives in the epic tracker; this is the durable plan._
