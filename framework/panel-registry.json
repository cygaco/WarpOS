{
  "$schema": "warpos/panel-registry/v1",
  "panels": {
    "readiness": {
      "name": "readiness",
      "opener": "/cockpit:readiness",
      "description": "Cross-product launch-readiness board — composite % per registered product",
      "run_context": "cli"
    },
    "models": {
      "name": "models",
      "opener": "/models:router",
      "description": "Model router — the Dispatch Console GUI (role -> provider -> model -> effort)",
      "run_context": "cli"
    },
    "admin": {
      "name": "admin",
      "opener": "node scripts/admin/preview.js",
      "description": "A product's in-app founder admin panel (run-in-product; never WarpOS itself)",
      "run_context": "in_app"
    },
    "roadmap": {
      "name": "roadmap",
      "opener": "node scripts/panel/roadmap.js",
      "description": "Roadmap 'what's next' board — ranked next-action + in-flight sprints + blockers",
      "run_context": "cli"
    }
  }
}
