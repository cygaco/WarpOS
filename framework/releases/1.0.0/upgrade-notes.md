# Upgrade notes — 0.17.0 → 1.0.0

> Skeleton generated by scripts/warpos/release-canonical.js. Replace before
> tagging.

## Pre-flight

1. Tag your current state: `git tag pre-warpos-1.0.0-update HEAD`.
2. Confirm clean working tree: `git status --porcelain` empty.

## Run the update

```bash
node scripts/warpos/update.js --to 1.0.0 \
  --source ../WarpOS \
  --target . \
  --dry-run

node scripts/warpos/update.js --to 1.0.0 \
  --source ../WarpOS \
  --target . \
  --apply
```

## Rollback

```bash
git reset --hard pre-warpos-1.0.0-update
```

(or restore from `.warpos/transactions/<latest>/backup/`).
