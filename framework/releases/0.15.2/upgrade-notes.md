# Upgrade notes — 0.15.1 → 0.15.2

> Skeleton generated by scripts/warpos/release-canonical.js. Replace before
> tagging.

## Pre-flight

1. Tag your current state: `git tag pre-warpos-0.15.2-update HEAD`.
2. Confirm clean working tree: `git status --porcelain` empty.

## Run the update

```bash
node scripts/warpos/update.js --to 0.15.2 \
  --source ../WarpOS \
  --target . \
  --dry-run

node scripts/warpos/update.js --to 0.15.2 \
  --source ../WarpOS \
  --target . \
  --apply
```

## Rollback

```bash
git reset --hard pre-warpos-0.15.2-update
```

(or restore from `.warpos/transactions/<latest>/backup/`).
