# {{SLUG}} — local environment (copy to .env.local; .env* is gitignored)
# Names only, no values. Add your product's keys as features need them.

# Public app name (safe to expose to the browser).
NEXT_PUBLIC_APP_NAME={{SLUG}}
