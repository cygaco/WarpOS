# Fix brief — SP-20260611-002 FIX1-G3c (backend-builder) — gauntlet attempt-1 finding 8

You are a BACKEND builder in an ISOLATED WORKTREE for the SP-20260611-002 gauntlet fix-cycle
(attempt 1). HIGH-risk enforcer integrity (BC-16). The cross-provider gauntlet (gpt-5.5 backend
lane) found that planning-principles `--enforce` fail-closed is INCOMPLETE — scan-time exceptions
are swallowed before the CLI catch sees them. Branch is off the integrated HEAD (G3c's original
work is in the tree — you EXTEND it).

## Target files (do NOT touch any other surface)
- scripts/checks/planning-principles.js
- Tests: tests/regression/SP-20260611-002/planning-principles-enforce.test.js (EXTEND with the scan-time-throw cell) — and S-LC-08 must still pass.

## Finding to fix (verbatim from the gpt-5.5 backend-reviewer, binding)

**MAJOR (finding 8) — planning-principles.js:259:** `--enforce` fail-closed on internal scan errors
is INCOMPLETE because scan-time exceptions are swallowed before the CLI catch sees them. A section
matcher throw returns `false` from the catch at lines 262-263 — which means "not missing" — and can
produce `ok:true`; unreadable files/dirs are also downgraded to notices. The enforce path only
fail-closes UNHANDLED exceptions.

## Fix intent
A scan-time exception under `--enforce` must FAIL CLOSED, not be swallowed into a "not missing"/
notice result. The inner catch (lines ~262-263) that returns `false` on a section-matcher throw is
the hole: under `--enforce`, a section-matcher throw (or an unreadable file/dir during the scan) must
PROPAGATE to a fail-closed outcome (exit 2, the AC-7.2 internal-error-fails-closed contract) — NOT be
converted to `false`/"not missing" (which can green the run). An internal error cannot mask findings
under --enforce. Keep the report-only (non-enforce) behavior tolerant; the fail-close is --enforce-only.

## Discipline (binding)
- Fix lands WITH an exploit fixture that REDs WITHOUT the fix (mutation-verify: revert → the new test
  REDs). Plant a section matcher that THROWS during the scan under --enforce → assert exit 2
  (fail-closed), NOT exit 0/ok:true. Also cover an unreadable file/dir during the scan under --enforce.
- Existing suites must still pass (no-regress): planning-principles-enforce (the original cells),
  S-LC-08 planning-principles. RUN them, show output.
- Fixtures namespaced under tests/regression/SP-20260611-002/. NO gate flips (--enforce proven
  fail-closed-capable in the harness, not wired into /scan:full — AC-X.4 stays honored).
- Do NOT redefine the shared legacy-cutoff helper. Do NOT touch G3a's surfaces.
- AC-8.4 (the check-ac-coverage proof-syntax residue) stays a DOCUMENTED RESIDUE — not in scope here.
- Commit in the worktree: `fix(SP-20260611-002/FIX1-G3c): planning-principles --enforce fail-closes on scan-time exceptions (section-matcher throw / unreadable file no longer swallowed to 'not missing') (gauntlet finding 8)`
- Final: ≤10-line envelope — finding DONE + test results (N/N) + mutation-verify + files + commit hash.
