# Fix brief — SP-20260611-002 FIX1-G1 (security-builder) — gauntlet attempt-1 findings 1, 2, 4

You are a SECURITY builder in an ISOLATED WORKTREE for the SP-20260611-002 gauntlet fix-cycle
(attempt 1). HIGH-risk gate-integrity (BC-16): these surfaces ARE enforcers. The integrated
build PASSED its own suites but the cross-provider gauntlet (gpt-5.5) found REAL bypasses. Fix
ALL THREE findings below. Branch is off the integrated HEAD (G1's original work is already in
the tree — you EXTEND it, don't redo it).

## Target files (do NOT touch any other surface)
- scripts/hooks/team-guard.js
- scripts/checks/mode-write-coverage.js
- Tests: tests/regression/SP-20260611-002/{team-guard-project-scope,mode-write-window}.test.js (NEW exploit fixtures) — and the existing team-guard / mode-write-coverage suites must still pass.

## Findings to fix (verbatim from the gpt-5.5 security-reviewer, binding)

**BLOCKER (finding 1) — team-guard.js:473:** `namedTeamVerified()` verifies only that a fresh team
with the supplied name carries the conductor; it does NOT verify the team belongs to THIS project
by slug or member cwd. A worker can pass a real FOREIGN team name such as `doogle-sprint` and skip
the sprint readiness gate.

**BLOCKER (finding 2) — team-guard.js:504:** sprint readiness selects the freshest team config
GLOBALLY under `~/.claude/teams`, not the freshest PROJECT-SCOPED team. A foreign fresh ε team
makes `teamReady` true and allows worker dispatch even when this project has no correct live team.

**MAJOR (finding 4) — mode-write-coverage.js:139:** mode-write corroboration accepts any same-target
lifecycle event within ±120 minutes, INCLUDING an event BEFORE the current `mode.json` mtime. A
direct out-of-band rewrite shortly after a legitimate same-mode switch greens the detector.

## Fix intent (the shape — β/lead-directed)
- Findings 1+2: PROJECT-SCOPE the team identity. A team satisfies the gate ONLY if it binds to THIS
  project — by slug AND/OR a member cwd that is the project (exact) or strictly under it. A foreign
  team NAME must fail; the globally-freshest-team selection must be filtered to project-scoped teams
  FIRST, then freshest-among-those. Use the SAME exact-match-after-normalize discipline as
  lifecycle.verify() (the existing roster exact-match pattern) — NO substring/parent-containment.
- Finding 4: the corroboration window must be POST-mtime — a lifecycle event only corroborates a
  mode.json write if it occurred AT OR AFTER the mode.json mtime (an earlier event cannot vouch for
  a later out-of-band rewrite). Keep the ±window as an upper bound but add the lower bound = mtime.

## Discipline (binding)
- Each fix lands WITH an exploit fixture that REDs WITHOUT the fix (mutation-verify: revert the fix →
  the new test REDs; restore → green). The reviewers found these BECAUSE the existing tests were
  happy-path on these vectors — your fixtures must plant the EXACT attack (foreign team name e.g.
  `doogle-sprint`; a globally-fresher foreign team; a pre-mtime corroboration event).
- Existing suites must still pass (no-regress): team-guard-verify, team-guard-gate, team-guard-sprint,
  lifecycle-roster-exact-match, mode-write-coverage. RUN them, show output.
- Fixtures namespaced under tests/regression/SP-20260611-002/. NO gate flips (report-only stays).
  Do NOT re-touch G1's kill-switch attestation (AC-2.4, already correct) except where finding 4 needs it.
- Commit in the worktree: `fix(SP-20260611-002/FIX1-G1): project-scope team identity (slug+cwd, foreign-name + global-freshest both fail) + post-mtime mode-write corroboration window (gauntlet findings 1,2,4)`
- Final: ≤10-line envelope — per-finding DONE + test results (N/N) + mutation-verify confirmation + files + commit hash.
