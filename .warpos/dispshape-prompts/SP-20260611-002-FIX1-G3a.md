# Fix brief — SP-20260611-002 FIX1-G3a (security-builder) — gauntlet attempt-1 finding 5

You are a SECURITY builder in an ISOLATED WORKTREE for the SP-20260611-002 gauntlet fix-cycle
(attempt 1). HIGH-risk enforcer integrity (BC-16). The cross-provider gauntlet (gpt-5.5 backend
lane) found that the AC-5.3 external-expected-source fix works ONLY in tests — the LIVE CLI still
self-derives. Branch is off the integrated HEAD (G3a's original work is in the tree — you EXTEND it).

## Target files (do NOT touch any other surface)
- scripts/checks/coverage-gate-scan.js
- (the registry / sprint-composition source it must read to resolve expected roles — read-only consume; do NOT modify the registry)
- Tests: tests/regression/SP-20260611-002/coverage-gate-scan-live-cli.test.js (NEW exploit fixture driving the REAL CLI path) — and the existing coverage-gate-scan-source suite must still pass.

## Finding to fix (verbatim from the gpt-5.5 backend-reviewer, binding)

**MAJOR (finding 5) — coverage-gate-scan.js:176:** The live CLI still calls `auditLedger(records)`
with no `expectedSource`, so production `/scan` remains self-derived and cannot catch the
omitted-role slip. The pure seam works only when tests inject `expectedSource`; the real caller
never resolves registry/sprint composition or exposes a flag/input for it.

## Fix intent
Wire the external expected-source into the LIVE CLI path. The real `auditLedger` call site (line ~176)
must RESOLVE the expected roles from the registry / sprint composition (the same source AC-5.3's pure
seam takes when tests inject it) and PASS it as `expectedSource` — so production `/scan` is no longer
self-derived. Expose it via the resolved composition (preferred) or a CLI flag/input as a fallback.
The omitted-role slip (a role that produced NO record) must be a gap in the REAL CLI run, not only
under test injection. Keep AC-5.3's pure seam intact (tests still inject) — you are wiring the
DEFAULT/live resolution, not removing the injection seam.

## Discipline (binding)
- Fix lands WITH an exploit fixture that drives the REAL CLI path (not the pure seam with injected
  source) and REDs WITHOUT the fix: plant a ledger missing a registry/composition-expected role →
  the LIVE CLI run must flag the gap. Mutation-verify: revert the live wiring → the new test REDs.
- Existing suites must still pass (no-regress): coverage-gate-scan-source, coverage-gate, waiver,
  legacy-cutoff-shared, dispatch-contract. RUN them, show output.
- Fixtures namespaced under tests/regression/SP-20260611-002/. NO gate flips (scan stays report-only;
  prove the expected-source resolution is LIVE without flipping enforcement on).
- Do NOT redefine the shared legacy-cutoff helper (import it if needed). Do NOT touch G3c's surfaces.
- Commit in the worktree: `fix(SP-20260611-002/FIX1-G3a): wire external expectedSource into the LIVE coverage-gate-scan CLI from registry/composition — omitted-role slip caught in production /scan, not only under test injection (gauntlet finding 5)`
- Final: ≤10-line envelope — finding DONE + test results (N/N) + mutation-verify + files + commit hash.
