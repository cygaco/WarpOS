# Fix brief — SP-20260611-002 FIX1-G2 (security-builder) — gauntlet attempt-1 finding 3

You are a SECURITY builder in an ISOLATED WORKTREE for the SP-20260611-002 gauntlet fix-cycle
(attempt 1). HIGH-risk authorization-gate integrity. The integrated build PASSED its own suites but
the cross-provider gauntlet (gpt-5.5) found a REAL approval bypass. Branch is off the integrated
HEAD (G2's original work is already in the tree — you EXTEND it).

## Target files (do NOT touch any other surface)
- scripts/hooks/authorization-gate.js
- Tests: tests/regression/SP-20260611-002/auth-floor-rm-with-write.test.js (NEW exploit fixture) — and the existing auth-floor / turbo suites must still pass.

## Finding to fix (verbatim from the gpt-5.5 security-reviewer, binding)

**BLOCKER (finding 3) — authorization-gate.js:119:** `node-e-fs` still approves a whole `node -e`
command if it contains ANY allowed write call, even if the same command ALSO contains
`rmSync`/`unlinkSync`. The tracked-delete floor only catches literal extracted targets, so
`fs.writeFileSync(...); fs.rmSync(variable)` can be approved and skip downstream guards. This
violates "rm/unlink not approvable" (AC-4.1).

## Fix intent
The node-e-fs scope match must be ALL-OR-NOTHING on destructive calls: if the `node -e` body
contains ANY `rmSync` / `unlinkSync` (or rm/unlink variants the scope recognizes), the command is
NOT approvable — regardless of co-present write/append/mkdir calls. Do NOT approve a command that
mixes an allowed write with a forbidden delete. The presence of a delete call POISONS the whole
command for auto-approval (it falls through to pass-through / no approve). This is the correct
reading of AC-4.1 ("rmSync/unlinkSync are NOT approvable") — the original fix narrowed the scope
match but a co-present write still flipped it to approve.

## Discipline (binding)
- Fix lands WITH an exploit fixture that REDs WITHOUT the fix (mutation-verify: revert → the new
  test REDs; restore → green). Plant the EXACT attack: a `node -e` with both `fs.writeFileSync(...)`
  AND `fs.rmSync(variable)` (variable form, so the literal-target floor can't catch it) — assert it
  is NOT approved (no decision:"approve").
- Existing suites must still pass (no-regress): auth-floor-tracked-delete, turbo-auth-monotonic,
  turbo-spend-anchor, turbo-self-lockout, plus S-LC-07 permission-profile + scan-turbo-spend. RUN them.
- A legitimate write/append/mkdir-only `node -e` (no delete) is STILL approved (no over-block) — assert.
- Fixtures namespaced under tests/regression/SP-20260611-002/. NO gate flips. Do NOT touch the live
  auth.json (fixture/inputs only — the self-lockout discipline still applies).
- Commit in the worktree: `fix(SP-20260611-002/FIX1-G2): node-e-fs is all-or-nothing on destructive calls — any rmSync/unlinkSync poisons auto-approval regardless of co-present writes (gauntlet finding 3)`
- Final: ≤10-line envelope — finding DONE + test results (N/N) + mutation-verify + no-over-block assertion + files + commit hash.
