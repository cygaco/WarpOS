# Fix brief — SP-20260611-002 FIX1-G3b (backend-builder) — gauntlet attempt-1 findings 6, 7

You are a BACKEND builder in an ISOLATED WORKTREE for the SP-20260611-002 gauntlet fix-cycle
(attempt 1). HIGH-risk enforcer integrity (BC-16) — provider-tier truthfulness. The cross-provider
gauntlet (gpt-5.5 backend lane) found two verdict-matrix gaps the original 18-cell matrix missed.
Branch is off the integrated HEAD (G3b's original work is in the tree — you EXTEND it).

## Target files (do NOT touch any other surface)
- scripts/warpos/provider-tier-check.js
- scripts/warpos/lib/provider-tier-config.js
- Tests: tests/regression/SP-20260611-002/provider-tier-matrix.test.js (EXTEND with the 2 new cells) — and S-LC-10 must still pass.

## Findings to fix (verbatim from the gpt-5.5 backend-reviewer, binding)

**MAJOR (finding 6) — provider-tier-check.js:269:** `unknown-self-attested` is gated only on
`signals.t1Met`, not on T2 being funded. For selected `t3`, `t1Met:true`, `t2_funded:false`, and no
T3 attestation, the report returns `unknown-self-attested` with `ok:true`. Missing T2 funding is
value-free detectable and should be `tier_short`.

**MAJOR (finding 7) — provider-tier-config.js:137:** Read errors are treated as absent greenfield
config. A present-but-unreadable path — for example a DIRECTORY at `--config-path` or permission
denial — returns `corrupt:false`, so the fail-closed corrupt-config hold does NOT apply. Only TRUE
absence should be greenfield; other read failures should be `corrupt:true`.

## Fix intent
- Finding 6: `unknown-self-attested` must require BOTH T1 met AND T2 funded (the reserved case is "T1
  and T2 met, only the T3 sub-floor genuinely undetectable"). When T2 is detectably unfunded for a
  selected tier that needs it, the verdict is `tier_short` (--enforce REDS, envelope ok:false), NOT
  unknown-self-attested. T2-funded is value-free detectable — gate on it.
- Finding 7: distinguish TRUE ABSENCE (ENOENT — greenfield, corrupt:false, framework defaults) from
  OTHER read failures (EISDIR / a directory at the path, EACCES / permission denial, parse-unreadable)
  → those are `corrupt:true` → fail-closed hold (never silently degrade to framework-default t1).
  Switch on the error code/kind: only ENOENT (genuine absence) is greenfield.

## Discipline (binding)
- Each fix lands as NEW verdict-matrix cell(s) that RED without the fix (mutation-verify: revert →
  the new cell REDs). Cell 6: {t3, t1Met:true, t2_funded:false, no-T3} → expect tier_short + ok:false
  + --enforce non-zero (currently unknown-self-attested + ok:true). Cell 7: {present-but-EISDIR/EACCES
  config} → expect corrupt:true + fail-closed hold (currently corrupt:false greenfield).
- Existing suites must still pass (no-regress): provider-tier-matrix (the original 18 cells),
  S-LC-10 provider-tier-check. RUN them, show output.
- Fixtures namespaced under tests/regression/SP-20260611-002/ (corrupt/unfunded config fixtures). NO
  gate flips (--enforce proven-capable on a planted shortfall, not flipped on in prod).
- Commit in the worktree: `fix(SP-20260611-002/FIX1-G3b): unknown-self-attested requires T2-funded (else tier_short) + read-error (EISDIR/EACCES) is corrupt not greenfield (gauntlet findings 6,7)`
- Final: ≤10-line envelope — per-finding DONE + matrix cells (N/N) + mutation-verify + files + commit hash.
