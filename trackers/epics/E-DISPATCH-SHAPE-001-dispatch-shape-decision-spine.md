<!-- EPIC TRACKER — generated by /epic:plan (scripts/epic/plan.js).
     Linked from ../../ROADMAP.md § Epics and ../../TRACKER.md.
     States (§19): Planned | Ready | Active | Blocked | Paused | Review Needed | Completed | Cancelled | Superseded -->

# E-DISPATCH-SHAPE-001 — Dispatch-Shape Decision Spine — one matrix, one resolver, invariants as machine preconditions

- **Epic label and number:** E-DISPATCH-SHAPE-001
- **Title:** Dispatch-Shape Decision Spine — one matrix, one resolver, invariants as machine preconditions
- **Owner:** President Agent
- **Parent roadmap area:** [ROADMAP.md § Epics](../../ROADMAP.md)
- **Goal:** TBD
- **Background:** TBD Plan artifact: [../../_planning/epics/E-DISPATCH-SHAPE-001.md](../../_planning/epics/E-DISPATCH-SHAPE-001.md).
- **Scope:** The dispatch decision spine only: scripts/dispatch/* (dispatch-shape.js resolver, dispatch-contract.json + reader, coverage-gate, gauntlet-verify seams), the three wrappers (dispatch-agent.js, dispatch-claude.js, dispatch-skill.js), scripts/hooks/dispatch-route-guard.js + scope-contract-guard.js taxonomy, scripts/hooks/lib/providers.js quota/breaker seam, full.js/epsilon-runtime.js env export, health checks gaining auth-posture rows.
- **Out of scope:** Re-fixing NOTAGAIN §6 receipts; E-LIFECYCLE-001 gate flips (operator-gated, §8); the sprint-engine boundary/manifest-flap frictions (RI-003/engine UX — separate); product-layer work.
- **Current state:** Active
- **Percent completion:** ~50% — **W1 LANDED 2026-06-10 via SP-20260610-007 (release RL-20260610-042):** T-305 recorded claude-fallback review lane (dispatch-claude --review-fallback; record stamps fallback:true+provider:claude+quota_fallback_from → coverage-gate cross_provider_required trips VISIBLY; build-chain roles refused exit 2; route-guard allowlist intact 52/52; the T-305 builder REAPED and the W0 timeout clamp FIRED LOUD — its first real-world firing — ε ran ONE bounded completion-retry, clean single commit, no residue); T-306 provider circuit breaker (scripts/dispatch/provider-breaker.js; providers.js writes provider-down.json on quota_exhausted [free_tier_limit_zero excluded], providerAvailable() consults at the SINGLE chokepoint upstream of every burn site, claude structurally unblockable, ALL faults fail-open, TTL parse-fail→explicit 30m); T-307 auth-posture surface (detectAuthTier parses auth.json CONTENT value-free via hasOwnProperty on BOTH codex paths; metered key reads "key (metered)" never oauth; auth_mode stamped in quota envelopes — closes the 3-day metered-drain class). Gauntlet: claude 2-lane execution-access BOTH PASS (qa first-try w/ 1 cosmetic minor [isReviewFallbackRoute exported-unwired]; backend retry after a transient Anthropic rate-limit killed the first run mid-review). β DECIDE 0.89 (EVT-dispatch-shape-w1-plan-design-001). DEBT: cross-family pass now owed on W0+W1 diffs (fb3dcfd-base) when quota recovers. Earlier: W0 LANDED 2026-06-10 via SP-20260610-006 (release RL-20260610-041): T-302 truthful builder advisory (validateDispatchForClass; 'builder'→build_chain_worker class, honest wording for unknown ids, NO refuse — that's W2); T-303 run-correlation export (WARPOS_RUN_ID/PHASE_ID/SPRINT_ID from full.js+epsilon-runtime onto wrapper env; coverage-gate run-scope satisfiable; was null 231/231); T-304 foreground-aware timeout (scripts/dispatch/timeout-policy.js FOREGROUND_CEILING_MS=540s fail-closed clamp, death-on-bound, standalone dispatch-timeout-sanity check 5/5 wrappers). Gauntlet: claude 2-lane execution-access (qa PASS; backend FAIL→fix-cycle afe02a4 clamped runProvider, the 4TH G8 wrapper the build missed — the fix-all-callers law caught inside its own fix — →re-review PASS). β DECIDE 0.88 (EVT-dispatch-shape-w0-plan-design-001, ε-consulted directly). DEBT: cross-family (GPT/gemini) review pass owed on the W0 diff when quota recovers (logged, same §H pattern). W1 (availability/fallback/auth-posture) is next.

## Definition of Done
- [ ] W0 ids/clocks true: the generic 'builder' role id resolves identically in the wrapper and the dispatch contract (bijection — alias row or guide sweep, one choice recorded); WARPOS_RUN_ID/WARPOS_PHASE_ID exported by full.js/epsilon-runtime onto wrapper env and stamped on records (run-scoped §17.4 coverage satisfiable on a real run); every wrapper timeout bound < its applicable ceiling (foreground ≤9min or mandated background) with a death record ALWAYS written; planted-violation tests for each (scrapped-id dispatch REFUSES; null run_id under a live run fails coverage; foreground bound >540s fails a /scan:full config check).
- [ ] W1 availability real: ONE recorded claude-fallback review lane (ledgered, visibly trips cross_provider_required rather than vanishing — gauntlet-verify sees it via planted fixture); a provider circuit breaker (provider-down state written on quota classification, TTL'd, consulted before dispatch — planted re-burn test); auth POSTURE surfaced (auth.json auth_mode parsed; metered-key reads as metered not oauth in dispatch-readiness/provider-tier; auth mode stamped into quota error envelopes).
- [ ] W2 resolver as the only door: the dispatch-contract ENFORCE ramp flipped after the W0 sweep; wrappers refuse shapes the resolver did not pick; dispatch-route-guard stops blessing recordless raw claude -p --agent and points fallback traffic at the W1 lane; the envelope contract either revived (reviewer prompts emit it + a consumer acts on validation) or deleted — the choice recorded as an ADR with its enforcer (the 36/36 = 100% validation-failure dead-gate state is named and ended).
- [ ] W3 per-failure-class review-lane policy: each risk class declares minimum lanes (execution-access lane REQUIRED for enforcer/gate changes; cross-family diff lane REQUIRED for distribution-sensitive logic), wired into sprint-hook-points conditions and coverage-gate cross_provider_required lane-shape requirements, planted fixture per class.
- [ ] Anti-rebuild guard honored: nothing from NOTAGAIN §6 (F-1/F-2/F-3, S-12 team gate, safe-spawn kernel, WG-1..9 receipts, E-LIFECYCLE-001's 12 sprints) is re-built — the epic extends, never re-fixes.

## Related definitions
- None currently recorded.

## Related sprints
- None currently recorded — minted via `/sprint:plan` at execution.

## Dependencies
- E-DISPATCH-INTEGRITY-001 F-1/F-3 (landed 2026-06-10) — this epic extends their predicates, never re-builds.
- E-LIFECYCLE-001 mode_profiles + provider-tier (landed, report-only) — W1's auth-posture rows extend provider-tier.
- Operator decisions in NOTAGAIN §8 (gate flips, codex OAuth) — parallel, not blocking W0/W1.

## Blockers
- None currently recorded.

## Risks
- W2's only-door flip is the blast-radius step (every dispatch path must already satisfy the resolver or work stops) — mitigate: ramp report-only-per-wrapper first, planted tests both ways, kill-switch env var.
- Quota outages during the epic limit cross-family review lanes — mitigate: W1 lands the recorded claude-fallback lane FIRST; W3 codifies the per-class minimums so the degradation is explicit, never silent.
- RI-007 (full.js fresh-run attaches to a closed registry-primary sprint) is still open — every sprint in this epic mints its id explicitly via add-sprint.js before invoking the engine.

## Decisions
- None currently recorded.

## Open questions
- None currently recorded.

## Session log
### 2026-06-10 — Session n/a
- Agent(s): Alex · Mode: sprint
- Work performed: authored E-DISPATCH-SHAPE-001 via /epic:plan (epic file + companion plan ../../_planning/epics/E-DISPATCH-SHAPE-001.md).
- Files changed: trackers/epics/E-DISPATCH-SHAPE-001-dispatch-shape-decision-spine.md, _planning/epics/E-DISPATCH-SHAPE-001.md · Paths changed: created the epic tracker file · Wirings changed: pending ROADMAP § Epics + TRACKER reconciliation (α at integration).
- Decisions: see § Decisions · Issues discovered: None recorded at plan time.
- Definitions added/changed: None.
- State change: (new) → Planned · Completion change: — → 0%
- Verification performed: section-completeness self-check (verifyEpicMarkdown) · Validation run: node scripts/trackers/validate.js (post-wiring, α) · Validation result: pending wiring.
- Next action: see § Current next action.
- Evidence/references: ../../_planning/epics/E-DISPATCH-SHAPE-001.md.

## Change log
### 2026-06-16 — W2 fix-first: resolver unknown-role weakness fixed (autonomous)
- Changed: `scripts/dispatch/dispatch-shape.js` `resolveAgent` unknown-role path — an unknown role (not in the registry, so `classForRole` returns null) whose id name indicates a build-chain worker (…builder/…fixer) or independent reviewer (…reviewer/redteam) now resolves to `subprocess-claude` / `subprocess-cross-provider` via a name-heuristic, instead of falling to "no distinguishing signal → inline" (the W2 plan's named "fix-first" weakness — it once picked inline for a real subprocess builder). Pick labeled `source="unknown-role-name-heuristic"` (auditable). +5 tests → `dispatch-shape.test.js` 31/31.
- Reason: W2's "fix the resolver weakness FIRST" prerequisite (per the plan §17 / W2 entry). Advisory-safe (the resolver is not yet enforced-on) + self-contained; known roles still resolve via the contract (regression test asserts it).
- Affected: `scripts/dispatch/dispatch-shape.js`, `scripts/dispatch/dispatch-shape.test.js`, `.claude/framework-manifest.json` (regen).
- Previous state: unknown builder-like ids resolved inline (the weakness).
- New state: unknown builder/reviewer ids resolve to their correct subprocess shape. W2 CORE (wrapper-refusal ENFORCE ramp + envelope ADR) remains unbuilt — the report-only ramp is safe to build next; the enforce-flip is operator-gated.

### 2026-06-10 — Session n/a
- Changed: created E-DISPATCH-SHAPE-001 (epic file + companion plan artifact) from the /epic:plan payload.
- Reason: operator directed planning this epic via /epic:plan.
- Affected: trackers/epics/E-DISPATCH-SHAPE-001-dispatch-shape-decision-spine.md (new), _planning/epics/E-DISPATCH-SHAPE-001.md (new); pending ROADMAP § Epics + TRACKER header (α integration).
- Previous state: not tracked.
- New state: Planned, 0%, plan authored.

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: PROVIDER-FAMILY SWITCH gemini->antigravity ('agy'): operator-directed 2026-06-11 in-session -- switch from gemini to antigravity ('agy') throughout the entirety of WarpOS. Ground truth 2026-06-11: agy v1.0.7 at %LOCALAPPDATA%\agy\bin\agy.exe (not yet on harness PATH); headless = 'agy -p <prompt> --print-timeout <dur>' + --model + 'agy models' + --sandbox; operator completed agy auth interactively. Migration class = provider-id cutover (anthropic->claude class LRN-2026-04-29; CUT-SAFETY: parity BEFORE deleting old literal; both layers per ED-026 class). Touches: providers.js PROVIDERS table+argv, safe-spawn ARG_POLICY new agy tool-id (fail-closed without it), dispatch-contract/shape provider axis, role-spec provider_model pins, catalog.js, test-dispatch-config.js, /models:check, scaffold/manifest ship surface, breaker+auth-posture rows for agy. Home: new sprint 'provider-family cutover gemini->agy' -- NOT big-bang; parity gauntlet on both routes before removal.
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:a2643c29 -->

### 2026-06-11 — fold (enforcement-requirement) — source: operator (in-session directive)
- Folded: CLI-AUTH-MODE VERIFICATION AT THE WARPOS LIFECYCLE BOUNDARY (scaffold/install/update): operator-directed 2026-06-11 -- extends ROADMAP backlog item 6 + orphaned A1/A3 per-role preflight (provider-smoke.js --per-role, built but unwired). NEW requirement from the resolved codex contradiction: CLIs DEFAULT to API-key auth when a key is present and LOOK authenticated when no explicit CLI login was ever run (codex confirmed 2026-06-11; verify gemini + agy equivalents) -- so install/scaffold/update flows MUST verify auth mode is explicitly CLI/plan-billed and treat key-present-but-no-CLI-login as LOUD warn/block: downstream END USERS would silently drain prepaid API wallets while every check reads green. Receipts: TRACKER codex-contradiction RESOLVED clause 2026-06-11, memory project_codex_two_billing_surfaces, ED-043. Enforcer: provider-smoke/provider-tier auth-mode rows wired into /warp:setup + /warp:update postflight + bootstrap scaffold; planted fixture: auth.json with only an API key must FAIL/WARN the install gate, never read as authenticated.
- Classification: enforcement-requirement
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:4ec869e3 -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: DISPATCH-CONSOLE / MODEL-ROUTER REFRESH: operator-directed 2026-06-11 in-session ('severely outdated... doesn't even have fable... not mapped to our new agent system... part of our best-shape agent + skill dispatch work' + 'allow me to filter by group/pod... agents that arent supposed to exist anymore should be removed'). Four sub-items: (1) CATALOG COMPLETENESS — claude-fable-5 (the model this very session runs on) is absent from the catalog's selectable options, AND scripts/models/check.js false-negatived 'all configured models are current' minutes before — check.js verifies configured-models-still-valid but NOT catalog-carries-latest-options; close the blind spot (it is the §H/BC-16 false-green class on the models surface). (2) ADR-0007 MAPPING — the router panel renders the pre-rewrite role list; rows must derive from _org/role-registry.json (registry as SSOT, NOTAGAIN Law 5; same class as audit N3 dispatch-readiness validating the scrapped roster + catalog.js validating scrapped role ids). (3) SCRAPPED-AGENT REMOVAL — rows for roles absent from the registry are removed (alias-aware via role-aliases), with the same alias-backed cut-safety as S-7. (4) PANEL UX — group/filter by department/pod (president/engineering/product/growth pods per the registry org tree) instead of one giant flat list. Natural home: the W2/W3 window alongside the resolver-as-only-door work (the panel must render the SAME contract the resolver enforces, or it teaches operators a dead roster).
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:7aa47ddc -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: MODEL-CATALOG LAYERING (defaults + local overrides): operator-directed 2026-06-11 in-session — 'there should be a default model setup, and then i should be able to customize it... gpt 5.5 in place of opus so i can use more claude tokens on fable... without disrupting the default warpos install. and the default warpos install should still use opus 4.8 as the highest claude model. and in the panel, i should be able to set the defaults too.' Requirements: (1) SHIPPED DEFAULT catalog layer = part of the WarpOS install (framework-owned; default highest Claude model = opus-4.8), survives and is updated by /warp:update; (2) LOCAL OVERRIDE layer = operator-owned per-install customization (e.g. route reviewer lanes to gpt-5.5 instead of opus to conserve Claude tokens for fable), NEVER clobbered by update (same defaults+local pattern as the settings compiler and the E6/W-9 product-overlay path registry — and the same compile-can-silently-drop hazard, LRN 2026-06-06, must be regression-tested); (3) effective route = deterministic merge (local wins), surfaced in the panel with provenance per row (default vs overridden); (4) the Dispatch Console edits BOTH layers explicitly — an edit prompts which layer it targets; defaults-editing writes the framework layer (maintainer/canonical context only), override-editing writes the local layer; (5) ship-coverage/manifest treatment: default layer ships, override layer is owner:project and never overwritten (MERGE_CONFLICT-free updates). Composes with the other router fold (fold:7aa47ddc): registry-derived rows + pod filtering + scrapped-agent removal + fable in options. Same W2/W3 window home.
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:84b4eaaa -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: PROVIDER-ENTRYPOINT INSTRUCTION DOCS (CLAUDE.md / AGENTS.md / CODEX.md / ANTIGRAVITY.md): operator-directed 2026-06-11 — ensure these are appropriate per the system, create the missing ones, and keep them updated as changes land. PLAN-ONLY as of 2026-06-11 (operator: do not build in the planning session; the executing session builds). State verified 2026-06-11: CLAUDE.md exists (the live framework doc); AGENTS.md exists but is an org-chart ROUTER doc, not written as the cross-CLI agent entrypoint (codex auto-reads AGENTS.md per the agents.md standard; antigravity expected same-or-own-file — verify at build time); CODEX.md and ANTIGRAVITY.md do not exist. DESIGN CONSTRAINT (the dispatch-guide duplicate-drift lesson, S-2/N-2): ONE source of truth + THIN SHIMS — CODEX.md and ANTIGRAVITY.md must be pointer shims (provider-specific notes only: codex = ChatGPT-OAuth-required auth surface + exec/stdin conventions; agy = headless contract once proven) referencing the shared canonical rules, NEVER body copies; AGENTS.md gains a compact entering-agent preamble (repo identity, dispatch-contract basics, guard expectations, output destinations, pointer to CLAUDE.md) while keeping its router content additively (additive-migration learning 2026-06-06). MAINTENANCE CONTRACT + NAMED ENFORCER (required at build time, not optional): a provider-entry-docs check asserting (a) all four files exist, (b) shims carry the required pointer block and stay under a thin-shim size bound (drift = a shim growing a body), (c) the shared preamble block is single-sourced (hash-parity or include-marker), wired into /scan:full + the release gate alongside the N-2 duplicate-doc-drift enforcer; absent that wiring the gap is logged via /enforcement:log before merge. Natural home: the W2 window with the other operator-surface items (console refresh folds 7aa47ddc/84b4eaaa).
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:72801243 -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: AGENT-INIT / DISPATCH-READINESS PREFLIGHT WIRING (operator-asked 2026-06-11 'how about updating the agent init?'; closes ROADMAP A1/A3, previously orphaned): provider-smoke.js --per-role — the REAL per-provider-x-per-role dispatch-readiness preflight, already BUILT but wired nowhere — gets wired into the agent-init boundary everywhere agents come up: (a) SessionStart (the mode/team init path), (b) /warp:health + /warp:doctor, (c) /agents:test, (d) the scaffold/install/update flows (composing with the CLI-auth-mode gate, fold 4ec869e3 — same boundary, one preflight that reports per-link verdict: CLI present / model resolvable / effort flag valid / permissions / AUTH MODE). Kills the presence-only false-green path (provider-health-check --summary reads green when a role's actual dispatch tuple is broken). Must be registry-derived (role-registry.json + role-aliases — NOT the scrapped roster, audit N3) and must include the agy provider row once the cutover lands (fold a2643c29). Enforcer: a planted broken-tuple fixture (role pinned to a ghost model) must turn the preflight RED at SessionStart and in /agents:test; presence-only green with a broken tuple = the failure being killed. PLAN-ONLY as of 2026-06-11; the executing session builds.
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:110ed873 -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: PER-PROVIDER DEFAULT SETTINGS — REGISTRY + PANEL + SHIP PATH (operator-directed 2026-06-11, extends fold 84b4eaaa): the router panel must also set each provider's DEFAULT SETTINGS — not just role->model routes. Concrete case: openai reasoning effort currently rides xhigh; the operator wants to dial 'actually, high is enough' in the panel and have it stick. Requirements: (1) a provider-defaults REGISTRY with per-provider knobs (default model, reasoning effort, timeout class, sandbox/approval flags) replacing the values currently HARDCODED in providers.js (readReasoningEnv per-role table, GEMINI_DEFAULT, codex -c model_reasoning_effort) — migrate ADDITIVELY (new registry consulted first, existing hardcoded values become the fallback rung, deprecate-in-place; 2026-06-06 additive-migration learning); (2) TWO LAYERS, same contract as 84b4eaaa: framework-shipped defaults (source lives in the _warpos/framework layer like paths.registry.json -> generated view in .claude/; maintainer edits in canonical SHIP downstream via release/update) vs local per-install overrides (owner:project, never clobbered by /warp:update); effective value = deterministic merge, local wins, per-knob provenance shown in the panel; (3) the panel edits BOTH layers — in canonical context a defaults edit writes the framework source (and thus ships); in a product install it writes the local override; (4) hazards with named tests: the settings-compile silent-drop class (LRN 2026-06-06 — merge must never drop knobs present in one layer and absent in the other; regression fixture) and the sealed-install explicit-inclusion class (LRN 2026-06-07 — generated provider-defaults view must be explicitly included in capsule/sealed installs); (5) env-var overrides (WARPOS_*/GEMINI_MODEL etc.) stay as the outermost emergency layer, documented in precedence order: env > local > shipped default > hardcoded fallback. Same W2-window home as the other console folds.
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:6375ecdf -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive) + NOTAGAIN.md @7c0718a
- Folded: NOTAGAIN RESIDUAL LEDGER — EVERY STILL-OPEN GAP MAPPED TO A WAVE (operator-directed 2026-06-11 'all the other issues in NOTAGAIN.md'; enumerated so no gap lives only in prose/memory). CLOSED BY W0+W1 (receipts, do not re-fix): G8/N1 timeouts, N8 run_id export, G1 builder advisory (refuse=W2), G2 recorded claude-fallback lane, G5 breaker, G4/N4 auth-posture. W2 ITEMS (resolver-as-only-door window): N2 ENFORCE ramp flip; N5 route-guard stops blessing recordless raw claude -p --agent; envelope contract ADR — revive-or-delete the dead validator (36/36 fail rate; STILL failing live 2026-06-11 on both codex probes 'no JSON object found'); N9 in-process/record-inprocess records gain output_digest+argv_schema_version+run_id so coverage-gate can accept honest manager-class records; N7 full.js execute phase actually invokes gauntlet-verify + coverage-gate; G6 full.js refuses fresh-run attach to a closed/primary sprint (the RI-007 mechanical fix, one-liner identified in audit); G7 gauntlet-verify read-side __dirname anchor (ED-016 read side — worktree-cwd false-REDs); G3 scope-contract-guard derives reviewer read-only handling from contract taxonomy/registry kind, not hardcoded heuristics; N12 scope-guard prompt heuristics re-pointed off scrapped ids; N10 gemini 75KB preflight guard keyed to security-reviewer (not legacy redteam only); N6 route-guard message paths.X-compliant; N11 registry epsilon.status updated to live (ADR-0009). W3 ITEMS (per-failure-class lanes window): lane policy per risk class (already DoD); ED-039 dispatch-claude.js production-reap fix applied to ALL callers (Law 1; RI-004 class closure); ED-038 team-gate honest-limit residual; ED-030 front-load-beta enforcement; ED-015 lead shutdown-liveness check; ED-017 verify-before-redispatch; ED-040 teammate-name guard — HIT AGAIN LIVE 2026-06-11 at warpos-sprint standup ('Epsilon (ε)' rejected by harness name regex; sanitize-or-document at the TeamCreate/Agent boundary so mode:sprint step-1.75 prompts never carry illegal names). TAIL: dispatch-skill earn-it loop (Section 13.6/13.7, zero skills subprocess_verified — single owner = this epic's skill axis, consuming the E-SYSTEM-ORG harness already built). Ghost-session class (telemetry Section 4, 3 orphaned one-event sessions) = observe-only unless it recurs post-W2. PLAN-ONLY as of 2026-06-11; the executing session sequences W2 items into the W2 sprint(s).
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) + NOTAGAIN.md @7c0718a · <!-- fold:20cf3bcf -->

### 2026-06-11 — fold (scope-addition) — source: operator (in-session directive)
- Folded: DISPATCH CONSOLE — DESIGN PASS + INVOCATION VISIBILITY + SUBPROCESS-SAFETY ASSURANCE (operator-directed 2026-06-11, extends folds 7aa47ddc/84b4eaaa/6375ecdf): (1) FULL VISUAL MAKEOVER through the design org — design-lead consult on the panel's IA/layout (it is an internal operator tool: density, pod grouping, provenance badges) + a design-quality Playwright APPROVE/REJECT gate on the rendered panel before merge (the same gauntlet rigor product UI gets; the design org reviews, never self-approves its own work). (2) CLI-vs-API DEFAULT INVOCATION SWITCH, per provider: the panel exposes which invocation route each provider defaults to — with the dispatch-contract Law 7 posture rendered inline (CLI mandatory; API allowed ONLY for no-CLI-equivalent capabilities) — operator can flip a provider's default where the contract permits, and a contract-violating flip is refused IN the panel with the reason (the panel teaches the contract, never tunnels around it; writes go through the same layered defaults registry as 6375ecdf). (3) LIVE DISPATCH-COMMAND VISIBILITY: each role row shows the CURRENT literal spawn command the wrapper would run (e.g. codex exec --sandbox workspace-write -c model_reasoning_effort=high -m gpt-5.5 - via safe-spawn, stdin prompt) derived from the SAME resolver/kernel code paths that execute it (never a hand-maintained string — that would itself drift), so bash-subprocess drift is VISUALLY detectable by the operator at a glance; rows whose effective route deviates from the resolver's pick get a loud drift badge (the dispatch-shape self-detection surfaced to the eye). (4) BASH-SUBPROCESS SAFETY — REUSE THEN GAP-CHECK, not re-research: the o3 deep-research corpus already exists at runtime/research/dispatch-subprocess-safety/ and produced the safe-spawn kernel (arg-allowlisted, shell:false, tool-resolved, tree-kill, BOM/CRLF-normalized stdin; 27/27 planted-violation tests; GPT-5.5 cross-reviewed FAIL->fixed x2). Executing-session tasks: (a) gap-check the corpus + kernel against the NEW surfaces this epic adds — agy ARG_POLICY entry (fail-closed until authored), the panel's write path, dispatch-skill's last raw spawnSync(shell:true) wire-through (the E-SYSTEM-ORG deferred tail — single owner now = this epic); (b) refresh research ONLY for uncovered areas (agy CLI semantics, Windows tree-kill on agy.exe); (c) enforcer = the existing planted-violation suites extended with an agy fixture + a panel-write fixture; every spawn surface in the epic routes through the kernel or is refused (no new raw spawn paths, Law 15).
- Classification: scope-addition
- Provenance: 2026-06-11 · source: operator (in-session directive) · <!-- fold:d1fc756f -->

### 2026-06-11 — fold (clarification) — source: operator (in-session FYI)
- Folded: AUTH-CHECK SCOPING — MACHINE-LEVEL ONCE, PROJECT-LEVEL CHEAP (operator-flagged 2026-06-11, refines folds 4ec869e3 + 110ed873): auth checks likely only need to run ONCE PER MACHINE, not per project — to be confirmed at design time. Evidence as of 2026-06-11: provider auth state is machine-scoped (~/.codex/auth.json, ~/.gemini/oauth_creds.json, agy under %LOCALAPPDATA%) and is shared by every project on the machine. BUT two things ARE project-scoped and must stay in the per-project check: (a) workspace TRUST (gemini GEMINI_CLI_TRUST_WORKSPACE / trusted-folders; agy config.toml [projects] trust_level rows — verified: WarpOS + every product dir has its own row); (b) per-project env-key overrides (.env.local OPENAI_API_KEY etc.) which can silently flip a project's effective billing surface away from the machine-level login. Design shape to evaluate: a MACHINE-LEVEL auth-posture registry (e.g. ~/.warpos/auth-posture.json) caching per-provider verified auth-mode verdicts with a TTL + invalidation triggers (auth.json mtime change, codex login event, quota_exhausted with auth_mode mismatch), so scaffold/install/update gates READ the cached machine verdict (fast, no probe) and only re-probe on stale/invalidated; the per-project slice checks trust + env-override divergence only (cheap, every install). Per the single-authoritative-writer learning (2026-06-09): ONE writer owns the machine registry (the probe/refresh path), gates are readers. Enforcer addition: a planted .env.local-override fixture must flag 'project billing surface diverges from machine login' at the project gate.
- Classification: clarification
- Provenance: 2026-06-11 · source: operator (in-session FYI) · <!-- fold:dff8cad7 -->

## Evidence log
### 2026-06-10 — Epic plan authored
- Evidence type: File changed.
- Detail/location: trackers/epics/E-DISPATCH-SHAPE-001-dispatch-shape-decision-spine.md; _planning/epics/E-DISPATCH-SHAPE-001.md.
- Verified by: Alex · Supports: § Definition of Done scoping (the plan exists + links round-trip).

## Verification log
| Item | Should exist? | State | Where / wired where | Proof (cmd/inspection) | Checked | By |
| --- | --- | --- | --- | --- | --- | --- |
| _planning/epics/E-DISPATCH-SHAPE-001.md (plan artifact) | Yes | Verified Exists | _planning/epics/ | written by /epic:plan | 2026-06-10 | Alex |
| ROADMAP § Epics entry for E-DISPATCH-SHAPE-001 | Yes | Missing But Required | ROADMAP.md | pending α integration | 2026-06-10 | Alex |

## Current next action
W0 DONE 2026-06-10 (SP-20260610-006 @ RL-20260610-041, gauntlet-clean; see § Percent completion for the full receipt). NEXT (open as of 2026-06-10): **W1 — make availability and fallback real**: (a) ONE recorded claude-fallback review lane (ledgered; visibly trips cross_provider_required — gauntlet-verify must SEE it via planted fixture); (b) provider circuit breaker (provider-down state written on quota classification, TTL'd, consulted before dispatch — ends blind retry storms); (c) auth-POSTURE surface (parse auth.json auth_mode; metered-key reads as metered in dispatch-readiness/provider-tier; auth mode stamped into quota error envelopes — closes the 3-day metered-billing drain class, ROADMAP backlog item 6). Mint the W1 sprint id explicitly (RI-007) before invoking the engine. ALSO OWED: cross-family review pass on the W0 diff (base 4d7301f → afe02a4) when GPT/gemini quota recovers.

## Completion record
- Final state: Not yet complete.
- Percent completion: n/a
- Completion timestamp: n/a
- Definition of done used: the § Definition of Done above.
- Evidence of completion: n/a (Planned).
- Session IDs / dates / agents: n/a.
- Related completed sprints: None.
- Remaining follow-up items: all sprint candidates listed in § Related sprints.
- Related untracked work: None.
- ../../TRACKER.md updated: No (pending α integration) · Roadmap reconciled: No (pending α integration).
