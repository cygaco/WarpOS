<!-- EPIC TRACKER — generated by /epic:plan (scripts/epic/plan.js).
     Linked from ../../ROADMAP.md § Epics and ../../TRACKER.md.
     States (§19): Planned | Ready | Active | Blocked | Paused | Review Needed | Completed | Cancelled | Superseded -->

# E-VLAD-001 — Vlad v1 — the agent+MCP virtual co-founder (install a co-founder)

- **Epic label and number:** E-VLAD-001
- **Title:** Vlad v1 — the agent+MCP virtual co-founder (install a co-founder)
- **Owner:** President α (epic) · ε (sprint conduct) · operator (product rulings)
- **Parent roadmap area:** Product — Master Console engine distribution channel (first shipping product surface)
- **Goal:** Ship installable v1 'Vlad': ONE installer delivering the public agent face + the closed local plain-node engine (Agent SDK) + the MCP stdio seam, running entirely on the founder's own Claude account (BYOK), certified on Claude Code — so a founder with a prototype gets a readiness audit, a roadmap, and executable sprint jobs with honest receipts, propose-first approval enforced in engine code, and branch-isolated writes applied only by preflighted ff-only merge.
- **Background:** Derived from the completed 6-round grill of _planning/vlad-agent-mcp-plan.md (operator rulings recorded in-file, 2026-07-28), _planning/vlad-why-us.md (differentiators: hosts build apps, Vlad builds launches), and _planning/vlad-transferability.md (34-family inventory: v1 is substantially a PORT, not a build — lastmile audit engine runs on stranger repos behind 3 refusal edits; transaction.js + release ff-only posture ARE the ratified write path; turbo's engine half IS the permission-level system). DoP priority ruling 2026-07-28: this is the next thing that reaches a user. Per products-own-session doctrine, the product is built in a NEW SIBLING REPO (operator sign-off required to create it); this epic tracks it from WarpOS, and every port is a COPY-out — canonical WarpOS assets are not modified. Plan artifact: [../../_planning/epics/E-VLAD-001.md](../../_planning/epics/E-VLAD-001.md).
- **Scope:** New sibling product repo (via /portfolio:new, local-only, operator sign-off) holding: engine (plain-node Agent SDK app), MCP stdio server, agent face file, installer; MCP v1 tool surface: get_status, get_readiness, run_job, send_message, approve_job, cancel_job, get_roadmap; Job model: propose-first universal; state machine running/needs_input/proposing/done; on-disk job journal at checkpoints; single active job per project (new jobs queue); proposals survive with re-validation on aged approval; Job #1 port: lastmile detect/score/adapters audit engine, WarpOS-refusal edits done in the PORTED copy, intake fallback for undetectable stacks, readiness-score convergence (one number); Write path port: transaction.js atomic apply/rollback + both-directions ff-only posture; every write on a branch; preflight (conflicts, drift, other sessions/worktrees) before apply; any doubt → draft-PR park; undo = revert; Permission levels port: turbo engine half (auto|notice|confirm|never profiles, TTL grants checked in code, spend-ledger warn+confirm for enormous jobs); per-project config file + level-change flow; propose-only default; Init flow: 3 ELI5 intake questions + silent repo-model detection + stage inference + memory scaffold (portfolio-registry port, inverted to path-primary index) + permission level + terms acceptance; Roadmap/sprint layer in free v1: gap report → ROADMAP.md → sprint plans as deliverable files written INTO their repo (state stays engine-side); Receipt contract: what I did · what I verified · the one thing I need — schema derived from portfolio dogfood runs; honest 'NOT verified' degradation when repo has no tests; Same-provider adversarial review lane (different tier + adversarial role prompt); completion-ledger discipline port (absence-is-death, fake-green guard, tri-state exits, boundary fixtures); Engine model access via the user's installed claude CLI (subscription-billed) with API-key guided-checklist fallback; ELI5 voice rule in all agent/receipt output; 'Vlad (AI)' attribution hygiene; lightweight terms covering prompt-injection risk + liability
- **Out of scope:** Daemon / proactive loop (nightly audits, morning notes, standing agenda) — Later/paid tier; Hosted engine variant + claude.ai-web support — Later; Cross-provider gauntlet + multi-provider deep research — premium tier (transferability insight 2: sold as MORE reviewers, never 'reviewers that work'); Owned channels (email-out, reply parsing, dashboard chat) — Later; Proxy billing / prompt secrecy — parked as future ENTERPRISE move; Multi-job parallelism + worktree collision layer — Later (v1 = single active job); Account/team shared-control model — whoever prompts Vlad controls Vlad; Vlad creating or deleting accounts/projects — ABSOLUTE refusal lines (thin by ruling; everything else permission-gated); Growth/audience suite as a shipped layer — SCAFFOLD in-repo; do not promise; Cost estimates in proposals — ruled out; warn+confirm on enormous jobs only
- **Current state:** Planned
- **Percent completion:** 0% — conservative; plan authored, no sprints landed.

## Definition of Done
- [ ] One installer command on a clean machine (Windows + macOS) installs agent + engine + .mcp.json entry, and @vlad responds in Claude Code with the init flow — no manual config edits
- [ ] Job #1 (read-only readiness audit) completes on a non-WarpOS repo and returns a receipt (what I did / what I verified / one thing I need) inside 10 minutes
- [ ] The ported audit engine runs clean on ≥3 registered portfolio repos (dogfood corpus) with zero WarpOS-specific refusals, and exactly ONE readiness number exists (score convergence done)
- [ ] Every job write lands on a branch; apply = preflight + ff-only merge of the pre-verified commit; a conflicting concurrent edit parks the job as a draft PR instead of applying; undo = one revert — each proven by an automated test
- [ ] approve_job is enforced in engine code: a test that drives run_job to a write-capable step WITHOUT an approval record fails closed; permission level 'auto' short-circuits approval ONLY for grant-covered actions
- [ ] Job state machine + on-disk journal: killing the editor mid-job leaves the repo clean, and next-session get_status names the interrupted job with resume/discard options — proven by test
- [ ] Roadmap job turns the audit's gap report into ROADMAP.md + sprint plan files written into the user's repo; the files survive engine uninstall
- [ ] Engine memory persists per-project across sessions, keyed path-primary; a moved folder is detected and offered relink
- [ ] Receipts and agent output pass an ELI5 spot-check rubric (non-technical, short); 'Vlad (AI)' attribution present in all owned-channel output
- [ ] A recorded end-to-end demo exists: install → init → audit → roadmap on a real non-WarpOS repo (the blog post's evidence)

## Related definitions
- None currently recorded.

## Related sprints
- **SP-VLAD-W1-ENGINE** — candidate — Engine skeleton: plain-node Agent SDK app + MCP stdio server + 4-core tool surface + job state machine + journal + permission-level config port (turbo engine half) + claude-CLI model-access seam (verify ToS/limits FIRST — gate for everything)
- **SP-VLAD-W1-AUDIT** — candidate — Job #1: port lastmile detect/score/adapters into the engine; remove WarpOS refusals in the ported copy; intake fallback for undetectable stacks; converge the two readiness scores; receipt schema v1 from portfolio dogfood
- **SP-VLAD-W2-WRITEPATH** — candidate — Write path: port transaction.js + ff-only posture; branch-per-job; preflight; draft-PR park; undo; approve_job enforced in code under permission levels
- **SP-VLAD-W2-FACE** — candidate — Agent face + installer: @vlad agent file (ELI5 voice, delegation rules, engine-liveness check), package-deal installer (agent+engine+.mcp.json), init flow (intake → detection → memory scaffold path-primary → level+terms)
- **SP-VLAD-W3-ROADMAP** — candidate — Strategy layer: gap-report → ROADMAP.md + sprint-plan deliverables into their repo; get_roadmap tool; state-vs-deliverables split enforced
- **SP-VLAD-W3-GAUNTLET** — candidate — Quality + ship: same-provider adversarial review lane; ledger discipline with boundary fixtures; portfolio dogfood run (≥3 repos); ELI5 rubric pass; demo recording; blog draft handoff

## Dependencies
- None currently recorded.

## Blockers
- None currently recorded.

## Risks
- None currently recorded.

## Decisions
- BYOK/their-account billing; proxy = future enterprise (grill r3) — collapses packaging to plain node
- Package deal: ONE installer, agent hard-requires engine (r1/T2)
- Claude Code = v1 certified host (r2)
- Write path: branch-isolated, preflighted ff-only apply, draft-PR park on any doubt, revert undo (r4, DoE cure, transferability insight 5)
- Permission levels propose-only→auto, per-project config, approval checked in code (r3/F3.4 + F2.3)
- Single active job per project; parallel jobs = Later (r4, α delegated ruling)
- Roadmap/sprint planning ships FREE in v1; vigilance (daemon re-ranking) is premium (r6)
- State stays engine-side; deliverables (ROADMAP.md etc.) are ordinary files in their repo (r6)
- ELI5 voice always; audience = non-technical vibe-coder founders ('past-Vlad') (r2/r3)
- No cost estimates; warn+confirm on enormous jobs (r3)
- Thin absolute refusals: never CREATE accounts / never DELETE accounts or projects; all else permission-gated; browser-driving IN scope with do-it-with-me at credential fields (r4)
- Prompt-injection residual: terms disclosure, not in-your-face UX; proposals cite their evidence (r4)
- Same-provider adversarial review in v1; cross-provider = premium 'more reviewers' (r5, transferability insight 2)

## Open questions
- Product repo name + slug (operator sign-off required to create the sibling repo — first approval point)
- claude CLI headless shell-out: ToS/limits verification outcome (gates W1-ENGINE seam design)
- Free/paid boundary mechanics under the bundle (trial? job quota? feature gate?) — deferred from r3; no v1 impact (v1 fully free)
- Installer distribution channel: npm package vs install script vs both
- Receipt schema final shape — deliberately open until portfolio dogfood data exists (W1-AUDIT)
- Engine repo license + closed-source posture mechanics for a plain-node distribution

## Session log
### 2026-07-28 — Session 956a915e-66ef-4a6c-ab2c-3fce728636b1
- Agent(s): alpha · Mode: sprint
- Work performed: authored E-VLAD-001 via /epic:plan (epic file + companion plan ../../_planning/epics/E-VLAD-001.md).
- Files changed: trackers/epics/E-VLAD-001-vlad-v1-agent-mcp-cofounder.md, _planning/epics/E-VLAD-001.md · Paths changed: created the epic tracker file · Wirings changed: pending ROADMAP § Epics + TRACKER reconciliation (α at integration).
- Decisions: see § Decisions · Issues discovered: None recorded at plan time.
- Definitions added/changed: None.
- State change: (new) → Planned · Completion change: — → 0%
- Verification performed: section-completeness self-check (verifyEpicMarkdown) · Validation run: node scripts/trackers/validate.js (post-wiring, α) · Validation result: pending wiring.
- Next action: see § Current next action.
- Evidence/references: ../../_planning/epics/E-VLAD-001.md.

## Change log
### 2026-07-28 — Session 956a915e-66ef-4a6c-ab2c-3fce728636b1
- Changed: created E-VLAD-001 (epic file + companion plan artifact) from the /epic:plan payload.
- Reason: operator directed planning this epic via /epic:plan.
- Affected: trackers/epics/E-VLAD-001-vlad-v1-agent-mcp-cofounder.md (new), _planning/epics/E-VLAD-001.md (new); pending ROADMAP § Epics + TRACKER header (α integration).
- Previous state: not tracked.
- New state: Planned, 0%, plan authored.

## Evidence log
### 2026-07-28 — Epic plan authored
- Evidence type: File changed.
- Detail/location: trackers/epics/E-VLAD-001-vlad-v1-agent-mcp-cofounder.md; _planning/epics/E-VLAD-001.md.
- Verified by: alpha · Supports: § Definition of Done scoping (the plan exists + links round-trip).

## Verification log
| Item | Should exist? | State | Where / wired where | Proof (cmd/inspection) | Checked | By |
| --- | --- | --- | --- | --- | --- | --- |
| _planning/epics/E-VLAD-001.md (plan artifact) | Yes | Verified Exists | _planning/epics/ | written by /epic:plan | 2026-07-28 | alpha |
| ROADMAP § Epics entry for E-VLAD-001 | Yes | Missing But Required | ROADMAP.md | pending α integration | 2026-07-28 | alpha |

## Current next action
Mint the Wave-0 sprint candidate(s) via /sprint:plan, then wire E-VLAD-001 into ROADMAP § Epics + the TRACKER header (α at integration).

## Completion record
- Final state: Not yet complete.
- Percent completion: n/a
- Completion timestamp: n/a
- Definition of done used: the § Definition of Done above.
- Evidence of completion: n/a (Planned).
- Session IDs / dates / agents: n/a.
- Related completed sprints: None.
- Remaining follow-up items: all sprint candidates listed in § Related sprints.
- Related untracked work: None.
- ../../TRACKER.md updated: No (pending α integration) · Roadmap reconciled: No (pending α integration).
