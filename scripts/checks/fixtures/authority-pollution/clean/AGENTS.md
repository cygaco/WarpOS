<!-- G2.3 CLEAN control — a NEUTRAL surface that DESCRIBES the org without ASSERTING identity/authority
     to the reader. The scan MUST return zero violations here: descriptive third-person mentions of the
     President role are legitimate handbook content; only reader-DIRECTED identity/default-binding/
     authority assertions are pollution. This proves the scan is not a blunt "President" grep. -->

# Provider-neutral handbook (CLEAN control)

This company is run by Alex, the President — a role played by whichever model is at the helm.

Dispatched workers are UNBOUND fail-closed; a worker never assumes the President's authority. The
operator's own top-level session binds to the helm role only through an explicit helm binding, never
through this ambient file.

A builder proposes changes in an isolated worktree; it may request that a change be merged, but the
trusted layer owns integration to main. Ambient prose like this file can never manufacture a binding.
